"""retrieval — 계약 v0.1 §검색 전략 비교(`POST /retrieval/compare`)의 3전략 (T2-1).

🔴 이 패키지의 경계(티켓 T2-1 · baseline §16.2·§34.6):

1. **LLM 호출 0.** 여기서 답을 «쓰지» 않는다 — 후보를 찾아 인용을 돌려줄 뿐이다.
   합성(synthesize)은 T2-3이며, 이 패키지에 외부 모델 호출이 생기면 그것은 범위 이탈이다.
   임베딩은 색인(T1-4)과 같은 로컬 모델뿐이다.
2. **사용자 입력 → SQL·Cypher 문자열 조립 0.** 질의문은 상수이고, 값은 전부 파라미터
   바인딩으로 들어간다. 질문 자체도 서버측 allowlist(`allowlist.py`)를 통과해야 한다.
3. **응답 스키마 확장 0.** 계약 v0.1이 정한 `[{ strategy, hits: [{evidenceId, score,
   excerpt}], elapsedMs }]`만 내보낸다(오케 판정 08-30). 전략이 더 알고 있는 것(경로·
   결합 내역)은 `excerpt` 안에서 사람이 읽을 수 있게 말하되, 새 필드를 만들지 않는다.
"""
