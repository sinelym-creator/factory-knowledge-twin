# D-83 검증 — webkit 드로어 초점 «순환» (`d83v`)

| | |
|---|---|
| 판정 | **PASS** — 축 ①~④·⑥ 전건 · ⑤ 반증 열 = **오케 가설(E4) 확증** · 대상 결함 0 |
| 대상 | PR #727 `lane/senku2-d83` **`bbeb82c`** · `nav-drawer.tsx` 패널 안 Tab roving(`indexOf(active)`) |
| 무대 後 | `:8370` · **pid 33252** · cwd **`_wt/senku2-d83/apps/web-console`** · **BUILD_ID `voUdWswwgvCP-JPzUfjzv`**(발주 일치) · `indexOf(active)` **1건** |
| 무대 前 | `:8367` · **pid 42640** · cwd `_wt/senku2-d82b/apps/web-console` · BUILD_ID `feHnwwhpGCz3YFObOKgA0` · HEAD `55b783d` · `indexOf(active)` **0건** |
| 그물 | `tests/web/d83_webkit_trap.mjs`(신규 · 7열) · cap 0 · live run 0 |
| 측정 | 2026-09-05 14:39~14:41 KST |
| 검증자 | 리바이2 48대 |

🔴 **판정선을 하나 더 세웠다 — 「이탈 0」만으로는 «순환»이 아니다.**
첫 링크에 **붙박여 아무 데도 못 가는** 상태도 이탈 0 을 낸다. 그래서 드로어 «안»에서 밟은
**서로 다른 링크 수**(`tabDistinctInside`)를 같이 센다. 前 열이 정확히 그 자리였다(이탈 5 · **distinct 1**).

---

## 1. 엔진 × 무대 표 (390 · 드로어 열림 · Tab/Shift+Tab 각 10회)

| 엔진 | 무대 | Tab 이탈 | **밟은 링크 수** | Shift 이탈 | Shift 링크 수 | 콘솔 실오류 |
|---|---|---|---|---|---|---|
| **webkit** | **後(처방)** | **0** | **3** | **0** | **3** | 0 |
| **webkit** | 前 | **5** | **1** 🔴 | **5** | **1** 🔴 | 0 |
| chromium | 後 | 0 | 3 | 0 | 3 | 0 |
| chromium | 前 | 0 | 3 | 0 | 3 | 0 |
| firefox | 後 | 0 | 3 | 0 | 3 | 0 |
| firefox | 前 | 0 | 3 | 0 | 3 | 0 |

- **축 ① PASS** — webkit 後 이탈 **0** · 3링크 순환.
- **축 ② PASS** — Shift+Tab 역순도 이탈 0 · 3링크.
- **축 ③ PASS** — chromium·firefox 는 **前後 불변**(회귀 0).
- 🔴 **前 열이 빨강이라 後 열의 초록이 뜻을 갖는다** — 같은 실행에서 이탈 5·distinct 1 을 봤다.
  (T7-2b 에서 잰 값 5/10 과 같다 — 다른 대(代)·다른 회차에서 같은 수가 나왔다.)

## 2. 축 ④ 닫힘 3갈래 + 초점 복귀 (webkit 後)

| 갈래 | 드로어 잔여 | 닫은 뒤 초점 |
|---|---|---|
| Esc | **0** | **`nav-menu-toggle`** |
| 스크림(패널 밖 좌표) | **0** | **`nav-menu-toggle`** |
| 링크 클릭 | **0** | (이동) |

## 3. 축 ⑤ 반증 열 — 🔴 **판정선 아님 · 결과만** (오케 가설 E4)

가설 = 「webkit 은 기본 설정에서 Tab 이 «링크»를 건너뛰고 폼 컨트롤만 밟는다 → 트랩이 첫 링크로
«되돌리기»만 해서 body ↔ 첫 링크 교대가 난다」.

| 열(둘 다 **前** 무대 · webkit) | Tab 이탈 | 밟은 링크 수 |
|---|---|---|
| 그대로 | **5** | **1** |
| **링크 3개에 `tabindex="0"` 주입** | **0** | **3** |

**⇒ 가설 확증.** 손잡이 하나(`tabindex`)만 바꿨더니 前 무대에서도 순환이 살아난다. 즉 원인은
**「webkit 이 링크를 기본 탭 순서에서 안 밟는다」 + 「트랩이 되돌리기만 한다」의 합**이고,
처방(roving)은 그 합을 코드 쪽에서 끊는다.

## 4. 못 잰 것 (이름으로)

- **실기기 Safari·VoiceOver** — Playwright WebKit 은 엔진이지 Safari 앱이 아니다. Safari 의
  「Tab 으로 각 항목 강조」 설정은 에뮬레이션 밖이다.
- **터치 축·키보드 활성화 클릭** — 이번 열은 Tab/Shift+Tab 과 포인터 클릭뿐이다.
- **다른 폭** — 드로어는 <md 전용이라 390 만 잰다.
- **공개면** — 두 무대 다 로컬 `next start` 다(승격 18 뒤 외부 재검 축).
