# D-95v — 투어 열림 신호 래치 수리 독검 (리바이2 60대)

- 창 = `date 15:35:07` ~ `15:37:33` · 구독 **0** · production·팀 공용 `:8020` 무접촉
- 검증 대상 = PR #943 `lane/senku2-d95` **`4a914a3`**

## 0. 두 열 — 손잡이는 셸 빌드 하나

| 포트 | 열 | 워크트리 | 빌드 |
|---|---|---|---|
| `:8196` | **前** | `_wt/levi2-load` | develop(수리 없음) |
| `:8198` | **後** | `_wt/levi2-d95v` | `lane/senku2-d95` **`4a914a3`** |
| `:8850` | 공통 | `fkt-levi2-stub-api` | 전용 ai-api(화면 축이라 API 는 변수 아님) |

## 1. ⓑ 앱바 「튜토리얼」 → 투어 열림 (412×600 × `/overview` 직행 2회 · 1440×900 × `/` 진입 2회)

| 열 | 412 `/overview` | 1440 `/` | 열림 판정 |
|---|---|---|---|
| 前 | **1 / 1**(11ms · 16ms) | **1 / 1**(20ms · 21ms) | 4/4 열림 |
| 後 | **1 / 1**(15ms · 16ms) | **1 / 1**(22ms · 20ms) | 4/4 열림 |

🔴 **이 축은 前에서도 이미 참이다 — 그러므로 이 4칸의 초록은 수리에 대해 아무 말도 하지 않는다(판정력 없음).**
내 자극은 「카드가 이미 마운트된 뒤 앱바를 누르는」 조건이라, 수리가 겨냥한 **「본문이 늦게 붙는 창」**을 밟지 못했다.
그 창을 만들려면 본문 청크 도착을 늦추는 무대가 따로 필요하다(이번 창 밖 · 회부).

## 2. ⓒ 닫기 → 새로고침 되열림 (선재 결함 동수리분)

| 열 | 닫고 새로고침 뒤 `intro-card` | 판정 |
|---|---|---|
| 前 | **1 · 1 · 1 · 1**(4칸 전부 **되열림**) | 결함 재현 |
| 後 | **0 · 0 · 0 · 0** | **수리 확인** |

→ 같은 명령·같은 4칸에서 **前 4/4 되열림 → 後 0/4**. 이 축은 판정력이 있다.

## 3. 🔴 러너 축 — M-1 스펙 패치 «전»에 물었다 (발주 ⓒ)

`t3-2-screens.spec.ts` 「🔴 D-1 — 안내는 «세션»에 묶인다」(`:488`) 를 **패치 없이** 두 열에 그대로:

| 열 | 결과 | 대상의 말 |
|---|---|---|
| 前 `:8196` | **FAIL** | `「?」를 눌러도 안 열린다` |
| 後 `:8198` | **PASS**(3.5s) | — |

→ **수리만으로 초록이다.** 센쿠2의 E4(「러너/손 갈림 = 계측기 축」) 가설은 이로써 **기각**된다 —
그 빨강은 러너의 것이 아니라 **대상의 것**이었고, 러너는 손보다 **더 빨리 눌렀을 뿐**이다(늦게 붙는 본문 = 바로 이 경주).
그러므로 **M-1 §2.5 스펙 패치(앵커)는 이 빨강을 위해서는 필요 없다.** 적용 여부는 다른 근거로 판단해야 한다(회부).

## 4. 판정

**PASS** — 대상 결함 0. 수리의 관측 가능한 이득 **2건**: ⓒ 되열림 4/4 → 0/4 · 러너 축 FAIL → PASS.
ⓑ 열림 4칸은 **前에서도 참**이라 판정에 넣지 않는다(초록이지만 검출력 0).

## 5. 내 계측기 자수

1. **ⓑ 를 「2/2 초록」으로 보고할 뻔했다.** 前 열을 같이 찍지 않았으면 **이미 참인 축의 초록**을 수리의 공으로 적었을 것이다. 발주의 판정선(2/2)을 그대로 따르는 것만으로는 이 함정을 피할 수 없다.
2. **내 자극이 수리가 겨냥한 조건을 밟지 못했다.** 「늦게 붙는 본문」은 내 무대에서 15~22ms 안에 다 붙는다 — 못 잰 것이지 「없다」가 아니다.
3. 러너 축은 **1회씩**이다(前 FAIL · 後 PASS). 반복하지 않았으므로 「흔들림 없음」은 주장하지 않는다.
