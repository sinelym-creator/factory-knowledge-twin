# T7-24 — X 예외 대장 2차(무대 «있는» 케이스)

- **좌석** 검증(리바이2 39대) · **발주** 스자쿠 34대 (2/2) · **기록** 2026-09-04
- **정본** `docs/plan/test-plan-v1.md` §6 · **1차 누계** X 6 PASS / 1 FAIL(D-48 · API 축) — `evidence/t7-23-x-first-pass.md`
- **판정선** 🔴 「죽지 않았다」가 아니라 **「정의된 대체 동작을 했고, 그 사실이 남았다」**.
  🔴 케이스마다 **무대가 «울렸다»를 먼저 수로 찍고** 대상을 판정한다 — 안 울린 회차는 `exit 2` 폐기.

## 조각 1 — X-05 / X-20 / X-21 (상류 다운 → 폴백)

### 무대 (E1 실측)

| 축 | 값 |
|---|---|
| 셸 | `lane/levi2-t7-23b` 트리 × `FKT_API_BASE=http://127.0.0.1:8101` **새 프로세스 빌드** → **`:8797`**(`INSTALL_RC=0` `BUILD_RC=0` · 파이프 없이 확인) |
| 상류 | 거울 스텁 `:8101`(`tests/web/_t723_mirror_stub.mjs`) · **끊김 스위치** `/__stub/down`·`/__stub/up` |
| 무접촉 | 실 ai-api `:8102`(다른 좌석이 읽는다) · 배포 `:8010` · 센쿠2 스택 |
| 🔴 무대 증인 | 스텁이 **실제로 끊은 요청 수** — ⓐ **2건** · ⓑ **4건**. 0 이면 그 열은 「안 잼」이고 `exit 2` 다 |

🔴 **상류를 «내 것만» 끊는 이유** — 실 `:8102` 를 죽이면 그것을 읽는 다른 좌석의 무대가 함께 죽는다.
자극은 내 소유물에만 넣는다.

### 두 경로를 따로 낸다

| 열 | 뜻 |
|---|---|
| ⓐ **쓰던 중 죽는다** | 세션이 있는 같은 컨텍스트에서 상류를 끊고 재적재 — **사용자가 겪는 길** |
| ⓑ **죽은 뒤 들어온다** | 새 컨텍스트 · 세션 없음 — 관문까지 포함한 길 |

### 판정표 (무대 → 자극 → 대체 동작 → 남은 흔적 → 시점)

| # | 무대(증인) | 자극 | 대체 동작 | 남은 흔적 | 시점·수 | 판정 |
|---|---|---|---|---|---|---|
| **X-05** | 스텁 끊김 2건 | ⓐ 상류 끊고 재적재 | — | `mode-badge` **`◌미연결`** · `fallback-banner` 「Live 상태를 확인하지 못했습니다(미연결) … 오류가 아닙니다」 · `static-replay-offer` 「정적 재생본으로 GS-01 보기」 | 재적재 후 4s 시점 · 표시 요소 **3종** | **PASS** |
| **X-20** | 스텁 끊김 2건 | ⓐ + **제안 손잡이를 실제로 누름** | `/incidents/INC-2026-014?run=STATIC-GS-01` 로 이동해 **정적 재생본이 그려짐** | 제목 「② Incident 조사 · INC-2026-014」 · `mode-badge` 여전히 `◌미연결` | 본문 글자 **1093(상류 살아 있음) → 137(끊김) → 5511(재생본)** · 오류 표지 **없음** | **PASS** |
| **X-21** | 스텁 끊김 2건 | 같은 실행 두 열 대조 | — | 상류 살아 있음: `◑REPLAY` + 「Live AI 합성이 꺼져 있습니다(소유자 게이트웨이 미도달)…」 / 끊김: `◌미연결` + 미연결 배너 + 재생본 제안 | 표시 **2종 → 3종** · 문면 **다름** | **PASS**(조용하지 않다) |

**빨강 확인** — ✓ 같은 실행에서 두 열이 다른 값을 냈고, **스텁이 실제로 2건을 끊었다**(자극 실재).
자극이 0건이면 그물이 `exit 2` 로 스스로 판정을 막는다.

### 🔴 이 세 칸에 붙는 «한정» — 초록의 폭을 좁혀 적는다

1. **X-20 의 폴백은 «자동 낙하»가 아니라 «제안형»이다.** 끊긴 직후 화면은 본문 137자로 줄고,
   **사용자가 「정적 재생본으로 GS-01 보기」를 눌러야** 5511자가 그려진다. 정본 §6 X-20 문면은
   「대체 경로가 **실제로 동작**」이다 — **누르면 동작한다**가 이 표의 사실이고, 「자동으로
   낙하하는가」는 **이 표가 답하지 않는다**(기대 해석은 오케 영역).
2. **내 「LIVE」 열은 완전한 LIVE 가 아니다.** 상류가 살아 있어도 배지는 이미 `◑REPLAY` 였다
   (합성 게이트웨이 `:8787` 미도달). 그러니 두 열의 정확한 이름은
   **「합성 게이트웨이 미도달 · 상류 살아 있음」 ↔ 「상류 미연결」**이다. X-21(침묵 금지)의
   판정에는 영향이 없다 — 두 상태의 표시가 실제로 달랐다.
3. **ⓑ 열(죽은 뒤 진입)은 `/overview` 가 아니라 관문 `/` 에 앉았다**(본문 82자 · 같은 미연결
   배너 + 재생본 제안). 관문 뒤 화면은 이 열에서 **안 잼**.

### 자수 (이 조각 · 내 계측기)

| # | 무엇 | 어떻게 잡혔나 |
|---|---|---|
| 1 | 「오류 화면인가」 정규식이 `500` 을 **경계 없이** 물어 첫 회차에 `errorish=true` 를 냈다. 그대로 뒀으면 **X-20 을 FAIL 로 회부**할 뻔했다 | 걸린 문자열을 **이름으로** 남기게 고치니 `null` 이었다 — 대상엔 오류 표지가 없었다 |
| 2 | 첫 판은 **제안 손잡이를 안 눌렀다** — 「본문 137자」만 보고 「대체 경로 없음」으로 읽을 뻔했다 | 「제안 ≠ 동작」을 갈라 **누르는 열**을 추가 |

## 조각 2 — X-18 (시드 두 번 실행)

### 무대

| 축 | 값 |
|---|---|
| 대상 | 🔴 **내 컨테이너만** — `fkt-levi2-postgres-1`(`:5534`) · `fkt-levi2-neo4j-1`(`:7587`) 을 `docker start` 로 기동 |
| 무접촉 | `fkt-senku2-t15-*` · 배포 `:8010` |
| 스크립트 | `data/seed.ps1 -Project fkt-levi2`(생성 → 적재 → 검증) · 그물 `tests/ops/t724x_seed_twice.sh` |
| 지문 | **표 수 · 총 행수 · 표별 행수의 sha256** 3종 — 「rc 0」이 아니라 **상태를 수로** 본다 |

### 🔴 빨강 확인이 «먼저»

「두 번 돌려서 같다」는 **아무것도 안 세는 자**로도 참이다. 그래서 시드 «전»에 일부러 행을 지워
지문이 움직이는지부터 봤다 — 안 움직이면 그물이 `exit 2` 로 판정을 막는다.

| 스냅 | 표 \| 총 행수 \| 지문 |
|---|---|
| snap0 (자극 전) | `32 \| 950372 \| 94da688caa9ceed7` |
| snapP (행 2개 삭제) | `32 \| 950371 \| d21c0f9c8bb72f8e` ⇒ **✓ 지문이 움직인다** |

### 판정표

| # | 무대(증인) | 자극 | 대체 동작 | 남은 흔적 | 시점·수 | 판정 |
|---|---|---|---|---|---|---|
| **X-18** | 지문이 삭제 1건에 반응함(빨강 확인 ✓) | `seed.ps1` **1회차** → **2회차** | 2회차가 **상태를 바꾸지 않았다** | 두 회차 모두 `rc=0`(리다이렉트로 받아 직접 확인 · **파이프 뒤 rc 아님**) | snap1 `32\|950313\|62f5621afdef3f73` → snap2 **완전 동일** · 31s / 34s | **PASS** |

### 🔴 이 칸에 붙는 «한정»

- **`snap0 ≠ snap1`** 이다(950372 → 950313). 시드는 seed 표를 **비우고 다시 넣기** 때문에,
  2일 묵은 내 컨테이너의 «옛 생성본» 상태가 **현재 정본 생성본**으로 교체된 것이다.
  이건 결함이 아니라 **이 컨테이너의 출발 상태가 최신이 아니었다**는 사실이다 —
  X-18 이 묻는 것은 **1회차 ↔ 2회차**이고 그 둘은 **완전히 같다**.
- **Neo4j 축은 안 잼** — `seed.ps1` 은 postgres 적재 스크립트다. 그래프 쪽 재실행 멱등은 이 표에 없다.
- **거절형(명시적으로 「이미 적재됨」이라 거절)인지 무해형(다시 넣어 같은 상태)인지** — 실측은
  **무해형**이다(TRUNCATE 후 재적재). 정본 X-18 기대는 둘 중 하나면 되므로 충족이다.

## 누계

**X 10 PASS · 1 FAIL(D-48 · API 축) · 14 미실행 / 25**
(1차 6 PASS + 1 FAIL · 2차 X-05·X-20·X-21·X-18 4 PASS)

## 조각 3 — X-16 (상류엔 도달했는데 응답만 유실 → 재시도)

> 🔴 **이게 멱등의 «진짜» 시험이다.** 판정선 = 「오류가 안 났다」가 아니라 **「서버 상태가 두 번
> 바뀌지 않는다」를 «수»로 보인다**(판정선 2 ①).

### 무대 · 증인

| 축 | 값 |
|---|---|
| 무대 | `apps/web-console/scripts/x-stages/blackhole-proxy.mjs --port 8811 --upstream 127.0.0.1:8102`(#556) |
| 🔴 증인(`GET /__stage`) | 자극 전 `forwarded 0 · upstreamAnswered 0 · dropped 0` → 자극 후 **`forwarded 2 · upstreamAnswered 2 · dropped 2`** (마지막 상류 응답 **200 · 70 B · 3 ms**) |
| 세는 자 | 🔴 **상류 자신의 기록** — ai-api 로그의 `POST /api/scenarios/{id}/runs … 200` 줄 수. 응답이 유실돼 runId 를 못 받으므로 **클라이언트가 아니라 상류가 센다** |
| 그물 | `tests/ops/t724x_x16_lost_response.mjs` |

### 판정표

| # | 무대(증인) | 자극 | 대체 동작 | 남은 흔적 | 시점·수 | 판정 |
|---|---|---|---|---|---|---|
| **X-16** | 상류 답함 **+2** · 클라이언트로 못 감 **+2** | 무대 통과 POST **첫 요청**(응답 유실 `UND_ERR_SOCKET`) → 1s 뒤 **재시도**(역시 유실) | — | — | 🔴 **상류가 만든 run 수 +2**(기대 **1**) · 대조군(무대 없이 1회) **+1** | **FAIL** |

**빨강 확인** — ✓ 무대 없이 1회 → **+1**(계수기 작동) · ✓ **상류는 답했는데 클라이언트는 못 받았다**가
같은 실행의 무대 증인으로 서 있음(그래야 「도달했는데 유실」이 참이다).

### 🔴 D-48 과의 관계 — 같은 부재, **더 넓은 경로**

D-48 은 「API 로 동시에 두 번」이었고 **화면 열은 PASS**(더블클릭이 가드된다)라 좁게 냈다.
X-16 은 다르다 — **재시도를 부르는 것이 «응답 유실»**이라, 화면 가드가 있어도 클라이언트·프록시·
사용자 어느 쪽이든 정상적으로 다시 보낸다. ⇒ **run 생성에 멱등 키가 없다는 같은 부재가,
이쪽에서는 «실제로 일어나는 길»에 놓인다.** D-48 의 폭을 이 행만큼 넓혀 읽어야 한다.

### 안 잼

- **화면 축**(사용자가 다시 누르는 길) — 🔴 배포된 blackhole 무대에 **경로 필터가 없어 «전부» 끊는다**.
  셸을 그 앞에 세우면 화면 자체가 안 서서 무대가 아니라 장애물이 된다. 그래서 이 열은 **API 축**이고
  화면 축은 **미검증**이다. (경로 한정 끊김이 생기면 그때 잰다.)
- **DB 행 수 축** — run 은 `run_store`(메모리)에 산다. DB 행 축은 이 케이스에 해당 없음.

## 조각 4 — X-07(지연 후 복구 · 화면) · X-11(용량 거절 · 화면)

### 무대 — 🔴 **셸 빌드를 손잡이에서 뺐다**

무대를 갈아 끼울 때마다 셸을 다시 빌드하면 «무대»와 «빌드»가 함께 움직여 무엇이 갈랐는지 못 가른다.
그래서 셸은 **한 번만** `FKT_API_BASE=http://127.0.0.1:8103` 으로 빌드해 **`:8104`** 에 세우고,
`:8103` **한 자리**에 무대를 갈아 끼웠다(`BUILD_RC=0`). 🔴 포트는 **내 대역**(`:8101`~`:8104`)만 쓴다 —
`:8790`~`:8795` 는 타 좌석 대역이라 첫 판을 폐기하고 옮겼다(그 판은 무대 증인 `forwarded 0` = 안 울림).

### X-07 — 지연 후 복구 · **PASS**

| 열 | 무대 증인 | 화면 계열(본문 글자) |
|---|---|---|
| **자극 `--delay-ms 3000`** | `forwarded 18 · upstreamAnswered 18 · delivered 12` · **응답 개시 3007~3147ms**(`heldMs 3004`) | 35ms `◌확인 중`(82) → **3338ms `◑REPLAY`**(35) → 12400ms(1072) → 15576ms(1081) |
| **대조군 `--delay-ms 100`** | `forwarded 22 · delivered 22` · **응답 개시 106~133ms** | 33ms `◌확인 중`(82) → **359ms `◑REPLAY`**(35) → 675ms(1072) → 994ms(1093) |

🔴 **「잠정 상태가 그려졌다 걷힌다」의 실체 = `mode-badge` 「◌확인 중」 → 「◑REPLAY」 전이**다.
그 지속이 **자극 3.3s ↔ 대조군 0.36s** 로 **지연에 비례**한다 — 잠정이 서고, 값이 오면 걷힌다. **PASS.**

> 🔴 **자수** — 내 `provisional` 계수(`aria-busy` · `[data-loading]` · `.animate-pulse` · `role=progressbar`)는
> **두 열 모두 0** 이었다. 대상의 잠정 표지를 **내가 지어낸 셀렉터로** 찾은 탓이다.
> 판정은 **화면이 실제로 쓰는 배지 전이**로 냈다. 손잡이는 화면에서 실측해야 한다.

### X-11 — 용량 거절이 «화면에 남는가» · **FAIL**

| 열 | 무대 증인 | 화면 표지 | 반복 요청(조용한 재시도 후보) |
|---|---|---|---|
| **자극 `--max-inflight 1`** | `arrived 30 · **rejected 18** · passed 12 · **peakInflight 1/1**` | `◌확인 중`(~1160ms) → `◑REPLAY` + `fallback-banner` — 🔴 **거절 표지 0** | 🔴 **10종이 2회씩**(`/api/live/status` · `/api/equipment/*/sensors/*/series` 8종 · `/overview`) |
| **대조군 `--max-inflight 99`** | `arrived 22 · **rejected 0** · passed 22 · peakInflight 6/99` | 같은 `◑REPLAY` + `fallback-banner` | `GET /overview` **1종만** |

- 🔴 **자극 열의 화면이 대조군과 «같다».** 503 을 **18건** 받았는데 화면에 남은 것은 두 열이 공유하는
  합성 배너뿐이다 ⇒ **「거절 받은 쪽이 화면에 거절로 남는가」 = 남지 않는다**.
- 🔴 **조용한 재시도 = 0 이 아니다.** 거절 표지가 한 번도 없는 채로 **10종의 요청이 각각 2회** 나갔다
  (대조군은 1종). 정본 §6 X-11 판정선의 「조용한 재시도 0」에 걸린다.
- **무대 증인이 자격을 준다** — `peakInflight 1/1`(상한에 실제로 닿음) · 거절 18 · 통과 12 가 **같은 버스트**에 있다.
  (`peakInflight < max-inflight` 였다면 이 판정은 무효였다.)
- **안 잼** — `Retry-After` 를 **화면이 읽고 되묻는지**(백오프 간격)는 이 창에서 안 쟀다. 반복 «수»만 셌다.

## 누계 (갱신)

**X 12 PASS · 3 FAIL(D-48 · X-16 · X-11) · 10 미실행 / 25**

## 조각 5 — X-23 / X-13 (`online:true` 인데 근거 0건 → 「모른다」로 가는가) · **미검증**

### 무대 증인이 «자극이 축에 안 닿았다»고 말한다

| 열 | `GET /__stage` (자극 뒤) | 골든 걸음 5 관측 |
|---|---|---|
| **자극**(`synthetic-gateway.mjs --port 8103`) | `liveStatusServed 2 · **liveStatusForcedTrue 2** · jsonRewritten **1** · **arraysEmptied 1** · countsZeroed **0** · **upgradesProxied 2**` · `lastSynthesis.path = **/api/work-orders/WOD-…** ($.evidenceIds)` | 후보 **2** · 근거칩 **4** |
| **대조군**(`--passthrough`) | `liveStatusForcedTrue 0 · jsonRewritten 0 · arraysEmptied 0 · upgradesProxied 2` | 후보 **2** · 근거칩 **4** |

**두 열의 화면이 «같다».** 그런데 이건 「근거가 0건이어도 화면이 멀쩡하다」가 아니다 —
🔴 **근거를 0건으로 만든 적이 없다.**

- 자극의 **절반은 닿았다**: `online` 은 상류 `false` → **강제 `true`** 로 2회 나갔다(`lastLiveStatus`).
- 자극의 **나머지 절반은 안 닿았다**: 비운 배열은 **1건**뿐이고 그 경로가 **`/api/work-orders/WOD-…`** 다.
  화면의 «후보·근거 칩»이 타는 경로가 아니다.
- 🔴 **이유** — run 이벤트는 **WebSocket** 으로 온다. 이 무대는 설계상 **업그레이드를 손대지 않고
  소켓째 넘긴다**(`upgradesProxied 2`). ⇒ **근거는 무대를 «지나가지 않는다».**

### 판정 — 🔴 **미검증**(PASS 도 FAIL 도 아니다)

「후보 2 · 근거칩 4」를 X-23 의 초록으로 읽으면 **자극 없는 초록**이다. 정본이 묻는 것은
「근거가 0건일 때 «모른다»로 가는가」인데, **근거가 0건인 화면을 만든 적이 없다.**

**닿게 하려면 둘 중 하나가 먼저다** — ① 무대가 **WS 프레임까지** 근거 배열을 비우거나
② 근거가 **HTTP 로 오는 경로**(예: 스냅샷 재적재)에서 자극을 건다. 어느 쪽도 이 조각의 범위 밖이라
**무대 확장 발주**로 올린다.

### 안 잼

- **X-13**(근거가 없는 질문 → 「모른다」) — 같은 이유로 **미검증**. 근거 0건 상태를 못 만들었다.
- 화면이 「모른다」를 **무슨 문면·무슨 손잡이로** 말하는지도 아직 실측 안 했다(그 상태를 못 만들어서).

## 누계 (갱신)

**X 12 PASS · 3 FAIL(D-48 · X-16 · X-11=D-49) · 2 미검증(X-23 · X-13) · 8 미실행 / 25**

## 조각 6 — X-06 (DB 연결 끊김) · **PASS**(API 축)

### 무대 — 🔴 **내 DB 만** 끊는다

`fkt-senku2-t15-postgres-1` 은 다른 좌석이 쓰고 내 `:8102` 도 그것을 문다. 그래서 **내 컨테이너
(`fkt-levi2-postgres-1` `:5534` · `fkt-levi2-neo4j-1` `:7587`)를 무는 «두 번째» ai-api 를 `:8105` 에**
따로 세우고, 자극은 **그 DB 에만** 넣었다(`docker stop fkt-levi2-postgres-1` → 측정 → `docker start`).

### 판정표

| # | 무대(증인) | 자극 | 대체 동작 | 남은 흔적 | 시점·수 | 판정 |
|---|---|---|---|---|---|---|
| **X-06** | 기준선 `/api/health` **`status:"ok"`** · `/api/plants` **200**(실데이터 1건) | `docker stop fkt-levi2-postgres-1` | 요청이 **503 으로 «정의된 거절»** | `{"error":{"code":"dependency_unavailable","message":"postgres 에 연결할 수 없다 — 잠시 후 다시 시도하라"}}` · `/api/health` **`degraded`** + `postgres: unavailable` | 🔴 **스택·드라이버·호스트·포트 누출 = 0건**(`traceback\|psycopg\|asyncpg\|\.py\|connection refused\|127\.0\.0\.1\|5534` 전수 grep) | **PASS** |

**빨강 확인** — ✓ **같은 실행에서** 기준선 `200`(실데이터) ↔ 자극 `503`. 「전부 503 을 내는 서버」와 구별된다.
**되돌림** — `docker start` 로 즉시 복구 확인(`status:"ok"`).

### 안 잼

- **화면 축** — 이 열은 API 축이다. `:8105` 를 무는 셸을 안 세웠다(셸은 빌드 시점에 상류가 박힌다).
  「사용자 말 오류가 **화면에** 뜨는가」는 **미검증**.
- **Neo4j 만 끊긴 경우**는 안 잼(이번엔 postgres 만 끊었다).

## 누계 (갱신)

**X 13 PASS · 3 FAIL(D-48 · X-16 · X-11=D-49) · 2 미검증(X-23 · X-13) · 7 미실행 / 25**

## 조각 7 — X-14 (인용이 깨진 응답 → 거절 · 화면에 «안 올림») · **PASS**

무대 = 셸 `:8798`(`3038501`) × 실 ai-api `:8102` · 그물 `tests/web/t724x_highlight_reject.mjs` ·
화면 = `/documents/DOC-MAN-0021?highlight=…`.

🔴 판정선을 **두 갈래**로 갈랐다 — ① 거절이 화면에 뜬다 ② **강조가 «지어지지» 않는다**.
②가 없으면 「거절 문구도 띄우고 강조도 그린」 최악을 못 잡는다.

**빨강 확인(같은 실행)** — 유효 앵커 `DOC-MAN-0021@r1#006` → `data-highlight="ok"` · **강조 노드 1** · 거절 표지 없음.
⇒ 「전부 거절하는 화면」과 구별된다.

| 자극 형태 | `cited-body` 상태 | 강조 노드 | 거절 표지 | 판정 |
|---|---|---|---|---|
| 빈 값 | `none` | **0** | 없음(강조 요청 자체가 없음) | PASS |
| 형식 아님 `not-an-anchor` | — | **0** | `highlight-rejected` 「인용 강조 요청이 거절됐다 — **문서가 없는 것은 아니다** · `highlight_mismatch`」 | PASS |
| 다른 문서 `DOC-OTHER-0001@r1#006` | — | **0** | 같은 표지 · 사유 병기 | PASS |
| 없는 좌표 `…@r1#99999` | — | **0** | 같은 표지 · 사유 병기 | PASS |
| 주입 형태 `…@r1#<script>alert(1)</script>` | — | **0** | 같은 표지 · **원문 그대로 문면에 표시(실행 흔적 0)** | PASS |

**5/5 PASS** · 🔴 **강조가 지어진 회차 0**. 「404(문서 없음)」와 「400(강조 요청이 틀림)」을 화면이
**갈라서** 말한다는 점도 문면에 남아 있다.

**안 잼** — 서버가 나눈 두 사유(`highlight_mismatch` ↔ `highlight_not_found`)를 **화면이 각각 다른
문면으로** 내는지는 이 표에서 못 갈랐다(5형태 모두 `highlight_mismatch` 로 회수됐다).

## 누계 (최종 · 39대)

**X 14 PASS · 3 FAIL(D-48 · X-16 · D-49) · 2 미검증(X-23 · X-13) · 6 미실행 / 25**

## 부기 (리바이2 40대 · 11:16 · 🔴 **판정 아님 — 무대 실측 정정만**)

운영자 하명(11:14 「진행 중인 것 완료 → 마감」)으로 **X-08·X-09·X-22·X-24·X-25·X-10 은 착수 전**에 멈췄다.
**새 판정 0건** · 누계 불변. 아래는 다음 대가 이 무대를 다시 쓸 때 **전언 대신 쓸 실측**이다.

### 무대 검산 3 (11:12 실측 · E1)

`:8102` `/api/health` = `postgres:ok · neo4j:ok`(build `003f3ef-levi2-t723`) · `/openapi.json` paths **21** ·
셸 `/overview` **307**. ⇒ 통과.

### 🔴 살아 있는 자리 / 죽은 자리 — 인계본의 「9자리 전부 떠 있다」는 **낡았다**

| 자리 | 11:12 실측 | 비고 |
|---|---|---|
| `:8102` ai-api | **살아 있음** | `003f3ef-levi2-t723` · 센쿠2가 읽기로 쓴다 |
| `:8105` ai-api | **살아 있음** | build `x06-levi2-own-db`(39대 X-06 무대) |
| `:8104` `:8796` `:8798` `:8799` 셸 | **살아 있음** | 아래 귀속표 |
| `:8101` 거울 스텁 · `:8103` 무대 한 자리 · `:8797` 셸 | 🔴 **무응답** | X-24·X-25 를 하려면 **거울 스텁부터 다시 세워야 한다** |

### 🔴 셸 귀속 = 포트 → PID → 커맨드라인 실측 (`Get-NetTCPConnection` + `Win32_Process`)

`/api/health` 는 **셸이 프록시하는 ai-api** 를 말할 뿐 **셸 자신의 트리**를 말하지 않는다. 그래서 프로세스로 짚었다.

| 포트 | 서빙 트리(워크트리) | `.next/BUILD_ID` mtime | 프록시 대상 |
|---|---|---|---|
| `:8798` | `_wt/levi2-t7-29v` | 11:03:44 | `:8102`(실) |
| `:8799` | `_wt/levi2-t7-23` | 09:20:18 | `:8102`(실) |
| `:8796` | `_wt/levi2-t7-28v` | 10:05:56 | `:8102`(실) |
| `:8104` | `_wt/levi2-t7-24` | 10:25:46 | 무응답(`:8103` 이 죽어 프록시 실패) |

**조각 7(X-14)이 「`:8798` = `3038501` 셸」이라 적은 것 — 확증한다.** 근거 2단:
① 그 포트를 서빙하는 트리 = `_wt/levi2-t7-29v`(프로세스 실측) ·
② 그 트리의 `merge-base 3038501 09a00b9` = **`3038501`** 이고 `diff 3038501..09a00b9` 는
`evidence/t7-29-hit-area-verification.md` · `tests/web/t729_hit_area.mjs` **2파일뿐** — `apps/`·`services/`·`packages/` **0줄**.
⇒ 셸이 서빙하는 앱 코드는 `3038501` 과 같다.
🔴 **다만 「코드가 같다」는 「그 시점에 빌드됐다」가 아니다** — `.next` 는 11:03:44 산출물이고, 그때 트리가
지금 HEAD 였다는 것까지는 안 쟀다. 다음 대는 무대를 **쓰기 전에** 이 두 축을 다시 찍어라.

### 다음 대에 남기는 것

- **X-08 · X-09**(320/2560 넘침 0 · 글꼴 24px) — 무대 재구성 불필요. 🔴 X-09 는 **자극 증인 먼저**:
  CDP `Page.setFontSizes` 로 키운 뒤 **computed font-size 가 실제로 변한 요소 수 > 0** 을 세라.
  0 이면 초록이 아니라 **미검증**(px 고정 디자인이면 자극이 축에 안 닿는다).
- **X-22**(`inert` 미지원 갈래) — 지원 엔진에선 그 갈래가 한 줄도 안 돈다. 조건을 강제로 참으로 만들고,
  **실제로 클릭해서 막히는지**까지 봐라(닿는다 ≠ 눌린다).
- **X-24 · X-25** — `:8101` 거울 스텁이 **죽어 있다**. 먼저 세우고, `/__stub/stats` 의 `refused` 를
  **자극 증인**으로 계수한 다음 판정해라. 중복 실행 0 은 X-16 과 같은 계수법.
- **X-10** — T7-16 재발주 판정문이 **없으면 미실행으로 남긴다**(지어내지 않는다).

---

## 조각 8 — X-24 · X-25 (폴백 축) · 리바이2 41대 · 2026-09-04 12:24 KST(`date` 실측)

### 무대 (3층 · 귀속을 값으로)

| 층 | 무엇 | 포트 | 귀속(포트→PID→커맨드라인) |
|---|---|---|---|
| 셸 | `next start`(트리 `_wt/levi2-t7-29bv` · **빌드 시점에 `FKT_API_BASE=:8101` 을 구움**) | `:8104` | 새 프로세스 · 12:23 재빌드 |
| 상류 스텁 | `tests/web/_t723_mirror_stub.mjs`(거울 · `/__stub/down` 은 **소켓을 끊는다**) | `:8101` | 새 프로세스 · upstream `:8103` |
| ai-api | 내 것(`c8bea88-x2` · `pg:ok neo4j:ok`) | `:8103` | 새 프로세스 · 로그 파일 소유 |

- 🔴 **공용 `:8102` 무접촉.** 「상류 다운」은 **내 스텁**에만 넣는다.
- 🔴 **셸의 목적지는 «빌드 시점»에 구워진다** — start 에만 주면 셸이 기동을 거부한다(오늘 실측).
  그래서 상류를 갈아끼우려면 **다시 굽는다**.
- 🔴 **이 무대의 「원래 경로」는 `◑REPLAY`(백엔드 도달)** 이다. Live 합성은 소유자 게이트웨이
  미도달이라 이 무대에서 애초에 안 선다. 즉 이 열이 시험한 폴백 층은
  **「백엔드 도달 → 백엔드 미도달」**이고, 그것이 X-24 가 묻는 축이다.

### X-24 — 폴백 «중» 원래 경로가 복구되면 되돌아오는가 (5열)

| 무대 | 자극 | 대체 동작(화면 표지) | 남은 흔적 | 시점 |
|---|---|---|---|---|
| 셸→스텁→ai-api | **P0 기준선**(상류 정상) | `mode=◑REPLAY` · 배너 = 「Live AI 합성이 꺼져 있습니다(소유자 게이트웨이 미도달)」 | `refused 0` · created 0 · `/api` 요청 14 | 0s |
| 〃 | **P1 상류 끊김**(`/__stub/down` · 소켓 거부) | **`mode=◌미연결`** · 배너 = 「Live 상태를 확인하지 못했습니다(미연결) … 정적 재생본으로」 | **`refused +2`** · created 0 · 요청 15 | +4s |
| 〃 | **P2 상류 복구**(`/__stub/up`) · **화면 조작 없이** | `mode=◌미연결` **그대로** | `refused 2` · created 0 · 요청 15 | +8s |
| 〃 | **P3 상류 복구** · **재방문 후** | **`mode=◑REPLAY`** · 배너 = 기준선과 동일 | `refused 2` · created 0 · 요청 28 | +12s |

- **자극 실재 ✓** — `refused` 델타 **+2**(스텁이 실제로 소켓을 끊었다).
- **낙하 ✓** — 화면 표지가 바뀌었다(조용히 넘어가지 않았다).
- **복귀 ✓** — 재방문 후 기준선과 **같은 표지**로 돌아온다.
- **중복 실행 0 ✓** — 왕복 동안 `created = POST200 − reuse` 델타 **0**(조사 조작 0회이므로 기대 0).
- **[X-24] PASS** — 🔴 **단, 조건을 붙인다: «자동» 복귀는 관측되지 않았다.** P2(복구 후 4초, 조작 없음)
  에서 화면은 여전히 `◌미연결` 이었고, **재방문(P3)에서야** 돌아왔다. 정본이 묻는 것은 「되돌아오는가」
  이므로 PASS 이되, **「상류가 살아나도 떠 있는 탭은 스스로 안 돌아온다」는 관측을 원장에 올린다**
  (후보 · 결함 여부는 정본이 「자동 복귀」를 요구하는지에 달렸다 — 내가 정할 일이 아니다).

### X-25 — 폴백의 폴백(대체 경로까지 실패)

**끊은 것(이름으로)** — 상류(스텁 `down` · 소켓 거부) **+ 브라우저에서 `/api/**` 전부 abort**.
즉 「상류도 없고 클라이언트가 대체 사본도 못 읽는다」. 🔴 셸 안의 정적 사본 **파일**을 지운 것은 아니다.

| t | `/api` 요청 누계 | 화면 |
|---|---|---|
| 5s | 2 | `mode=◌미연결` · 배너 「Live 상태를 확인하지 못했습니다(미도달) … 정적 재생본으로」 · 본문 「이 화면의 데이터를 지금 가져오지 못했다. 사유: TypeErr…」 |
| 10s / 15s / 20s / 25s | 2 · 2 · 2 · 2 | **동일** |
| 30s | 3 | **동일** |

- **정의된 마지막 상태 ✓** — 마지막 두 관측이 완전히 같다(수렴).
- **화면이 그 상태를 «말한다» ✓** — 조용한 폴백이 아니다(배너 + 본문에 사유).
- **요청 유계 ✓** — 마지막 5초 증가 **1건**(무한 재시도 아님 · 30초 총 3건).
- **[X-25] PASS**
- 🔴 **관측 1(후보)** — 본문 사유 문자열이 **`TypeError…` 를 그대로 노출**한다. 「사유를 말한다」는
  좋지만 **엔진 예외 이름은 방문자에게 쓰는 말이 아니다.** 결함 등급은 오케·정본 판단.

### 안 잰 것 (이름과 함께)

- 셸 **내부 정적 사본 «파일»** 을 실제로 못 읽게 만든 열(여기서는 **클라이언트 경로**를 끊었다).
- **자동 복귀 대기 시간을 늘린 열**(P2 는 4초만 기다렸다 — 더 기다리면 돌아올 수도 있다).
- 다중 탭 · 다른 엔진 · 조사 «실행 중»에 상류가 끊기는 열(여기서는 조사 조작 0회).

### 재현

```bash
node tests/web/_t723_mirror_stub.mjs --port=8101 --upstream=http://127.0.0.1:8103
FKT_API_BASE=http://127.0.0.1:8101 pnpm build && FKT_API_BASE=http://127.0.0.1:8101 pnpm exec next start -p 8104
node tests/web/t724x_fallback_recovery.mjs --shell=http://127.0.0.1:8104 --stub=http://127.0.0.1:8101 --log=<ai-api 로그>
```

## 🔴 X-16 칸 갱신 — 옛 FAIL 은 **PASS** 로 (판정선 개정 · PR #577)

조각 3 의 X-16 **FAIL** 은 「재시도 → run 2」를 결함으로 읽은 것이다. 그 뒤 오케가 판정선을 (A)로
재정의했다: **「비종결 창 «안» 재시도 → run 1」** 이고, **창 «밖» 재시도로 새 run 이 생기는 것은 규칙의 정의**다.
T7-30 독립 검증(#577 · `evidence/t7-30-d48-verification.md`)에서 **창을 그 무대에서 실측**(30~47ms)해
창 안 간격으로 다시 치자 후 세대는 `created +1`, 전 세대는 `+2` 였다(같은 실행 · 빨강 확인 ✓).

⇒ **X-16 = PASS**(옛 FAIL 라벨은 «남의 무대에서 잰 창(103~114ms)»을 그대로 쓴 데서 왔다).

## 대장 누계 (조각 8 시점 재계수)

**X 18 PASS · 1 FAIL · 2 미검증 · 4 미실행 / 25**

직전 정본(**14 PASS · 3 FAIL · 2 미검증 · 6 미실행**)에서 네 칸이 움직인다:

| 칸 | 전 | 후 | 근거 |
|---|---|---|---|
| **X-16** | FAIL | **PASS** | 판정선 (A) 개정 + 창을 그 무대에서 실측해 재측정(#577) |
| **D-48**(X-15 축) | FAIL | **PASS(종결)** | 처방 #571 독립 검증 PASS(#577 · 전 2·2·2 → 후 1·1·1) |
| **X-24** | 미실행 | **PASS** | 이 조각 |
| **X-25** | 미실행 | **PASS** | 이 조각 |

⇒ PASS 14 + 4 = **18** · FAIL 3 − 2 = **1**(= **D-49 / X-11**) · 미검증 **2**(X-23 · X-13) ·
미실행 6 − 2 = **4**(**X-08 · X-09 · X-22 · X-10**). 합 = 18 + 1 + 2 + 4 = **25** ✓

---

## 조각 9 — 공개면 외부 재검(승격 8회차 뒤) · 리바이2 41대 · 2026-09-04 12:28~12:31 KST

**대상** `https://factory-knowledge-twin.vercel.app` · **공인 경유 근거(같은 분 실측)**
- `curl -D -` → **`Server: Vercel`** · HSTS · CSP 존재 · **연결 `remote_ip = 216.198.79.3`**(사설·tailnet 아님)
- 브라우저 열에서도 첫 응답 헤더 `server = "Vercel"` 을 같은 실행에서 값으로 남겼다.

### ① 배너 ✕ 히트 «세로» — 두 폭

| 폭 | ✕ 히트 | 띠 높이 | 글리프 중심 y | 판정 |
|---|---|---|---|---|
| 1440 | **44×44** | 36.5 | 69.5 | **≥44 ✓** |
| 390 | **44×44** | 79 | 141.8 | **≥44 ✓** |

- 같은 실행의 로컬 `:8104`(같은 `ef633a2` 빌드) 열과 **모든 값이 일치** — 공개면이 그 빌드를 서빙한다는 교차 근거.
  🔴 이 열은 **A/B 가 아니다**(두 열 다 `ef633a2`). 그물이 찍는 「전/후」 라벨은 이 실행에서 **이름일 뿐**이다.
- `nav-incidents` 는 공개면에서도 36(= fine 매체 · **D-50 재정의대로** · T7-33 참조).

### ② D-1 「안내 닫기 → 재열람」 1회

| 단계 | 화면에 «있는» 표지(실측 열거) | 결과 |
|---|---|---|
| 닫기 전 | `tour-invite` · `tour-start` · `tour-later` · **`tour-never`** | 안내가 떠 있다 ✓ |
| 「다시 보지 않기」 클릭 | 위 4개 **전부 사라짐** | 닫힌다 ✓ |
| `intro-reopen`(「안내와 둘러보기 다시 보기」) 클릭 | **`tour-callout` · `tour-title` · `tour-progress` · `tour-skip` · `tour-next`** 새로 등장 | 돌아온다 ✓ |

**판정 = PASS.**

🔴 **계측기 자수(같은 실행에서 잡았다)** — 첫 판정선은 「초대 카드(`tour-never`)가 다시 뜨는가」였고
그 선으로는 **FAIL** 이 나왔다. 그러나 **화면에 있는 testid 를 전부 열거**해 두었기 때문에,
재열람 뒤 뜬 것이 초대 카드가 아니라 **«둘러보기» 콜아웃**이라는 사실이 같은 출력에 남아 있었다.
안내는 **형태를 바꿔 돌아온다** — 손잡이를 지어내면 없는 빨강이 나온다. 판정선을 정정해 재실행했다.

### 안 잰 것

- 공개면에서의 **조사 실행(live)** · 다른 엔진 · 모바일 실기기 · `nav-incidents` 의 coarse 열(T7-33 이 이미 잼).

---

## 조각 10 — X-08 · X-09 (반응형·글꼴 축) · 리바이2 41대 · 2026-09-04 12:35 KST

**무대** 셸 `:8104`(`ef633a2` · `FKT_API_BASE=:8101` 로 구움) → 스텁 `:8101` → ai-api `:8103`.
**그물** `tests/web/t724x_zoom_font.mjs`.

### X-08 — 320 / 2560 가로 넘침 (5열)

| 무대 | 자극 | 대체 동작 | 남은 흔적 | 시점 |
|---|---|---|---|---|
| 320px · `/overview` · `/incidents` · `/compare` | 🔴 **대조군 먼저**: 뷰포트+200px 짜리를 심었다 → 걷었다 | 심은 상태 넘침 **+200px**(검사가 본다) | **실제 넘침 0px** · `scrollWidth 320 = clientWidth 320` · 넘친 요소 **0건** | 각 라우트 +2.2s |
| 2560px · 같은 3 라우트 | 〃 | 심은 상태 넘침 **+200px** | **실제 넘침 0px** · `2560 = 2560` · 0건 | 〃 |

- 🔴 **대조군이 6칸 전부에서 빨강을 냈다** — 「넘침 0」이 「검사가 아무것도 안 본다」와 구별된다.
- **[X-08] PASS**(두 폭 × 3 라우트 = 6칸 전부 넘침 0).

### X-09 — 브라우저 기본 글꼴 24px

🔴 **자극 증인 먼저.** CDP `Page.setFontSizes({standard:24, fixed:24})` 는 «기본» 글꼴을 바꾼다 —
화면이 px 로 못 박아 두면 한 요소에도 안 닿고, 그때의 「넘침 0」은 **미검증**이다.

| 폭 | root font-size | 변한 요소 | 크기 분포(전 → 후) | 넘침 |
|---|---|---|---|---|
| 1440 | **16px → 24px** | **353 / 353** | 16→24 · 15→22.5 · 13→19.5 · 12→18 · 19→28.5 · 22→33 · 18→27 · 40→60 | 0 → **0** |
| 390 | **16px → 24px** | **353 / 353** | 같은 배율 | 0 → **0** |

- 🔴 **「변한 요소 수」만으로는 내 색인 사고와 구별이 안 되므로 «크기 분포»를 함께 냈다.**
  전 분포가 후 분포로 **정확히 ×1.5** 로 옮겨간다(16→24 · 12→18 · 13→19.5 …) — 이 델타는
  내가 자리를 잘못 맞춘 것이 아니라 **화면이 rem 으로 크기를 정하고 있다**는 뜻이다.
- **[X-09] PASS** — 기본 글꼴 24px(1.5배)에서 **두 폭 모두 가로 넘침 0**.

### 안 잰 것 (이름과 함께)

- 브라우저 **«확대»(zoom)** 축(글꼴 축과 다른 축이다) · 사용자 스타일시트 · 다른 엔진 ·
  이 3 라우트 밖의 화면(조사 실행 중 화면 · 근거 상세 등).

---

## 조각 11 — X-22 (inert 폴백) · X-10 (강제 색) · 리바이2 41대 · 2026-09-04 12:40~12:43 KST

### X-22 — `inert` 미지원 폴백이 **포인터를 실제로 막는가** · 🔴 **FAIL**

정본 `docs/plan/test-plan-v1.md:150` = 「감시 형태 대체가 **포인터를 실제로 막는다**」.
실물(`components/tour/tour-overlay.tsx:220~251`): `supportsInert` 가 거짓이면 `inert` 를 **아무 데도 안 걸고**,
남는 것은 `focusin` 감시(포커스가 새면 말풍선으로 되돌림)뿐이다 — **클릭 차단 코드가 없다.**

🔴 지원 엔진에서는 그 갈래가 한 줄도 안 도므로 **조건을 강제**했다: 문서 로드 «전» `addInitScript` 로
`delete HTMLElement.prototype.inert`.

| 열 | 런타임 `"inert" in HTMLElement.prototype` | 🔴 자극 증인(inert 걸린 요소) | 점 소유자 | 포커스 감시 | **실제 클릭** |
|---|---|---|---|---|---|
| **지원**(대조군) | `true` | **15건** | `div`(대상 것 아님) · 대상의 inert 조상 = `nav` | 되돌려짐 → `tour-skip` | `/overview` → `/overview` **막힘 ✓** |
| **강제**(프로토타입 제거) | `false` | **0건** | **`a[nav-compare]`(대상 것)** | 되돌려짐 → `tour-skip` | `/overview` → **`/compare`** 🔴 **눌렸다** |

- **갈래가 갈렸다 ✓**(true/false) · **자극 증인 ✓**(15 → 0) · **무대 ✓**(두 열 다 투어 켜짐)
- **대조군이 「막힘」을 보였다 ✓** — 지원 열에서 같은 클릭이 주소를 안 바꿨다. 그러니 강제 열의
  주소 변경은 **내 그물이 못 보는 것**이 아니라 **대상이 안 막은 것**이다.
- **[X-22] FAIL** — 폴백은 **포커스만** 지키고 **포인터는 안 막는다**. 정본이 요구하는 것은 포인터다.
  (참고: 포커스 감시는 **두 열 모두에서 작동**했다 — 폴백이 «죽어 있다»는 뜻이 아니라 «축이 다르다»는 뜻이다.)

**안 잼** — 실제 구형 엔진(여기서는 «조건»만 강제) · 키보드 축 · 터치 축 · 다른 투어 스텝.

### X-10 — 강제 색 모드 · **PASS**

정본 `:130` 은 「미측 · T7-16 재발주」였고 `forced-colors` 판정문·그물은 리포에 **0건**이었다(오케 grep).
이번에 **처음** 잰다. 🔴 **자극 증인 먼저**: `Emulation.setEmulatedMedia({forced-colors: active})` 뒤
페이지 안에서 `matchMedia("(forced-colors: active)").matches` 가 **true**(기준선은 false).

| 라우트 | 매체 | 보이는 요소 | 사라짐 | 배경에 먹힌 글자 |
|---|---|---|---|---|
| `/overview` | true | 288 → 288 | **0건** | **0건** |
| `/incidents` | true | 46 → 46 | **0건** | **0건** |
| `/compare` | true | 56 → 56 | **0건** | **0건** |

**[X-10] PASS**(3 라우트 · 사라짐 0 · 먹힌 글자 0).

🔴 **계측기 자수** — 첫 판은 요소 정체를 `testid|TAG|x,y` 로 잡았고 `/overview` 에서 **244건 «사라짐»**
이 나왔다. 그런데 **보이는 요소 수는 288 → 288 로 그대로**였다 — 강제 색에서 테두리·글꼴이 바뀌며
요소가 몇 px 밀렸을 뿐이고, **좌표를 정체에 넣은 내 키가 그 이동을 «소멸»로 읽은 것**이다.
정체는 좌표가 아니라 **트리 안의 자리**가 정한다 → 자식 색인 경로를 키로 바꾸자 전 라우트 0건.
🔴 **총계 불변(288→288)이 그 거짓 빨강을 잡아 준 감시자였다.**

**안 잼** — **대비 «비율»**(강제 색에서는 OS 가 색을 정하므로 이 그물은 「글자가 배경에 먹히는가」만 잰다) ·
실제 OS 고대비 테마 · 아이콘·테두리 시인성 · 3 라우트 밖 화면.

## 대장 누계 (조각 11 · X 2차 종료 시점)

**X 21 PASS · 2 FAIL · 2 미검증 · 0 미실행 / 25**

| 칸 | 전 | 후 | 근거 |
|---|---|---|---|
| **X-08** | 미실행 | **PASS** | 조각 10 |
| **X-09** | 미실행 | **PASS** | 조각 10 |
| **X-10** | 미실행 | **PASS** | 조각 11(처음 실측) |
| **X-22** | 미실행 | **FAIL** | 조각 11(대조군이 막힘을 보인 같은 실행) |

⇒ PASS 18 + 3 = **21** · FAIL 1 + 1 = **2**(**D-49/X-11** · **X-22**) · 미검증 **2**(X-23 · X-13) ·
미실행 **0**. 합 = 21 + 2 + 2 = **25** ✓ — **X 대장의 «미실행»은 이로써 0 이다.**

---

## 조각 12 — X-23 · X-13 재실행 · 리바이2 41대 · 2026-09-04 12:47~12:57 KST

**판정** — **X-23 = API·스냅샷 축 PASS / 화면 축 «미검증»(이유 명시)** · **X-13 = «미검증» 유지(이유 명시)**
대장 누계는 **변하지 않는다**: X 21 PASS · 2 FAIL(D-49 · D-52) · **2 미검증(X-23 · X-13)** · 0 미실행 / 25.
🔴 다만 이번 회차로 **「왜 미검증인지」가 «경로 이름»으로 바뀌었다** — 39대의 「자극 미도달(원인 불명)」과 다르다.

### 무대 3층 (귀속을 값으로)

| 층 | 무엇 | 포트 | PID |
|---|---|---|---|
| 셸 | `_wt/levi2-x23stage`(**`origin/develop` 5fd8cc0 새 빌드** · `FKT_API_BASE=:8812` 로 구움) | `:8104` | 21328 |
| 게이트웨이 | `synthetic-gateway.mjs --evidence-keys evidenceIds,evidence,citations` (`--block-upgrade` 열도 실행) | `:8812` | 28564 |
| 게이트웨이(끔) | 같은 도구 · `--evidence-keys nosuchkey` = **역방향 대조군** | `:8811` | — |
| ai-api | 내 것(`c8bea88-x2`) | `:8103` | 17820 |

**selftest 3분기 — 🔴 파이프 없이 실제 종료 코드로 확인**
`--selftest` **rc 0**(PASS) · `--selftest --passthrough` **rc 1**(FAIL 이 정상) · `--selftest --evidence-keys nosuchkey` **rc 1**(FAIL 이 정상).

### X-23 (5열)

| 무대 | 자극 | 대체 동작 | 남은 흔적 | 시점 |
|---|---|---|---|---|
| ai-api 직결 | 없음(**대조군**) | — | `GET /api/runs/<id>` 근거 배열 2본 · **총 길이 4**(`candidates[0].evidenceIds` 3 · `[1]` 1) | 0s |
| 게이트웨이 **ON** | `evidenceIds,evidence,citations` 비움 | 같은 요청이 **총 길이 0** | **`snapshotRewritten Δ1`** · `lastSnapshotRewrite = {path:"/api/runs/<id>", arrays:[{$.candidates[0].evidenceIds 3→0},{$.candidates[1].evidenceIds 1→0}]}` | +1s |
| 게이트웨이 **끔**(`nosuchkey`) | 🔴 **빨강 확인** | 근거 **총 길이 4**(대조군과 동일) | — | +1s |
| 셸 화면 `/incidents/<inc>?run=<runId>` | 같은 ON 무대 | **근거 카드 19건 · 후보 2건 · 근거 링크 19건** | 화면 구간 경로 델타 `{snapshot 2, polling 1, wsProxied 0, wsBlocked 1}` | +6s |

- **자극은 실재했다** — 대조군 4 ≥ 1(「비어 있던 것을 비운 것이 아니다」) · ON 0 · **끔 열 4**(같은 실행).
- 🔴 **그런데 화면은 근거를 그렸다.** 브라우저가 **실제로 받은** 응답을 잡아 보니:

| 브라우저가 받은 응답 | 크기 | 남아 있던 근거 id | 키 |
|---|---|---|---|
| `GET /api/runs/<id>` | 879B | **3** | `evidenceIds` |
| `GET /api/runs/<id>/events` | 13,613B | **16** | `evidenceIds`, `evidence` |

  게이트웨이는 같은 창에서 스냅샷 2건·폴링 1건을 재작성했다고 세는데, **브라우저에 도착한 본문에는 근거가 그대로 있다.**
  ⇒ **화면이 읽는 근거의 출처가 내가 비운 그 응답이 아니다.** 그러므로 화면 축은 **초록도 빨강도 아니라 «미검증»**이다.
  🔴 **가설(미검증)** — 셸이 replay run 에 대해 **자기 정적 사본(`lib/static-replay`)** 으로 답한다면 상류를 비워도 화면은 안 변한다.
  이건 **다음 회차가 증명할 일**이지 지금 판정문에 사실로 적을 것이 아니다(가설과 실측을 섞지 않는다).

**[X-23 · API/스냅샷 축] PASS** · **[X-23 · 화면 축] 미검증 — 자극이 화면이 읽는 경로에 안 닿았다.**

### X-13 — 정본 원문 인용

> `| X-13 | 데이터 | 🔴 **근거가 없는 질문** | 「모른다」 · **지어내지 않는다** |` (`docs/plan/test-plan-v1.md:133`)

같은 무대에서 근거를 0 으로 만든 뒤 화면을 읽었으나, **위와 같은 이유로 화면에는 근거가 19건 그대로 있었다.**
즉 **「근거가 없는 상태」를 화면에서 만들지 못했다** → 「모른다」로 가는지, 「지어내는지」를 물을 무대가 서지 않았다.
**[X-13] «미검증» 유지** — 사유 = **자극(스냅샷/폴링 재작성)이 화면의 근거 출처에 닿지 않는다.**
(참고 값 · 판정 아님: 근거가 살아 있는 상태의 `candidate-rationale` 문면은 관측치를 인용하고 있었다.)

### 🔴 판정 축에서 뺀 것

- **`confidenceNote` 문장** — 상류 원문이 그대로 오는 **무대 인공물**이므로 이 판정문의 축이 아니다(오케 지시).

### 계측기 자수 (내 손)

1. **첫 판은 run 을 «내 node 세션»에서 만들고 브라우저로 열었다** → 화면이 「서버가 이 조사를 찾지 못했습니다 —
   다른 세션의 조사」라고 답했고, 그때의 「근거 카드 0건」은 **비워서 0 이 아니라 run 이 없어서 0** 이었다.
   하마터면 그 0 을 X-23 의 **PASS 로 적을 뻔했다.** run 을 **브라우저 안에서** 만들도록 고쳤다.
   **0 의 뜻은 그 창이 정한다.**
2. **「화면이 근거를 그렸다」를 곧바로 FAIL 로 찍었다**(2회차 출력). 브라우저가 받은 본문에 근거가 남아 있었으므로
   그것은 대상의 부정직이 아니라 **내 자극의 미도달**이다. 판정선을 «미검증»으로 정정했다.

### 안 잰 것 (이름과 함께)

- **진행 «중» run 의 WS/폴링 경로**(이번 열은 완료된 replay run) · **색인 있는 스택의 live run** ·
  **셸의 정적 사본이 실제로 답하는지**(위 가설) · 다른 엔진.

### 재현

```bash
node scripts/x-stages/synthetic-gateway.mjs --port 8812 --upstream 127.0.0.1:8103 \
     --evidence-keys evidenceIds,evidence,citations
node scripts/x-stages/synthetic-gateway.mjs --port 8811 --upstream 127.0.0.1:8103 --evidence-keys nosuchkey
FKT_API_BASE=http://127.0.0.1:8812 pnpm build && FKT_API_BASE=http://127.0.0.1:8812 pnpm exec next start -p 8104
node tests/web/t724x_evidence_zero.mjs --api=http://127.0.0.1:8103 --on=http://127.0.0.1:8812 \
     --off=http://127.0.0.1:8811 --shell=http://127.0.0.1:8104
```

---

## X-25 재열람 — 「못 닿은 화면이 사유를 사람 말로 말하는가」 (#588 `describeWhy` · D-51)

**리바이2 42대 · 2026-09-04 13:44~13:53 KST · 판정 = PASS** · 그물 `tests/web/t724x_unavailable_why.mjs` ·
무대 `tests/web/_x25_upstream_stage.mjs`(:8812 · 상류 `:8103`).

### 두 열 (손잡이 하나 = 셸 빌드)

| | 전 열 | 후 열 |
|---|---|---|
| 셸 | `:8107` · **`38c91c9`**(#588 «전») | `:8106` · `452de78`(#588 포함) |
| 산출물 검증 | `describeWhy` 문면 grep = **0건** | 문면·`data-why` grep = **적재됨** |
| BUILD_ID | `pTH611Rt060HhH960JMk7` | `iy7TUSpu8_xP-YIBdoRXm` |

🔴 **발주문의 「전 열 = `:8104`(`38c91c9`)」는 전언이었고 틀렸다.** `:8104` 가 쓰는 트리의 HEAD 는
**`596791d`(= #588 병합본)** 이라 **이미 수리가 실려 있었다**(`.next` 13:02:31 빌드 · 서버 13:03:00 기동 =
그 산출물을 읽는다). 그대로 썼으면 「전·후가 같다」를 대상의 사실로 적을 뻔했다. 그래서 `38c91c9` 를
따로 굽아 진짜 전 열을 세웠다. **인계된 sha 도 전언이다 — 산출물에서 심볼을 grep 해 확인한다.**

### 축별 실측 (열마다 무대 증인 델타 «먼저»)

| 열 | 무대 증인(통과·끊음·상태부여) | 전 열 문장 / `data-why` | 후 열 문장 / `data-why` |
|---|---|---|---|
| **정상(대조군)** | 22·0·0 | 안내 화면 **안 뜸**(h1 「① Factory Overview」) | 안내 화면 **안 뜸** |
| **소켓 끊김** | 2·**2**·0 | 「**TypeError**」 / **속성 없음** 🔴 | 「서버와 연결이 끊겼습니다. 마지막으로 받은 상태를 보여 드리고 있습니다.」 / `TypeError` ✓ |
| **HTTP 503** | 2·0·**2** | 「**HTTP 503**」 / 속성 없음 🔴 | 「서버 응답 오류(503)」 / `HTTP 503` ✓ |
| **HTTP 501** | 2·0·**2** | 「**미구현(501)**」 / 속성 없음 🔴 | 「아직 제공되지 않는 기능입니다(501).」 / `미구현(501)` ✓ |
| **불변 축** | — | `screen-unavailable`·`data-kind=unavailable` | **같음**(표시만 바뀌었다) |

전 열 rc=1(FAIL 6축) · 후 열 rc=0. **같은 그물·같은 무대에서 빨강이 실제로 났다** — 이 초록은 검출력 있는 초록이다.

### 🔴 무대 설계 — 반쪽 스텁이라야 자극이 산다

상류를 **통째로** 끊은 첫 판에서 `/overview` 는 안내 화면이 아니라 **307 → `/`** 로 튀었다(본문 82자·h1 없음).
관문 `/api/sessions` 까지 죽어 **화면을 그리기 «전에»** 되돌린 것이다 — 그때 내가 잰 것은 사유가 아니라 관문이다.
그래서 자극을 **판정하려는 그 호출에만** 넣도록 고쳤다(`only=/api/plants` 경로 접두사 · 관문·나머지는 그대로 흐른다).
**자극은 첫 검사 층에서 죽는다.**

### 안 잰 것 (이름과 함께)

- `TimeoutError`·`AbortError` 열 — 상류를 «느리게» 만드는 무대가 따로 필요(계약 타임아웃보다 긴 홀드).
- 「그 외(모르는 코드)」 열 — 폴백 문장 「서버에 닿지 못했습니다.」는 **미검증**.
- `/overview` 외 화면(`documents`·`evidence`·`incidents`·`work-orders`) · `not-found` 종류 · 브라우저측(클라이언트) 경로.

---

## X-22 강제 열 재검 — 「폴백이 포인터를 막는가」 (#590 `ec0547e` · D-52)

**리바이2 42대 · 2026-09-04 13:38~13:55 KST · 판정 = PASS(FAIL → PASS)** ·
그물 `tests/web/t724x_inert_fallback.mjs`(41대 것 그대로) + 회귀 `tests/web/t724x_inert_guard_teardown.mjs`(신규).

### 두 열 (손잡이 하나 = `tour-overlay.tsx` **1파일 35줄**)

`git diff --stat 596791d..452de78 -- apps/ services/ packages/` = **`tour-overlay.tsx` 1건뿐**.
전 열 `:8104`(`596791d`) · 후 열 `:8106`(`452de78`) — 두 열 모두 `FKT_API_BASE=:8812` 로 구웠다.

| 축 | 전 열(`596791d`) | 후 열(`452de78`) |
|---|---|---|
| 갈래 갈림(지원 true / 강제 false) | ✓ | ✓ |
| 🔴 자극 증인 — inert 걸린 요소 수 | 지원 **14** / 강제 **0** | 지원 **14** / 강제 **0** |
| 지원 열 클릭(대조군) | `/overview` 유지 = 막힘 | `/overview` 유지 = 막힘 |
| **강제 열 클릭** | `/overview` → **`/compare`** 🔴 **눌렸다** | `/overview` **유지** ✓ |
| 포커스 복귀(두 열) | 배경 포커스 → `button[tour-skip]` 로 되돌려짐 | 같음 |

### 🔴 회귀 축 — 처방이 «푸는 쪽»도 하는가 (규격 ⑤)

#590 은 **문서 전역에 캡처 단계 `pointerdown`·`click` 가드**를 단다. 정리에서 안 떨어지면 투어가 끝난 화면에서
**모든 클릭이 죽는다** — 원래 결함보다 큰 회귀다. 「막는 쪽」만 시험하면 이 축이 통째로 빈다.
강제 열에서 실측(후 열 `:8106`):

- 투어 «중» 클릭 `/overview` → `/overview` = **막힘**(수리 목적) ·
  건너뛰기 → 투어 꺼짐 ✓ · 투어 «후» 클릭 `/overview` → **`/compare` = 통과** ✓ ⇒ **PASS**.

### 안 잰 것 (이름과 함께)

- **실제 구형 엔진** — 여기서는 `delete HTMLElement.prototype.inert` 로 «조건»만 강제했다.
- **터치 축**(`touchstart`) · **키보드 활성화 클릭** · 투어를 «완주»로 끝낸 경우 · 첫 단계 외 다른 스텝.

### 계측기 자수 (내 손)

1. **인계된 「전 셸 = `38c91c9`」를 그대로 쓸 뻔했다** — 실제 트리는 `596791d`(#588 병합본)였다.
   X-22 에는 문제가 없었으나(#590 은 미포함), X-25 에는 **수리가 이미 실린 셸**이었다.
   포트→PID→커맨드라인→트리 HEAD→**산출물 심볼 grep** 까지 내려가서야 갈렸다.
2. **첫 X-25 판의 「안내 화면 안 뜸」을 대상의 빨강으로 적을 뻔했다** — 실제로는 307 리다이렉트(내 자극이 관문을
   죽였다). 화면만 봤으면 「수리가 TypeError 열에서 동작하지 않는다」로 회부했을 것이다.

### 재현

```bash
node tests/web/_x25_upstream_stage.mjs --port=8812 --upstream=http://127.0.0.1:8103   # 무대
# 후 열
FKT_API_BASE=http://127.0.0.1:8812 pnpm build && FKT_API_BASE=http://127.0.0.1:8812 pnpm exec next start -p 8106
# 전 열(X-25) = 38c91c9 를 같은 방식으로 구워 :8107
node tests/web/t724x_unavailable_why.mjs      --shell=http://127.0.0.1:8106   # PASS / :8107 이면 FAIL
node tests/web/t724x_inert_fallback.mjs       --shell=http://127.0.0.1:8106   # PASS / :8104 이면 FAIL
node tests/web/t724x_inert_guard_teardown.mjs --shell=http://127.0.0.1:8106   # PASS
```

### 무대 수정 후 재확인 (#593 CodeQL 2건 · 2026-09-04 13:53 KST)

CodeQL 이 무대 파일 `_x25_upstream_stage.mjs` 에 2건을 냈다 — ① `js/regex-injection`(high · `new RegExp(only)`
의 `only` 가 쿼리 값) ② `js/stack-trace-exposure`(medium · 502 본문의 `e.message`). 둘 다 실재하는 지적이라
① 정규식을 **만들지 않고** 경로 접두사 `startsWith` 로 ② 원문은 무대 로그(`console.error`)로만 내고 본문은
고정값으로 고쳤다. `upstreamErr` 계수는 그대로다 — 이 파일이 존재하는 이유는 「상류가 스스로 죽었다」와
「내가 끊었다」의 **구분**이지 문면이 아니다.

🔴 **무대를 고쳤으므로 판정을 다시 봤다 — 두 열 «모두».** 후 열만 보면 초록이 그대로라, 수정이 자극을
죽였어도 못 알아챈다. **빨강이 살아 있는지가 진짜 확인 축이다.**

| | 무대 증인(통과·끊음·상태) 정상/끊김/503/501 | rc |
|---|---|---|
| 후 열 `:8106` | 22·0·0 / 2·**2**·0 / 2·0·**2** / 2·0·**2** | **0**(PASS) |
| 전 열 `:8107` | 같음 | **1**(6축 빨강 그대로) |

수정 전과 **증인 수·판정 모두 동일**. 무대 수정은 동작을 바꾸지 않았다.
