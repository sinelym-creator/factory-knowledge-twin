# 무대 처분 — 의존 그래프와 정지 실측 (2026-09-06)

- 좌석: 검증(리바이2 53대) · 발주 `00:09` · 구독 **0** · 워크트리 **무접촉**(별 발주)
- 순서 = ① 의존 그래프 → ② 정지 «전» 회부·승인 → ③ 승인분만 정지 → ④ 전후 리스너 실측
- 근거 등급: **E1** = 내 손 실측 · **E3** = 소견

## 0. 계측 방법 (이 표가 무엇의 초록인지)

- 리스너 맵 = `psutil.net_connections(kind='inet')` 의 `LISTEN` 만 · 포트→pid.
- 프로세스 축 = pid 별 **`cwd` 직독**(명령줄은 보조) · `exe` · `cmdline` · **`environ()`**.
- 의존(역방향) = 「누가 이 포트를 보는가」를 **전 프로세스의 명령줄 «과» env 양쪽**에서 문자열 스캔.
- 🔴 **자기 계보(self + 조상 pid) 배제** · 🔴 **2.5초 간격 2표본의 교집합만 「산 것」**으로 센다.
  (내 셸이 스스로 잡혀 거짓 의존을 만든 전례가 있다.)
- 🔴 **명령줄만 봤으면 절반을 놓친다.** 웹콘솔은 런처 `cmd.exe /c set FKT_API_BASE=... && next start` 가
  포트를 명령줄에 갖지만, **실제로 도는 자식 `node` 는 그 값을 env 로만** 갖는다. env 를 안 읽으면
  「그 API 를 보는 것 없음」이라는 **거짓 초록**이 난다 — 이번에 2본이 그 자리였다.

## 1. 후보 7본 — 리스너 실측 (E1 · `00:11:21`)

발주 표의 pid 와 내 리스너 실측이 **7/7 일치**(MATCH).

| 포트 | pid | 프로세스 | cwd(워크트리 귀속) |
|---|---|---|---|
| 8190 | 22312 | `python -m uvicorn app.main:app --port 8190` | `~/repos/_wt/levi2-stage/services/ai-api` |
| 8811 | 35244 | `python -m uvicorn … --port 8811` | `~/repos/_wt/levi2-x23x13/services/ai-api` |
| 8813 | 43428 | `python -m uvicorn … --port 8813` | `~/repos/_wt/levi2-x23x13/services/ai-api` |
| 8815 | 36252 | `node … next start -p 8815` | `~/repos/_wt/levi2-x23c/apps/web-console` |
| 8817 | 5160 | `node … next start -p 8817` | `~/repos/_wt/levi2-o25v/apps/web-console` |
| 8830 | 36208 | `python -m uvicorn … --port 8830` | `~/repos/_wt/levi2-t51pr2/services/ai-api` |
| 8090 | 29872 | `python -m uvicorn … --port 8090` | `~/repos/_wt/senku2-t51/services/ai-api` |

**정지 금지 목록도 실측으로 확인**: `:8787`→44604 · `:8797`→45448 · `:8010`·`:8020`→25216·27296(Docker Desktop).
넷 다 후보 집합 **밖**이다.

## 2. 의존 그래프 (E1)

「보는 쪽 → 보이는 쪽」. 자기 부모(uvicorn 런처)와 자기 런처 `cmd.exe` 는 **소비자가 아니므로 제외**했다.

| 포트 | 이 포트를 보는 것 | 그 소비자가 후보인가 | 판단 |
|---|---|---|---|
| 8811 | `:8817`(5160 · env `FKT_API_BASE=…:8811`) | **예** | 짝을 이룬 후보 — 함께 내리면 반쪽이 안 남는다 |
| 8813 | `:8815`(36252 · env `FKT_API_BASE=…:8813`) | **예** | 위와 같다 |
| 8815 | 없음 | — | 종단(UI) |
| 8817 | 없음 | — | 종단(UI) |
| 8190 | 🔴 `:8820`(45120 · `~/repos/_wt/levi2-t76/apps/web-console`) | **아니오(목록 밖)** | **살림** — 내리면 목록에 없던 무대가 죽는다 |
| 8830 | 🔴 `:8831`(19988 · `~/repos/_wt/levi2-t51pr2/apps/web-console`) | **아니오(목록 밖)** | **보류** |
| 8090 | **없음**(아무도 안 본다) | — | **보류** — 사유는 의존이 아니라 **소유** |

- **{8811, 8813, 8815, 8817} 는 닫힌 집합**이다 — 이 넷 밖에서 넷 중 하나를 보는 것 **0건**.
- `:8090` 은 의존이 0이지만 cwd 가 **`~/repos/_wt/senku2-t51`(센쿠2 소유 트리)**이고, 자기 env 는
  production `:8787` 을 가리킨다. 🔴 **의존 없음 ≠ 처분 가.** 남의 좌석 자산은 내 판정 대상이 아니다.
- `:8830` 보류의 둘째 근거 = 좌석 재개점의 전대 인계 **「`t51pr2` 무접촉」**.

## 3. 컨테이너 대조 (E1)

`docker ps -a` 전건의 `Mounts.Source` **50줄 전수** 중 `_wt` 히트 **0건** —
후보 7 트리 어느 것도 컨테이너 바인드 볼륨에 걸려 있지 않다.
(「git 이 깨끗하다」와 「지워도 되는 게 없다」는 다른 사실이므로 마운트를 따로 센다.)

## 4. 정지 실행 — 승인분만 (E1)

승인(오케 `00:15`) = **5본 정지** `8811·8813·8815·8817·8090` · **살림** `8190` · **보류** `8830`.
`:8090` 은 의존이 0이었고 소유(센쿠2 트리) 때문에 내가 보류했던 것을, 오케가 **소유 확인을 자기 판정으로
대체**해 승인했다(폐하 `00:07` 승인 목록 안 · 워크트리 `~/repos/_wt/senku2-t51` 은 무접촉). 그 판정은 내 것이 아니다.

집행 = `Stop-Process -Force`(리스너 pid 5개) · 웹 콘솔 → API 순서. `00:16:46` 전건 `STOP_ISSUED`.

### 전후 리스너 실측 (같은 계측기로 3회)

| 포트 | 라벨 | PRE `00:16:37` | POST+5s `00:17:09` | POST+13s `00:17:18` |
|---|---|---|---|---|
| 8811 | STOP | 35244 | **NONE** | **NONE** |
| 8813 | STOP | 43428 | **NONE** | **NONE** |
| 8815 | STOP | 36252 | **NONE** | **NONE** |
| 8817 | STOP | 5160 | **NONE** | **NONE** |
| 8090 | STOP | 29872 | **NONE** | **NONE** |
| 8190 | KEEP | 22312 | 22312 | 22312 |
| 8830 | HOLD | 36208 | 36208 | 36208 |
| 8820 | KEEP 의 소비자 | 45120 | 45120 | 45120 |
| 8831 | HOLD 의 소비자 | 19988 | 19988 | 19988 |
| 8787 | production gw | 44604 | 44604 | 44604 |
| 8797 | develop gw | 45448 | 45448 | 45448 |
| 8010 / 8020 | production / develop api | 25216·27296 | 동일 | 동일 |

🔴 **표본을 두 번 뜬 이유** = uvicorn 계열은 감시 부모가 자식을 되살릴 수 있다. 「죽었다」를 한 시점으로
찍으면 **되살아난 뒤에 초록을 적을** 수 있다. +5s·+13s 두 시점 모두 `NONE` 이라야 정지다.

### 잔여 프로세스 — 감시 부모가 남았는가

정지한 5본의 부모·런처(`42560`·`38480`·`38744`·`9540`·`38504`)는 **전건 자진 종료**(GONE) —
고아 감시자 **0**. 내가 승인 밖 pid 를 죽인 것은 **0건**이다.
살아 있는 `23520`·`27016`(=`:8190`/`:8820` 계열)과 `32264`·`24724`(=`:8830`/`:8831` 계열)는
**살림·보류 대상의 것**이라 그대로 두었다.

### 거동 확인 — 「리스닝 없음」과 「죽었다」를 가르고, 살린 것은 «답하는가»로

| 대상 | 프로브 | 결과 |
|---|---|---|
| 정지 5본 | `GET /` | **전건 연결 거부**(curl `000`) |
| `:8820` (살린 것의 화면) | `GET /` | **200** |
| `:8831` (보류한 것의 화면) | `GET /` | **200** |
| `:8190` · `:8830` | `GET /api/health` | **200** |
| production `:8787` | 무토큰 `GET /health` | **401**(살아서 거절 = 생존) |
| develop `:8797` | `GET /health` | **200** |
| production `:8010` · develop `:8020` | `GET /api/health` | **200** |

🔴 **살린 것은 「리스너가 있다」가 아니라 「200 을 준다」로 확인했다.** 의존 하나를 잘못 끊었으면
`:8820` 이 리스닝은 유지한 채 502·에러 화면을 냈을 것이고, 포트 표만 봤으면 그것을 초록으로 적었다.

## 5. 자수 (내 계측기)

1. 처음 생존 확인을 `GET /health` 로 쳤고 `:8010`·`:8020`·`:8190`·`:8830` 이 **404** 를 냈다.
   대상의 결함이 아니라 **내가 route 를 틀린 것**이다(정답은 `/api/health`). 404 를 그대로 적었으면
   「승격 무대가 이상하다」는 없는 결함을 지어냈다.
2. 후속 측정 스크립트를 인라인으로 escape 하다 `SyntaxError` 를 냈다 — 대상과 무관한 내 손 실패다.
   측정은 파일로 떨어뜨린 스크립트로 다시 돌렸고, 그 사이 대상 상태는 바뀌지 않았다(+5s·+13s 두 표본).

