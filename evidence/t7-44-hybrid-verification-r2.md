# T7-44 재검 r2 — 슬롯 손실 수리 확인 (독립 검증 · 리바이2 53대)

- 무대 = develop `:8020`(`fkt-ai-api:dev-947f63d`) · 게이트웨이 `:8797` · production 무접촉
- 구독 소비 **정확히 3발**(승인분 전량 · 1발 확인 → 2발) · 대조군 = **`v0.5-d85`**(vector · 같은 시대)
- 등급 **E1** 내 손 · **E2** 대상 기록 · **E3** 소견 · 앞 회차 = `evidence/t7-44-hybrid-verification.md`

## 0. 판정 — **PASS**

| 판정선 | 문턱 | 실측 | 결과 |
|---|---|---|---|
| ① | `step.completed(vector).strategy=hybrid` 3/3 | hybrid · hybrid · hybrid | **PASS** |
| ②'-a | vector 단계 doc-chunk **5/5** | **5 · 5 · 5** | **PASS** |
| ②'-b | `runEvidenceCount` **≥ 23** | **23 · 23 · 23** | **PASS** |
| ②'-c | summary 「과수집 K · record M건 제외」 실물 | `인용 후보 5건(구조화 축 record 1건 제외 · 과수집 10)` 3/3 | **PASS** |
| ③ | `v0.5-d85` 대비 지표 1·5·6·7 하락 0 | 하락 **0** · m7 은 오히려 +1 | **PASS** |
| ④ | dup 0 · 게이트 16/16 | 0·0·0 · 16/16 | **PASS** |

**r1 의 FAIL 이 닫혔다.** 잃었던 `DOC-MAN-0021@r1#001` 이 돌아왔고 근거 수가 22 → 23 으로 복귀했다.

## 1. 자극 전 — 무대가 «수리»를 싣고 있는가

빌드 태그는 자기 신고다. 태그와 **심볼을 따로** 봤다.

| 축 | 값 | 방법 |
|---|---|---|
| `/api/health.build` | **`947f63d`** | 내 curl(E1) — 스크립트 상태 출력이 아니다 |
| 러닝 컨테이너의 수리 | `workflow.py:176 hybrid_search(..., top_k=TOP_K * 2)` · `:181 overfetched = len(hits)` · `:190 if strategy == "hybrid" and len(citations) >= TOP_K: break` · `:229 … 과수집 {overfetched}` | `docker exec` grep(E1) |
| 임베딩 | `loading` → **`ready`**(`01:22:12`) 뒤 발사 | E1 |
| 교정 게이트 | **16/16** `all 16 cells stand` · 구독 **0** | E1 |

## 2. 판정선 ①·②' — vector 단계 (E1)

| 열 | strategy | doc-chunk | summary | 합집합 |
|---|---|---|---|---|
| `v0.5-d85`(vector) | (키 없음) | 5 · 5 · 5 | `인용 후보 5건` | `#001` `#005` `#006` `0022#003` `0028#000` |
| `v0.5`(r1 hybrid · FAIL) | hybrid | **4 · 4 · 4** | `…4건(record 1건 제외)` | 위에서 **`#001` 빠짐** |
| **`v0.6`(r2 · 이번)** | **hybrid** | **5 · 5 · 5** | `…5건(record 1건 제외 · 과수집 10)` | **d85 와 완전 동일** |

```
v0.6 − d85 = []      (어휘 다리 «새» 청크 — 기록 축, 게이트 아님)
d85 − v0.6 = []      (소실 0 — r1 의 −1 이 사라졌다)
DOC-MAN-0021@r1#001 ∈ v0.6 = True
```

- 🔴 **수리의 주어를 확인했다**: 과수집(`TOP_K*2`) → record 제거 → **doc-chunk 를 TOP_K 개 채운 뒤 절단**.
  r1 에서 「필터는 옳고 **슬롯 예산**이 결함」이라 짚은 그 축이 그대로 닫혔다.
- 🔴 **자기 신고와 내 계수의 경계**: 「인용 후보 5건」은 내가 이벤트로 **독립 계수**해 일치를 확인했다(5/5/5).
  그러나 **「과수집 10」과 「record 1건 제외」는 교차 검증할 수 없다** — 걸러진 record 도, 소비되지 않은
  나머지 히트도 이벤트로 나오지 않는다. 이 두 수는 **대상의 자기 신고(E2)**로만 적는다(§5-1).

## 3. 판정선 ③ — `v0.5-d85` 대비 (E1)

| 지표 | d85(vector) | v0.5(r1) | **v0.6(r2)** | 하락? |
|---|---|---|---|---|
| 1 `m1.perId['SOP-BRG-INSP-014']` | T·T·T | **F**·T·T | **T·T·T** | **없음**(복귀) |
| 5 `m5_unsupported` | 0·0·0 | 0·0·0 | 0·0·0 | 없음 |
| 6 `m6.byId / byAlias` | 0/0 | 0/0 | 0/0 | 없음 |
| 7 `m7.requiredHit` | 2·2·1 | 2·1·1 | **2·2·2** | 없음(**+1**) |
| 근거 수 `runEvidenceCount` | 23·23·23 | 22·22·22 | **23·23·23** | 없음(복귀) |

- 채점기 교정 대조 `baselineStable=true`(21 축 · d85 기준) · `runsUsable` 3/3 · `excluded` 0.
- m7 이 `2·2·2` 로 올라간 것은 **주장하지 않는다** — n=3·질문 1건에서 `1→2` 한 칸은 흔들림과 구분되지 않는다(§5-2).

## 4. 기록 축 (게이트 아님 — 수치로만)

1. **어휘 다리로만 잡힌 새 청크 = 0건.** 수리 뒤에도 hybrid 의 어휘 축은 `GS-01`(`Q-MULTIHOP-001`)에서
   벡터 축이 이미 잡은 5건 밖의 청크를 상위로 올리지 못한다. 🔴 이것은 **수리의 실패가 아니다** —
   수리가 산 것은 「record 가 doc-chunk 칸을 먹지 않는다」이고, 그건 섰다. 어휘 축의 기여는
   **이 질문의 데이터 성질**이라 게이트에서 내려온 축이다(티켓 §3 개정 (b)).
2. **`DOC-MAN-0021@r1#001` 복귀 = True**(3 run 전부).
3. **vector 단계 `elapsedMs`**(센쿠2 미측 축):

   | 열 | 표본 | 중앙값 |
   |---|---|---|
   | d85(vector) | 109 · 70 · 49 | 70 |
   | v0.5(r1 hybrid) | 201 · 67 · 68 | 68 |
   | **v0.6(r2 hybrid + 과수집 2배)** | 144 · 85 · 58 | **85** |

   🔴 「과수집이 +15ms」라고 **읽지 않는다**. 각 열의 첫 run 이 셋 다 튄다(109·201·144 = 예열)
   — 표본 3에 예열 1이 섞인 분포라 **차이를 흔들림과 못 가른다**. 방향(2배 수집이 공짜는 아니다)만
   E3 로 적고, 값은 위 표본 그대로 남긴다.

## 5. 못 잰 것 (값이 아니라 이름으로)

1. **「과수집 10」·「record 1건 제외」의 교차 검증** — 이벤트에 나오지 않는 수라 자기 신고로만 있다.
   재려면 계약 표면을 넓혀야 하고(이 티켓 범위 밖), 지금은 **일치 여부를 물을 수 없는 축**이다.
2. **m7 `1→2` 상승의 성격** — 처방 덕인지 n=3 흔들림인지 못 가른다. d85 도 `2·2·1` 로 한 run 이 1이었다.
3. **검색 품질 향상량** — n=3 · 질문 1건이라 주장하지 않는다(앞 회차와 동일).
4. **오설정 기동 거부** — `Literal["vector","hybrid"]` 로 코드에서 확인했을 뿐, 잘못된 값으로 실제
   기동을 시도하지 않았다(무대를 죽이는 자극 · 승인 밖).
5. **게이트 칸 수의 시대차** — d85 report 의 `gateCells` 는 **15**, v0.5·v0.6 은 **16** 이다. 두 열 다
   «전건 통과»지만 **같은 16칸을 통과한 것은 아니다**. 「16/16」을 d85 에 소급해 적지 않는다.

## 6. 자수 (내 계측기)

1. 앞 회차에서 `payload.evidence` 를 리스트로 가정해 세 열이 전부 `0건`으로 나왔던 파서를, 이번엔
   **처음부터 단일 객체 + `payload.step` 필드 필터**로 짰다. 같은 함정을 두 번 밟지는 않았다.
2. 이번 회차에 **내 손으로 틀린 것은 없다**(위 §5 는 못 잰 것이지 틀린 것이 아니다). 다만
   §2 의 「과수집 10」을 초록의 근거로 쓰지 않았다는 점을 명시해 둔다 — 교차 검증이 안 되는 수는
   **판정선의 근거가 아니라 기록**이다.
