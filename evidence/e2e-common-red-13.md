# e2e 공통 빨강 — 주어 가르기 (리바이2 60대)

- 무대 = **`:8196` ↔ 리바이2 ↔ `_wt/levi2-e2e13`** (pid 30068 · cwd 실측 · develop tip `40cc534` 빌드본 · api base `:8020`)
- 창 = `date 13:27:51` ~ `13:42:02` · 구독 0
- 두 열은 **자극(워커 8) 먼저**, 대조(워커 1) 나중에 돌렸다 — 대조 열을 먼저 돌리면 그 열이 슬롯·캐시를 쥐어 자극 열의 조건을 먹는다.

## 0. 측정 모델

`N failed` 는 **내 러너가 센 수**다. 대상의 말은 각 테스트가 남긴 **expect 문면·status·`error.code`** 이고,
「무대 없음」류 문면은 **대상의 결함이 아니라 자극이 성립하지 않았다는 내 그물의 신고**다. 아래 표는 그 셋을 섞지 않는다.

## 1. 두 열

| 열 | 빨강 | 초록 | skip | 소요 |
|---|---|---|---|---|
| 워커 **8**(자극) | **25** | 112 | 5 | 3.0m |
| 워커 **1**(대조) | **11** | 127 | 4 | 9.3m |

- **공통(두 열 다 빨강) = 9**
- **8-only = 16** → 부하 조건(별건 · 이름 목록만)
- **1-only = 2** → 직렬 순서 의존 의심(`mode-badge:136`, `t3-2-screens:424`) · 이번 축 밖

🔴 총계를 옮기지 않았다. 25 와 11 은 **다른 조건에서 센 수**이고, 둘의 차 14 에는 이름이 없다.

## 2. 공통 9건 — 단독 재실행 1회씩

| # | 테스트 | 단독 | 대상의 말(문면) | 주어 |
|---|---|---|---|---|
| 1 | `mode-badge:24` 전이 마운트→REPLAY | **재현** | `data-mode` Expected `replay` / Received **`live`** | **실결함 후보** |
| 2 | `mode-badge:121` 응답 없음 → 미연결 | **재현** | Expected `unavailable` / Received **`live`** | **실결함 후보** |
| 3 | `shell:50` AppShell 4요소가 5화면 전부 | **재현** | `/overview 배너 슬롯` — element(s) not found | **실결함 후보** |
| 4 | `t3-2-screens:488` D-1 안내는 세션에 묶인다 | **재현** | 「?」를 눌러도 안 열린다 · count Expected 1 / Received 0 | **실결함 후보** |
| 5 | `t3-3-evidence:135` 계약 밖 kind 는 404 | **재현** | `data-kind` Expected `not-found` / Received **`unavailable`** | **실결함 후보** |
| 6 | `t42b-shell-axes:171` 큐 표시 | 재현 | **`run.queued 가 0 — 큐가 서지 않았다(자극 실패)`** | **무대 조건**(자극 미성립) |
| 7 | `t42b-shell-axes:208` 용량 거절 503 | 재현 | **`503 이 안 났다 — 용량 거절을 잴 무대가 없다`** | **무대 조건**(자극 미성립) |
| 8 | `t6-6-synthesis-pending:226` 합성 대기 | 재현 | **`무대 없음 — /api/live/status.online 이 false`** | **무대 조건**(게이트웨이 부재) |
| 9 | `t3-2-screens:326` 이벤트 상세 패널 | **단독 PASS(1.8s)** | — | **미분류 = 모른다** |

### 무대 축 실측(6·7·8 의 주어를 가른 값)

| 축 | 값 |
|---|---|
| `:8196 /api/live/status.online` | **false** (2표본 13:42:26 · 13:42:32) |
| 게이트웨이 `:8797` | **죽음**(curl rc 000) |

→ 6·7·8 의 빨강은 **그물 자신이 「무대 없음」이라고 적은 것**이다. 이건 FAIL 이 아니라 **exit 2 자리**이고,
대상 결함으로 세면 없는 결함을 짓는다. 게이트웨이가 선 무대에서 다시 물어야 판정이 생긴다.

🔴 **1·2 에 붙은 단서**: 같은 무대에서 `/api/live/status.online` 은 **false** 인데 배지는 **`live`** 를 말했다.
두 값이 어긋나므로 **화면 축의 실결함 후보**로 올린다. 다만 이 두 테스트는 라우트 모킹을 쓰므로,
「모킹이 안 걸렸다」와 「배지가 틀렸다」를 가르는 것은 **수리 발주 때 spec 의 모킹 줄부터** 봐야 한다 — 여기서 단정하지 않는다.

## 3. 판정

- **실결함 후보 N = 5** (1·2·3·4·5) — 부하와 무관하게 워커 1 에서도, 단독에서도 재현
- **부하 조건 M = 16** (8-only · 이름 목록은 `w8-only` 집합 · 이번 축 밖)
- **무대 조건 = 3** (6·7·8 · 게이트웨이 부재 · 판정 불성립)
- **미분류 K = 1** (9 · 전수에서 빨강, 단독에서 초록 — 「모른다」)
- 1-only 2건은 별도(직렬 순서 의존 의심)

🔴 **13 이라는 수는 이번 열에서 재현되지 않았다.** 인계된 13 은 다른 시점·다른 무대의 계수이고,
이번 조건에서 공통은 **9** 였다. 전대의 수를 그대로 옮기지 않는다.

## 4. 부하 조건 16건 (8-only · 이름만)

`t3-5-wo-screen` 7건(127·207·274·328·416·477·538) · `t3-2-screens` 2건(285·337) ·
`phase2-evidence` 2건(273·333) · `t6-6-synthesis-pending:365` · `t3-6-keyboard:222` ·
`t4-4-viewport-mobile:145` · `shell:63` · `mode-badge:97`.

워커 1 에서는 전건 초록이었다. **부하 자체가 결함이 아니라는 뜻은 아니다** — 「이 조건에서만 빨강」이라는 사실이고,
작업지시서 화면(`t3-5`) 7건이 한 파일에 몰린 것은 **세션·상태를 공유하는 화면일 가능성**을 가리킨다(가설 E4 · 미검증).

## 5. 내 계측기 자수

1. **정리 발주 집행 중 내가 팀 develop DB 를 내렸다**(D-92). 이 무대의 첫 `/api/health` 는 `degraded` 였고,
   그 상태로 전수를 돌렸으면 **빨강의 주어가 「DB 없음」**이 되어 부하 축을 못 갈랐다. 복구(13:27:16 `ok` · 59/59) 뒤에 열었다.
2. **게이트웨이 `:8797` 이 죽어 있다는 것을 전수 «전»에 재지 않았다.** 6·7·8 의 빨강을 보고 나서야 쟀다.
   판정 축이 무대 전제에 걸려 있으면 **창을 열기 전에** 그 전제부터 찍어야 한다.
3. 단독 재실행은 **1회씩**이다. 9번(전수 빨강·단독 초록)은 1회로는 「흔들림」과 「순서 의존」을 못 가른다 — 그래서 **미분류**다.
