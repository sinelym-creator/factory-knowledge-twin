# D-93b — spec 패치 적용 + 서버 축 1줄 (리바이2 60대)

- 무대 = **`:8196` ↔ 리바이2 ↔ `_wt/levi2-d93b`**(pid 5764 · cwd 실측 · develop `d320ced` prod 빌드 · api base `:8020`)
- 창 = `date 13:57:07` ~ `14:04:34` · 구독 **0**(모킹·정적 축만 · live 조사 0)
- 회부본 = `_handoff/senku2-d93.md`(센쿠2 54대) · 적용 대상 = `tests/**`(검증 좌석 scope)

## 0. 측정 모델

`N passed/failed` 는 **내 러너가 센 수**. 대상의 말은 **status·`error.code`·화면 문면**이다.
「skip」은 **미측**이지 초록이 아니다 — 아래 표에서 이름으로 구분한다.

## 1. 前後 (같은 무대 · 같은 명령 · 단독 1회씩)

| # | 테스트 | 前 | 後 | 주어 |
|---|---|---|---|---|
| ① | `mode-badge` 전이 마운트→REPLAY | FAIL | **PASS** | **spec 결함**(route glob) — 처방이 물었다 |
| ② | `mode-badge` 응답 없음 → 미연결 | FAIL | **PASS** | **spec 결함** 2겹(glob + 문면 리터럴) |
| ③ | `mode-badge` 모킹 없이 실제 ai-api | FAIL | **SKIP(사유 명시)** | **무대 조건** — 미측이지 FAIL 아님 |
| ④ | `shell:50` 배너 슬롯 | FAIL | FAIL(무변) | **무대 조건** — 이번 패치 대상 아님(회부 유지) |
| ⑤ | `t3-2-screens:488` 안내 재열기 | FAIL | FAIL(무변) | **주어 미확정** — 센쿠2 ④ 그대로 이월 |
| ⑥ | `t3-3-evidence` 계약 밖 kind | FAIL | **PASS** | **spec 결함**(id 가 계약 개정으로 «계약 안»이 됐다) |

**같은 파일 전수 회귀**: `mode-badge.spec.ts` = **4 passed · 1 skipped** · `t3-3-evidence.spec.ts` = **8 passed**. 새 빨강 0.

## 2. 적용한 패치 (tests/** 만 · apps/** 무접촉)

1. **route glob** `**/api/live/status` → `**/api/live/status**`
   세션이 선 뒤의 폴링은 `?sessionId=` 를 달고 나가 앞 형태를 **지나친다** — 배지 값을 실서버가 정했다.
2. **자극 도달 가드**(회부본의 권고) — `routeLive()` 가 히트를 세고, 판정 «전» `expectStimulusLanded()` 가
   `hits > 0` 을 묻는다. **0건 모킹은 언제나 「실서버가 답한 값」을 내므로**, 가드가 없으면 이 결함은 조용히 돌아온다.
3. **③ 의 무대 전제를 실측해 skip** — `/api/live/status.online` 이 true 면(게이트웨이 생존) 이 축은 성립하지 않는다.
   **빨강 대신 사유를 적고 건너뛴다**: 「못 쟀다」와 「틀렸다」는 다른 값이다.
4. **⑥ 의 id 교체** `GP-not-a-doc-chunk` → `ZZ-not-a-doc-chunk`(§3 참조).
5. **문면 리터럴 2건을 주장으로** — `"오류가 아닙니다"` → `/오류.{0,3}아닙니다/`(화면은 「오류**는** 아닙니다」) ·
   `"Q-34"`(원장 번호라 화면에 없다) → **사유를 말하는가** + **「서버에 닿지 못했습니다」로 아는 척하지 않는가**(D-68b).
   🔴 느슨하게 만든 게 아니라 **판정선을 낱말에서 주장으로 옮긴 것**이고, 그래서 §4 로 검출력을 다시 물었다.

## 3. ⓑ 서버 축 1줄 — `GET :8020/api/evidence/<계약 밖 id>`

**실측**(쿠키 없음 · 14:00:32)

| id | status | `error.code` |
|---|---|---|
| `GP-not-a-doc-chunk` | **401** | `session_required` |
| `GP-anything` | **401** | `session_required` |
| `ZZ-not-a-doc-chunk` | **404** | `not_found` |
| `MR-9999-0000` | **404** | `not_found` |
| `MR-2025-0087`(실재) | 200 | — |

→ **갈래를 가르는 변수는 「계약 밖인가」가 아니라 `GP-` 접두**다(한 손잡이만 바꾼 대조군 2행이 그것을 말한다).

**계약 대조**(`packages/contracts/rest-api-v0.1.md:221` · v0.1.17 append · 09-04):
> `GET /evidence/{evidenceId}` — `GP-` 접두 근거 해석 … 🔴 **소유권** … 그 run 이 요청 세션 것이 아니면 **404** · **쿠키 없음 = 401 `session_required`**

→ **서버는 계약대로 답했다.** `GP-` 는 **계약이 아는 갈래**(graph-path)가 됐고, 쿠키 없는 딥링크에 401 은 성문된 값이다.
**판정 = spec 기대값 결함**(서버 결함 아님 · 화면 결함 아님). 이 그물은 **계약 v0.1.17 이전에 고른 id** 를 계속 쓰고 있었다.
화면이 그 401 을 `unavailable` 로 적은 것도 「모르는 것을 아는 척하지 않는다」 쪽이다.

## 4. 처방이 검출력을 팔지 않았는가 (교정 칸 · 같은 무대)

위양성을 고치는 수정은 **전부 통과시키는 쪽**으로도 간다. 그래서 되돌려 물었다.

| 교정 | 조작(한 변수) | 결과 |
|---|---|---|
| A | glob 을 **옛 형태**로 되돌림 | `mode-badge.spec.ts` **2 failed** — 가드·판정선이 문다 |
| B | `OUT_OF_KIND` 를 **계약 안 실재 id**(`MR-2025-0087`)로 | **1 failed**(`data-kind` Expected `not-found`) |

둘 다 즉시 되돌렸다(`grep` 로 원상 확인). **초록에 검출력이 있다.**

## 5. 내 계측기 자수

1. **後 열 1차를 `file:line` 으로 잡았다가 「No tests found」를 받았다.** 내 패치가 줄을 밀었기 때문이다 —
   **판정 대상을 파일 위치로 지목하면 처방이 그 위치를 옮긴다.** 줄 번호를 다시 읽어 재실행했고, 그 사이의 빈 결과는 값이 아니다.
2. **③ 을 skip 으로 바꾼 것은 빨강 하나를 줄인다.** 그래서 **사유 문면을 판정 산출에 남겼다** —
   집계만 보면 「좋아졌다」로 읽히지만, 이 칸의 참값은 **미측**이다.
3. `mode-badge` 의 `online:true → LIVE` 행에는 **가드를 달지 않았다**(발주 범위 ①② 밖). 같은 결함이 그 행에 남아 있을 수 있다 — **안 본 자리**로 적어 둔다.
