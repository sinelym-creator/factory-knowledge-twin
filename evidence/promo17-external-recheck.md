# 승격 17 공개면 외부 재검 (`promo17ext`)

| | |
|---|---|
| 판정 | **PASS** — 축 ①②④⑤ 전건 · ② 착지 6/6 · ③ 실측값 병기(판정선 아님) · **대상 결함 0** · **자수 1**(내 그물 창) |
| 무대 | 공개면 `https://factory-knowledge-twin.vercel.app`(production) · **live run 0 · cap 0** |
| 밖의 근거 | **`remote_ip=64.29.17.3`**(공인 · `curl -w %{remote_ip}` · tailnet `100.x` 아님) · `Server: Vercel` · `X-Vercel-Id: icn1::bx2g9-…` · **Tailscale 헤더 0건** — 🔴 자극 «전»에 찍었다 |
| 배포 귀속(O-18) | 배포 `dpl_6KfnuHEt…` **meta `githubCommitSha` = `7fa6263eb808…`** = main **`7fa6263`** 일치 · `target=production` · `READY` · alias 에 공개 URL 포함 · 커밋 문면 = 「promote develop to main (17) — D-81 drawer focus trap, D-82 rail prefetch」 |
| 그물 | `o26_rail_prefetch_probe.mjs`(무대만 `--base`) · `d81_focus_trap.mjs` · `d82_nav_regression.mjs`(`--settle` 추가) · `promo17_reset_button_control.mjs`(신규 · 자수 대조군) |
| 측정 | 2026-09-05 13:36~13:41 KST |
| 검증자 | 리바이2 48대 |

---

## 1. 축 ① 레일 프리페치 = 판정선 (1280 · 새 컨텍스트 3회)

| 열 | 클릭 «전» INC `?_rsc=` 프리페치 | `307` | 착지 | `/enter` 응답 |
|---|---|---|---|---|
| 레일 1280 | **0 · 0 · 0** | **0** | 3/3 인시던트 | 2454 · 1593 · 1447 ms |
| 드로어 390 | **0 · 0 · 0** | **0** | 3/3 인시던트 | 1379 · 1415 · 3796 ms |

🔴 **이 초록의 뜻**: 공개면은 `/enter` 왕복이 **1.4~3.8초**라 처방이 없었다면 프리페치가 세션을 앞지르는 조건이
**3/3 에서 성립**한다(O-26 실측: 공개면 前 빌드 3/3 이 세션보다 앞섬 · 로컬 前 무대는 1/3 뿐이었다).
**그 조건에서 프리페치가 0** 이라는 것이 D-82 처방이 공개면에 실려 동작한다는 뜻이다.

## 2. 축 ② 착지 (보조 · 🔴 비결정)

레일 3/3 · 드로어 3/3 = **6/6**. 🔴 되돌림은 비결정(O-26 재현률 1/3)이라 **착지만으로는 처방 유무를 못 가른다** — 주축은 축 ①이다.

## 3. 축 ③ 첫 클릭 지연 — 공개면 실측값 (🔴 판정선 아님 · 다른 무대와 비교 금지)

| 열 | 클릭 → INC 응답(ms) |
|---|---|
| 레일 1280 | **303 · 314 · 312** |
| 드로어 390 | 291 · 301 · 295 |

로컬 무대(13~14ms)와 **비교하지 않는다** — 대륙 간 왕복이 포함된 다른 무대의 값이다(발주 명시).

## 4. 축 ④ D-81 드로어 초점 가둠 (390 · Tab 순환 1회)

| 열 | Tab 이탈 | Shift+Tab 이탈 | 닫힘 3갈래 | 닫은 뒤 초점 |
|---|---|---|---|---|
| **공개면(처방)** | **0** | **0** | 링크·Esc·스크림 전부 **닫힘 + 재개방** | `nav-menu-toggle` **복귀** |
| 前(로컬 `:8368` = `senku2-d79`) | **4** | 3 | — | — |

🔴 **前 열은 «계측기 생존 증명»으로 두었다** — 이탈 4건은 **D-79 기준선 4와 일치**한다. 즉 이 그물은 이탈이 있으면 실제로 빨강을 낸다. 무대가 다르므로(로컬) 그 열은 **판정이 아니라 대조**다.
스크림 소유자 = `nav-drawer-scrim`(button) — `tabIndex=-1` 이 스크림 클릭을 죽이지 않았다.

## 5. 축 ⑤ 헤더·콘솔·D-79 6칸

- 보안 헤더 4종 — CSP · `X-Content-Type-Options: nosniff` · `Referrer-Policy: no-referrer` · HSTS `max-age=31536000; includeSubDomains`.
- **콘솔 실오류 0**(WS 잡음도 0).
- D-79 6칸(앱바 · 상태행 · 드로어 형상 · 닫힘 3갈래 · 레일 · 가로 넘침) **전건 PASS**(`--settle 3500`).

## 6. 자수 1 — 내 그물의 창이 공개면 시계보다 짧았다

첫 실행에서 `a_bar390` 이 빨강이었다(390 에서 `reset-button` **0**). 회부하기 «전»에 주어를 갈랐다:

| 대조열(대기 2500ms) | `reset-button` |
|---|---|
| 390 cold(컨텍스트 첫 페이지) | **1** |
| 390 warm(두 번째 페이지) | **1** |
| 1280 cold | **1** |

**폭 탓도 «첫 페이지» 탓도 아니었다** — 내 그물이 goto 뒤 **1500ms**만 기다렸는데, 공개면은 `/enter` 왕복이 **1.4~3.8초**라 **세션이 서기 전 화면**을 찍었다(리셋은 `{session && …}` 조건부). 그물은 로컬 무대(≈100ms)에서 눈금이 잡혀 있었다.
처방: 대기창을 **`--settle` 값으로 선언**하게 고치고(3500ms) 재측정 → **6칸 전건 PASS**. 🔴 **창은 무대의 시계로 정한다.**

## 7. 못 잰 것 (이름으로)

- **라이브 run 축 0** — 발주대로 cap 0 이라 배지·되감기·이벤트 축은 이 창이 말하지 않는다(승격 16 재검이 마지막 실측).
- **재방문(쿠키 보유) 진입** — 모든 회차가 새 컨텍스트다.
- **`prefetch={false}` 사정거리 밖 링크**(본문·카드 링크)는 재지 않았다.
- **前 열의 공개면 판**은 없다(승격 전 공개면은 이미 사라졌다) — 前 대조는 로컬 무대다.
