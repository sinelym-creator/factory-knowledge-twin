# O-12 독립 검증 판정문 — 「누가 당신을 아는가」를 주장하는 칩은 하나뿐

> 검증 좌석(리바이2 44대) · 2026-09-04 · 발주 = 스자쿠 38대 19:17
> 대상 = PR **#634** `b69d59a` · 대조군 = **`685f47b`**(그 lane 출발점)
> 그물 = `tests/web/o12_chips.mjs` · 대상 변경 = `components/app-shell.tsx` **1파일**(+7 −2)
> 발단 = **폐하 공개면 스크린샷**(19:26 · 「정적 · 609b 열람 1」과 「내 세션 tA_G」가 나란히)

## 판정 — **PASS**

## 0. 무대

| | 대상 | 대조군 |
|---|---|---|
| 셸 | `127.0.0.1:8168` @ `b69d59a`(`.next/BUILD_ID A6E6ELCZUe46tT18uOxXu`) | `127.0.0.1:8169` @ `685f47b`(`4E0T0JFVjSQ4oKKNlTDc_`) |
| ai-api | 둘 다 `:8152` — 손잡이는 셸 빌드 하나 | 같음 |
| 무대 울림 | 정적 화면 본문 6086자 · `mode-badge` 1 | 6111자 · 1 |

## 1. 열별 실측 (자극 순서 = `/overview`(세션 발급) → **정적 주소** `/incidents/INC-2026-014?run=STATIC-GS-01`)

| 열 | 쿠키 | 정적 칩 | 세션 칩 | 판정 |
|---|---|---|---|---|
| **대상 · 세션 있음 + 정적** | `fkt_sid`,`fkt_session` | **0** | **1**(「내 세션 DngD」) | **PASS** |
| **대상 · 세션 없음(직행)** | **`[]`** | **1** | **0** | **PASS** |
| **대조군 · 세션 있음 + 정적** | `fkt_sid`,`fkt_session` | 🔴 **1** | 🔴 **1** | 증상 재현 |
| 대조군 · 세션 없음(직행) | `[]` | 1 | 0 | (양쪽 같음) |

- 🔴 **대조군이 폐하가 보신 그 화면을 재현한다** — 두 칩이 동시에 선다. 대상에서만 정적 칩이 사라진다.
- 🔴 **자극이 실재했는지 먼저 확인했다** — 「세션 있음」 열의 쿠키가 실제로 `fkt_session` 을 갖는다.
  정적 주소로 **직행**하면 쿠키가 `[]` 라 두 칩이 겹칠 자리 자체가 없다(그 회차의 초록은 판정력이 없다).
- **390 앱바 넘침 0**(대상) · **콘솔 0**(양쪽).

## 2. 자수 (내 계측기)

1. 🔴 1차 회차의 콘솔 빨강 1건(`405 Method Not Allowed`)은 **내 그물이 `/enter` 를 GET 한 것**이다.
   `/enter` 는 GET 이 405 이고, 세션은 `/overview` 진입에서 발급된다(쿠키로 확인). 경로에서 빼고
   다시 돌려 **콘솔 0** 을 확인했다 — 그 빨강은 대상의 것이 아니었다.

## 3. 안 잰 것 (이름으로)

1. **정적 칩이 사라진 뒤의 «정적 기능»** — 열람 계수·「리셋」 손잡이가 세션 보유 방문자에게 필요한지는
   이 티켓의 축이 아니라 판정하지 않았다. 지금은 **정적 칩과 함께 그 손잡이도 안 보인다**(구조상 같은 컴포넌트).
2. **공개면 실측** — 이 판정은 **로컬 두 빌드**의 것이다. 폐하가 보신 화면은 승격 전 배포본이며,
   공개면에서 사라지는지는 **승격 뒤 재검**이 답한다.
3. 다른 정적 주소(근거 화면 `?run=STATIC-GS-01`) · 폭 1440·390 외.

## 4. 재현

```
# 무대 : 각 트리에서 FKT_API_BASE=http://127.0.0.1:8152 pnpm build → npx next start -p 8168 / 8169
cd tests/web && npm install
MSYS_NO_PATHCONV=1 node o12_chips.mjs --base http://127.0.0.1:8168 --out C:/…/tgt.json
MSYS_NO_PATHCONV=1 node o12_chips.mjs --base http://127.0.0.1:8169 --out C:/…/ctl.json
```
