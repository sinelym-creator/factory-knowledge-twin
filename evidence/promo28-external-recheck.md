# 승격 28 — 공개면 외부 재검 (리바이2 60대)

- 창 = `date 16:01:26` ~ `16:03:48` · 구독 **0** · 무대 0(밖에서만) · production 읽기만
- 배포 = `dpl_6TkwKWgsJ5E9rTCkSp3sBpyxYjXq` · commit **`81aec01`**(#949) · target production · **ready 16:01:13**
- alias 실측 = `factory-knowledge-twin.vercel.app` 이 이 배포에 붙어 있다(배포 레코드)

## 0. 🔴 前 칸은 없다 — 내 「기준선」이 이미 後였다

16:01:26 에 찍은 「前」은 **ready(16:01:13) 뒤 13초**다. 즉 그 값은 前이 아니라 **두 번째 後**다.
증거: 前/後라 부른 두 응답의 **Next buildId 가 같다**(`clGD_BqAgwxku4F2cGMTm`) · 청크 목록 지문도 동일(`31becbab0b1f`).
**그러므로 이 재검에는 전이의 前 칸이 없다**(미측). 「전이했다」는 내 두 응답이 아니라 **아래 §1 의 두 근거**가 말한다.

## 1. ① 전이 — 새 배포가 공개면에 «올라와 있는가»

| 근거 | 값 |
|---|---|
| 배포 레코드 | `81aec01` · target production · **alias 에 공개 도메인 포함** |
| `/` SSR | **200** · 18,827B · `X-Vercel-Cache: MISS`(캐시본 아님) · `X-Vercel-Id: icn1::iad1::…` |
| 🔴 **처방 심볼이 실제로 서빙되는가** | 루트가 부르는 청크 **10본을 받아 grep** → **`fkt:tour-open` 이 1본에 존재** |

→ D-95(투어 래치)의 심볼이 **공개면이 내려주는 코드 안에** 있다. 「병합됐다」가 아니라 **「올라갔다」**의 근거다.

## 2. ② `/api/health build` — 두 vantage

| vantage | 값 |
|---|---|
| A 직결(내 머신 · `icn1` 입구) | **`30274ba`** |
| B 밖 경유(`r.jina.ai`) | **`30274ba`** |

→ **불변이 정상**이다(ai-api 컨테이너 값 · 이번 승격도 web 축). 두 vantage 가 같은 값을 준다.

## 3. ③ 작업지시서 종단(배지·출구) — **미측**

| 축 | 값 |
|---|---|
| `/work-orders/<임의>` 무세션 | **307**(셸 가드) |
| 루트 청크 10본 grep `wo-final` | **0본** |

🔴 **이 0 을 「처방이 없다」로 읽으면 안 된다.** 작업지시서 화면은 **다른 라우트의 청크**로 오고, 그 라우트는
**세션 없이는 열리지 않는다**(307). 내가 받은 10본은 **루트가 부르는 것들**뿐이다 — 즉 **안 본 자리**다.
E-7v 독검(#945)에서 같은 처방을 `6eafdc7` 무대로 **前後 실측해 PASS** 했고, 그 코드가 `81aec01` 에 실려 있다(#942 병합분).
공개면 실물 렌더 확인은 **세션이 있는 눈**(폐하 실기기 또는 세션 발급 축)이 필요하다 — **미측으로 남긴다**.

## 4. ④ 투어 직행 클릭(412×600) — 1/1 열림

| 축 | 값 |
|---|---|
| `/overview` 진입 | 셸 가드가 **`/`** 로 보냄(공개면 무세션의 정상 거동) |
| `intro-reopen` 버튼 | **존재**(1) |
| 클릭 → `intro-card` | **열림(1/1)** · **5,383ms** |

→ 열린다. 🔴 다만 **5.4초**다(내 로컬 무대는 15~22ms). 네트워크 + 콜드 람다로 설명되지만 **재보지 않았으므로 원인은 주장하지 않는다** — 값만 남긴다.

## 5. ⑤ 문 생존

| 축 | 값 | 읽기 |
|---|---|---|
| `GET /api/evidence/GP-…` 무쿠키 | **401** `session_required` | 계약 `rest-api-v0.1.md:221` 대로 |
| D-87 `/openapi.json` · `/docs` · `/redoc` | **307** | 셸 가드가 먼저 막음(문서면 미제공 · 단 **D-87 처방은 이 열에서 시험되지 않음**) |
| D-87 `/api/docs` | **404** | **ai-api 까지 닿아 404** = 처방이 실제로 돈 칸 |

## 6. 판정 — **조건부 PASS**

- 선 축: ① 전이(심볼 grep) · ② health 두 vantage 불변 · ④ 투어 열림 1/1 · ⑤ 문 401 + D-87 404 1칸
- **미측 2**: ③ 작업지시서 종단 실물 렌더(세션 필요) · 전이의 **前 칸**(창이 이미 지나 있었다)
- 대상 결함 **0**

## 7. 내 계측기 자수

1. **「前 baseline 을 먼저 찍었다」고 보고했는데, 그 시각은 이미 ready 뒤였다.** 배포 레코드의 `ready` 를 먼저 읽었어야 했다 — **전제를 확인하기 전에 잰 값은 이름부터 틀린다.**
2. **루트 청크의 `wo-final` 0 을 결함으로 회부할 뻔했다.** 내가 받은 청크는 루트의 것뿐이다 — **안 본 자리와 없는 것은 다르다.**
3. ④ 는 **1회**다. 5.4초의 원인도 재지 않았다.
