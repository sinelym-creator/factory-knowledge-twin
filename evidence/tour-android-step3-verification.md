# 투어 4/9 걸음 — 안드로이드 폰 재측 (긴급)

- **검증 좌석** 리바이2 58대 · 발주 = 오케 긴급 재측(운영자 실기기 화면 착신)
- **증상 신고** `:8788`(#903 판) 안드로이드 폰에서 화면 「4/9」 걸음의 대상이 화면 아래에 있고 콜아웃만 떠 있음
- **무대** 셸 `:8195` · 트리 `ede14a0`(승격 대상 `cafb658` 과 `apps/`·`services/`·`packages/`·`data/` diff **0**) · API `:8020` · production 무접촉 · 구독 0
- **기기** Pixel 7 UA · `deviceScaleFactor` 2.625 · `isMobile`/`hasTouch` true
- **측정 모델** 값은 대상의 것이다. 내 러너가 틀린 자리는 자수 절에 적었다.

## 판정 — tip 트리에서 증상 미재현 (E1)

🔴 **칸마다 「같은 걸음인가」부터 찍었다**: 4칸 전부 제목 **「다섯 단계가 실제로 지나간 자리」**(index 3 · `timeline`) · 링 높이 **527 동일**.

| vp | mode | ring top | bottom | ring h | scrollY | `top ≥ 16` |
|---|---|---|---|---|---|---|
| 412×915 | direct | **246** | 772 | 527 | 316 | PASS |
| 412×915 | walk | **194** | 721 | 527 | 397 | PASS |
| 412×600 | direct | **89** | **615** | 527 | 473 | PASS |
| 412×600 | walk | **36** | 563 | 527 | 555 | PASS |

- `cells=4 fail=0 unmeasured=0`
- **정착 값과 +3초 값이 전 칸 동일** — 늦게 끌어당기거나 밀려나는 움직임 **0**.
- 폐하 경로(walk) 열의 진행 자취: `tour-next → tour-next → **tour-goto**`. index 2(`start`)가 link 걸음이라 `tour-next` 가 아니라 `tour-goto` 로 넘어간다는 회부가 **실측으로 확인**됐다.

⇒ 신고된 「스크롤 미추종 · 콜아웃만 뜸」은 **이 트리에서 재현되지 않는다**. `:8788` 은 **#903 판(FIX2 이전)** 이므로 그 화면과 정합한다.

## 잔여 1칸 — D-89 (P2 · 승격 26 비차단)

412×600 **direct** 는 링(527)이 뷰포트(600)에 **들어가는** 걸음인데 **bottom 615 > 600** 이다. 판정선의 「들어가는 걸음은 `bottom ≤ vh` 병기」 기준으로 **하단 15px 초과**.

- 같은 뷰포트의 **walk 열은 563 ≤ 600 으로 충족** — **경로에 따라 갈린다**.
- 오케 판정: **D-89 로 등재 · P2 · 승격 26 비차단** · 시계열 진단 뒤 다음 승격에서 수리.

## 자수

🔴 **자수 15 — 다른 걸음을 재고 초록을 낼 뻔했다.** 1회차에서 walk 열의 링 높이가 **60** 이었다(direct 열은 **527**). `tour-goto` 를 셀렉터에 안 넣어 index 2 에서 멈춘 채로, 그 자리의 작은 요소를 재고 **PASS** 를 찍었다. 걸음 **제목**과 **링 높이**를 열마다 찍자 갈렸다.

⇒ **모양이 같아야 같은 걸음이다.** 두 열을 나란히 둘 때는 판정값 옆에 **정체를 말하는 값**(제목·치수)을 함께 적어야 한다. 값만 보면 「둘 다 초록」으로 접힌다.

그물 = `tests/web/tour_android_step3.mjs`(두 뷰포트 × 두 경로 · 걸음 자취와 제목을 함께 출력).
