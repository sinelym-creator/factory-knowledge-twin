# D-64 독립 검증 판정문 — 느린 답을 「미연결」이라 부르지 않는다

> 검증 좌석(리바이2 44대) · 2026-09-04 · 발주 = 스자쿠 38대 19:36
> 대상 = PR **#637** `70b6f14` · 대조군 = **`f70835f`**
> 발단 = **폐하 공개면 실측**(19:26 · 근거 화면에서 돌아가려다 「응답 시간 초과」)
> 그물 = `tests/web/d64_badge_timeline.mjs`(배지 시간축) · 무대 도구 = `tests/web/_delay_proxy.mjs`

## 판정 — **PASS**(축 ①~⑤)

## 0. 무대 — 손잡이는 «지연» 하나

| | 대상 | 대조군 |
|---|---|---|
| 셸 | `127.0.0.1:8176` @ `70b6f14`(`BUILD_ID XI4ipjs1ZdThydWhYBNQ6`) | `127.0.0.1:8177` @ `f70835f`(`vX_8mqdPhrWGYDWNi-5Oc`) |
| API 통로 | **둘 다 지연 프록시 `:8175`** → ai-api `:8152` | 같음 |
| 프록시 | `/api/live/status` 만 +N ms · **다른 경로는 통과**(실측: `/api/health` 0.049s vs `/api/live/status` 3.013s) | 같음 |

🔴 **끊지 않고 «붙잡았다»** — 연결을 끊으면 그건 다른 자극(연결 실패)이고 다른 화면을 낸다.
🔴 **배지를 1초 간격으로 찍었다** — 이 축은 한 시점의 값이 아니라 **전이**다. 끝점 두 개로는
「지나가는 확인 중」과 「끝내 미연결」을 못 가른다.

## 1. 축별 실측

| 축 | 자극 | 대상 `70b6f14` | 대조군 `f70835f` | 판정 |
|---|---|---|---|---|
| ① 3초 지연 | `+3000ms` · 20초 창 | 확인 중 → **`◑ REPLAY`(4.2s)** · **미연결 0회** | 확인 중 → **`◌ 미연결`(3.2s)** | **PASS** |
| ② 7초 지연 | `+7000ms` · 60초 창 | 확인 중 **유지** → **`◌ 미연결`(36.6s)** · 호출 4회 | **`◌ 미연결`(3.2s)** · 호출 2회 | **PASS** |
| ③ 복귀 전이 | `+7000ms` → t≈13s 에 **지연 0** · 50초 창 | 확인 중 → **`◑ REPLAY`(30.5s)** · **미연결 0회** | (해당 없음) | **PASS** |
| ④ 시한 소비자 | 정적 grep | `TIMEOUT_MS = 2000` **불변**(대조군과 같은 줄·같은 값) · `LIVE_STATUS_TIMEOUT_MS = 6000` 은 `liveStatus()` 안에서만 · 그 함수 소비자 = `components/live-status.tsx:164` **한 곳** | `LIVE_STATUS_TIMEOUT_MS` **부재** | **PASS** |
| ⑤ 정적 화면 + 세션 | 정적 주소 · 20초 창 | 배지가 정적 화면에서도 **확인 중 → REPLAY** 로 갱신 · `/api/live/status` **3건** | **동일**(3건 · 같은 전이) | 가설 ⓐ **참 · D-64 무관** |

- **②의 36.6초** — 발주 문면의 「40초+」보다 이르다. 규칙과 맞다: **첫 회차 시한 초과(6s) → 폴링 30s → 2회차 초과 = 36s**.
  🔴 **발주 문면이 아니라 실측이 정본**이다(≈36.6s).
- **①과 ②의 대조군 값이 같다(3.2s)** — 대조군은 2초 시한이라 3초든 7초든 첫 회차에서 내려간다. 그 «같음»이 처방의 자리를 보여 준다.

## 2. 외부 vantage 분포 (D-64 근거 · 대조군 열 · 19:37~38 실측)

🔴 **좌표 정정** — `…-git-main-…vercel.app`·`…-limyounguk-s-projects.vercel.app` 은 **Vercel SSO 보호**(302 → `vercel.com/sso-api`)라 공개면이 아니다.
공개로 200 을 주는 주소는 **`https://factory-knowledge-twin.vercel.app`** (연결 IP `216.198.79.195`).

| 값 | 실측(n=20 · 전건 200) |
|---|---|
| p50 | **0.461s** |
| p95 | **1.670s** |
| max / min | 1.670s / 0.443s |
| **2초 초과** | **0 / 20** |
| 🔴 **콜드 첫 호출** | **2.784s** — **2초 시한 초과** |

⇒ 따뜻할 때는 2초를 안 넘고, **콜드 첫 호출이 넘는다**. 「화면 진입 첫 호출이 그 회차였다」는 진단과
어긋나지 않는다. 🔴 **이 값은 유선 PC vantage** 다 — 폐하 폰(모바일망) 경로는 **못 쟀다**.

🔴 **6초 초과 회차가 실재한다(전언 1건)** — 오케 순찰 실측 19:50:39: 같은 공개 엔드포인트가 **1회 7초**
(직후 3회 0.6초). 🔴 **내가 잰 값이 아니다**(내 20회의 max 는 1.670s). 그 회차가 참이면 **6초 시한도
넘는 회차가 있다**는 뜻이고, D-64 는 「그런 회차 «한 번»으로는 미연결로 내려가지 않는다」를 지킨다
(축 ② 실측: 7초 지연에서 첫 회차는 「확인 중」 유지) — 즉 **시한 값이 아니라 «연속 2회» 규칙이 그 회차를 막는다**.

## 3. 자수 (내 계측기)

1. **축 ⑤ 의 호출 3건에는 seed 화면(`/overview`)의 호출이 섞여 있다** — 내 그물은 `t0` 이후 전부를 센다.
   「정적 화면 «만»의 호출 수」는 이 회차로 말할 수 없다. 다만 **정적 화면에서 배지가 갱신되는 것**은
   그 화면에서 폴링이 돈다는 증인이다(확인 중 → REPLAY 전이).
2. 축 ③ 의 프록시 교체 사이 **몇 초 동안 포트가 비었다**(연결 실패 회차가 섞였을 수 있다). 판정에는
   영향이 없다(그 회차도 「못 받음」이고, 대상은 그래도 미연결로 내려가지 않았다).

## 4. 안 잰 것 (이름으로)

1. **모바일망 vantage** — 폐하가 겪은 경로. 유선 PC 로만 쟀다.
2. **콜드 경로의 화면 축** — 콜드 2.784s 는 `curl` 값이고, 그 순간 화면 배지가 무엇이었는지는 안 봤다.
3. **6초를 넘는 «첫» 회차가 두 번 연속인 실제 공개면 회차** — 로컬 프록시로만 만들었다.
4. **`LIVE_FIRST_TIMEOUT_MS`(콜드) 경로** — `Math.max` 로 6000 과 비교되지만 그 갈래는 안 밟았다.

## 5. 재현

```
# 무대 : 각 트리에서 FKT_API_BASE=http://127.0.0.1:8175 pnpm build → npx next start -p 8176 / 8177
node tests/web/_delay_proxy.mjs --port 8175 --upstream http://127.0.0.1:8152 --delay-ms 3000
node tests/web/d64_badge_timeline.mjs --base http://127.0.0.1:8176 --out C:/…/t3.json --seconds 20
node tests/web/d64_badge_timeline.mjs --base http://127.0.0.1:8177 --out C:/…/c3.json --seconds 20
#   지연을 7000 으로 바꿔 60초 창 · 도중 0 으로 교체해 복귀 전이
```
