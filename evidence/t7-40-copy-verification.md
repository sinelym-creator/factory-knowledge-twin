# T7-40 독립 검증 — 방문자 문구 존댓말화 (리바이2 43대)

- **대상** = PR #606 `lane/senku2-d54` head **`a1ef9f7`** · **대조군** = `origin/develop` **`db94d8b`**
- **판정선** = 발주 원장 T7-40 행 ①~④ · 투어 규격 §⑧-7 · 근거 등급 **E1**
- 산출 = `evidence/t740/target.json` · `evidence/t740/control.json` · 그물 `tests/web/t740_copy_scan.mjs`

## 0. 무대와 귀속 (양방향)

| | 값 |
|---|---|
| 대상 셸 | `:8130` · `a1ef9f7` 체크아웃 · BUILD_ID **`M6YkTvcd4JXCDaBuFdwjh`** |
| 대조군 셸 | `:8131` · `db94d8b` 체크아웃 · BUILD_ID **`4ML9LQQmroAjqiDR5dJ1s`** |
| 상류 | 둘 다 로컬 ai-api `:8118`(`0c2bc38` 트리 · 자기신고 `build:0c2bc38-levi2-43`) |
| 적재 역확인 | 옛 문구 「세션 안에서 열렸다」 — 대조군 산출물 **4청크**, 대상 **0** |

## 1. 축 ① 금지 패턴 — **PASS**(단, 판정력이 닿는 화면에서만)

정규식은 발주문 문자열을 **그대로** 썼다: `이다\.|한다\.|둔다\.|없다\.|아니다\.|«|»|계약 v0\.|원장|Q-\d|D-\d|T\d-`

| 화면 | 대상 히트 | 대조군 히트 | 이 화면의 판정력 |
|---|---|---|---|
| overview | 0 | **0** | 🔴 **없음** — 대조군이 안 울었다 |
| incident-run | 0 | **0** | 🔴 **없음** |
| evidence | **0** | **2** | 있음 ✅ |
| tour-step-1 | 0 | 0 | 없음 |
| tour-step-2 | **0** | **1** | 있음 ✅ |
| tour-step-3 | 0 | 0 | 없음 |
| **합계** | **0** (rc=0) | **3** (rc=1) | — |

대조군이 문 3건(= 이 그물이 실제로 무는 증거):
- `evidence` / `record-view` — 「이 근거에는 revision·색인 축이 «없다» … 계약 v0.1.1」
- `evidence` / `record-view` — 「… 이 레코드 자체가 SSOT 라는 사실이 답한다.」
- `tour-step-2` / `tour-title` — 「알람은 «울린 행»의 값이 앵커다」(대조표 22행)

🔴 **「전 화면 0」을 「전 화면 통과」로 읽지 말 것.** overview·incident-run·tour 1·3 은 **대조군도 0** 이라
이 그물이 그 화면에 대해 아무 말도 못 한다. 판정이 선 화면은 **evidence·tour-step-2 둘뿐**이다.

**제외 처리(발주 요구: selector 로 명시)** — 문서 본문 = `[data-testid="cited-body"]`.
🔴 **제외는 «삭제»가 아니라 «표시»로 했다** — 모든 히트를 담고 `excluded` 로 갈라 셌다.
이번 회차 **제외 노드 0 · 제외 히트 0** ⇒ 🔴 **제외 축은 시험되지 않았다**(`cited-body` 가 밟은 화면에 없었다).

## 2. 축 ② 값 불변 — **PASS**

| 집합 | 결과 |
|---|---|
| `data-testid` | **동일** |
| `data-why` | 동일(둘 다 `[]`) |
| `data-kind` | 동일(`["record"]`) |
| `data-mode` | 동일(`["live","replay"]`) |
| `data-runcap-*` | 동일(둘 다 `[]` — 상류가 옛 빌드라 애초에 안 뜬다) |
| `data-tour-placement` | 동일(`["anchor","before"]`) |
| `data-tour-clear` | 동일(`["yes"]`) |

## 3. 축 ③ 기존 e2e — `shell.spec.ts` **8/9** · 실패 1건은 **내 무대 탓**

| 열 | 결과 |
|---|---|
| 대상 `:8130` | 8 passed · **1 failed** (`배너 슬롯은 «조건부»다` :63) |
| 대조군 `:8131` | 8 passed · **1 failed** (**같은 테스트 · 같은 단언**) |

🔴 **두 열이 «똑같이» 빨강이면 그건 대상의 답이 아니다.** 실패 단언은
`getByTestId('fallback-banner') Expected 0 / Received 1` — 그 테스트는 임의 세션 id(`api:abcd1234efgh`)를
쿠키에 심는데, 내 ai-api 는 그 id 를 모르므로 셸이 폴백 배너를 그린다. **무대 조건**이며 T7-40 회귀가 아니다.
⇒ 이 한 건은 **안 잼**(내 무대에서는 성립 불가). 나머지 8건 전건 초록.

### 축 ③-b `shell.spec.ts:79` 세션 칩 단언 수정 (내 write scope)

**바꾼 것** — 칩 «본문»만 보던 단언을 **「본문 + `title` 을 합친 면」** 에서 찾도록 바꿨다.
자리만 옮기면 통과하고, 식별자가 **아예 사라지면 여전히 실패**한다.

🔴 **완화는 검출력을 파는 쪽이라, 같은 회차에 «평범한 위반 1건»을 주입해 물었다.**
칩이 다른 식별자를 보이게 만들자(`MUTA-…`) **실패**했다 —
`Expected substring: "WXYZ" / Received: "내 세션 MUTA 내 세션 MUTA · …"`. **그물은 아직 문다.**

**호환** — 수정 후 **대상(:8130)·대조군(:8131) 양쪽 모두 통과**한다. 그래서 #606 병합 «전»에도 develop 이 깨지지 않는다.

🔴 **센쿠2에게 보내는 회신 — 조건부다.** 「본문에서 식별자 제거 가능」은 **`a1ef9f7` 기준으로만 참**이다.
그 판은 `title` 에 `내 세션 {chipLabel}` 을 **이미 싣고 있다**(`app-shell.tsx`). 반면 **`db94d8b` 의 `title` 에는
식별자가 없다** — 그 판에서 본문만 지우면 식별자가 **어느 면에도 없어진다.** ⇒ **제거는 `a1ef9f7` 이 병합된
뒤에만** 안전하고, 별도 운반 속성(`data-session`)을 쓰려면 그것을 **먼저** 넣어야 한다.

## 4. 안 잼 (0 이 아니다)

1. **`documents` 화면** — 밟은 화면들에 `/documents/...` 링크가 없어 못 갔다.
2. **투어 4~9걸음** — 4번째에서 «다음» 손잡이가 없다(사람이 눌러야 넘어가는 걸음). **3걸음만** 쟀다.
3. **제외 축**(`cited-body`) — 노드 0건이라 시험되지 않았다.
4. **overview·incident-run·tour 1·3** — 대조군이 안 울어 **판정력 없음**.
5. `shell.spec.ts` 외 e2e 17본 — 이번 회차에서 안 돌렸다.

## 5. 내 계측기 자수

1. 🔴 1차 실행에서 TreeWalker 가 **`<script>` 안의 RSC 직렬화 payload**(`self.__next_f.push(...)`)까지 훑어
   금지 패턴 히트로 셌다. **방문자가 읽는 글이 아니다** — 내 그물이 만든 빨강이었다. `script/style/template/noscript`
   제외 + `display:none`·`visibility:hidden` 제외로 고쳤다.
2. 1차 실행에서 `--out` 에 `/c/Users/...` 꼴을 줬더니 노드가 `C:\c\Users\...` 로 해석해 **쓰기가 실패**했고,
   그 예외 때문에 **rc=1** 이 났다. 🔴 나는 그 rc=1 을 하마터면 「대조군이 울었다」로 읽을 뻔했다 —
   **종료 코드가 판정인지 크래시인지부터** 물어야 한다.
3. ai-api 는 **오케 전용 메인 체크아웃의 venv 인터프리터**로 띄웠다(내 워크트리에 venv 없음).
4. 표본 1회 · 1440×900 · chromium.

## 6. 판정

**축 ①② PASS**(판정력이 닿는 화면 한정) · **축 ③ 8/9**(실패 1건 = 무대 조건 · 대조군 동일) ·
**축 ③-b 수정 완료 + 검출력 재확인** · **축 ④ 대조군 rc=1 성립**.
🔴 **미완** = §4 의 안 잼 5종. 그중 **투어 4~9걸음**과 **`documents` 화면**은 다음 회차 발주 후보다.
