# 승격 30 — 외부 재검 (리바이2 61대)

- production = main **`ceebc34`**(#976) · Vercel `dpl_Cz4zQtvbuHarFkVAgHtEMonmNPew` READY(메시지 ts **18:33:02** 기준) · 공개 도메인 `https://factory-knowledge-twin.vercel.app`
- 前 열 창 = `date 18:30:35` ~ `18:33:00`(**승격 «전»**) · 後 열 창 = `18:33:09` ~ `18:35:15`
- 🔴 **구독 0** · production **읽기만**(변경 0) · 근거 **E1**

## 0. 판정 — **PASS** (D-96 처방이 공개면에 착지 · 새 빨강 0)

| 축 | 前(`4225b4f` 화면) | 後(`ceebc34` 화면) |
|---|---|---|
| ② 세션 없는 `GET /overview?intro=1&tour=1` | `307` · **`Location: /`**(쿼리 폐기) | `307` · **`Location: /?next=%2Foverview%3Fintro%3D1%26tour%3D1`** |
| ② 외부 URL 거절 · 키 제거 | — | **4/4**(§3) |
| ③ 느린 × fresh × 412×600 early 클릭 | **0/3** | **3/3** |
| ④ 빠른 조건 | **0/3** | **3/3** |
| ④ D-95 회귀(앱바 튜토리얼 → 투어) | — | **열림 4/4** · 되열림 0 · 오류 0 |

## 1. 🔴 前 열은 승격 «전»에만 잴 수 있었다

이 재검의 前 열(②③④)은 **배포가 바뀌면 영영 사라지는 값**이다. 그래서 「Vercel READY」를 기다리지 않고
승격 진행 중에 먼저 찍었다 — 앞 창(승격 29)에서 배포 sha 전이의 前 값을 놓쳐 「안 잰 것」으로 남긴 자리의 처방이다.

| 前 열 실측(18:30:35~18:33:00) | 값 |
|---|---|
| `307 Location` | `/` |
| early × 느린 × 1280 | **0/3** |
| early × 느린 × **412×600**(폐하 경로) | **0/3** |
| fast × 412×600 | **0/3** |

## 2. ① 배포 전이의 표지 — 무엇이 쓸 수 있고 무엇이 못 쓰는가

| 후보 | 前 | 後 | 판정 |
|---|---|---|---|
| `/api/health` 의 `build` | `4225b4f` | `4225b4f` | 🔴 **못 쓴다** — 서버(ai-api)는 이번 승격 대상이 아니라 **불변이 정상** |
| `_next/static/<buildId>` | `immutable` | `immutable` | 🔴 **못 쓴다** — 상수 문자열이라 배포마다 안 바뀐다 |
| **공개면 HTML 길이** | **18827 B** | **18839 B** | 쓸 수 있다(전이 관측) |
| **`307 Location` 문면** | `/` | `/?next=…` | 🔴 **가장 강한 표지** — 처방 자체의 거동이다 |

🔴 **표지를 「배포 메타」가 아니라 «처방의 거동»에서 골랐다.** `X-Vercel-Id` 는 요청마다 바뀌어 배포 동일성을 말하지 않고,
HTML 길이 12 B 차이는 전이의 «증상»일 뿐 처방을 특정하지 않는다. `Location` 이 갈린 것이 **이 승격이 실린 증거**다.

## 3. ② 외부 URL 거절 · 키 제거 (공개면 `POST /enter` · `Location` 원문)

| 입력 `next` | 결과 | 기대 |
|---|---|---|
| `https://evil.example` | `/overview` | PASS |
| `//evil.example` | `/overview` | PASS |
| `/overview?intro=1&evil=1` | **`/overview?intro=1`** | PASS — 허용 키만 남기고 **버린다** |
| `/overview?intro=1&tour=1` | **`/overview?intro=1&tour=1`** | PASS — 통과·재조립 |

🔴 앞 두 칸(거절)은 **前에서도 `/overview`** 였을 것이다(前은 `next` 를 읽지 않으므로) — 그 칸들은 이 승격을 시험하지 못한다.
**판정력은 뒤 두 칸에 있다**: 「허용 키만 남긴다」와 「통과·재조립」은 처방이 있어야만 나오는 값이다.

## 4. ③④ 화면 — 자극 실재를 값으로

느린 조건 = CDP `400kbps · RTT 400 · CPU×4` · fresh 컨텍스트 · 412×600.
열마다 **조건 문자열 · 첫 로드 ms · 클릭 시점 수화 여부**를 함께 찍는다.

| 열 | 첫 로드 | 클릭 시 수화 | 최종 URL | 말풍선 | 결과 |
|---|---|---|---|---|---|
| 前 early | 2271~2807ms | false | `/overview` | 1개 | **0/3** |
| **後 early** | 2271~2945ms | false | `/overview?intro=1&tour=1` | 2개 | **3/3** |
| 前 fast | 457~520ms | false | `/overview` | 1개 | **0/3** |
| **後 fast** | 496~534ms | false | `/overview?intro=1&tour=1` | 2개 | **3/3** |

판정선은 **쿼리 보존 AND 말풍선 ≥1** 둘 다다.

### D-95 회귀 (앱바 튜토리얼 → 투어 열림)

| 폭 · 진입 | 열림 | 소요 | `search` | 닫고 새로고침 되열림 | 오류 |
|---|---|---|---|---|---|
| 412 `/overview` | 1 | 1206ms | `?intro=1&tour=1` | 0 | 0 |
| 412 `/overview` | 1 | **3621ms** | **`(none)`** | 0 | 0 |
| 1440 `/` | 1 | 626ms | `?intro=1&tour=1` | 0 | 0 |
| 1440 `/` | 1 | 19ms | `?intro=1&tour=1` | 0 | 0 |

**열림 4/4 · 되열림 0 · 오류 0** = D-95 판정선 충족.
🔴 **관측 1건**: 2회차만 `search=(none)` 이다(소요도 3621ms 로 가장 느렸다). 판정선은 「`intro-card` 가 서는가」이고 그것은 **섰다**(opened=1).
주소 반영이 늦은 회차로 보이나, **이 창에서 기전을 확인하지 않았다** — 판정으로 옮기지 않고 관측으로 남긴다(§6).

## 5. 구독 0 — 드릴이 스스로 센다

드릴이 자기 페이지의 `POST …/runs` 요청을 듣고 센다. 화면 축 전 실행에서 **0건**. Live 실행 버튼을 누르지 않았고 조사 프로브도 쏘지 않았다.

## 6. 내 계측기 자수 · 안 잰 것

**자수**
1. **① 의 표지 후보 둘을 버렸다.** `/api/health` 의 `build` 는 이번 승격이 **화면만**이라 불변이 정상이고,
   `_next/static/immutable` 은 상수라 배포마다 안 바뀐다. 둘 중 아무거나 「전이 없음」의 근거로 적었으면 **틀린 빨강**이 됐다.
2. **D-95 프로브가 `playwright` 패키지를 요구했다**(내 트리에는 `@playwright/test` 만 있었다) — 대상 결함이 아니라 내 무대 결손이었다. 같은 버전으로 맞춰 풀었다.

**안 잰 것**
- **D-95 2회차 `search=(none)` 의 기전** — 열림은 섰으나 주소 반영이 늦은 회차. 표본 1이라 파고들지 않았다(E4).
- **前 열의 외부 URL 거절 4칸** — 승격이 이미 진행 중이라 그 열을 다시 열 수 없었다. 판정력은 뒤 두 칸이 갖는다(§3).
- **`reason` 문장 3종 공개면 렌더 · 걸쇠 축** — `online:false` 가 필요하고 만들려면 실소모/실패 주입이 든다(구독 0 전제).
- **실기기 느린 회선**(운영자 축) · 다른 엔진.
