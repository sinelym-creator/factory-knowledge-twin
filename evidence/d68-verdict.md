# D-68 판정문 — 그래프 경로 근거 `GP-…` 조회

> **축별 append 문서.** a(서버 축)는 리바이2 45대 · b(셸 축)는 이어 붙인다.
> 🔴 **이어 쓰는 사람은 §a-5 «자수·재현» 절을 먼저 읽어라** — 이 판정문의 수 중 어떤 것이
> 무엇의 초록인지, 어떤 빨강이 계측기의 것이었는지가 거기 있다.

---

## a — 서버 축 (`GET /evidence/GP-*`) · **PASS**

| | |
|---|---|
| 판정 | 🔴 **PASS** — 판정선 ①~⑥ 전건 · 대조군 빨강 확인 |
| 대상 | PR #653 `lane/senku2-d68` head **`f138a5b`** |
| 대조군 | **`d38f4a8`**(처방 «전» · 같은 그물 · **같은 DB**) |
| 판정선 | 계약 `packages/contracts/rest-api-v0.1.md` **v0.1.17 append** 「판정선(독립 검증)」 ①~⑥ (+⑦ 관측) |
| 그물 | `tests/api/d68_gp_evidence.py`(신규 · 이 PR) |
| 측정 | 2026-09-04 21:36:45 KST · `rc=0` |
| 검증자 | 리바이2 45대 |

### a-1. 무대 (증인을 «수»로 먼저)

| | 대상 | 대조군 |
|---|---|---|
| API | `127.0.0.1:8190` (PID 35920) | `127.0.0.1:8191` (PID 33668) |
| 트리 | `_wt/levi2-d68v` (`f138a5b`) | `_wt/levi2-d68ctl` (`d38f4a8`) |
| DB | **같은 것** — `fkt-levi2-postgres-1`(:5534) · `fkt-levi2-neo4j-1`(:7587) | 〃 |
| run | `RUN-c9f2d10efee0` completed | `RUN-ee0961181606` completed |
| GP 표본 | **5본** | **5본** |
| record 표본 | 9본 | 9본 |

- **귀속** — 「코드가 같다」가 아니라 **저 프로세스가 내 트리를 읽는가**로 찍었다: 서버 traceback 이
  `_wt/levi2-d68v/services/ai-api/app/investigation/workflow.py` 를 가리킨다(대상) · 포트→PID 위 표.
- **표본 0이면 `exit 2`** — 그물은 세션·run·GP 중 하나라도 0이면 색을 내지 않는다. 실제로 **2회 `exit 2`** 를 냈다(§a-5).

### a-2. ⑤ 를 «먼저» — 대조군의 빨강 (이것이 없으면 아래 초록은 판정력이 없다)

🔴 **대조군 열은 «대조군 서버 자신이 만든 run»의 GP id 로 세웠다.**
run 저장소는 프로세스 메모리(`RunStore`)라, 대상에서 만든 GP id 를 대조군에 물으면
**처방이 있든 없든 404** 다 — 그 404 는 「처방 부재」가 아니라 「그 run 을 모름」의 것이고,
그 열은 **판정력이 0** 이다. 발주 문면(「같은 그물 404 전건」)은 지키되 **404 의 주어를 갈랐다.**

| 축 | 값 |
|---|---|
| ⑤ 대조군 `GET /evidence/GP-ee0961181606-0{0..4}` | **404 × 5/5** |
| 주어 확증(코드 줄) | 대조군 `routers/knowledge.py` 에 `_GRAPH_PATH_PREFIX = "GP-"` 갈래 **부재**(grep 0) · 대상 `:59`·`:74`·`:152` 에 존재 |

⇒ **빨강의 주어 = 처방 부재.** 아래 초록은 이 빨강 위에서만 뜻을 갖는다.

### a-3. ①~⑥ (대상)

| # | 판정선 | Actual | 판정 |
|---|---|---|---|
| ① | GP 전건 200 · `kind=graph-path` · `sourceId`=종단 targetId · **`excerpt` = 이벤트 문면과 동일 문자열** · `meta.path{label,hops,nodes,edges}` | **5/5** — 다섯 검사 전부 `o` | **PASS** |
| ① 부 | 구현 자기 신고(`schemas.py`: 「`text` 와 `excerpt` 는 같은 문자열」) | **`text==excerpt` 5/5** | **PASS** |
| ② | 타 세션 404(존재 누설 0) | **404 × 5/5** | **PASS** |
| ③ | 무쿠키 401 `session_required` | **401 + code 일치 × 5/5** | **PASS** |
| ④ | 범위 밖 idx 404 | `GP-c9f2d10efee0-**99**` → **404** 1/1 | **PASS** |
| ⑤ | 대조군 404 전건(빨강) | **404 × 5/5** (§a-2) | **PASS** |
| ⑥ | document-chunk·record 회귀 200 · 형상 불변 | **5/5** — 기존 키 **전건 값 동일** · 사라진 키 **0** · 추가 키 4(`excerpt`·`meta`·`score`·`sourceId`)가 이 kind 에서 **전부 null** | **PASS** |

🔴 **`excerpt` 는 이벤트에서 뽑아 `==` 로 물렸다** — 라우트가 문장을 다시 조립하지 않는다는
처방의 주장(`workflow.py` 주석 「걸음 문장은 여기서 한 번만 짓는다」)을 **같은 실행에서** 확인했다.

### a-4. ⑦ 관측 (판정 아님 · 값만)

| 표면 | 같은 replay run 에 대해 |
|---|---|
| `GET /graph/paths?byRun=RUN-b62ea496f2c8` | **501 `replay_path_source_absent`** |
| `GET /evidence/GP-…` | **404 `not_found`** × 3 |

🔴 **두 표면의 판정이 갈린다.** 계약 v0.1.17 과 구현 주석(`knowledge.py:93`)은 재생 run 에서
「`/graph/paths?byRun=` 과 **같은 판정**」을 내겠다고 적었는데, 실물은 갈렸다.

**원인 — 실측으로 좁혔다(추정 아님)**

```
이 replay run   = RUN-b62ea496f2c8
GP 가 가리키는 run = RUN-6c01759e7f43   (같은가 = False)
그 run 조회      = 404 「run RUN-6c01759e7f43 를 찾을 수 없다」
```

재생본의 GP evidenceId 는 **녹화 당시 run id** 를 담고 있다. 라우트는 `GP-` 접두에서
run id 를 되세우므로(`_graph_path_run_id`) 되세운 run 을 서버가 모르고, `record` 가 `None` 이라
**`record is not None and record.mode == "replay"` 인 501 갈래에 도달하지 못한다.**
⇒ 이 표면에서 **501 은 구조적으로 도달 불가능한 갈래**다(현 fixture 기준).

- 오케 21:35 고지(「replay 501 문면은 자극 불가 · 사유 = 녹화 run id 복원」)와 **독립적으로 같은 결론**에 닿았고, 여기 값이 그 사유의 실측분이다.
- **판정에 넣지 않는다** — 계약 ⑦은 관측 항목이고, 이 갈림이 D-68 처방의 결함인지 fixture·계약 문면의 문제인지는 **오케·발주자 판단**이다. 회부만 한다.

### a-5. 🔴 자수·재현 (이어 쓰는 사람이 먼저 읽을 절)

**대상 결함 검출 = 0.** 이 축에서 나온 빨강은 **전부 내 것**이었다. 넷을 적는다.

| # | 내가 본 것 | 진짜 주어 | 어떻게 갈랐나 |
|---|---|---|---|
| 1 | 「세션 발급 실패」 | **내 도구** — `dict(r.headers)` 로 눕히며 대소문자 무시가 죽어 `Set-Cookie` 가 «없는 헤더»가 됐다(uvicorn 은 소문자) | 수동 `curl`/`HTTPMessage` 경로는 되고 그물만 안 됨 ⇒ 도구 |
| 2 | 「근거 0건」 | **내 도구** — `step.evidence` 의 `evidence` 는 **단수 dict** 인데 리스트로 순회해 키 문자열을 돌았다 | dict/list 양쪽 수용 후 record 9본 등장 |
| 3 | 「run 이 즉사(`vector: LookupError`)」 | **내 무대** — 내 DB 의 `document_chunk` 가 **0행 / 임베딩 0건**이었다 | 대상·대조군이 **똑같이** 죽고, 죽은 자리가 D-68 처방과 무관한 `vector` 단계 ⇒ 대상 무관. `build_index.py` 로 chunk 59건 적재 후 해소 |
| 4 | 「⑥ 회귀 FAIL 5/5」 | **내 판정선** — 본문 칸을 `excerpt` 로 물었다(record·doc-chunk 는 v0.1.1 의 **`text`**). 이어서 「키 집합 동일」로 잡은 것도 **과한 자**였다(처방은 additive) | 응답 실물 확인 → 판정선을 「기존 키 값 동일 + 새 키 null」로 정정 |

**부수 자수 2**
- `POST /scenarios/{id}/runs` 본문 `sessionId` 를 「계약 문면에 없다」고 적었다 — **내 미독**(계약 본문 `rest-api-v0.1.md:34` 에 있다 · 오케 21:29 정정). v0.1.17 절만 읽고 본문 계약을 안 읽었다.
- `build_index.py` 는 `FKT_POSTGRES_DSN` 을 **안 읽는다** — `PGPORT`/`POSTGRES_PORT` 기본 **5434**를 쓴다. 내 DB 는 5534 라 연결이 **타임아웃**으로 죽었다. 🔴 5434 에 누군가의 스택이 떠 있었다면 이 명령은 **남의 파생 색인을 지우고 다시 만들었을 것**이다(이 스크립트는 「삭제 후 재생성」이다). 실측 = 그때 5434 는 LISTEN 0. **파괴 명령의 대상은 기본값에 맡기지 않는다** — `--dsn` 명시로 돌렸다.

**`exit 2` 를 낸 회차 2** — 무대 미성립(색인 0)에서 그물이 색을 내지 않고 멈춘 회차. 「안 잼」이지 빨강이 아니다.

### a-6. 안 잰 것 — 이름으로

1. **화면 축** — 판정선 ③(폐하 경로: 조사 → GP 클릭 → 경로 본문)·④(404 문면 정정)는 **b 셸 PR 소관**. 여기서 안 쟀다.
2. **재기동 뒤 404** — 프로세스를 죽였다 살려 GP 가 404 가 되는지 안 쟀다(계약 「run 소멸 = 404」).
3. **다중 워커** — 워커 1 무대로만 쟀다. `RunStore` 가 프로세스 메모리라 워커 2+ 에서는 규칙이 프로세스 단위로만 선다(v0.1.14 선례).
4. **replay 501 문면** — 위 §a-4 대로 **도달 불가**라 자극하지 못했다.
5. **GP 표본은 한 시나리오(GS-01)의 5본** — 다른 시나리오·다른 hops 분포는 안 쟀다.
6. **동시성·부하** — 안 쟀다.

---

## b — 셸 축 (근거 화면 · 404 문면)

*(다음 대에서 append — 대상 PR #655 `be38ca7` · 대조군 `9b837b1`)*

---

## b — 셸 축 (근거 화면 · 404 문면) · **조건부 PASS**

| | |
|---|---|
| 판정 | 🔴 **조건부 PASS** — ⑧⑨⑩⑪⑬(chromium) 전건 · **⑫ 미도달(안 잼)** · **⑬ webkit 1건 판정 보류** |
| 대상 | PR #655 `lane/senku2-d68b` head **`be38ca7`** · 셸 `:8192` → API `:8190`(`f138a5b`) |
| 대조군 | **`9b837b1`** 셸 `:8193` → API `:8191`(`d38f4a8`) — 🔴 **짝 통째** |
| 그물 | `tests/web/d68b_evidence_screen.mjs`(신규) |
| 측정 | 2026-09-04 21:51:08 KST · chromium 1280 · chromium 390 · webkit 390 |

🔴 **열은 «짝»이다.** 처음에 나는 손잡이를 하나로 줄이려고 「옛 셸 + 새 서버」를 대조군으로 세우려 했다.
그러면 GP 가 **200 으로 와서** 판정선 문장(「서버에 닿지 못했습니다.」)이 **아예 안 나온다** — 자극이
사라진 열이다. 오케 21:41 이견이 옳았고, 대조군을 **옛 셸 + 옛 서버 통째**로 바꿨다.

### b-1. ⑪ 를 먼저 — 대조군의 빨강

| 폭·엔진 | 옛 짝의 GP 근거 경로 |
|---|---|
| chromium 1280 · chromium 390 · webkit 390 | **「서버에 닿지 못했습니다.」 출현 = 3/3**(「그런 항목이 없습니다.」 0) |

⇒ 폐하가 본 그 오표기가 **옛 짝에서 재현**된다. 아래 초록은 이 빨강 위에서만 뜻을 갖는다.

### b-2. 판정 (3폭 전수)

| # | 판정선 | Actual(3폭 동일) | 판정 |
|---|---|---|---|
| ⑧ | `graph-path-body` 1 | **1** | PASS |
| ⑧ | 걸음 `li` = `meta.path.hops+1` | **3 = 3**(서버 `meta.path.hops`=2) | PASS |
| ⑧ | 본문 `<a>` 0 | **0** | PASS |
| ⑧ | walk == 서버 `excerpt` | **동일**(65자 == 65자 · 서버에서 «따로» 받아 대조) | PASS |
| ⑨ | 404 문면 = 「그런 항목이 없습니다.」 · 「서버에 닿지 못했습니다.」 0 | **출현 · 0** | PASS |
| ⑩ | 정적 재생본 미스 문면 | **「그런 항목이 없습니다.」 출현 · 「서버에 닿지 못했습니다.」 0** — 아래 주 | PASS |
| ⑪ | 대조군 = 「서버에 닿지 못했습니다.」 출현(빨강) | **3/3 출현** | PASS |
| ⑫ | 투어 9/9 카드 「튜토리얼」 1 · 「?」 0 | 🔴 **미도달 — 3/9 에서 멈춤** | **안 잼** |
| ⑬ | 콘솔 실오류 0 | chromium 2폭 **0/0** · webkit **4건(판정 보류)** | 아래 주 |

- **⑧ 도달** = GP 링크 5본 · 화면 흐름(「조사 시작」 클릭)으로 만든 세션 · 도달 776~1122ms(기다린 시간을 값으로).
- **⑩ 주** — 재생본 GP id 를 직접 방문했을 때 나온 문면은 **「그런 항목이 없습니다.」**이지
  「이 정적 재생본에는 담기지 않은 자리입니다.」가 **아니었다**. 오표기(「닿지 못했습니다」)는 0 이라
  판정선의 핵심은 지켜졌지만, **`STATIC_MISS` 갈래를 밟는 자극은 못 만들었다** — §a-4 대로 재생본의
  GP id 가 녹화 run 을 가리켜 서버가 404 로 답하고, 셸은 그것을 `not_found` 로 옳게 그린다.
  **그 갈래는 「안 잼」이다.**
- **⑫ 주** — `tour-await-click` 단계(3/9)를 그물이 넘기지 못했다. 🔴 `tour-progress` 의 textContent 는
  **빈 문자열**이고 진행 표시는 callout 본문의 「n/9」 였다(실측 · 내 첫 판정선은 없는 자리에서 읽었다).
  자리를 고친 뒤에도 3/9 에서 멈춰 **9/9 카드를 읽지 못했다.** 번들 grep(`tour-steps.ts:206` 이
  「상단 「튜토리얼」을 누르면 됩니다」로 바뀐 것)은 확인했으나, 🔴 **「문자열이 코드에 있다」와
  「그 카드가 그걸 그린다」는 다른 사실**이라 **판정에 넣지 않는다.**
- **⑬ 주(판정 보류)** — webkit 회차에서 `Fetch API cannot load … due to access control checks`
  4건이 잡혔다. 🔴 **그러나 이 수는 대상 열에만 있는 경로(⑫ 투어 순회)에서 났고 대조군은 그 경로를
  돌지 않았다 — 모집단이 다르다.** 손잡이를 맞춰 **같은 자극(투어 3단계 순회)을 webkit 으로 대상·대조군에
  각각** 돌린 격리 실험에서는 **양쪽 다 0건**이었다. ⇒ **처방 귀속 미증명.** 대상 결함으로 회부하지 않고
  「webkit + 로컬 http 무대 + RSC 프리페치」 축의 관측으로 남긴다.

### b-3. 🔴 자수 (b 축)

| # | 내가 본 것 | 진짜 주어 |
|---|---|---|
| 5 | 「조사 시작 없음」으로 무대 미성립 2회 | **내 무대** — `FKT_API_BASE` 에 `/api` 를 붙여 셸이 `/api/api/…` 를 불렀다(404). base 는 접미 없이 준다 |
| 6 | 위를 고쳤는데도 안 고쳐짐 | **내 무대** — 옛 셸 프로세스가 포트를 쥔 채였고(`EADDRINUSE`) 응답한 건 구 빌드였다. `taskkill //PID` 가 안 먹어 PowerShell `Stop-Process` 로 잡았다 |
| 7 | 「조사 시작」 0 | **내 도구** — 투어 초대가 덮고 있었고(닿는다≠눌린다), 데이터가 오기 «전»에 찾았다. Esc 로 걷고 문구를 기다리게 고쳤다 |
| 8 | ⑨ FAIL 3/3 | **내 도구** — href 가 `GP-…-00?run=RUN-…` 라 쿼리를 안 뗀 채 `-97` 치환을 걸어 «없는 id» 가 아니었다 |
| 9 | ⑬ FAIL(첫 회차 3건·둘째 8건) | **내 판정선** — 대조군이 대상과 다른 경로를 돌아 모집단이 갈렸다. 같은 경로를 돌게 한 뒤 chromium 0/0 |
| 10 | ⑫ 「진행 표시 없음」 | **내 도구** — `tour-progress` 는 비어 있고 「n/9」 는 callout 본문에 있었다 |
| 11 | 빌드 성공으로 보임 | **내 도구** — 명령 마지막이 `tail` 이라 `install rc=1` 인데 「exited with code 0」로 보였다. `.next/BUILD_ID` 실재로 확인 |

**대상 결함 검출 = 0** (a·b 통틀어).

### b-4. 안 잰 것 — 이름으로

1. **⑫ 투어 9/9 카드** — 3/9 에서 막혀 못 읽었다. 남은 길 = `tour-await-click` 단계의 대상 요소를 규격대로 눌러 넘기기.
2. **⑩ `STATIC_MISS` 갈래** — 그 문면을 내는 자극을 못 만들었다(재생본 GP 는 서버 404 로 간다).
3. **⑬ webkit** — 위 주 대로 판정 보류.
4. **실기기 0 · 1280 외 넓은 폭 0 · firefox 0.**
5. **폐하 경로 전체**(조사 → GP 클릭 → 본문)는 «링크 클릭»이 아니라 href 로 이동해 재현했다 — 클릭 경유 자체는 ⑧ 도달에서만 밟았다.
