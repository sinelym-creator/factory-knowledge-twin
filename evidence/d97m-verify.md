# D-97-MV — 「이 run 이 합성 호출을 몇 번 썼는가」를 계측 칸이 말하는가 (리바이2 62대)

> 대상 = PR #985 `40f54b8`(센쿠2) · 계약 = v0.2.5(#986 착지 `ce96e39`) · 대조군 = develop `25087fd`
> 창 = `date 19:27` ~ `19:35` · 🔴 **구독 소모 0**(스텁 무대 · Claude CLI 호출 0 · 실 게이트웨이 무접촉)

## 0. 판정 — **조건부 PASS** · 회부 2건

🔴 **정정(오케 지적 `date 19:37`)** — 앞판은 ①을 「PASS」로 적었으나 표의 `streamerr` 행은 참값 1 ↔ 신고 0 으로 **불일치**다. ① 은 **5/6 조건부 PASS** 가 맞다 — 불일치를 «별 항»으로만 떼면 그 사실이 판정줄에서 사라진다.

| 축 | 판정 | 한 줄 |
|---|---|---|
| ① known-true / 대조군 | **조건부 PASS (5/6)** | 네 계약 갈래는 참값↔신고 일치 · 값이 실제로 갈린다 · 🔴 `streamerr` **1열 불일치** = §3 회부 |
| ② 두 칸 항상 실림 | **PASS**(합성층) | 재요청 0 열에서도 `calls 1` · `safetyRetried false` 가 실린다 |
| ③ 로그 형상 | **PASS** | `synthesize calls=%d safetyRetried=%s` ASCII 1줄 · 前 트리엔 0건 |
| ④ 거동 변경 0 | **PASS**(합성층) · 상한 축은 **코드 동일** | 새 키 2개를 뺀 페이로드·`axis`·인용이 6열 전건 동일 |
| ⑤ README 문면 ↔ 코드 | 🔴 **회부 2** | 「`calls`(1\|2)」인데 실물은 **0 도 낸다**(같은 문단이 「0 도 값」이라고 말한다) |
| ⑥ 안 잰 것 | — | 실 CLI 미호명 «발생률» · run/이벤트 실적재 · 상한 관측 · 화면 |

🔴 **회부 1(결함) = 200 을 받고 본문 안에서 끊긴 갈래에서 `calls` 가 0 이다.** 호출은 갔고 응답도 시작됐는데 계수는 0 이다 — 이 칸이 존재하는 이유(사후 예산 계수)가 바로 그 자리에서 샌다. §3.

## 1. 무대 · 측정 모델

- **무대** = 없음(컨테이너 0 · DB 0 · 포트 = 매 열 임시 포트). `live_synthesis.synthesize()` 를 직접 부른다.
  - 그물 = `tests/stubs/d97m_stub_gateway.py`(스텁 게이트웨이) · `tests/api/d97m_unit_probe.py`(탐침)
  - 대상 트리는 **인자**다(`FKT_TARGET_ROOT`) — 같은 계측기로 前(`25087fd`)·後(`40f54b8`) 두 열을 낸다.
- 🔴 **두 계측기를 다른 칸에 적는다.**

| 칸 | 누가 재는가 | 무엇의 값인가 |
|---|---|---|
| `stub` | 내 스텁이 실제로 **받은** `POST /synthesize` 건수 | **참값** |
| `calls`·`safetyRetried` | 대상의 **자기 신고**(`synthesis_payload()`) | 판정 대상 |

판정선은 「대상이 2라고 말했는가」가 아니라 **「둘이 같은가」**다. 자기 신고만 보면 무엇이든 그 값으로 보내는 문도 초록이 된다.

- **발췌는 손대지 않는다.** 미호명은 **답 문장에서 SAF id 를 빼는 것**으로 만든다 — 발췌에서 빼면 `safety_rules_in_evidence()` 가 0 을 거둬 자극 자체가 사라지고, 그 초록은 대상에 대해 아무 말도 하지 않는다.
- 심은 규정 id 는 코퍼스 실재값(`SAF-LOTO-01`·`SAF-PPE-01`) — 지어낸 모양이 정규식 경계에 안 걸려 「줬다고 착각」하는 자리를 피한다.

## 2. ①②④ — 6열 실측 (`date 19:30`~`19:34`)

| 열(스텁 손잡이) | 스텁 참값 | 대상 `calls` | `safetyRetried` | `axis` | 前 트리 신고 |
|---|---|---|---|---|---|
| `named`(전부 호명) | **1** | **1** | false | live | (키 없음) |
| `retry`(1회차 미호명→호명) | **2** | **2** | **true** | live | (키 없음) |
| `omitted`(둘 다 미호명) | **2** | **2** | **true** | live(+`safetyOmitted true`) | (키 없음) |
| `guardfail`(200 · 가드가 물림) | **1** | **1** | false | live-rejected | (키 없음) |
| `streamerr`(200 후 본문 끊김) | **1** | 🔴 **0** | false | live-rejected | (키 없음) |
| `unreachable`(서버 없음) | **0** | **0** | false | live-rejected | (키 없음) |

- **前 칸**(develop `25087fd`)은 여섯 열 모두 `axis`·`model` 뿐이다 — 2호출 run 과 1호출 run 이 페이로드로 **구별되지 않았다**. 센쿠2 판독 §3 을 읽기가 아니라 실측으로 확인한 자리.
- **④ 거동 변경 0**: 새 키 2개를 제외한 페이로드·`axis`·`rationale.citedEvidenceIds` 가 **6열 전건 前後 동일**.
- **상한 축**: 이 처방의 diff 5파일에 `routers/investigations.py`·`settings.py` 가 **없다** — 계수 자리(`_admit_run_caps`)는 바이트로 같다. 다만 **관측은 안 했다**(§5).

### 교정 칸 — 「늘 1」 을 내는 계측기는 걸리는가

같은 탐침으로 처방을 **일부러 고장 내**(`payload["calls"] = 1`) 다시 쟀다.

| 열 | 참값 | 고장판 신고 | 걸리는가 |
|---|---|---|---|
| named | 1 | 1 | (못 가름) |
| retry | 2 | 1 | ✅ |
| omitted | 2 | 1 | ✅ |
| guardfail | 1 | 1 | (못 가름) |
| streamerr | 1 | 1 | (못 가름 · §3 참조) |
| unreachable | 0 | 1 | ✅ |

⇒ 이 초록에는 검출력이 있다. **한 열만 봤으면 「늘 1」이 통과했다.** 고장은 되돌렸다(`git checkout` · porcelain 0 · `MUTATION` grep 0).

🔴 `streamerr` 열에서는 **고장판이 «옳은» 값을 낸다** — 참값 1 을 내는 쪽이 고장난 상수이고, 처방은 0 을 낸다. 그 뒤집힘이 §3 의 결함을 가리킨다.

## 3. 🔴 회부 1 — `calls` 가 「200 을 받고 본문 안에서 끊긴 회차」를 세지 않는다

- **기전**: `calls += 1` 은 `_post()` 가 **정상 반환한 뒤**에 있다. 스트리밍 갈래에서 게이트웨이가 200 을 낸 뒤 본문에 `{"kind":"error"}` 를 실으면(D-24b · `_read_ndjson`) `_GatewayStreamError` 가 `_post` 를 **뚫고 나가** `except Exception` 으로 떨어진다 — 그 경로의 `LiveResult` 는 `calls=0` 이다.
- **왜 결함인가**: 계약 v0.2.5 는 0 을 「**도달하지 못한** 회차(주소 없음·발췌 0건)」로 정의한다. 이 갈래는 도달했고 200 까지 받았다. 실 게이트웨이라면 **CLI 가 이미 돌았을 수 있는 자리**이므로 구독이 탔는데 예산표에는 0 으로 남는다.
- **대조군이 이것을 결함으로 만든다**: `guardfail` 열은 같은 「200 을 받고 거절」인데 **1 을 센다**(처방이 주석으로 명시한 그 주장은 참이다). 두 열의 차이는 **실패가 상태코드로 왔는가 본문 안으로 왔는가** 뿐이다.
- **처방 후보**(판정은 오케 몫): `_post` 호출을 감싸 **응답 객체를 받은 시점**에 세거나, `_read_ndjson` 이 던지는 예외를 `_post` 안에서 받아 「호출은 갔다」를 계수에 반영한다.
- **비고**: 이 갈래는 `log.info("synthesize calls=…")` 도 남기지 않는다(그 줄은 루프 «뒤»에 있고 거절은 그 전에 return 한다). 즉 **페이로드에도 로그에도 안 남는 소모**다.

## 4. 🔴 회부 2 — README 문면이 자기 문단 안에서 갈린다

`services/ai-api/README.md` 새 절:

> 호출 수는 … 의 **`calls`(1|2)** 와 `safetyRetried`(bool)로 매 run 남는다 — 둘 다 **항상** 실린다(0/false 도 값이다).

같은 문장이 값 범위를 `1|2` 로 적고 곧바로 「0 도 값」이라 말한다. 스키마는 `minimum 0 · maximum 2` 이고 실물도 0 을 낸다(§2 두 열). **`calls`(0|1|2)** 로 정정 회부한다. 표의 「조사 1건당 합성 호출 **1~2**」·「구독 호출 시간당 **최대 6**」은 코드 실물(`for attempt in (1, 2)` · `run_cap_global_per_hour=3`)과 **일치**한다.

## 5. 안 잰 것 — 이름으로 남긴다

1. **run 페이로드·이벤트 실적재** — `workflow.py:383` 이 `synthesis_payload()` 를 그대로 싣는 것은 **정적으로만** 확인했다. 「이벤트가 실제로 그 키를 달고 나온다」는 전 스택(ai-api+DB) 관측이 필요하고 이 창에서 세우지 않았다.
2. **`hourlyCap.used` 증가분이 run 축(1)인가** — 계수 코드가 diff 밖이라는 **코드 동일성**까지다. 관측 아님.
3. **화면** — 이 칸의 소비자는 계측·evidence 다(화면 소비자 0). 안 봤다.
4. **실 CLI 미호명 «발생률»** — 스텁이 그것을 결정적으로 만든다. 이 문서는 비율에 대해 한 마디도 하지 않는다.
5. **HTTP 4xx/5xx 로 거절된 회차** — 게이트웨이가 200 전에 거절하면 `calls 0` 이다. 그 갈래는 CLI 가 돌기 «전»일 가능성이 높아 §3 과 같은 무게로 보지 않았고, **재지도 않았다**.

## 6. 대상의 자기 그물

`services/ai-api/tests_unit/test_d97_call_count.py` 4건 = 전건 통과(`4 passed`). 네 축은 계약이 적은 네 갈래(SAF 없음·재요청·재요청 뒤 누락·미도달)와 1:1 이고, **§3 의 갈래(200 후 본문 끊김)와 `guardfail` 갈래는 그 목록에 없다**. 내 열이 하나 더 물어서 나온 자리다.

## 7. 내 계측기 자수

1. **`validator.js` 를 0바이트로 만들었다**(ⓐ 조각). 파일을 직접 열어 쓰다 인코딩 예외가 나 truncate 만 남았다 — `git checkout` 복원 후 **임시 파일 → `os.replace`** 로 바꿨다. 이후 모든 패치는 그 순서로 했다.
2. **③ 로그 축을 처음엔 「안 남는다」로 읽을 뻔했다.** 내 탐침이 로깅 레벨을 안 열어 `log.info` 가 안 찍힌 것이었다. `basicConfig(INFO)` 를 «먼저» 켠 뒤 재측했고, 前 트리를 같은 조건으로 돌려 0건인 것까지 확인해서야 갈림이 값이 됐다.
3. **첫 실행에서 stdout·stderr 를 한 파일로 합쳐** 대상의 로그가 내 JSON 을 깨뜨렸다(파싱 실패). 측정 면에 내 출력이 섞이면 판정 전에 죽는다 — 두 스트림을 갈랐다.
4. **em dash 가 cp949 콘솔을 못 넘어** 첫 탐침이 `UnicodeEncodeError` 로 죽었다. 대상이 아니라 내 출력의 죽음이다 — `reconfigure(encoding="utf-8")` 로 고쳤다.
5. **스텁 패치 한 번이 문법을 깨뜨렸다**(escape 가 실제 줄바꿈으로 들어감). `ast.parse` 로 잡았다 — 그물을 고치면 그물부터 다시 물어야 한다.

---

# §8 D-97-MV2 재검 — 수리본 `8373873` (리바이2 62대)

> 창 = `date 19:42` ~ `19:58` · 🔴 **구독 소모 0**(스텁 게이트웨이 · 실 CLI 0) · 무대 = **전용 스택**(develop `5534`/`7587` 무접촉)

## 8.0 판정 — **PASS** · 남은 회부 1(경)

| 축 | 판정 | 값 |
|---|---|---|
| ① 9열 재실행 | **PASS** | `streamerr` **0 → 1** · 나머지 열 불변 · 9열 전건 참값 일치 |
| ② 반대 대조군(4xx) | **PASS** | 소모 **0** · 요청은 **1건 받음**(raw) — 「전부 1」 계측기 차단 |
| ③ 고장 주입 | **PASS(단, 이동은 없다)** | 무는 열 = `retry`·`omitted`·**`http4xx`**·`unreachable` |
| ④ 실적재 + 상한 단위 | **PASS** | 이벤트에 실림 · 3 run 에 cap **1·2·3** · 같은 창 호출 **2·4·6** |
| ⑤ README 정정 | **PASS** | `(0|1|2)` + 경계 문장 — 실측 9열과 **한 칸도 안 어긋난다** |
| ⑥ 안 잰 것 | — | §8.5 |
| (경) 로그 갈래 | 🔴 **회부** | 소모 1인 거절 갈래(`streamerr`·`guardfail`)에 `calls=` 줄이 **없다** |

## 8.1 ① 9열 (단위층 · `date 19:42`)

| 열 | 소모 참값 | raw 요청 | 대상 `calls` | 前(`40f54b8`) |
|---|---|---|---|---|
| `named` | 1 | 1 | **1** | 1 |
| `retry` | 2 | 2 | **2** | 2 |
| `omitted` | 2 | 2 | **2** | 2 |
| `guardfail`(200·가드 물림) | 1 | 1 | **1** | 1 |
| `streamerr`(200 후 본문 끊김) | 1 | 1 | **1** | 🔴 **0** |
| `noresult`(200·result 줄 없음) | 1 | 1 | **1** | (안 잼) |
| `http4xx`(200 전 거부) | **0** | **1** | **0** | (안 잼) |
| `unreachable` | 0 | — | **0** | 0 |
| `noaddr`(주소 빈 값) | 0 | — | **0** | (안 잼) |

- **참값의 뜻을 바꿨다**: 스텁이 이제 **소모 계수**(CLI 를 부를 자리까지 갔는가)와 **raw 요청 수**를 따로 낸다. `http4xx` 는 raw 1·소모 0 — 이 한 칸이 「요청을 받았으니 1」로 세는 계측기를 가른다.

## 8.2 ③ 고장 주입 — 🔴 **발주가 기대한 「이동」은 일어나지 않는다(일어날 수 없다)**

| 열 | 참값 | 수리본 | 고장판(늘 1) | 무는가 |
|---|---|---|---|---|
| named | 1 | 1 | 1 | no |
| retry | 2 | 2 | 1 | **YES** |
| omitted | 2 | 2 | 1 | **YES** |
| guardfail | 1 | 1 | 1 | no |
| streamerr | 1 | 1 | 1 | **no** |
| http4xx | 0 | 0 | 1 | **YES** |
| unreachable | 0 | 0 | 1 | **YES** |

수리 «전» `streamerr` 가 문 것은 **처방이 0 을 냈기 때문**이다(참값 1 ≠ 신고 0). 수리 뒤 그 열의 참값과 신고가 **둘 다 1** 이라, 「늘 1」 상수와 구별되지 않는다 — **그 자리를 대신하는 것이 `http4xx`**(참값 0)다. 그래서 ②(반대 대조군)가 이 축의 **필수 조건**이었다. 고장은 되돌렸다(porcelain 0 · `MUTATION` grep 0).

## 8.3 ④ 실적재 — 전용 스택 관측

- **무대(세운 자리에서 기록)**: ai-api `fkt-levi2-d97mv-api` **`:8860`**(이미지 `fkt-levi2-d97mv-ai-api:latest` · `/api/health.build` = **`8373873`** 자기 신고) · postgres `fkt-levi2-d97mv-postgres-1` **`:5540`** · neo4j `fkt-levi2-d97mv-neo4j-1` **`:7580`/`:7595`** · 볼륨 = `~/.fkt-volumes/fkt-levi2-d97mv`(리포·워크트리 **밖**) · 스텁 게이트웨이 **`:8871`**(좌석 = 리바이2 · 워크트리 `_wt/levi2-d97mv`) · 컨테이너 env `FKT_LOCAL_SYNTHESIS_GATEWAY=http://host.docker.internal:8871` **`printenv` 실측**.
- **이벤트 실적재**(같은 corpus 발췌에 `SAF-LOTO-01`·`SAF-PPE-01` 실재):

| run | 스텁 참값 | 이벤트 경로 | 값 |
|---|---|---|---|
| `RUN-9ea56d121e44`(retry) | 2 | `[33].payload.synthesis.calls` | **2** · `safetyRetried` **true** |
| `RUN-61cab1853ef0`(named) | 1 | `[33].payload.synthesis.calls` | **1** · `safetyRetried` **false** |

- **상한 단위**(같은 세션 3 run · 스텁 `retry`):

| run | `x-fkt-run-cap-used` | 그 시점 누적 호출(스텁) |
|---|---|---|
| 1 | **1** | 2 |
| 2 | **2** | 4 |
| 3 | **3** | 6 |

⇒ 계수 단위는 **run** 이다(호출이 2회여도 +1). 그리고 **「시간당 3 = 최대 6 호출」이 실측으로 성립**한다 — README 표가 적은 그 값이다.
🔴 상한 상태는 `/api/live/status` 가 아니라 **run 생성 응답 헤더**(`x-fkt-run-cap-*`)에 있다. `live/status` 본문만 보고 「상한 칸이 없다」고 적었으면 안 잰 것을 잰 것처럼 적었을 뻔했다.

## 8.4 🔴 남은 회부(경) — 거절 갈래에 `calls=` 로그가 없다

`log.info("synthesize calls=…")` 는 여전히 **루프 뒤 1곳**(`live_synthesis.py:763`)이라, 거절로 먼저 return 하는 갈래는 그 줄을 안 남긴다. 같은 실행의 로그 9줄에서 `calls=` 는 **3줄**(`named`·`retry`·`omitted`)뿐이고, **소모 1인 `streamerr`·`guardfail`·`noresult` 는 0줄**이다. 페이로드에는 남으므로 앞판 결함(어디에도 안 남음)은 해소됐다 — 발주문 ①의 「로그 1줄도 그 갈래에서」는 **미충족**이다. 판정은 오케 몫이다.

## 8.5 안 잰 것

- 화면(이 칸의 소비자 0) · 실 CLI 미호명 **발생률** · README 가 적은 「본문 파싱 실패」·「발췌 0건」 두 갈래(나머지 6갈래는 8.1 에서 실측) · **전역(서비스 전체) 시간당 상한**은 세션 축 헤더로만 봤다.

## 8.6 자수 (이 조각)

1. **`Set-Cookie` 를 대문자 키로 찾아** 「세션을 못 만든다」로 읽을 뻔했다 — uvicorn 은 소문자다. 헤더는 **케이스 무시**로 찾는다.
2. **본문 `sessionId` 를 내가 지어** 422(쿠키와 본문이 다르다)를 맞았다. 서버가 발급한 값을 그대로 쓴다.
3. **스텁을 재기동했다고 믿고 잰 한 열이 옛 모드(named)로 돌았다** — 포트 기준 kill 이 안 먹었고 새 프로세스는 바인드에 실패했는데 그 실패를 안 봤다. `/_stub/calls.mode` 로 **모드를 실측**한 뒤에야 값이 됐다. 「띄웠다 ≠ 그 모드로 떴다」.
4. **첫 상한 실측이 「3 run = 3 호출」로 나와** run 축과 호출 축이 구별되지 않았다(앞 run 중 하나가 vector 단계에서 실패해 호출 0이었다). ai-api 를 재기동해 계수기를 비우고 **retry 열로 3 run** 을 다시 돌려서야 2·4·6 이 나왔다.
5. **무대 첫 기동이 두 번 죽었다** — `FKT_EXPOSE_API_DOCS` 빈 문자열(pydantic 거부) · 마이그레이션 전 seed. 둘 다 무대 설정이지 대상 결함이 아니다.
