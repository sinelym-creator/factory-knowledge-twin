# D-82b 검증 — 입장 «후»에는 프리페치를 돌려준다 (`d82bv`)

| | |
|---|---|
| 판정 | **PASS** — 축 ①~⑤ 전건 · 대상 결함 0 |
| 대상 | PR #720 `lane/senku2-d82b` **`55b783d`** · `prefetch={hasSession ? undefined : false}`(3파일) |
| 무대 後 | `:8367` · **pid 42640** · cwd **`_wt/senku2-d82b/apps/web-console`** · **BUILD_ID `feHnwwhpGCz3YFObOKgA0`**(발주 일치) · `hasSession` **3건** |
| 무대 前 | `:8369` · **pid 31412** · cwd **`_wt/senku2-d82/apps/web-console`** · BUILD_ID `tJawNWM1_8UlSTyXHaQEM` · HEAD `fb1a708` · `hasSession` **0건** |
| 그물 | `tests/web/d82b_prefetch_restore.mjs`(신규) · `d82_nav_regression.mjs`(축 ⑤) |
| 측정 | 2026-09-05 14:09~14:12 KST · **cap 0 · live run 0** · 각 열 3회(회차마다 새 컨텍스트) |
| 검증자 | 리바이2 48대 |

🔴 **`:8367` 의 pid 가 30956 → 42640 으로 바뀌어 있었다**(무대가 새 빌드로 재기동). 전언을 그대로 썼으면
**옛 무대를 재고 초록을 냈을** 자리다 — 포트는 주어를 기억하지 않는다.
🔴 **경계는 시간 창이 아니라 «URL 전이»다**(발주 명시) — `waitForURL('**/overview')` 가 풀린 순간에
before/after 를 가른다. 「3초 안에 온 것」으로 가르면 무대가 빠르거나 느릴 때 **같은 사실이 다른 색**을 낸다.

---

## 1. 축 ①②③ — 1280 레일 (3회)

| 열 | 입장 «전» 프리페치 | 입장 «후» 프리페치 | 그중 `Cookie` 동반 | `307` | `200` | 착지 |
|---|---|---|---|---|---|---|
| **後(D-82b)** | **0 · 0 · 0** | **2 · 2 · 2** | **2 · 2 · 2** | **0** | 2 · 2 · 2 | **3/3** |
| 前(D-82 만) | 0 · 0 · 0 | **0 · 0 · 0** | 0 | 0 | 0 | 3/3 |

- **축 ① PASS** — 입장 전 프리페치 **0**(D-82 가 지킨 것을 D-82b 가 깨지 않았다).
- **축 ② PASS** — 입장 후 **2건/회차 = 3회 6건**, **전건 쿠키 동반**, **전건 200**, **307 0**
  (센쿠2 자기 실측 「6/6/0」과 일치 — 다만 그 값은 전언이었고, 위 표가 내 열의 실측이다).
- 🔴 **前 열이 이 축의 «판정력»을 증명한다** — D-82 만 있는 무대는 입장 후에도 **0** 이다. 즉
  「입장 후 ≥1」은 그냥 참인 값이 아니라 **D-82b 가 만든 값**이다(대조군 없는 초록은 아무것도 못 가른다).
- **축 ③ PASS** — 착지 3/3(양 무대 모두 · 되돌림 0).

## 2. 축 ④ — 390 드로어 (3회)

| 열 | 드로어 형상 | 입장 후 프리페치 | 착지 |
|---|---|---|---|
| 後 | `count=1` · `role=dialog` · `aria-modal=true` · 링크 **3**(3회 모두) | **0** | **3/3** |
| 前 | 동일 | 0 | 3/3 |

🔴 **드로어의 «입장 후 0」은 결함이 아니다** — 드로어는 열기 «전»엔 DOM 에 없으므로 프리페치할 기회 자체가
없다(O-26·D-82 에서 이미 확인된 사정거리). 이 칸은 «처방이 안 들었다»가 아니라 «해당 없음»이다.

## 3. 축 ⑤ — D-79 6칸 회귀 (後 무대 1회)

앱바 구성 · 상태행(390 새 줄 / 768·1280 같은 줄) · 드로어 형상 · 닫힘 3갈래 · 레일 3/3·토글 숨김 ·
가로 넘침 0 — **전건 PASS** · **콘솔 실오류 0**(WS 잡음 0).

## 4. 못 잰 것 (이름으로)

- **공개면 값이 아니다** — 두 무대 모두 로컬 `next start` 다. 승격 뒤 외부 재검에서 **≈300ms 회복량**을
  재려면 공개면 열이 따로 필요하다(승격 17 재검 실측: 프리페치 끈 상태의 첫 클릭 지연 = 레일 303~314ms).
- **재방문(쿠키 보유 진입)** — 모든 회차가 새 컨텍스트다. 「이미 세션이 있는 사람의 첫 화면」에서
  프리페치가 즉시 도는지는 이 창이 말하지 않는다.
- **입장 «중»(전이 직전 수 ms)** — 경계는 URL 전이 한 점이라, 그 순간에 걸친 요청의 귀속은 이 창의 정의를 따른다.
- **webkit·firefox** — 이번 열은 chromium 뿐이다(D-83 은 별건).
