# D-70 판정문 — 작업지시 화면의 좁은 폭 붕괴와 «잠긴 초안»

> 🔴 **이어 쓰는 사람은 §4 «자수» 절을 먼저 읽어라.**

| | |
|---|---|
| 판정 | 🔴 **PASS** — ⑲⑳㉑㉓ 전건 · **㉒ = 스펙 갱신 포함**(계약 변경 반영) |
| 대상 | PR #661 `lane/senku2-d70` head **`a929a12`** · 셸 `:8197` |
| 대조군 | **`7a37cc6`** 셸 `:8198` · **API 는 양쪽 다 `:8190`**(셸만 다른 열) |
| 그물 | `tests/web/d70_wo_narrow.mjs`(신규) + `tests/web/e2e/t3-5-wo-screen.spec.ts`(**갱신**) |
| 측정 | 2026-09-04 22:26 KST · chromium 360·390·1440 · `rc=0` |
| 검증자 | 리바이2 45대 |

**창을 열기 전에 «대상이 처방을 실었는가»부터 찍었다** — `grep -c 'wo-parts-lock'` = 대상 **1** · 대조군 **0**.

---

## 1. ⑲⑳ 좁은 폭 — 대조군의 빨강이 발주 예상값과 정확히 맞는다

| 축 | 대상 360 | 대조군 360 | 대상 390 | 대조군 390 |
|---|---|---|---|---|
| 문서 넘침 `sw/cw` | **360/360**(넘침 0) | 🔴 **412/360** | **390/390** | 🔴 **412/390** |
| 「+ 추가」 pill 높이 | **24**(1줄) | 🔴 **39**(두 줄) | **24** | 🔴 **39** |
| 「삭제」 pill 높이 | **24** | 26 | **24** | 26 |
| 입력칸 폭 | **280** | 🔴 **16** | **310** | 🔴 **16** |
| 카드 폭 | **320**(=360−40) | 🔴 **40** | **350**(=390−40) | 🔴 **40** |

⇒ **⑲ 전건 PASS · ⑳ 빨강 전건 확인.** 대조군 수(넘침 412 · pill 39 · 카드 40)가 발주문이 예고한 값과
**그대로 일치**한다 — 내가 재는 자리가 폐하 실기기 증상의 그 자리다.

**부품 행 수**를 열마다 함께 찍었다(전부 **1개**) — 「데이터가 달라 폭이 다르다」를 배제하기 위해서다.

## 2. ㉓ 1440 불변

| 축 | 대상 | 대조군 |
|---|---|---|
| 카드 폭 | **740** | **740** |
| 문서 넘침 | 1440/1440 | 1440/1440 |
| pill 높이 · 입력칸 폭 | 24 · 545 | 24 · 545 |
| 콘솔 오류(차집합) | **0** | 0 |

⇒ **넓은 폭은 손대지 않았다.** 🔴 판정선 주의 — 1440 은 **2열 레이아웃이 정상**이라 카드가 뷰포트를
다 쓰지 않는다(처방 주석 「1440 에서는 740px」). 그래서 ㉓ 은 「뷰포트−여백」이 아니라 **대조군과 같은가**로 쟀다.

## 3. ㉑ 잠긴 초안 — 편집 UI 부재 + 남아 있어야 하는 것

| 축 | 대상 | 대조군 |
|---|---|---|
| `wo-part-add` · `wo-part-delete` · `wo-part-name` | **0 · 0 · 0** | 🔴 **1 · 1 · 1** |
| 읽기 목록 `wo-part` | **1**(>0) | 1 |
| 🛡 `wo-safety-delete` | **2**(>0 · 설계 의도로 남는다) | 2 |
| `wo-parts-lock` 문면 | **「✅ 승인됨 · 편집 잠김」** | 🔴 **없음** |

🔴 **«0건»만 세면 화면이 통째로 비어도 초록이다.** 그래서 같은 자리에서 ① 읽기 목록이 남아 있고
② 잠김 사유가 문면으로 있고 ③ 🛡 안전 조치의 삭제는 **여전히 있다**까지 함께 못박았다.
문면은 머리 배지(`:128` `"✅ 승인됨"`)를 그대로 빌린 것이라 **새 낱말이 늘지 않았다**.

## 4. ㉒ e2e — 스펙을 갱신했다 (계약 변경 반영)

`tests/web/e2e/t3-5-wo-screen.spec.ts` 를 **실제로 돌렸다**(센쿠2 미실행분).

| 회차 | 대상 `:8197` | 대조군 `:8198` |
|---|---|---|
| **갱신 전** | 4 failed / 3 passed | 3 failed / 4 passed |
| **갱신 후** | **4 passed / 3 failed** | 3 passed / **4 failed** |

**차집합이 판정이다** — 갱신 전 «대상에만» 실패한 것은 **⑤ 승인** 하나였고, 실패 지점은 `:484`
`expect(getByTestId("wo-part-add")).toBeDisabled()` → **element(s) not found**. 처방이 잠김에서
버튼을 **그리지 않으므로** 옛 판정선이 죽은 것이다 ⇒ **계약 변경**(잠김 = 편집 UI 부재)을 스펙에 반영했다:

```
toHaveCount(0) × add·delete·name
+ wo-part(읽기) > 0 · wo-parts-lock 문면 == 「✅ 승인됨 · 편집 잠김」 · wo-safety-delete > 0
```

🔴 **완화가 아니라 이동임을 같은 회차에서 확인했다** — 갱신한 ⑤ 는 **대상에서 통과하고 대조군에서 실패**한다.
판정선을 옮기면서 검출력을 팔지 않았다.

**③④⑥ 은 양쪽 모두 실패 = 처방과 무관한 기존 실패**다(갱신 전후 불변). 이 PR 에서 손대지 않았고
**「안 잼」이 아니라 «기존 빨강»으로 이름을 남긴다** — 별건 회부 대상:
- `:328` ③ 잠금 — procedures·safety 편집 칸 0 · 삭제 시도 문구
- `:403` ④ 반려 — 사유 빈 = 확인 비활성
- `:516` ⑥ 소유권·가드 — 「그런 작업지시 초안이 없다」 문면

## 5. 🔴 자수

**대상 결함 검출 = 0.**

| # | 내가 본 것 | 진짜 주어 |
|---|---|---|
| 1 | 「무대 미성립 · `wo-parts` 0」 2회 | **내 도구** — 밖에서 `fetch` 로 만든 세션 쿠키를 브라우저에 «옮겨 심어» 남의 셸 초안을 열려 했다. `/work-orders/<id>` 가 `/overview` 로 되돌아왔다. **열마다 그 셸에서 입장하고 그 셸에서 초안을 만들도록** 고쳤다 |
| 2 | ㉑ 「승인 st=422 · 잠기지 않음」 | **지어낸 입력** — `/approve` 본문 스키마를 내가 만들었다. 정본 흐름은 **화면 `wo-approve` → 모달 `wo-confirm`**. 🔴 **허용 스키마가 있는 표면의 4xx 는 대상의 답이 아니다** |
| 3 | ㉓ 1440 「카드 폭 FAIL」 | **내 판정선** — 좁은 폭의 자(뷰포트−여백)를 1440 에 댔다. 그 폭은 2열이 정상이고 대조군도 **같은 740** 이다 ⇒ 판정선을 «불변»으로 옮겼다 |
| 4 | ㉑ 행의 `승인 st=0/pending` | **클릭 직후 읽기가 일렀다** — `data-state` 를 확인 직후에 읽었다. 승인 성사 여부는 **다시 그린 화면의 계수**(add 0 · lock 문면)로 판정했고 그쪽이 정본이다 |
| 5 | e2e 첫 회차가 `:8000` 을 쳤다 | **기본값이 남을 가리켰다** — 스펙의 `API` 기본이 `http://127.0.0.1:8000`(센쿠2 대역)이다. 읽기 진단 1회였고, 이후 전 회차는 `FKT_API_BASE=…:8190` 을 명시해 돌렸다. **무접촉 대역에 기본값으로 닿는 자리** — 별건 회부 |

## 6. 안 잰 것 — 이름으로

1. **엔진** — chromium 만. webkit·firefox 는 안 쟀다.
2. **실기기 0** — 폐하 증상은 안드로이드 실기기다. 여기 값은 헤드리스 에뮬레이션이다.
3. **rejected 초안** — ㉑ 은 `approved` 만 쟀다(문면은 `⛔ 반려됨 · 편집 잠김` 이 될 것이나 **안 잼**).
4. **부품 여러 개** — 초안의 부품 행이 **1개**뿐이라 여러 행이 좁은 폭에서 쌓이는 모양은 안 쟀다.
5. **e2e ③④⑥** — 기존 빨강. 원인 안 팠다.
