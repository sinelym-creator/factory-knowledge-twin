# D-67 독립 검증 판정문 — 좁은 폭에서 초대 카드 본문이 눌리지 않는다

> 검증 좌석(리바이2 44대) · 2026-09-04 · 발주 = 스자쿠 38대 20:08
> 대상 = PR **#645** `3d43375` · 대조군 = **`af522cf`** · 그물 = `tests/web/d67_invite_layout.mjs`
> 발단 = **폐하 아이폰 Safari 실물**(19:57) · 재현 조건(폐하 회신 20:09) = **세로 화면 · 글자 확대 여부 모름**

## 판정 — **PASS**(초대 변형 · 3엔진 · 640 대역) · 🔴 **resume 변형 = 못 잼**(아래 3절)

## 0. 무대 — 🔴 인계 포트를 쓰지 않았다

센쿠2 무대 `:8358`·`:8359` 는 살아 있었으나 **귀속이 안 됐다**(그 SSR 에서 처방 심볼
`sm:flex-row`·`sm:basis-[18rem]` 이 **양쪽 0건**). 「전언은 전언」이므로 **내 무대를 세웠다**.

| | 대상 | 대조군 |
|---|---|---|
| 셸 | `127.0.0.1:8182` @ `3d43375`(`BUILD_ID pzpzGWcWW_piUJ1897KM2`) | `127.0.0.1:8183` @ `af522cf`(`5VrSCq9xEcXd8Tk6pSMfR`) |
| ai-api | 둘 다 `:8152` | 같음 |
| 엔진 | chromium 145.0.7412.4 · webkit · firefox(설치본) | 같음 |

## 1. 초대(invite) 변형 — 본문 폭 · 방향 · 줄 수

| 폭 | 대상 본문(px) | 대상 방향 | 대조군 본문(px) | 대조군 방향 | 판정 |
|---|---|---|---|---|---|
| 390 | **310**(비 0.886) · 줄 [1,3] | `column` | **310**(0.886) · [1,3] | `row`(wrap 2줄) | 같음 — 이 폭은 결함 창이 아니다 |
| **639** | **559**(0.933) · [1,2] | **`column`** | 🔴 **227.8**(0.380) · **[1,4]** | `row` | **갈림** |
| **640** | **560**(0.933) · [1,2] | **`row`** | 🔴 **228.8**(0.381) · **[1,4]** | `row` | **갈림** |
| **641** | **561**(0.933) · [1,2] | `row` | 🔴 **229.8**(0.382) · [1,4] | `row` | **갈림** |
| 1280 | 600.8(0.618) · [1,2] | `row` | 600.8(0.618) · [1,2] | `row` | 회귀 0 |

- 🔴 **발주가 말한 「640 대조군 229 ↔ 대상 560」이 그대로 재현**됐다(228.8 ↔ 560).
  본문이 카드 폭의 **38%** 로 눌리고 줄 수가 **4줄**로 늘어나는 것이 앞판의 증상이다.
- **경계는 정확히 640** — 대상의 `flex-direction` 이 **639 `column` → 640 `row`** 로 갈린다(`sm` = 640px).
  639 와 640 은 **같은 처방의 다른 갈래**이고, 두 갈래 모두 본문 폭이 559/560 으로 온전하다.
- **엔진 3종이 같은 값** — chromium·webkit·firefox 전부 대조군 228.8 / 대상 560.
  ⇒ 🔴 **이 증상은 엔진 고유가 아니다.** 「WebKit 만의 flex 계산」 가설은 이 무대에서 **성립하지 않는다**
  (구현이 자기 PR 에 「재현 위에 서 있지 않다」고 적은 것과 같은 방향).
- **버튼 3개(`tour-start`·`tour-later`·`tour-never`) 전 폭·전 엔진 가시** · **문서 넘침 0** · 콘솔 0.

## 2. 자수 (내 계측기)

1. 🔴 **`stacked` 를 잘못 정의했다** — 1차 회차에서 「본문과 버튼의 y 가 다르면 세로」로 재고
   **640·641·660 모두 「세로」**라는 값을 얻었다. 가로(`row`)라도 `flex-wrap` 이면 버튼이 다음 «줄»로
   내려간다 — 그건 **방향이 아니라 배치 결과**다. `getComputedStyle(el).flexDirection` 을 직접 읽도록
   고치고 나서야 **경계가 정확히 640** 이라는 사실이 보였다. 이름이 뜻과 달랐던 것이 오독의 원인이다.
2. **인계 포트 귀속 실패를 그대로 적었다** — `:8358`·`:8359` 에서 처방 심볼이 0 이라 쓰지 않았다.
   그 포트가 무엇인지는 **내가 밝히지 않았다**(센쿠2 자원 · 무접촉).

## 3. 🔴 못 잼 — **resume(「이어서 보기」) 변형**

폐하 증상이 난 카드가 **재개 변형**이라는 추가 하명(20:10)을 받고 그 열을 세우려 했으나 **무대가 서지 않았다**
(초대 카드 0 · `exit 2` · 색 없음). 시도한 경로: 초대 → 「나중에」(`tour-later`) → 「튜토리얼」 재열기 →
안내 닫기 → 초대 카드 기대. `resume` 은 `status === "dismissed"` 일 때 서는데(`tour-provider.tsx:211`),
그 상태를 내 순서로는 만들지 못했다. **이 축은 이 회차에서 시험되지 않았다** — 초록도 빨강도 아니다.

## 4. 안 잰 것 (이름으로)

1. **resume 변형 전부**(위 3절) — 폐하 증상의 실제 카드다. 다음 회차의 1착.
2. **실기기** — 폐하 아이폰 Safari. 이 판정은 **playwright 엔진 에뮬**이다. 승격 13 뒤 폐하 1회 열람이 그 축.
3. **landscape** — 폐하 회신이 「세로 화면」이라 가로는 안 쟀다.
4. **글자 확대(동적 타입)** — 폐하도 「모름」. 확대 상태에서 같은 폭이 어떻게 되는지 안 쟀다.
5. **320px 이하** · 640~1280 사이의 다른 폭.

## 5. 재현

```
# 무대 : 각 트리에서 FKT_API_BASE=http://127.0.0.1:8152 pnpm build → npx next start -p 8182 / 8183
cd tests/web && npm install
MSYS_NO_PATHCONV=1 node d67_invite_layout.mjs --base http://127.0.0.1:8182 --out C:/…/tgt.json \
  --engines chromium,webkit,firefox --widths 390,639,640,641,1280
MSYS_NO_PATHCONV=1 node d67_invite_layout.mjs --base http://127.0.0.1:8183 --out C:/…/ctl.json …
```
