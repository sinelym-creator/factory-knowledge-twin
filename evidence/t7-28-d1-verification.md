# T7-28 — D-1 처방 독립 검증 (안내 카드 ✕ 가 투어 중에 눌리는가)

- **좌석** 검증(리바이2 39대) · **발주** 스자쿠 34대(10:01:29) · **기록** 2026-09-04
- **대상** `origin/develop` **`de80464`**(#563 병합) · **처방 = 센쿠2 T7-28**(허용 노드를 React 상태로)
- **판정** 🔴 **PASS** — 두 경로 × 5 회차 = **10 칸 전부 PASS** · 같은 빌드로 **GS 10/10** · 콘솔 오류 **0**

## 0. 🔴 창을 열기 «전»에: 이 빌드가 처방을 실었는가

「병합됐다」는 전언이다. 실측으로 확인한 것 —

| 확인 | 값 |
|---|---|
| 새 파일 | `apps/web-console/components/tour/tour-allowed.tsx` **신설**(+67줄) |
| 배선 3곳 | `app-shell.tsx` `<TourAllowedProvider>` · `overview-body.tsx` `registerAllowed(el)` · `tour-overlay.tsx` `useTourAllowed()` |
| 옛 결함 경로 | `querySelector('[data-testid="intro-card"]')` **호출 없음** — 그 문자열은 **주석에만** 남았다(tour-overlay.tsx:198) |
| 빌드 | 새 프로세스 · `INSTALL_RC=0` · `BUILD_RC=0`(🔴 파이프 없이 직접 확인) |

## 1. 무대

| 축 | 값 |
|---|---|
| 셸 | `lane/levi2-t7-28v`(`de80464`) × `FKT_API_BASE=http://127.0.0.1:8102` build·runtime 일치 → **`:8796`** 새 프로세스 |
| 상류 | **실 ai-api** `:8102`(스텁 아님) · `/api/health` `status:"ok"` · postgres 10ms · neo4j ok |
| 브라우저 | chromium · 1440×900 |
| 그물 | `tests/web/t723_d1_split.mjs`(39대 · T7-23 축①에서 이 결함을 19/19 FAIL 로 찍은 바로 그 자) |
| 대조군 | 🔴 **매 회차 · 별도 컨텍스트 · 양면** — 양성(자유 상태 own 21/25 · 클릭 성공) · 음성(`main` 강제 inert → own 21→0 · 클릭 타임아웃). **5 회차 전부 ✓** |

## 2. 판정표 (두 경로 × 5 회차 · 원점 = 「묻기 시작한 시각」)

| 회차 | 경로 | 첫 표본 | 카드 첫 등장 | 카드 등장 시 own | `main` inert | 조상 inert | 닫기 | 남은 카드 |
|---|---|---|---|---|---|---|---|---|
| r1 | 재열람 | 0ms | 51ms | **21/25** | **false** | `[]` | **성공 19ms** | **0** |
| r1 | 주소 직접 | 39ms | 110ms | **21/25** | false | `[]` | **성공 44ms** | **0** |
| r2 | 재열람 | 0ms | 58ms | **21/25** | false | `[]` | **성공 33ms** | **0** |
| r2 | 주소 직접 | 43ms | 43ms | 0/25 → 곧 21/25 | false | `[]` | **성공 57ms** | **0** |
| r3 | 재열람 | 1ms | 46ms | **21/25** | false | `[]` | **성공 28ms** | **0** |
| r3 | 주소 직접 | 47ms | 47ms | 0/25 → 곧 21/25 | false | `[]` | **성공 59ms** | **0** |
| r4 | 재열람 | 0ms | 47ms | **21/25** | false | `[]` | **성공 21ms** | **0** |
| r4 | 주소 직접 | 44ms | 44ms | 0/25 → 곧 21/25 | false | `[]` | **성공 48ms** | **0** |
| r5 | 재열람 | 0ms | 47ms | **21/25** | false | `[]` | **성공 23ms** | **0** |
| r5 | 주소 직접 | 35ms | 35ms | **21/25** | false | `[]` | **성공 44ms** | **0** |

**재열람 5/5 PASS · 주소 직접 5/5 PASS · 투어 10/10 칸에서 떴음**(자극 실재).

### 🔴 앞판과의 대조 — 이 표가 무엇을 뒤집었는가

| | T7-23 축①(수리 전 · `003f3ef`/`7c1ef75`) | 이 표(수리 후 · `de80464`) |
|---|---|---|
| 재열람 | **19/19 FAIL** · 카드 등장 시 `own=0/25` · `main inert=true` · 조상 `["main"]` · 닫기 **8초 타임아웃** | **5/5 PASS** · `own=21/25` · `main inert=false` · 조상 `[]` · 닫기 **19~33ms** |
| 주소 직접 | 실 상류 **1/7 PASS**(비결정) | **5/5 PASS** |

**같은 그물 · 같은 상류 종류(실 ai-api) · 같은 원점.** 바뀐 것은 대상 코드뿐이다.

### 🔴 「0/25 → 곧 21/25」 3칸의 뜻 — 막힘이 아니다

주소 직접 열 3회차에서 **카드가 붙은 «그 프레임»의 격자가 0/25** 였다. 그러나 같은 회차에서
곧 21/25 로 앉았고 **닫기가 44~59ms 에 성공**했다. 즉 이건 **«막혔다»가 아니라 «아직 안 앉았다»**다
— 조상 `inert` 은 그 시점에도 `[]` 였다(갇힘의 표지가 없다). 🔴 **한 시점은 대상의 답이 아니다**:
전이 축은 계열로 읽고, 판정은 **닫기 결과**가 낸다.

### 🔴 센쿠2 `ctl-4` 하위 사례 — **이 10칸에서 재현되지 않았다**

발주가 지목한 「`main inert=false` · 조상 0 인데 닫기 실패」 조합은 **0회**다(닫기 실패 자체가 0회).
🔴 **「없다」가 아니라 「이 10칸에서 안 나왔다」**로 적는다 — 비결정 열에서 5 회차는 부재의 증명이 아니다.

## 3. 같은 빌드 · 골든 시나리오 재완주

`tests/web/t717_golden_walk.mjs --base http://127.0.0.1:8796` → **집계 `{"PASS":10}` · 10/10**.
사람처럼 화면 손잡이로만 걷는 그물이다(대상 좌표를 넣고 들어가지 않는다).

| 걸음 | 관측(발췌) |
|---|---|
| 4 조사 완주 | 타임라인 1 · run-step 5 · 단계상태 `done×5` · 상태 문면 「완료」 · **완주=true** |
| 5 원인 후보 | 후보 **2** · 근거 칩 **4** |
| 6 근거 화면 | `trust-header` **1** · 출처·상태 문면 있음 |
| 7 작업지시서 | 입력칸 **2** · 승인 버튼 **1** |
| 8 승인 | 확인 모달 **1** → 승인 표시 **true** · ⚠ **감사 문면 = false**(아래 한정) |
| 9 비교 | `compare-strategy` **3** · 각주 1 · 본문 11310자 |
| 10 콘솔 | **런타임 오류 0** |

## 4. 한정 — 🔴 이 PASS 가 «말하지 않는» 것

1. **승인 걸음의 「감사 문면 = false」** — 승인 표시는 떴지만 **감사 흔적 문면은 이 그물이 못 읽었다**.
   그물의 판정선상 걸음 자체는 PASS 이나, **auditId 문면 축은 여전히 「안 잼」**이다(38대 인계와 동일).
2. **1440×900 · chromium · prod 빌드**만이다. `next dev` · 다른 폭 · 다른 엔진 · 투어 2걸음 이후는 안 잼.
3. **n=5 × 2 경로.** 주소 직접 열은 수리 전에 **비결정**이었으므로, 5/5 는 「이 창에서 한 번도 안 나왔다」이지
   「영원히 안 난다」가 아니다.
4. **배포(`:8010`) 실물은 안 잼** — 이 표는 로컬 prod 빌드다.

## 5. 자수 (이 회차 · 내 계측기)

| # | 무엇 | 어떻게 잡혔나 |
|---|---|---|
| 1 | 요약표의 `main inert` 열이 `true(0ms)` 로 찍혀 한순간 「아직 갇힌다」로 읽힐 뻔했다 | 그 `true` 는 **t=0 표본**(아직 이전 문서)의 것이었다. 회차별 원자료의 `mainInertAtFirstCard` 를 보니 **10칸 전부 false · 조상 `[]`** 였다. 요약은 계열을 접은 값이고, 판정은 **카드 등장 시점의 값 + 닫기 결과**로 낸다 |

## 6. 재현

```bash
FKT_API_BASE=http://127.0.0.1:8102 FKT_API_BASE_BUILD=http://127.0.0.1:8102 pnpm build   # rc 직접 확인
FKT_API_BASE=http://127.0.0.1:8102 pnpm exec next start -p 8796
node tests/web/t723_d1_split.mjs --row=fix-r1 --base=http://127.0.0.1:8796 --json=r1.json   # ×5
node tests/web/_t723_table.mjs r1.json … r5.json
node tests/web/t717_golden_walk.mjs --base http://127.0.0.1:8796
```

- 종료 코드 **2** = 대조군이 안 울린 회차(무대 폐기 · 판정 금지). 이 회차는 5/5 모두 0 이었다.
