# T5-6 — P6 «공개 경계» 최종 스캔 (baseline §15.2 · §16 · §34.6)

> 검증 좌석(리바이2 26대) · 2026-09-02 13:49~14:30 KST · 대상 = `origin/develop` **966c539**
> 자극 **0** — 공개 URL·컨테이너·8011 접촉 없음. 읽기 전용 스캔이다.

---

## §0 무엇을 어떻게 쟀나

### 0.1 근거 등급

E1 = 이 창에서 내가 실행한 실측 · E2 = 리포 문서·원장 인용 · E3 = 내 소견 · E4 = 가설.
**판정은 E1 만으로 한다.** E3 는 「회부」로만 올린다.

### 0.2 🔴 모집단이 둘이다 — 트리와 이력을 섞지 않는다

판정문 `t5-5-gate-verdict.md` §35.4 ⑦ 은 「`ci_hygiene` 3게이트 0히트(**작업 트리** 461파일)」로
**부분 — «이력» 축 빈 칸** 이었다. 트리의 0 을 이력의 0 으로 옮겨 적으면 그 빈 칸이 조용히 초록이 된다.
그래서 이 스캔은 두 모집단을 **따로 세고 따로 보고**한다.

| 모집단 | 크기(E1) | 도구 |
|---|---|---|
| 작업 트리 | 추적 **471**건 · 스캔 **469**건(`.github/workflows/` 2본 제외 = CI 와 동일) | `tests/api/public_boundary_scan.py` |
| git 이력 | 커밋 **832**건(`--all` · 도달 가능 전부) | 같은 도구의 이력 축(`git log -G`) |

🔴 **이 문서의 계수는 «이 PR 이 얹히기 전» 트리(966c539)의 것이다.** 이 PR 이 파일 2본을 더하면
같은 명령이 **471파일 · A3 49 · A5 10** 을 낸다 — 늘어난 3건은 **이 문서와 스캐너 자신의 문면**이다
(A3 = `tailscale funnel` 이라는 낱말 · A5 = 탐지용 env 이름 문자열). 내 산출물이 측정 면에 섞이는
자리라 미리 적어 둔다. `ci_hygiene_drill` 3게이트는 이 PR 을 얹은 뒤에도 **0히트 · rc 0**(E1).

### 0.3 🔴 「0」의 세 가지 뜻 — 이 문서는 셋을 다른 칸에 적는다

1. **돌렸고 0건이다**(판정력 있는 0)
2. **그 패턴을 안 봤다**(사정거리 밖 — 각 축의 「못 가르는 것」 줄)
3. **계측기가 못 가른다**(형태가 없는 대상 — A4 의 실명·실장비 ID)

각 축은 **대조군**(합성 양성·음성 표본)을 통과해야 초록을 낼 자격을 얻는다.
대조군이 깨진 축은 초록이 아니라 **판정력 없음(exit 2)** 으로 죽는다.

### 0.4 실행한 것 (이것이 이 문서의 경계다)

| # | 무엇 | rc(자식 직독) | 산출 |
|---|---|---|---|
| ① | `public_boundary_scan.py --tree`(A1~A6 · 신설) | **1** | 트리 히트 90건 |
| ② | `public_boundary_scan.py`(트리 + 이력 4축) | **1** | 이력 히트 커밋 42건(축 합) |
| ③ | `ci_hygiene_drill.py`(기존 · 재실행) | **0** | 3게이트 히트 0 / 471파일 |
| ④ | `surface_scan.mjs`(기존 · 재실행) | **0** | 계약 밖 0 / 모집단 53파일 |
| ⑤ | 대조군 — 심은 사본 1본으로 ①③④ 를 동시에 흔들기 | ①**1** ③**1** ④**1** | §1-2 |

🔴 rc 는 전부 **자식 프로세스에서 직독**했다(`| tail` 뒤의 `$?` 는 tail 의 것이다 — 25대 자수 ① 재발 0).

---

## §1 계측기가 살아 있는가 — 판정보다 먼저

### 1-1. 내장 대조군(표본 17종 · 양성 10 · 음성 7)

여섯 축 전부 **양성 표본에서 빨강 · 음성 표본에서 초록**. 한 축이라도 어긋나면 도구가 exit 2 로 죽는다.
음성 표본에는 **위양성 함정**을 일부러 넣었다 — `/home/runner/…`(러너 표준 경로) ·
`user@example.com` · `100.5.5.5`(CGNAT 밖 사설) · `cur.execute(sql, (wo_id,))`(정상 바인딩).

### 1-2. 🔴 끝에서 끝까지 대조군 — 심은 사본 1본(커밋 0 · 즉시 삭제)

`apps/web-console/lib/_ctrl_boundary_probe.ts` 에 여섯 축의 위반을 하나씩 심고 `git add -N` 으로
**추적 모집단에 넣은 뒤** 세 도구를 같은 순간에 돌렸다. 측정 후 파일과 인덱스 등록을 되돌렸다
(되돌린 뒤 재실행으로 원래 계수 복귀 확인 — 트리 히트 90 · `ci_hygiene` rc 0).

| 도구 | 평시 | 대조군 심은 뒤 | 무엇이 증명됐나 |
|---|---|---|---|
| `public_boundary_scan --tree` | rc 1 · 히트 90 | rc 1 · **A1 5→7 · A2 0→2 · A3 48→49 · A4 11→12 · A5 8→9 · A6 18→19** | 여섯 축 전부 **트리 파일 경로**로 빨강을 낸다(메모리 안 정규식만이 아니다) |
| `ci_hygiene_drill` | rc 0 · 0히트 | rc **1** · G-2 1건 · G-3 1건 | 기존 그물도 살아 있다 — 🔴 **단 심은 2형태 중 1형태만 잡았다**(§4 ①) |
| `surface_scan.mjs` | rc 0 · 계약 밖 0 | rc **1** · 계약 밖 **1** | Q-71 수정이 **검출력을 팔지 않았다**(위양성만 걷어냈다) |

🔴 ④ 의 「계약 밖 0」은 이 대조군이 없으면 **「그물이 아무것도 못 잡는 상태의 0」과 구분되지 않는다.**

---

## §2 트리 축 — 축별 결과 (E1)

> 계수 규칙: **히트 ≠ 위반**. 히트를 분류한 뒤에만 위반으로 센다(25대 §5-1 ① 계보 — 위양성 전수를
> 판정에 인용해 과대계상한 자리). 아래 「분류」 열이 그 작업이다.

| 축 | 히트 | 모집단 | 분류 | **위반** | 못 가르는 것(이 축의 0 이 좁아지는 자리) |
|---|---|---|---|---|---|
| **A1** 시크릿 패턴 | 5 | 469파일 | 합성 프로브 5(표지 `LEVI2PROBE`·`AAAABBBB`·`pw@host` 육안 확인) | **0** | 형태 있는 토큰·webhook·따옴표 대입만. 자연어로 적힌 비밀번호·외부 저장소의 키는 못 본다 |
| **A2** 개인 절대경로 | **0** | 469파일 | — | **0** | `C:\Users` · `/home/<u>` · `/Users/<u>` 3형태. `/home/runner`(러너 표준)는 패턴에서 뺐다 |
| **A3** tailnet·100.x·Funnel | 48 | 469파일 | 실호스트 `*.ts.net` **22히트/7파일**(distinct 4) · CGNAT **100.64/10 IP 8히트/4파일**(distinct 2) · `tailscale funnel` **명령 문면 19히트/4파일** | 🔴 **판단 회부**(§4 ②) | 사설 DNS 별칭·단축 URL 뒤의 tailnet 은 못 본다 |
| **A4** 실데이터 표지 | 11 | 469파일 | 형태 위양성 10(문서 id `@r2` · `vercelId` · score 소수 · Discord 메시지 id) + 합성 DSN 1 | **0** | 🔴 이메일·휴대폰·주민번호 **형태**만. **실명·실장비 ID·실주소는 형태가 없어 못 가른다** — 이 축의 0 은 그 3형태에서의 0 이다 |
| **A5** Claude 구독 노출 | 8 | 469파일 | 전부 **env 이름·게이트웨이 호스트 «문자열»**(누출 드릴의 표본과 탐지 정규식) | **0** | 정적 축이다. 런타임에 공개 라우트가 실제로 모델을 부르는지는 **이 그물이 못 잰다** |
| **A6** 임의 SQL·Cypher·코드 실행 | 18 | 150파일(제품 코드) | 전부 **식별자 f-string**(값은 `$1`·`$2` 바인딩) · 보간원 = 모듈 리터럴 상수(`NodeSpec(...)` 튜플 · 함수 안 상수 튜플) · `# noqa: S608` 주석 동반. **`eval`·`exec`·`os.system`·`shell=True`·`new Function`·`child_process.exec` = 0건** | **0** | 파라미터 바인딩의 «올바름»과 ORM 내부는 못 잰다 |

**A3 분류 합(49)이 히트(48)보다 1 많은 이유**: 한 줄이 호스트와 IP 를 동시에 물면 축 히트는 1,
분류는 2로 센다. 계수를 맞추려 어느 쪽도 지우지 않았다.

**A1 5건의 좌표**(전부 합성): `evidence/t2-4-recheck-matrix.py:23` ·
`services/ai-api/tools/audit_replay_fixture.py:332` · `tests/api/credential_leak_drill.py:107` ·
`tests/api/replay_fixture_drill.py:304,307`.

**A3 실호스트·IP 가 있는 7파일**: `apps/web-console/lib/contract.ts` · `apps/web-console/lib/server-dns.ts` ·
`apps/web-console/scripts/retry-drill.mjs` · `docs/plan/ticket-ledger.md` ·
`evidence/d12-enter-retry-verification.md` · `evidence/t4-3-public-rc-verification.md` ·
`tests/web/d12b_cause_redaction_probe.mjs`.
🔴 이 문서는 **호스트·IP 원문을 싣지 않는다** — 산출물이 다음 스캔의 히트가 되는 자리다.

---

## §3 이력 축 — §35.4 ⑦ 의 빈 칸을 채운다 (E1)

`git log --all -G<패턴>` · 커밋 **832**건.

| 축 | 히트 커밋 | 읽는 법 |
|---|---|---|
| A1 시크릿 | **6** | distinct 토큰 **6개** 전수 확인 → `ghp_LEVI…` · `AKIALEVI…` · `sk-ant-api03-LEV…` ×2 · `sk-ant-api03-AAA…` · `sk-completed-012…`(정상 문자열이 물린 위양성). **전부 합성 프로브 · 28~37자로 실키 길이에 못 미친다** ⇒ **누출 0** |
| A2 개인 절대경로 | **14** | 🔴 **D-003(Q-30)으로 이미 재결된 잔존**이다 — 「ⓐ HEAD 제거까지 · 이력 재구성 불요」(2026-08-30 운영자 승인). 새 사안이 아니다 |
| A3 tailnet·100.x | **17** | §4 ② 와 같은 대상. 이력 재구성은 D-003 과 같은 이유로 **여기서 제안하지 않는다** |
| A4 실데이터 형태 | **5** | 트리 축과 같은 형태 위양성 계열(문서 id·픽스처) |

🔴 **`-G` 의 뜻을 오독하지 마라**: 히트 커밋 = 「그 패턴의 등장 **횟수가 바뀐** 커밋」이다.
**도입 커밋과 제거 커밋을 둘 다 센다.** A2 의 14 는 「유출 14건」이 아니라, D-003 의 제거 작업
커밋들까지 포함한 수다(제목이 `fix(evidence): replace personal absolute path…` 인 커밋이 여기 들어 있다).

🔴 **이력 그물의 생존 대조군**: A2 는 D-003 으로 잔존이 **확정**돼 있다. 그래서 「A2 이력 히트 0」이
나오면 그물이 죽은 것이다 — 도구는 그 경우 exit 2 로 죽게 만들었다. 이번 실행에서 A2 는 14 로 나왔고,
따라서 **A1·A3·A4 의 수도 「그물이 돌긴 했다」는 바탕 위에 있다.**

---

## §4 🔴 회부 (오케 → 원장 · 내가 단독 판정하지 않는다)

> **오케 판정 착신 14:03**(스자쿠 20대) — ① = 결함 확정 **D-19** · ② = 갈라서 판정(**②-a 유지 / ②-b 결함 D-20**).
> 아래 각 항의 「판정」 줄이 그것이다. 🔴 **수리는 이 PR 에 섞지 않는다** — 파일 소유 scope 대로 별 발주다.

### ① CI 시크릿 게이트가 «Anthropic 키 형태»를 못 본다 — **신규 · E1 · D-15 와 같은 계열**

- **사실**: `.github/workflows/ci.yml` 3번째 게이트 = `sk-[A-Za-z0-9]{20,}`.
  실제 키 형태 `sk-ant-api03-…` 는 **3자째 하이픈**에서 문자 클래스가 끊겨 **영원히 통과**한다.
  `ci_hygiene_drill.py` 의 `SECRET` 도 같은 식이라 같은 사각을 갖는다.
- **대조군(E1)**: 한 파일에 `sk-ant-api03-…`(37자)와 `ghp_…`(36자) **두 형태**를 같이 심었다 →
  내 그물 **2건 검출** / `ci_hygiene` G-2 **1건만**(잡은 것 = `ghp_` 줄 · 놓친 것 = `sk-ant-` 줄).
  같은 파일·같은 순간이다. 리포 전체로도 `sk-` 하이픈 형태는 **4파일**에 있는데 CI 식으로는 **0건**이다.
- **지금 새고 있는가 = 아니다.** 그 4파일은 전부 합성 프로브다(§2 A1). 결함은 **「새면 못 잡는다」** 쪽이다.
- **내가 안 고친 이유**: `.github/` 는 검증 좌석 write scope 밖이고, 드릴만 넓히면 **정본(CI)보다 넓은 축**이
  된다 — 그 축의 빨강은 CI 의 빨강이 아니다. **게이트 수리는 별건 발주**로 올린다(ci.yml + 드릴 동시 개정).
- 🔴 **내 축은 지금 CI 정본보다 넓다**(`sk-[A-Za-z0-9-]{20,}`). 그래서 이 문서의 A1 히트 4파일은
  **CI 위반이 아니다** — 전부 합성 프로브이고 **누출 0** 이다. 넓힌 축의 히트를 정본의 빨강으로 옮겨 적지 않는다.
- **판정(오케 14:03)**: **결함 확정 · 원장 D-19 신설.** 수리 = `ci.yml` 정규식 확장(`sk-` 뒤 `[A-Za-z0-9-]`) ·
  종결 조건 = 심은 `sk-ant-…` 사본이 **빨강** + 기존 `ghp_` 검출 **무변** · 담당 = 구현 좌석 cross-scope 발주.
  검증 좌석의 드릴은 **넓히지 않는다**(정본이 먼저 움직인 뒤 따라간다).

### ② 실 tailnet 호스트·CGNAT IP 가 추적 파일에 남아 있다 — **판단 회부 · E1(사실) + E3(의견)**

- **사실(E1)**: 실호스트 `*.ts.net` **22히트 / 7파일**(distinct 4) · CGNAT `100.64/10` **8히트 / 4파일**(distinct 2).
  이력 축 **17커밋**.
- **규율은 이미 성문(E2)**: `docs/deployment/runbook.md:9` 「개인 절대경로는 `<worktree>`,
  **tailnet 호스트는 `<tailnet-host>` 로 쓴다**」. 그런데 **집행 게이트가 0**이고 7파일이 규율 밖이다.
  (`ci.yml` 의 3게이트에 tailnet 축이 없다 — E1.)
- **기존 판단과의 관계(E2)**: 원장 D-15 행에 「tailnet 호스트명은 공개 API 호스트(next.config rewrite
  공개)라 누출 아님」이 있다. 🔴 그 문장은 **호스트명 한정**이며 **CGNAT 100.x 내부 IP 는 다루지 않았다.**
- **내 소견(E3)**: baseline §34.6 의 공개 금지 목록에 「**실제 Tunnel 내부 주소**」가 있다.
  공개 Funnel FQDN 은 「공개 API 호스트」로 읽을 여지가 있으나, **tailnet 내부 IP(100.64/10)는
  그 여지가 없다.** 다만 이것은 크리덴셜이 아니고, 처리 선택지는 D-003 과 같은 성질의
  **비용 대 위험 판단**이다(ⓐ HEAD 마스킹까지 · ⓑ 이력 재구성 · ⓒ 수용). **판정은 오케·폐하 몫.**
- **부수 사실**: 규율을 지킨 표기(`<host>.ts.net` 플레이스홀더)와 실호스트가 **같은 파일 안에 섞여 있다**
  (`docs/plan/ticket-ledger.md`). 규율의 문제가 아니라 **집행(게이트)의 부재**다.

**판정(오케 14:03) — 갈라서 재결됐다.**

| 갈래 | 실측 | 판정 |
|---|---|---|
| **②-a 호스트명 `*.ts.net`** | 22히트 / 7파일 | **D-15 판정 유지 — 공개 API 호스트 · 누출 아님 · 치환 불요.** 이 갈래는 결함으로 세지 않는다 |
| **②-b CGNAT `100.64/10` 내부 IP** | 8히트 / 4파일 · 이력 17커밋과 같은 대상 | 🔴 **결함 확정 — 원장 D-20.** §34.6 「실제 Tunnel 내부 주소」 해당 · 수리 = `<tailnet-ip>` 치환 + **게이트 신설** · 🔴 **이력에 남은 값은 치환으로 지워지지 않는다**(리포가 이미 Public) ⇒ 그 부분은 **폐하 결정 사항으로 상신**(D-003 선례와 같은 성질) |

**수리 분담(오케 배정)**: `apps/` 2파일 = 구현 좌석 · `evidence/`·`tests/` = 검증 좌석(나) · 원장 = 오케.
🔴 **이 PR 에는 수리를 넣지 않는다** — 이 조각의 산출은 «스캔 결과·분류·대조군·회부 좌표»까지다.
좌표는 **파일·행만** 적고 **값 자체는 마스킹**한다.

---

## §5 판정 — 판정문(`t5-5-gate-verdict.md`) 갱신분

| 판정문 위치 | 종전 | **T5-6 이후** |
|---|---|---|
| §35.4 ⑦ Git history secret scan | 「부분 — «이력» 축 빈 칸」 | **조건부 충족** — 이력 832커밋 실측 · 시크릿 토큰 distinct 6 전부 합성(누출 0) · 절대경로 잔존 = D-003 재결분. 🔴 조건 = §4 ① 게이트 사각 |
| §35.4 ⑥ stack trace·secret·구성값 미노출 | 조건부 | **조건부 유지** — 트리 A1 위반 0 을 근거로 더하되, §4 ① 이 붙는다 |
| §35.5 ⑤ credential·volume 미포함 | 충족(추적 파일 축) | **충족 유지** + 이력 축 실측 병기(누출 0) |
| §16.2 「임의 SQL·Cypher·코드 실행」 | 그물 없음 | **정적 축 충족** — 제품 코드 150파일에 `eval`·`exec`·`os.system`·`shell=True`·`new Function`·`child_process.exec` **0건** · SQL·Cypher 는 식별자만 상수 보간 + 값은 바인딩(18곳 전수 확인) |
| §15.2 Claude 구독 노출 | — | **정적 축 충족** — 제품 코드에 SDK import·`api.anthropic.com` 호출 **0건**(히트 8은 전부 누출 드릴의 표본 문자열) |
| 공개 경계 전체 | — | 🔴 **결함 2건(D-19 · D-20) 미수리 상태에서 「공개 경계 PASS」로 부르지 않는다**(§4). ②-a(호스트명)만 D-15 유지로 닫혔다 |

**결론의 사정거리**: 이 판정의 주어는 **`origin/develop` 966c539 의 추적 트리 469파일과 도달 가능 커밋
832건**이다. 미추적 파일·리모트 밖 브랜치·GitHub 측 설정(Actions secret·리포 가시성)·배포 인스턴스의
런타임 응답은 **재지 않았다**.

---

## §6 🔴 내 계측기 자수 (이 창 1건 — 대상 결함으로 세지 않는다)

| # | 무엇 | 진범 · 어떻게 알았나 |
|---|---|---|
| ① | **A1 의 끝-대-끝 대조군이 1차에 불발**했다 — 심었는데 A1 히트가 5에서 안 움직였다 | **내 손**. 대조군 파일에 키를 `"sk-" + "ant-api03-…"` 로 **런타임 조립**해 심었다(자기 게이트 회피 습관을 대조군에까지 적용한 실수). 문자열이 끊겨 있으니 어떤 스캐너도 못 본다 — **「막았다」가 아니라 「자극이 없었다」**. 통짜 리터럴로 다시 심어 재현 → A1 **5→7**, 그리고 그 재현이 §4 ① 을 드러냈다(같은 파일에서 CI 그물은 1건만 잡았다). 🔴 이 한 건을 안 물었으면 **A1 축의 초록을 「트리 경로로 검증된 초록」이라 잘못 적을 뻔했다** |

**재현 절차**: `python tests/api/public_boundary_scan.py`(트리+이력) · `--tree`(트리만) ·
대조군 = `apps/web-console/lib/` 아래 임시 `.ts` 파일에 여섯 축 위반을 **통짜 리터럴로** 심고
`git add -N` → 세 도구 실행 → `git rm --cached` + 삭제 → 재실행으로 원 계수 복귀 확인.

---

## §7 다음 조각에 남기는 것

- **D-19 수리 검증**(구현 좌석이 `ci.yml` 을 고친 뒤) — 종결 조건 = 심은 `sk-ant-…` 사본 **빨강** +
  기존 `ghp_` 검출 **무변**. 🔴 수정이 검출력을 파는 자리이므로 **양쪽을 같이 재지 않으면 종결로 세지 않는다.**
  정본이 넓어진 뒤에야 `ci_hygiene_drill.py` 의 `SECRET` 을 같은 식으로 맞춘다(검증 좌석 몫).
- **D-20 수리**(분담: `apps/` 2파일 = 구현 · `evidence/`·`tests/` = 검증) — 값 치환 `<tailnet-ip>` +
  **게이트 신설**. 게이트는 이 도구의 **A3 IP 갈래를 CI 로 승격**하는 것이 가장 싸다(정규식이 이미 있다).
  🔴 치환은 **트리만** 고친다 — 이력 잔존은 폐하 결정 사항(§4 ②-b).
- **A4 의 사정거리** — 실명·실장비 ID·실주소는 형태가 없어 못 가른다. 이 축을 정말 닫으려면
  **데이터 생성기가 synthetic 임을 선언하는 표지**(예: 시드 매니페스트)를 대조군으로 삼아야 한다.
- **A5 의 런타임 축** — 「공개 라우트가 모델을 부르지 않는다」는 정적으로만 확인했다.
  공개 인스턴스 자극이 허용되는 창에서 라우트 전수 1회를 재야 완결된다.
- **D-15 잔여 종결**: 「사정거리 밖」으로 남아 있던 `/home/<user>`·`/Users/<user>` 형태를
  이번에 실제로 재서 **트리 0**(469파일). 그 행은 이제 닫아도 된다.
