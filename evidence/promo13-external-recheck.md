# 승격 13 공개면 재검 판정문 (promo13ext)

> 검증 좌석(리바이2 44대) · 2026-09-04 20:28~20:40 · 발주 = 스자쿠 38대 20:28
> 공개면 = **`https://factory-knowledge-twin.vercel.app`** · main `b4b06dd` · 배포 ai-api `b4b06dd`
> 그물 = `tests/web/promo13_external.mjs` · `tests/web/promo13_gaps.mjs` · `tests/web/pub_stale_cookie.mjs`

## 판정 — **PASS**(ⓐⓑⓒⓓⓕⓖ) · 🔴 **ⓔ = 못 잼**(공개면 390 에 투어 초대 카드가 서지 않는다 · 원인 미규명)

## ⓐ D-55 「승격 전 쿠키」 — **PASS**

| 축 | 실측 |
|---|---|
| 승격 «전» 캡처 | 20:04:17 · `fkt_sid` 접두 **`rcGj`**(값 미출력 · state 는 리포 밖) |
| 서버가 옛 세션을 아는가 | 🔴 옛 쿠키로 `GET /api/runs` → **401 `session_required`**「세션이 유효하지 않다 — 다시 발급받아라」 |
| 무쿠키 대조 | **401** · 문면은 「세션이 없다 — POST /api/sessions 로 발급받아라」(**다른 문면** = 두 401 이 구분된다) |
| 브라우저 경로 | 옛 쿠키로 진입 → **자동 재입장** → 새 세션 **`23Ct`** · `enterCount 1` · **401 화면 0** · 콘솔 0 |

🔴 도구의 `status401 = 0` 은 「401 이 없었다」가 아니라 **브라우저가 그 401 을 보지 못한다**는 뜻이다
(재입장은 서버 측에서 일어난다). 그래서 401 은 **API 직접 호출로 따로 찍었다**.

## ⓑ 외부 vantage — **PASS**

| 축 | 실측 |
|---|---|
| 연결 IP | **`216.198.79.195`**(공인 · 밖에서 들어갔다는 근거) |
| `/` | **200** · 0.712s |
| `/api/health` | **200** · 0.945s · **`build b4b06dd`** · postgres `ok` · neo4j `ok` |
| `/api/live/status` | **200** · 0.688s |
| `/api/runs?sessionId=x`(무쿠키) | **401 `session_required`** = **라우트 실재**(v0.1.16 착지) |
| Vercel 배포 | `githubCommitSha` **`b4b06dd06b7fca9268f6bf5728a7435c063c4cb1`** · state `READY` · target `production` |

## ⓒ 폐하 경로 3건 — **PASS**

| 경로 | 실측 |
|---|---|
| ① 「튜토리얼」 클릭 | 안내 카드 **0 → 1** · 투어 표지 **1** ⇒ D-65 착지 확인 |
| ② 조사 → 근거 → 「이 조사로 돌아가기」 | live run `RUN-85140aa45c75` 시작 → 근거 진입 → 경로 표시 **「Incidents › 조사 RUN-… › 근거 EQ-CNC-204」**(D-58 착지) → 링크 클릭 → **1.34초에 같은 run 도달** |
| ② Overview 「이 세션의 조사 N건 · 이어보기」 | **「이 세션의 조사 1건 · 이어보기 →」** · 클릭 → **1.33초에 같은 run 도달**(T7-41b 착지) |
| ③ 근거 화면 배지 | 6표본 전부 **`◉ LIVE`** · **「미연결」 0회**(D-64 축 · 공개면에서 재현 시도 중 2초 초과 회차 없음) |

## ⓓ 정적 주소 + 세션 = 칩 1개 — **PASS**

정적 주소(`/incidents/INC-2026-014?run=STATIC-GS-01`)에 세션을 가진 채 진입:
**정적 칩 0 · 세션 칩 1** ⇒ 폐하 스크린샷(19:26)의 「두 칩 동시」 증상 **해소 확인**(O-12 착지).

## ⓕ 콘솔 — **PASS**

**실오류 0** · 제외 대상(WS) **3~4건**(`wss://…/api/ws/runs/…` 연결 실패 — 발주가 제외로 지정한 기지 사항).
🔴 제외했지만 **셌다**는 사실을 남긴다.

## 🔴 ⓔ D-67 카드 390 — **못 잼**

| 폭 | 공개면 초대 카드 | 투어 표지 |
|---|---|---|
| **390** | **0** | **0** |
| 1440 | **1** | 1 |

새 컨텍스트(webkit)로 두 번 세워도 **390 에서는 초대 카드가 서지 않는다**. 로컬 무대(`:8182`)에서는
같은 빌드 계보가 390 에서 그 카드를 띄웠으므로 **환경 차**다. `tour-allowed`·`tour-provider` 에
**폭 게이트는 없다**(grep 0). ⇒ 🔴 **원인 미규명 · 이 축은 공개면에서 시험되지 않았다.**
로컬 판정(#646·#648)은 그대로 유효하다 — 다만 **공개면 확인은 비어 있다.**

## ⓖ 재캡처 3장 — 완료

`fkt-overview-1600-v3.png`(이어보기 카드 있는 상태) · `fkt-run-replay-1600-v3.png` · `fkt-evidence-1600-v3.png`(경로 표시) · 1600×1000 · 팀 채널 첨부.

## 자수 (내 계측기)

1. 🔴 **1차 회차 크래시** — ⓒ① 에서 투어를 열어 둔 채 ⓒ② 「조사 시작」을 눌러 오버레이에 intercept 됐다.
   **대상 결함이 아니다**(투어 중 알람 조작 잠김 = 규격 §⑧-7). 투어를 닫는 절차를 넣었다.
2. 🔴 **1.5초 고정 대기로 「이동 안 함」을 만들었다** — 1차에서 「돌아가기」·「이어보기」가 URL 그대로였는데,
   `waitForURL` 로 바꾸자 **1.34초·1.33초에 도달**했다. 그 회차의 「실패」는 내 대기의 것이었다.
3. **ⓔ 1차 실패는 컨텍스트 오염** — 같은 컨텍스트에서 이미 투어를 열어 상태가 남아 있었다.
   새 컨텍스트로 다시 쟀고, 그래도 390 은 0 이라 위 「못 잼」이 남았다.

## 안 잰 것 (이름으로)

1. **ⓔ 공개면 390 초대 카드** — 위 절.
2. **실기기(폐하 아이폰 Safari)** — 승격 13 뒤 폐하 1회 열람이 그 축.
3. **2초 초과 회차의 배지** — 이 회차의 공개면은 빨라서(0.69s) D-64 의 「느린 답」 조건이 서지 않았다.
   ⇒ 그 축은 **로컬 지연 프록시 판정(#642)** 이 정본이고, 공개면 재현은 없다.
4. landscape · 글자 확대 · 다른 정적 주소.
