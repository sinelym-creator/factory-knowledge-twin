# T7-23 축② — X 예외 대장 1차(무대 불요 묶음)

- **좌석** 검증(리바이2 39대) · **발주** 스자쿠 33대 (3/3) · **기록** 2026-09-04
- **정본** `docs/plan/test-plan-v1.md` §6 · **그물** `tests/web/t723x_exceptions.mjs` · `tests/web/t723x_double_click_ui.mjs`
- **판정선** 🔴 「죽지 않았다」가 아니라 **「정의된 대체 동작을 했고, 그 사실이 남았다」**.
  🔴 **빨강 확인이 없는 케이스는 초록이라도 «미검증»** — 막힘 표본만 보면 「전부 거절하는 문」도 통과한다.

## 0. 무대 (E1 실측 · 09:33:39 복귀 확인)

`:8102` 내 ai-api(`/api/health` → `status:"ok"` · postgres 50ms · neo4j 42ms) · 셸 `:8799`(develop `003f3ef` 빌드) ·
DB = `fkt-senku2-t15-*`(읽기만) · 배포 `:8010` 무접촉.
⚠ **09:29:01 Docker 엔진(WSL) 정지 → 09:32:37 복귀.** 그 창에서 낸 회차는 `exit 2` 로 폐기했고, 이 표는 **복귀 뒤** 회차다.

## 1. 결과 — **X 6/7 PASS · 1 FAIL · 0 미검증** (이번 조각 7건 · 대장 전체 25건)

| # | 축 | 관측 | 판정 | 빨강 확인 |
|---|---|---|---|---|
| X-01 | 입력 | 빈`""` → **400** `question_not_approved` · 공백만 → 400 · 정상 → 200 | **PASS** | ✓ 정상 200 ↔ 자극 400 (같은 실행) |
| X-02 | 입력 | 5만 자 → **413**(바이트 축 미들웨어) · 되돌아온 질문 **에코 없음** · 상한 알림 **있음** · 조용한 절단 **없음** | **PASS** | ✓ 정상 길이 200 ↔ 5만 자 413 |
| X-03 | 입력 | SQL·Cypher·프롬프트 주입 3종 → **전부 400** · 스택/드라이버 누출 **0** · **주입 뒤 정상 질의 200** | **PASS** | ✓ 주입 «전» 200 ↔ 주입 «후» 200 (테이블 생존) |
| X-04 | 입력 | `WO-9999` → **404** `not_found` 「작업지시 초안 WO-9999 를 찾을 수 없다」 · `WOD-999999999999` → 404 · **있는 `WOD-374c3de1a763` → 200** | **PASS** | ✓ 있는 id 200 ↔ 없는 id 404 |
| X-12 | 권한 | 무세션 API → **401** `session_required`(한국어 · 스택 0) · 무세션 셸 `/overview` → **307 → `/`** · 세션 있음 → 200 | **PASS** | ✓ 세션 있음 200 ↔ 없음 401 |
| **X-15** | **멱등** | 🔴 **API 축**: 새 세션 · 같은 본문 **동시 2 POST** → run **2개**(`RUN-a4511c442919`·`RUN-7c4a87ff1547`) · 대조군(다른 새 세션 1회) → **1개** | **FAIL** | ✓ 1회 → 1 종 ↔ 2회 동시 → 2 종 |
| | | **UI 축**(사용자 더블클릭 `start-from-headline`): 클릭①·② **둘 다 감** → `POST /runs` **1건** · runId **1 종** | **PASS** | ✓ 1클릭 1 종 ↔ 2클릭 1 종 |
| X-19 | 멱등 | 리셋 전 run 조회 **200** → 리셋① 200 → 조회 **404** → 리셋② 200 → 조회 **404** · 리셋 뒤 세션 사용 가능 **200** | **PASS** | ✓ 리셋 전 200 ↔ 후 404 = 리셋이 실제로 버린다 |

### 🔴 X-15 = 결함 후보 **D-48** — 회부하되 **좁혀서**

- **성립하는 문장** — 「**API 로 같은 요청을 동시에 두 번 보내면 run 이 두 개 생긴다.**」 정본 §6 X-15 기대(「중복 생성 0 · run 이 둘 생기지 않는다」) 위반.
- **성립하지 «않는» 문장** — 「사용자가 더블클릭하면 run 이 둘 생긴다.」 **화면 열은 PASS**다(2클릭 → POST 1건).
- **안 잼** — UI 열이 «무엇 때문에» 하나인지(클라이언트 가드인지 · 첫 클릭의 화면 이동으로 두 번째가 요청을 못 낳은 것인지)는 **안 갈랐다**. 둘은 다른 사실이고, 가드가 아니라 이동 때문이라면 **다른 화면·다른 속도에서 되살아난다**.
- **판정선 근거** = `docs/plan/test-plan-v1.md` §6 X-15 행 · 「판정선 2 ①」(멱등 = 「상태가 한 번만 바뀌었다」를 **수**로).

## 2. 이 조각에서 «안 잰» 것 — 🔴 0 이 아니라 이름과 함께

- **X-05 / X-20 / X-21**(상류 다운 → REPLAY 낙하가 «표시»되는가) — **안 잼**. 실 ai-api `:8102` 를 죽이면
  **센쿠2가 읽기로 쓰는 상류**가 함께 죽는다. 그래서 `_t723_mirror_stub.mjs` 에 **끊김 스위치**
  (`/__stub/down`·`/__stub/up` · `refused` 계수 = 자극 실재 증인)를 넣어 «내 상류만» 끊는 무대를 준비했다.
  그 스위치를 물 셸(빌드 시점에 `FKT_API_BASE` 가 박힌다)은 아직 안 세웠다.
- **X-18**(시드 2회) — **안 잼**. `fkt-levi2-*` 기동 뒤 별도 조각.
- **X-01~03 의 «화면» 열** — **안 잼이 아니라 «표면 없음»**: 셸 전 화면 훑기 결과 자유 입력 표면이 **0**이다
  (`/compare` 는 체크박스 3개 · `/enter` 는 405 · `/work-orders`·`/evidence` 는 404). 입력 예외는 **API 축에서만** 성립한다.
- X-06·07·08~11·13·14·16·17·22~25 — 이 조각 범위 밖(2차 발주 대기).

## 3. 자수 — 🔴 **전부 내 계측기** (회부 «전»에 걸러냈다)

| # | 무엇 | 어떻게 잡혔나 |
|---|---|---|
| 1 | 문 열림 대조군에 **내가 지어낸 질문**을 썼다 → 400 `question_not_approved`. 하마터면 「compare 가 깨졌다」로 읽을 뻔했다 | 그 400 의 코드를 읽었다. `/api/scenarios` 가 주는 **승인 목록**에서 가져오도록 고침(리터럴 안 박음) |
| 2 | X-15 **대조군을 먼저** 돌려 그 run 이 슬롯을 잡았고, 자극의 두 번째 요청이 429 → 「distinct 1 = 멱등」으로 **초록을 만들 뻔** | 첫 회차와 값이 뒤집혀 재현을 의심 → **자극 열 먼저 · 각 열에 새 세션** |
| 3 | X-19 가 **앞 케이스의 쿠키**를 물고 옛 세션을 리셋 → 404. 「리셋이 안 된다」로 읽을 뻔 | 라우터가 「자기 세션만」이라 답했다. **자기 세션에서** 재설계 |
| 4 | X-19 대조 축을 「활성 run 이 있으면 재생성이 막힌다」로 잡았는데 **그 전제가 거짓**이었다(같은 세션에 run 2개가 나란히 200) | 로그에서 두 200 을 봤다. 축을 **「리셋 전 만든 run 이 보이는가」**(200→404)로 교체 |
| 5 | **429(속도 제한)와 503(의존 불가)** 을 안 가르고 「리셋이 안 됐다」로 읽을 뻔 | 두 코드가 같은 자리에서 갈렸다. 코드를 판정문에 함께 싣고 재시도 축을 넣음 |
| 6 | `Retry-After` 를 그대로 따르다 그물이 **10분 넘게 멈췄다** | 배경 실행이 안 끝났다. 그 축은 X-19 재설계로 삭제 |

## 4. 재현

```bash
node tests/web/t723x_exceptions.mjs --api=http://127.0.0.1:8102 --web=http://127.0.0.1:8799
node tests/web/t723x_double_click_ui.mjs --web=http://127.0.0.1:8799
```

- 종료 코드 **2** = 문이 안 열렸거나 대조군이 안 울린 회차(무대 폐기 · 판정 금지).
