# 승격 29 — 외부 재검 (리바이2 61대)

- production 실물 = main `4225b4f` · Vercel `dpl_8UAt4jhW4mq8A3BC9BPVUMGKDpE6`(READY 17:43) · 공개 도메인 `https://factory-knowledge-twin.vercel.app`
- 창 = `date 18:24:01` ~ `18:28:03` · 🔴 **구독 0** · production **읽기만**(변경 0) · 근거 **E1**
- 트리 = `_wt/levi2-promo29x`(`lane/levi2-promo29x` · develop `f6c9fe4`)

## 0. 판정 — **PASS** (축 5개 실측 · 새 빨강 0)

| 축 | 값 |
|---|---|
| ① 외부 vantage 배포 sha | `r.jina.ai` 경유 `/api/health` → **`"build":"4225b4f"`** |
| ② 공개면 `/live/status` 형상 · 401 baseline | `{"online":true,"checkedAt":…}` **57 B** · `GET /api/scenarios` → **401 `session_required`** |
| ③ D-89 링 412×600 | **3/3 PASS** — 전 회차 `top 36 · bottom 563 · vh 600` |
| ④ 배지 · 금칙어 | `data-mode=live` · `mode-badge-why` **0개**(§3) · 본문 금칙어 **0건** |
| ⑤ replay 완주 | `synthesis-badge` = 「◉ live 합성 claude-opus-5」 · 후보 카드 **5개** · 오류 화면 **0개** |

## 1. 🔴 구독 0 — 드릴이 스스로 감시한다

이 창의 전제는 「Live 실행 버튼 0 · 프로브 0」이다. 그 전제를 **문장으로 약속하지 않고 값으로 남긴다**:
드릴이 자기 페이지의 요청을 듣고 **`POST …/runs`** 가 나가면 세어 찍는다.

전 실행(배지 1 · 링 3 · replay 2) **모두 `0건`**. 화면은 읽기(GET)와 정적 replay 만 탔다.

## 2. ① 「밖」의 근거 — 공개 URL 을 쳤다는 사실만으로는 밖이 아니다

| 경로 | `/api/health` build |
|---|---|
| `r.jina.ai` 경유(외부 vantage) | **`4225b4f`** |
| 내 직접 요청 | `4225b4f` |

두 값이 같다 = 내가 직접 친 것도 같은 배포를 보고 있다. 응답 헤더는 `Server: Vercel` · `X-Vercel-Cache: MISS` ·
`X-Matched-Path: /api/[...path]` — **Vercel 엣지를 지난 응답**이고 tailnet 경로의 표지가 없다.
브라우저 축에서도 문서 본문에 `Tailscale`·`tailnet` 문자열 **0건**.

🔴 **못 한 것**: 전이의 «前» 값(`81aec01`)은 이 창이 열렸을 때 **이미 지나갔다**. 지금 값이 `4225b4f` 라는 사실만 참이고,
「`81aec01` → `4225b4f` 로 바뀌는 순간」은 관측하지 못했다(§5).

## 3. ④ `mode-badge-why` 0개는 «구현 누락»이 아니다

발주 문면은 「배지 본문 `mode-badge-why` **존재** + 금칙어 0」이었다. 그러나 정본(v0.2.4 화면 문면 개정 표)은

| 상황 | 배지 | 문장 |
|---|---|---|
| `online:true` | **LIVE** | **(없음)** |

공개면 실측이 `data-mode=live` · `/live/status` `online:true` 이므로 **문장이 없는 것이 정본대로**다.
「존재」를 판정선으로 삼았다면 이 칸은 **거짓 빨강**이 됐다. 문장 3종의 렌더는 `online:false` 상태에서만 서고,
그 상태를 공개면에서 만들려면 **실제 소모나 실패 주입**이 필요하다 — 이 창은 구독 0 이라 만들지 않았다(§5).

## 4. 내 계측기 자수

1. **「오류 화면 1개」를 회부할 뻔했다.** 내 셀렉터가 `[role="alert"]` 을 오류로 셌는데, 잡힌 1건은
   **`data-testid=null` · 문면 `""`** 인 **빈 aria-live 영역**이었다(`error-screen` 은 0개). 화면이 아무 말도 하지 않는 자리를
   오류로 적을 뻔했다. 판정선을 「전용 testid 이거나 **문면이 있는** alert」로 좁히니 **0개**가 됐다.
2. **발주 문면과 정본이 갈린 자리를 정본으로 맞췄다**(§3). 발주의 「`mode-badge-why` 존재」를 그대로 판정선에 넣지 않았다.
3. **「밖」을 URL 로 주장하지 않았다** — 헤더·문서 표지·외부 vantage 대조를 함께 적었다. 다만 그 판별이 «갈리는지»를
   내부 tailnet 경로로 직접 대조하지는 못했다(그 경로를 이 창에서 열지 않았다 · §5).

## 5. 안 잰 것

- **배포 sha 전이의 «前» 값** — 창이 열렸을 때 이미 `4225b4f` 였다. 전이 순간은 배포 메타(오케 실측)가 답한다.
- **`reason` 문장 3종 렌더(공개면)** — `online:false` 상태가 필요하고, 그 상태를 만들려면 **실소모 또는 실패 주입**이 든다.
  구독 0 전제라 만들지 않았다(E4).
- **걸쇠 해제 두 축 · 시간당 상한 소진 문장** — 같은 이유로 미측정.
- **외부 판별의 대조군** — tailnet 경로를 같은 창에서 열어 「표지가 실제로 갈리는가」를 보지 못했다.
- **다른 엔진·실기기.**
