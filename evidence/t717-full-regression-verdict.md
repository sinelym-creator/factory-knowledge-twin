# T7-17 — 전체 회귀 + 골든 시나리오 완주 판정문 (리바이2 38대 · 2026-09-04)

> 폐하 하명 3축 — ① e2e/playwright 전량 재실행 ② 골든 시나리오(GS-01) 완주 ③ 전체 기능 훑기.

## 0. 무대 (sha 를 «값으로»)

| | |
|---|---|
| 웹콘솔 | **`f76cc0b`** = `origin/develop`(`2372281`)을 `lane/levi2-t717` 에 병합한 것 · `pnpm build` · `http://127.0.0.1:8799` |
| ai-api | 🔴 **내가 띄운 것** `http://127.0.0.1:8102`(`services/ai-api`, 같은 워크트리 코드) · `postgres:ok · neo4j:ok` · **계약 표면 21 paths** |
| DB | `fkt-senku2-t15-postgres-1`(:5536) · `fkt-senku2-t15-neo4j-1`(:7589) |
| 무접촉 | 🔴 **`:8010`(배포 컨테이너)·`:8101`(스텁)·`:8797`·`:8798`(구현 무대)·`main`** |
| 조건 | chromium · 1440×900 · `retries: 0` · `online:false`(⇒ REPLAY) |

## 1. 🔴 첫 회차는 «무대 결함»이었다 — 지우지 않고 남긴다

**첫 실행 = 76 passed · 63 failed · 3 skipped.** 이 63 은 **대상 결함이 아니다.**

- 나는 `FKT_API_BASE` 를 **`:8101`** 로 걸었다(발주문 문구 「두 무대가 같은 `:8101` 을 본다」를 실측 없이 채택).
- 실측: `:8101` 은 `/docs` **404** · `/openapi.json` **404**(paths **0**) ⇒ **FastAPI ai-api 가 아니라 얕은 스텁**이다
  (`/api/live/status` 와 `POST /api/sessions` 만 답한다).
- **preflight 가 못 걸렀다** — 그 두 홉만 재기 때문이다. ⇒ **깊은 API 를 부르는 스펙만 무더기로 빨강**이 되고
  **얕은 화면 스펙은 전건 초록**이었다. 형태가 정확히 「환경이 안 섰다」다.
- 🔴 **같은 무대의 초록(76)도 함께 폐기한다.** 초록만 살리고 빨강만 버리면 «유리한 쪽으로 고르는» 것이다.
- **자수** — 전대 재개점 §1 에 「`:8010` 은 **진짜** ai-api」라고 적혀 있었는데, 발주문 문구를 우선했다. **발주문도 전언이다.**

**처방(내 scope)** — `preflight` 에 **깊은 검사** 1건: `/openapi.json` 경로 수 **하한 10**.
🔴 **지금 수(21)를 박지 않았다** — 계약이 늘면 그 수가 먼저 늙는다.
**양방향 대조군 성립** — 얕은 스텁(`:8101`)을 물리면 preflight 가 **실제로 던진다**(RC=1).

## 2. 축① e2e 전량 — **135 / 142**

| 회차 | 무대 | passed | failed | skipped |
|---|---|---|---|---|
| 1차 | `:8101` 스텁 | ~~76~~ | ~~63~~ | 3 | ← **전량 폐기(무대 결함)** |
| 2차 | `:8102` · `728b66f` | 133 | 5 | 4 |
| 3차 | `:8102` · `f76cc0b` | 133 | 5 | 4 |
| **4차(정본)** | `:8102` · `f76cc0b` · 그물 2건 교정 | 🔴 **135** | **3** | 4 |

**대조군** — ⓐ `shell.spec.ts` **9/9**(무대가 섰다) ⓑ preflight 깊은 검사가 스텁에서 **운다** ⓒ 교정한 스크림 축은
같은 실행 안에서 **다른 색을 넣으면 갈린다**(비교가 죽지 않았음).

### 남은 3본의 «주어»

| # | 스펙 | 주어 | 근거 |
|---|---|---|---|
| ① | `t3-2:484` D-1 | 🔴 **대상 결함**(D-1 재개방 · 오케 판정) | 아래 §3 |
| ② | `t42b:208` ⓖ-2 | **무대 없음 = 안 잼** | 스펙 문면 「503 이 안 났다 — 용량 거절을 잴 무대가 없다」 |
| ③ | `t6-6:226` | **무대 없음 = 안 잼** | 스펙 문면 「`online:false` … 이 축의 빨강은 무대의 것」 · 합성 게이트웨이 `:8787` 필요 |

🔴 ②③ 는 **X 축(폴백 6건 · X-20~X-25)에서 무대와 함께** 재는 것으로 오케와 합의. 지금 끼우면 또 「무대 없는 빨강」이다.

### 내가 고친 그물 2건 (검출력을 팔지 않았는지 함께 적는다)

1. **`reset-modal:44` R-3 — 판정선 노화.** 앞판은 `--color-scrim` 이라는 **이름**이 브라우저에 있는지 물었다.
   Tailwind v4 `@theme inline` 은 그 이름을 **일부러 CSS 변수로 안 내보낸다.** 실측: `--color-scrim`="" 이고
   **`--color-bg` 도 ""** ⇒ `--color-*` 가 **전부** 없다(이름 하나의 사고가 아니라 설계층이 바뀐 것).
   **대상은 무결** — class `bg-scrim`(하드코딩 아님) · 칠해진 색 `rgba(0,0,0,0.55)` = 실토큰 `--fkt-scrim`(`#0000008c`) **일치**.
   ⇒ **원래 재려던 것**(토큰 계층을 지나는가)을 살아 있는 축으로 다시 묻게 바꿨고, **같은 실행 대조군**
   (다른 색을 넣으면 비교가 갈리는가)을 함께 넣었다.
2. **`t3-3:197` STALE 주입 — 무대 이름이 낡았다.** 스펙 기본값이 `fkt-levi2-postgres-1`(**없는 컨테이너**)이었다.
   이미 `FKT_PG_CONTAINER` 로 인자화되어 있어 **파일을 안 고치고** `fkt-senku2-t15-postgres-1` 로 지정해 돌렸다 →
   🔴 **PASS**(주입 전 FRESH → 주입 후 STALE → 화면 배지 stale → 대조군 fresh → 원복 FRESH).
   스펙 자체의 **귀속 가드**(`servedStale`)가 「내가 흔든 DB 를 이 셸이 읽는가」를 함께 증명한다.

### playwright 밖의 층

- **contract 74/74 통과 · 커버리지 56/56**(`node tests/contract/run.js --quiet --strict-coverage` · RC 0).
- 🔴 **안 잼**(0 아님) — `tests/api`(python) · `tests/graph` · `tests/schema` · `tests/data`.

## 3. 🔴 대상 결함 1건 — D-1 재개방

| | |
|---|---|
| **① 어디서** | `/overview` · 앱바 「**?**」(`intro-reopen`)로 안내를 **다시 연 뒤** |
| **② 무엇을 하면** | 안내 카드의 「**안내 닫기**」(✕)를 누른다 |
| **③ 무엇이 일어나야** | 카드가 닫히고 주소의 `?intro=1` 이 지워진다 |
| **④ 무엇이 일어났나** | 🔴 **버튼이 눌리지 않는다.** 5×5 격자 **25/25 점**을 셸 본문 `div.flex.min-w-0.flex-1.flex-col` 이 먹는다. 실제 클릭 **8초 타임아웃**, 카드 그대로 1개 |
| **대조** | **첫 진입에서는 21/25 가 제 것 = 눌린다.** 같은 버튼이 **경로에 따라** 갈린다 |
| **기전** | 「?」는 `/overview?intro=1&**tour=1**` 로 간다 — 안내와 투어가 **같이** 열리고, 투어가 `main`·`app-bar`·레일·배너를 **`inert`** 로 만든다 ⇒ 안내 카드가 그 «뒤 화면»에 들어간다 |

**부정 판정은 «해 보고» 냈다** — 스캔만이 아니라 실제로 눌렀고, 눌린 뒤 상태(카드 수·주소)까지 읽었다.
재현 그물 = `tests/web/t717_intro_reopen_cover.mjs`(전/후 두 상태를 같은 실행에서 찍는다).

## 4. 축② 골든 시나리오(GS-01) — **완주 · 10/10**

그물 `tests/web/t717_golden_walk.mjs` · 원장 `evidence/t717-golden-walk.json`.
🔴 **사람처럼 화면의 손잡이로만 걸었다**(대상 좌표·딥링크를 미리 넣지 않았다). `/compare` 만 `via:"url"`.

| # | 화면 | 한 일 | 관측 | 판정 |
|---|---|---|---|---|
| 1 | 진입 | 쿠키 없이 `/overview` | 세션 가드 통과 · 모드 배지 `◑REPLAY` | PASS |
| 2 | 개요 | 알람·설비·그래프를 센다 | 알람 1 · 설비 12 · svg 17 · `AL-20260826-0041 · EQ-CNC-204 · 임계 6.3 → 관측 6.3047` | PASS |
| 3 | 개요→조사 | 「조사 시작」 클릭 | `/incidents/INC-2026-014?run=RUN-…` 로 이동 | PASS |
| 4 | 조사 | 최대 90초 지켜봄 | `run-step` **5** · 단계 상태 `["done"×5]` · 상태 문면 **「완료」** | PASS |
| 5 | 후보 | 후보·근거칩 계수 | 후보 **2** · 근거칩 **4** · 첫 후보 「스핀들 베어링 마모 FM-BRG-WEAR」 | PASS |
| 6 | 근거 | 근거칩 클릭 | `/evidence/MR-2025-0087?run=…` · `trust-header` 1 | PASS |
| 7 | 작업지시서 | WO 링크 클릭 | WO 화면 · 편집칸 2 · 승인 버튼 1 | PASS |
| 8 | 승인 | 승인 → 확인 모달 → 승인 | 모달 1 · 확인 클릭 성공 · **「승인됨」 표시 확인** | PASS |
| 9 | 비교 | `/compare` | `compare-strategy` **3** · 각주 1 · 질문 패널 1 | PASS |
| 10 | 전체 | 콘솔 오류 수집 | **런타임 오류 0** | PASS |

🔴 **8번의 «감사 흔적(auditId) 문면»은 «안 잼»** — 승인 상태는 확인했으나 감사 ID 를 화면에서 못 읽었다.
0 이 아니라 **안 잰 칸**이다.

🔴 **자수 — 첫 회차 GS 표는 내 손잡이가 틀려 있었다.** 나는 화면에 없는 이름(`run-stage`·`data-run-status`·`table th`)으로
물어 놓고 **「완주 못 함」·「비교 열 0」**이라는 가짜 관측 2건을 만들었다. DOM 을 실측해 살아 있는 표지
(`run-step`·`run-progress`·`compare-strategy`·`compare-footnote`)로 바꾸고 다시 걸었다. **계측기가 먼저 거짓말한다.**

## 5. 유리/불리 (고정 1줄)

**유리** = 재시도 0·8워커로 사람보다 많이·빠르게 밟고, 실패마다 trace 를 남긴다.
**불리** = **한 브라우저(chromium)·한 폭(1440×900)·한 회차**만 본다 — 사람은 다른 폭·다른 순서로 들어가고,
스크롤·재시도로 막힌 곳을 우회한다. 그리고 이 그물은 **「보기 좋은가」를 못 잰다.**

## 6. 재현

```
# ai-api (내 포트) — 배포·스텁 무접촉
cd services/ai-api && .venv/Scripts/python.exe -m uvicorn app.main:app --port 8102 --no-proxy-headers
# 셸 — 빌드·런타임 값이 같아야 한다
cd apps/web-console && FKT_API_BASE=http://127.0.0.1:8102 FKT_API_BASE_BUILD=http://127.0.0.1:8102 pnpm build
FKT_API_BASE=http://127.0.0.1:8102 pnpm exec next start -p 8799
# 전량
cd tests/web && FKT_WEB_BASE=http://127.0.0.1:8799 FKT_API_BASE=http://127.0.0.1:8102 \
  FKT_PG_CONTAINER=fkt-senku2-t15-postgres-1 npx playwright test
# 골든 시나리오
node tests/web/t717_golden_walk.mjs --base http://127.0.0.1:8799 --out evidence/t717-golden-walk.json
```
