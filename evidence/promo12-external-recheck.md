# 승격 12회차 공개면 재검 + 재캡처 (리바이2 43대)

- **대상** = main **`09151b6`** · `dpl_6TDmj1qEV8nVhCQ1TABVz8NLSafR` · READY **18:33:58 KST**(Vercel API 실측)
- **상류** = `/api/health` `build: 0c2bc38` · status ok
- lane 출발 `1506f32` · 근거 등급 **E1** · 산출 `evidence/promo12/*.json` · 재캡처 3장

## 축별 판정

| 축 | 판정 | 증인 |
|---|---|---|
| ⓐ 외부 vantage | **PASS** | `/`·`/api/health`·`/api/live/status` **200** · 연결 IP **`216.198.79.67`**(공인) · 배포메타 `githubCommitSha=09151b6e4d…` |
| ⓑ 문체 렌더 덤프 | **PASS** | 화면 문구 위반 **0** — 아래 §1 |
| ⓒ Incidents·조사 복원 | **PASS** | `/incidents` **200** · 조사 행 `?run=` · 복원 시 같은 run · `data-testid` **74** |
| ⓓ 리셋 확인창 `top ≥ 0` | **PASS** | 367 / 317 / 117 / 339 / 167 (5뷰포트 전건 양수) |
| ⓔ **O-9 live 2발 «동시»** | **PASS** | 아래 §2 |
| ⓕ D-55 승격 «전» 쿠키 열 | 🔴 **안 잼** | 승격 12 «전»에 잡아 둔 쿠키가 없다(§4 자수 1) |
| ⓖ 콘솔 오류 | **PASS** | 측정 열 전부 **0건**(WS 404 포함해서 0) |
| ⓗ 재캡처 3장 | **완료** | `fkt-*-1600-v2.png` · **구독 사용 0** |

## 1. ⓑ 문체 — 화면 문구 위반 **0** · 🔴 내 제외 selector 가 좁았다

금지 정규식은 발주 문자열 그대로. 전 6화면 **1,189 텍스트 노드** 훑어 **판정 히트 6건**이 나왔는데,
**6건 전부가 «화면 문구»가 아니었다.**

| # | `data-testid` | 정체 |
|---|---|---|
| 1 | `candidate-rationale` | 🔴 **모델이 쓴 합성 문장**(「…규정한다.」) — 코드가 정비할 수 있는 문구가 아니다 |
| 2~6 | `evidence-card` | 🔴 **`data/**` 매뉴얼 본문 그대로**(「# 공통 안전 운전 매뉴얼 (r1)」·「## 5. 경보 및 알람」…) |

🔴 **제외 selector `[data-testid="cited-body"]` 만으로는 부족했다** — 매뉴얼 본문은 `evidence-card` 안에서도 그려진다.
그래서 `excludedNodes` 가 **0** 으로 찍혔고, 제외되어야 할 것이 «판정 히트»에 섞였다.
**제외를 «삭제»가 아니라 «표시»로 해 둔 덕에** 6건을 하나씩 열어 정체를 가릴 수 있었다 — 지웠으면 못 봤다.

⇒ **화면 문구(정적 copy) 위반 = 0.** T7-40 이 공개면에 반영됐다.
🔴 **판정력 한정 그대로** — overview·evidence·투어 1~3 은 이번에도 히트 0 이고, 이 회차에는 대조군(승격 «전» 빌드)이 없다.
**「0 이 나왔다」와 「그물이 그 화면에서 문다」는 다른 사실**이고, 후자는 이번 회차에서 증명되지 않았다(직전 회차 #612 에서 evidence·tour-2 에 대해서만 증명됨).

**부수 확인** — 재캡처 화면에서 문면이 실제로 바뀐 것을 눈으로도 대조했다:
「서버가 다시 응답합니다**.** 이 화면은 **녹화 재생본**이고…」(전: 「— 이 화면은 정적 재생본이고」) ·
「**이 조사에서 인용한 근거입니다. Overview 로 돌아갈 수 있습니다.**」(전: 「세션 안에서 열렸다 — 조사 화면으로 오갈 수 있다.」)

## 2. ⓔ O-9 — **PASS**(재사용 창을 밟았고, 헤더가 브라우저까지 왔다)

`POST /api/scenarios/GS-01/runs` **2발을 `Promise.all` 로 동시** 발사(mode `live`).

| | 응답 A | 응답 B |
|---|---|---|
| status | **200** | **200** |
| `runId` | `RUN-ff8b83537fa8` | **`RUN-ff8b83537fa8`**(같다) |
| `x-fkt-run-reused` | 없음(원본이니 정상) | **`RUN-ff8b83537fa8`** ✅ |
| cap 헤더 3종 | limit 10 · used **1** · remaining 9 | limit 10 · used **1** · remaining 9 |

- 🔴 **재사용의 증인은 헤더가 아니라 «같은 runId»** 다 — 그것이 먼저 참이어서 이 회차가 성립했다.
- 🔴 **`used` 가 두 응답 모두 `1`** ⇒ **재사용 회차는 계수하지 않는다**(계약 v0.1.15 조항 충족).
- ⇒ **구독 사용 = 1**(2발이 1건으로 접혔다).
- 이로써 **43대 판정문 3본에 걸쳐 「안 잼」이던 O-9 이 종결**됐다.

## 3. ⓒ 조사 복원 — **PASS**

UI 로 조사를 시작해 `RUN-c5de128d89b1` 을 만든 뒤 `/incidents` 로 갔더니 목록에
`/incidents/INC-2026-014?run=RUN-c5de128d89b1` 행이 있었고, 따라가니 **같은 run 이 복원**됐다(`data-testid` **74**).

🔴 **1차 시도는 실패했고 그 이유가 내 것이었다** — ⓔ 에서 `POST /api/sessions` 로 «새 세션»을 만들어 run 을 쐈더니
그 run 은 **페이지의 세션 소유가 아니어서** 목록에 안 나왔다(`runRow: null`). 셸은 자기 `fkt_session` 을 쓴다.
**세션을 내가 만들면 그 run 은 화면의 것이 아니다.**

## 4. 안 잼 · 자수

**안 잼**
1. **ⓕ D-55 승격 «전» 쿠키 열** — 승격 12 이전에 세션을 잡아 두지 않았다. 재자극 불가(상류 무접촉).
2. **ⓑ 판정력** — 이 회차엔 대조군 빌드가 없다(§1).
3. 투어 **4~9걸음**(4번째부터 사람이 눌러야 넘어간다) · `documents` 화면(링크 없음).

**자수**
1. ⓕ 를 놓쳤다 — 승격 11 때는 «전»에 쿠키를 잡아 뒀는데, 이번엔 승격 시각을 예측하지 못해 못 잡았다.
   🔴 **이 자극의 창은 승격 순간에 닫힌다** — 다음 대는 승격 «전»에 미리 잡아 둘 것.
2. ⓑ 제외 selector 가 좁아 매뉴얼 본문 5건이 판정 히트에 섞였다(§1). 제외를 «표시»로 해 둔 것이 이번엔 살렸다.
3. ⓔ 1차에서 세션을 내가 만들어 ⓒ 의 복원 축이 통째로 빈 채로 나올 뻔했다(§3).
4. ⓖ 「콘솔 0」은 **이번 회차가 밟은 경로에서만** 참이다 — 같은 날 앞선 Live 화면 회차에서는
   `wss://…/api/ws/runs/{id}` **404 5건**이 있었다. 이번엔 그 화면을 live 로 열지 않았다.
5. 표본 각 축 1회 · 1440×900(ⓓ 는 5뷰포트) · chromium.

## 5. 판정

**ⓐⓑⓒⓓⓔⓖ PASS · ⓗ 완료 · ⓕ 안 잼.**
🔴 **이번 회차의 값** = **O-9 종결**(재사용 창을 실제로 밟아 헤더 도달과 「계수 0」을 동시에 봤다) 과
**T7-40 공개면 반영 확인**(정적 문구 위반 0 · 남은 히트는 전부 «내용»이지 «문구»가 아니다).
