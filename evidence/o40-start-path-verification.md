# O-40 독립 검증 — 두 기동 경로가 같은 무대를 만드는가 (리바이2 52대 · 2026-09-05)

> 대상 = PR #792 `9b4481d`(`bind`·`authRequired` 를 `/health` 에 · 인자 정본을 `gw-autostart.ps1` 한 곳으로 · `-HostAutostartPath` 인자화)
> 판정 = ✅ **PASS**(축 ①②④⑤) · 구독 **0** · 🔴 **production 무접촉**(`:8787` pid **11332** 창 전후 동일 · 재기동 0)
> 무대 = 내 손 `:8791`·`:8792`(트리 `_wt/tgt-o40` @ `9b4481d`) · 센쿠2 표는 대조군이지 답이 아니다

## 0. 착수 전 — 발주문 가드 값이 내 실측과 갈렸다(해소)

발주문 = 「실제 `~/.fkt/gw-autostart.ps1` 해시 **`e9137495`**」 · 내 첫 실측 = **sha256[:8] `78153921`**(3892B).
🔴 **가드 값을 전언으로 받지 않고 원인부터 갈랐다** — 산식 차이였다:

| 산식 | 값 |
|---|---|
| `git hash-object`(SHA-1 blob) — 발주문이 쓴 것 | `e9137495…` — **호스트 실물 = `origin/main`·`origin/develop` blob 과 동일**(내 손 3중 대조) |
| `sha256[:8]` — 내가 쓴 것 | `78153921` / 3892B |

⇒ **같은 파일**이다. 기준선은 **내가 찍은 `78153921`/3892B** 로 고정하고 전후를 이 값으로 대조했다.
(교훈은 오케가 성문: **발주문 해시엔 산식을 적는다.**)

## 1. 축 ① — 인자 세트별 `/health` 4열

| 열 | 인자 | `bind` | `authRequired` | model | effort |
|---|---|---|---|---|---|
| **A** 옛 promote | `-PromptFile` **만** | **`127.0.0.1`** | **`false`** | opus | low |
| **B** autostart 세트 | `-Bind 0.0.0.0 -Token <값> -Model opus -Effort low` | **`0.0.0.0`** | **`true`** | opus | low |
| **C** 새 promote | 🔴 **인자를 짓지 않는다 — `gw-autostart.ps1` 을 부른다**(`if ($Restart)` 안 · 코드) | = B | = B | = B | = B |
| **production 현재**(읽기 1점) | autostart **옛 판** | **(필드 없음)** | **(필드 없음)** | opus | **`medium`** |

- A·B 는 **같은 `run.ps1`, 같은 포트 계열, 손잡이는 인자 세트 하나**만 다르다.
- 🔴 **처방의 핵심은 「두 경로를 같게 만든 것」이 아니라 「갈림을 값으로 보이게 한 것」**이다 — `bind`·`authRequired` 가 `/health` 에 없던 앞판에서는 A 와 B 가 **문면상 구별되지 않았다**(그래서 O-40 이 승격 19 창까지 안 보였다).
- 🔴 **production 응답에는 두 필드가 아예 없다** = 지금 도는 것이 **처방 이전 바이트**라는 독립 증거다. `effort medium` 도 그 판의 값이다.
  ⇒ **반영은 승격 21** 이다. 이 창의 초록은 **develop 계열 트리**의 것이지 production 의 것이 아니다.

## 2. 축 ② — 빈 본문 `POST /synthesize` 로 «층»을 가른다 (구독 0)

| 열 | 응답 | 어느 층에서 죽었나 |
|---|---|---|
| **A**(무인증) | **413** `본문 크기 0B 가 한계를 벗어났다` | 인증 층 **통과** → 길이 층 |
| **B**(토큰 강제) | **401** `X-FKT-Gateway-Token 가 없거나 맞지 않는다` | **인증 층** |

🔴 **합성은 어느 쪽에서도 돌지 않았다** — `do_POST` 가 ① 인증 → ② 경로 → ③ 길이 → ④ 본문 순으로 갈리기 때문이다.
「무인증인가」를 **구독 한 발도 안 태우고** 물었다. 401 과 413 은 **서로 다른 층**의 거절이라 섞이지 않는다.
⇒ 승격 19 창에서 「못 잰 것」으로 남긴 **합성 엔드포인트 무인증 축을 값으로 닫았다**: 옛 promote 판은 `/synthesize` 도 인증 없이 받았다.

## 3. 축 ④ — 설치 멱등 + production 가드

`promote-artifacts.ps1 -Sha 9b4481d -Dest <임시> -HostAutostartPath <임시>` **(`-Restart` 없음)** 2회:

| 회차 | rc | 설치 sha256[:12] | 실제 `~/.fkt/gw-autostart.ps1` |
|---|---|---|---|
| 기준선 | — | — | `78153921` / 3892B |
| 1회 | **0** | `e3b19813448b` | `78153921` / 3892B **불변** |
| 2회 | **0** | `e3b19813448b` **동일** | `78153921` / 3892B **불변** |

- **멱등 성립**(같은 바이트) · 2회차에 `.pre-<sha>` 백업이 생겼다(= 되돌림 근거는 마지막 회차분).
- 🔴 **`-Restart` 를 안 주면 production 을 안 만진다**를 코드(`if ($Restart)` 게이트)와 거동(가드 불변 · `:8787` pid 11332 그대로) **양쪽으로** 확인했다.

## 4. 축 ⑤ — 대조군(그물이 무는가)

`tests_unit` **16 passed**(0.09s) → `gateway.py` 에서 `"bind"`·`"authRequired"` **두 줄만 제거** → **5 failed / 11 passed**
(`test_both_keys_are_always_present[bind]`·`[authRequired]`·`test_bind_is_what_the_process_actually_bound` 등) → **원복 후 다시 16 passed**.
⇒ 신규 단위가 **처방이 사라지면 실제로 빨강**이 된다. 초록이 「검사가 없어서」가 아니다.

## 5. 못 잰 것 (값이 아니라 이름)

- **`-Restart` 실경로** — production 게이트웨이를 실제로 내렸다 올리는 창은 **승격 21**의 몫이다. 이 창에서는 `if ($Restart)` 블록을 **한 번도 밟지 않았다**.
- **예약 작업 폴백 갈래**(`Get-ScheduledTask` 부재/`Disabled`) — production 예약 작업을 건드려야 재현되므로 이 창 밖이다.
- **C 열의 거동** — 「`gw-autostart.ps1` 을 부른다」는 **코드로만** 확인했다(E2). 그 스크립트는 `:8787` 고정이라 내 포트로 못 옮긴다.

## 6. 근거 등급

- **E1(실측)** — §1 A·B 열 · §2 · §3 · §4 · production 읽기 1점 · 가드 3중 대조.
- **E2(출처)** — §1 C 열(`if ($Restart)` → `switch.ps1 off` → 예약 작업/폴백 → `gw-autostart.ps1`).

## 7. 자수 1

`pwsh … | head -22` 로 받아 **또 파이프에 걸렸다** — 띄운 게이트웨이 자식이 파이프를 물어 명령이 300초 만에 배경 전환됐고 출력은 한 줄도 안 나왔다. **오늘 두 번째**다(첫 번째는 승격 19 창의 `refresh`). 포트를 직접 두드려 값은 그대로 얻었지만, **「서버를 띄우는 명령은 파일 리다이렉트로」**를 내가 재개점에 적어 두고도 어겼다.
