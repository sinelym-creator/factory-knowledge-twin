# T7-D 가로 넘침 재측 — 세 열 (리바이2 35대 · 2026-09-03)

발주 = 스자쿠 30대 20:41(수리 sha `992ea73` · **넘침 축만**). **측정만 · `apps/**` 무접촉.**

## 🔴 세 무대를 «같은 실행»에 세웠다

| 열 | 무대 | 빌드 |
|---|---|---|
| 기준선 | `:3102` | develop(`9509124`) prod |
| **회귀** | `:3103` | **`ee3daab`** prod |
| **수리** | `:3104` | **`992ea73`** prod |

「고쳐졌다」는 **고쳐지기 전 열이 같은 실행에서 빨간 것**으로만 성립한다. 수리 무대 하나만 초록이면
그 초록의 주어가 처방인지 그날의 무대인지 못 가른다. 🔴 회귀 열이 안 빨가면 `exit 2` 로 죽게 해 뒀다.

## 결과 — 3엔진 × 4폭 × 5화면 × 2 pointer, 무대마다 60칸

| 무대 \| pointer | 넘침 | 값 |
|---|---|---|
| 기준선 \| coarse | **0/60** | — |
| 기준선 \| fine | 0/60 | — |
| 🔴 **회귀 \| coarse** | **60/60** | `391/390` · `769/768` · `1025/1024` · `1281/1280` |
| 회귀 \| fine | 0/60 | — |
| ✅ **수리 \| coarse** | **0/60** | — |
| ✅ 수리 \| fine | 0/60 | — |

**오류 0.** ⇒ **수리 성립.** 회귀 열이 같은 실행에서 60/60 빨갛고, 수리 열이 0/60 초록이다.
`fine` 열은 세 무대 전부 0 — **변하지 않아야 하는 대조군**이 실제로 안 변했다.

**구조 확인**(diff `ee3daab..992ea73` 실독): `left:50%→0` · `width:max(100%,2.75rem)→100%` ·
`translate(-50%,-50%)→translateY(-50%)`. 🔴 **`overflow:hidden` 없음** — 숨긴 게 아니라
**넘칠 수 없는 형태**로 바꿨다. `::before` 가로가 부모와 같아지므로 **가로로 삐져나올 수 없다.**

## 🔴 대가 — 사정거리가 줄었다 (값으로)

수리는 `::before` 를 **세로만** 넓힌다 ⇒ **가로가 44 미만인 자리는 가로 미달이 남는다.**

| 자리 | 시각 | 기준선 히트 | 회귀 히트 | **수리 히트** | 수리 `::before` |
|---|---|---|---|---|---|
| `intro-reopen` | 32×32 | 33×32 | 45×44 | **33×44** | **32px** × 44px |
| `fallback-banner` ✕ | 10.6×19.5 | 11×20 | 44×45 | **11×45** | **10.625px** × 44px |

- **세로는 개선이 유지된다**(32→44 · 20→45).
- 🔴 **가로는 회귀본보다 좁아졌다**(45→33 · 44→11) — 넘침을 없앤 대가다. 두 자리는 **가로 미달로 남는다**.
- 이건 결함이 아니라 **처방이 선택한 교환**이다. 가로까지 넓히려면 그 자리의 **부모 여백**을 손봐야 하고
  구현이 그것을 **별건으로 회부**했다. 이 표가 그 별건의 근거값이다.

## 안 잰 것

- **다른 축은 다시 안 돌렸다**(44px 전수 · 정렬 · 스킴 · e2e) — 발주 지시. 그 값들은
  `evidence/t7-recheck-verdict.md`(sha `ee3daab` 기준)에 있다. 🔴 **수리본에서 그 축들이
  그대로인지는 «안 잰 것»이다** — 다만 수리 diff 가 `::before` 3줄뿐이라 사정거리는 좁다.
- **dev vs prod 갈림** — 구현은 chromium **dev** 에서 넘침을 재현 못 했고 나는 **prod** 3엔진에서
  60/60 을 봤다. 「같은 sha 를 dev 한 번 prod 한 번」이면 1회로 갈리는데 **이번 발주 밖**이라
  안 팠다. **별건 등재분.**
