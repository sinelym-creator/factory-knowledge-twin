# T6-1 live 합성 — 독립 검증 (7축)

> 좌석: 검증(리바이2 27대) · 발주: 오케(스자쿠 22대 20:35 → 23대 승계 20:49) · 대상 develop **`7ca563d`**(#407 + #409) · 스키마 #406
> 근거 등급: E1 실측 / E2 출처 / E3 소견
> 🔴 **판정 = 잰 6축 충족 · 대상 결함 0 · 회부 1건(귀속 미확정) · 축 ⑦ 미실행(승격 뒤 go 대기)**

## §0 무엇으로 쟀나 — 대상 서버를 내가 세웠다

| | |
|---|---|
| ai-api | 🔴 **내 인스턴스 `fkt-levi2-t61-api`(호스트 8020)** — 배포 이미지 `fkt-deploy-ai-api:6e1487d` 에 **내 트리 `services/ai-api/app` 를 `:ro` 로 마운트**해 `7ca563d` 코드를 싣고, `/models`·`data/replay` 는 읽기 전용 공유. `FKT_BUILD_SHA=7ca563d-levi2`(**자기 신고**이므로 컨테이너 안 `live_synthesis.py` 실재로 따로 확인 · E1) |
| 배포 ai-api(8010) | **무접촉** — 읽기(`printenv`)만 했다 |
| 게이트웨이 | **8787 = 센쿠2 31대가 띄운 background 프로세스**(구현 좌석 인계 20:50 · 기동 주체·수명 그쪽) · 축 ⑥ 에서만 사용 · **무접촉 유지** |
| 셸 | **3012** — `_wt/levi2-t6-1v` 를 `FKT_API_BASE_BUILD=http://127.0.0.1:8020` 로 새로 빌드(축 ⑤ 는 재빌드가 전제다 — 3011 은 T6-1 이전 코드) |

🔴 **왜 실물 게이트웨이로 축 ①②를 자극하지 않았나.** `services/synthesis-gateway/gateway.py:101`
`_validate_response` 가 인용 id 를 **자기가 먼저** 검사해 502 로 문다(코드 확인 E1 · 구현 좌석
경고 20:36 과 일치). 그 경로로는 ai-api 의 `apply_guard` 가 **한 번도 안 돌고도**
`live-rejected` 가 나온다 — **다른 층이 낸 빨강**이다. 그래서 게이트웨이 «자리»에 스텁을 세웠다
(`tests/api/t61_gateway_stub.py` · HTTP 200 + 계약 형상 · 다른 것은 인용 id 하나뿐).

## §1 축 ① 근거 결속 — **충족** (E1 · 대조군이 갈랐다)

| 열 | 스텁이 준 것 | `synthesis` | 후보 rationale |
|---|---|---|---|
| **자극** | 200 · 진짜 id 1 + **가짜 id 1건**(`EV-STUB-NOT-IN-RUN-0001`) | `axis=live-rejected` · `model=stub-fake` · `rejectedReason=「인용 id 가 run 근거집합 밖이다(1건)」` | **0/2 = 부분 채택 0** |
| **대조군** | 200 · **진짜 id 만** | `axis=live` · `model=stub-real` | **2/2 실재**(`citedEvidenceIds=[EQ-CNC-204]`) |

- **가드 도달 계수 = 각 1건**(스텁 자취 JSONL — 후보 2 · 근거 19 · 주입 id 기록). 🔴 이 줄이 0이면
  위 판정은 무효다.
- **결정적 순위 유지** — 자극 열의 순위 `FM-BRG-WEAR`(1) → `FM-TOOL-IMB`(2) 가 축 ② off 열
  (`deterministic`)과 **동일**하다.
- 🔴 대조군이 없으면 이 초록은 「가드가 막았다」가 아니라 「스텁이 원래 안 먹힌다」일 수 있었다.

## §2 축 ② 갈림 3 — **충족** (E1 · 각 1회 · 자극 실재 계수 병기)

| 조건 | 실제 자극 | `synthesis` |
|---|---|---|
| env **off**(`FKT_LOCAL_SYNTHESIS_GATEWAY` 없음) | 컨테이너 재기동 · 게이트웨이 주소 0 | `axis=deterministic`(사유 없음 · rationale 0) |
| **미도달**(8799) | 8799 `curl` = **000**(아무도 안 듣는다) | `axis=live-rejected` · `「게이트웨이 미도달(ConnectionRefusedError)」` |
| **CLI 부재**(8788) | 🔴 **별 포트에 진짜 게이트웨이를 `SYNTHESIS_CLI_BIN=fkt-no-such-cli-27` 로 기동** · 그 로그에 `POST /synthesize → 503` **1건 실재** | `axis=live-rejected` · `「게이트웨이 503 · CLI 를 찾지 못했다(fkt-no-such-cli-27)」` |

🔴 `SYNTHESIS_CLI_BIN` 은 **게이트웨이 프로세스**가 읽는다(`gateway.py:31` · E1). ai-api 실행 줄에
붙이면 아무 효력이 없다 — 그렇게 쟀으면 거짓 초록이었다(구현 좌석이 한 번 물린 자리).
세 열 모두 순위 동일 · rationale 0.

## §3 축 ③ 경계 — **충족(코드 축)** · 🔴 실제 로그 면은 못 잼

| 항 | 실측 |
|---|---|
| 컨테이너 env | `docker exec fkt-deploy-ai-api env` **20건** 중 `FKT_LOCAL_SYNTHESIS_GATEWAY`·`ANTHROPIC*`·`CLAUDE*`·`SYNTHESIS*` = **0건** |
| 키 파일 | `git grep -E "sk-ant-[A-Za-z0-9_-]{10,}"` **0건** · `(ANTHROPIC\|CLAUDE)_API_KEY` 대입 형태 **0건**(hit 10파일은 전부 **검사기·판정문의 패턴 문자열**) · 게이트웨이가 API 키를 읽는 자리 **0**(CLI 경유) |
| 프롬프트 원문 로그 | `gateway.py:246` 「사유만 남긴다 — 프롬프트·근거 원문은 쓰지 않는다」 · 채택 로그 = `후보 %d · 근거 %d · %dms · %s`(계수·모델·ms 만) |

🔴 **이것은 코드 축이다.** 8787 의 실제 stdout 은 **센쿠2 프로세스 소유**라 내가 못 본다. 내가
띄운 8788 게이트웨이의 로그 면에서는 프롬프트·근거 원문이 **없음을 실측**했다(같은 코드).

## §4 축 ④ fixture 심사 — **충족** (E1)

- `python -m tools.audit_replay_fixture` → **PASS**(이벤트 32 · 문자열 **285개 · 7,569자** 스캔 · 6축 위반 0)
- `--self-test` → **PASS**(축마다 검출 + **오탐 0** · 밖에서 온 표본 `sk-ant`·`ghp_`·`AKIA`·호스트 형상 포함)
- 기록본: `"axis": "live"` **실재** · `rationale` **실재** · seq **0..31 단조·중복 0** · ts 단조
  (총 50.887s · 최대 간격 35.875s = 합성 대기 · 간격 **10종/31** ⇒ 균등 가공 아님) · **LF**(CR 0)
- 녹화기 `record_replay_fixture.py:15` 「가공하지 않는다 — ts·seq 를 다시 매기지 않는다」(코드 축)
- 정적 replay: `data/replay/static/manifest.json` 의 `sha256` = 실파일 **일치**(`6e81c7c8…`) · events 32/32

## §5 축 ⑤ 화면 — **충족**(두 열) · e2e 는 조건이 갈린다

### 5-1. 렌더 (E1 · `tests/web/t61_screen_probe.mjs`)

| 열 | 배지 | 후보/rationale |
|---|---|---|
| **채택** | 1개 · `data-axis=live` · 「◉ live 합성 stub-real」 | **2 / 2** · 첫 문장 + 「인용 1건 · EQ-CNC-204」 렌더 |
| **거부** | 1개 · `data-axis=live-rejected` · 「◌ live 거부 stub-fake」 | **2 / 0** + 사유 표시 |

🔴 채택 열만 봤으면 「늘 뜬다」와 구별이 안 된다. 거부 열에서 **문장이 0이고 사유가 뜬다**는
것까지 봐야 배지가 축을 말한 것이 된다.

### 5-2. e2e 회귀 (워커 4 · 14 파일 · 170.3s)

**130 통과 / 3 빨강 / 3 skip**. 🔴 기준선 133/0/3 과 **조건이 다르다** — 기준선 스택의 ai-api 는
게이트웨이가 없어 `/live/status.online=false` 였고, 내 스택은 게이트웨이가 붙어 **`online=true`**
다(실측 `{"online":true}`). 그래서 3 빨강의 주어를 갈랐다:

| # | 빨강 | 주어 |
|---|---|---|
| ① | `mode-badge` 「online:false → REPLAY」인데 **live** | **내 스택 조건** — 그 축의 전제(`online=false`)가 성립하지 않는다 ⇒ **못 잼**이지 결함 아님 |
| ② | `shell` `/overview` **fallback 배너 없음** | 같은 원인 — 배너는 오프라인일 때 선다 |
| ③ | `reset-modal` 「취소인데 **되돌렸습니다**」 | 🔴 **회부**(§7) — 단독 워커 1 에서도 재현(6/1) · 대조군(옛 코드 3011 + 배포 8010)은 **7/0** |

## §6 축 ⑥ 지연 — **Target 미충족**(실측 · n=3 · 실물 CLI)

| # | 벽시계(run 전체) | 합성 step `elapsedMs` | axis · model |
|---|---|---|---|
| 1 | **26.50s** | **19,133ms** | live · `claude-opus-5[1m]` |
| 2 | **26.73s** | **18,385ms** | live · `claude-opus-5[1m]` |
| 3 | **21.48s** | **18,674ms** | live · `claude-opus-5[1m]` |

- **Target 10s(baseline «잠정 목표»)** 대비 합성 단계만 **18.4~19.1s** ⇒ **미충족**.
- 🔴 README 표 값은 **인용하지 않았다**(발주 규율) — 위는 전부 오늘 내 손 실측이다.
- 실물 CLI 열의 rationale: 후보마다 **문장 3개** · 인용 **6건/5건**(전부 실제 evidence id 형상).
- 🔴 이 3회가 구독을 태운 전부다. e2e·화면 축은 **스텁**으로 돌려 CLI 소모 **0**(429 자극 0).

## §7 회부 1건 (오케 → 원장)

| 사안 | 등급 | 내용 |
|---|---|---|
| `reset-modal` 「취소 → 아무 일도 없다」 축이 내 스택에서 빨강 | **E1(재현) · 귀속 미확정** | 취소했는데 `role=status` 에 「되돌렸습니다」가 보인다. **단독 워커 1 재현**(6/1)이라 병렬 간섭이 아니다. 대조군(옛 코드 3011 + 배포 ai-api 8010)은 **7/0**. 🔴 두 열이 **코드(T6-1)와 ai-api 인스턴스** 두 축에서 동시에 다르므로 **T6-1 귀속으로 단정하지 않는다** — 좁히려면 「옛 코드 + 내 인스턴스」 한 열을 더 세워야 하고, 그것은 이 창의 범위 밖이다 |

## §8 🔴 내 계측기 자수 (2건 — 대상 결함으로 세지 않는다)

| # | 무엇 | 진범 |
|---|---|---|
| ① | 첫 «대조군»이 실은 **자극 열의 재실행**이었다 | **내 손** — 이 셸에 `pkill` 이 없어 옛 스텁이 8790 을 계속 물었고 새 프로세스는 죽었다. `/health` 가 `mode:fake` 로 답한 것으로 잡았고, **포트→PID** 로 죽인 뒤 다시 쟀다. 「대조군 실패」로 읽었으면 오판이었다 |
| ② | 축 ⑤ 첫 시도에서 상대 경로(`cd ../web`)를 틀려 프로브가 안 돌았다 | **내 손** — 산출 0. 경로를 고쳐 재실행 |

## §9 잰 범위 · 안 잰 것

- **잰 것**(E1): 축 ①②③(코드 축)④⑤⑥ · 각 축의 자극 실재 계수 · 대조군.
- **안 잰 것**: 축 ⑦ **공개 replay**(승격 뒤 · 오케 go 대기) · 8787 의 **실제 로그 면**(센쿠2 소유
  프로세스) · 429 운영값(**자극 금지**) · `reset-modal` 의 귀속 좁히기(§7) · 배포 ai-api(8010)의
  거동 일체(무접촉).
- **자원**: `fkt-levi2-t61-api`(8020) · 셸 3012 · 스텁 8790 · 게이트웨이 8788 은 이 판정 뒤 내린다
  (보고 후). **8787 은 무접촉**으로 남긴다.
