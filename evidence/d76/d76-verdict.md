# D-76 독립 검증 — 셸 빌드의 `FKT_API_BASE` 기본값 제거

> 검증 좌석(리바이2 46대) · 발주 = 스자쿠 40대 23:46:09 · lane `levi2-d76verify` · 출발 `b6b3ab2`
> 대상 = 센쿠2 #679 `lane/senku2-d76` head **`9504fe4`** · 대조군 **`b6b3ab2`**(= `811037f`, 대상 2파일 diff **0**)
> 측정 = 2026-09-04 23:50~23:57 KST · **cap 0** · `:8199` 무대 **무접촉**(별도 워크트리에서 빌드)

---

## 0. 판정 — **PASS**

센쿠2 자기 표(E1 주장) **①② 모두 내 손으로 재현**되었고, 🔴 **결함의 몸통(대조군)** 도 실측으로 섰다.

| 열 | sha | `FKT_API_BASE` | rc | `.next/BUILD_ID` | 산출물에 구워진 값 |
|---|---|---|---|---|---|
| **대조군** | `b6b3ab2` | **미지정** | **0** | **실재** | 🔴 **`http://127.0.0.1:8000` × 8** |
| **① 대상** | `9504fe4` | **미지정** | **1** | **없음** | — (컴파일 산출 0) |
| **② 대상** | `9504fe4` | `http://127.0.0.1:8190` | **0** | 실재 | **`http://127.0.0.1:8190` × 4** · `:8000` 잔존 **0** |

**대조군이 이 티켓의 근거다**: 처방 전에는 미지정 빌드가 **실패하지 않고**, **조용히 센쿠2 대역을
구워** 넣었다. 빌드는 서고 화면은 뜨므로 아무도 알아채지 못한다 — 그 「초록」이 결함이었다.

① 문면(로그 원문):
```
Error: [FKT] 빌드 중단 — FKT_API_BASE 를 지정하라(기본값 없음 · 무접촉 대역을 굽지 않는다)
```
센쿠2 신고 문면과 **일치**. `.next/BUILD_ID` 부재로 **컴파일 산출 0** 을 실물로 확인했다
(파이프 뒤 rc 는 믿지 않는다 — 산출물 실재로 확인).

② 굽힌 값은 `.next/server/chunks/lib_contract_ts_*.js` 에 **실재**하며 내가 준 값과 같다.

---

## 1. 죽는 자리가 «하나»인가 — 코드 + 실측으로 가름

처방 트리에 `FKT_API_BASE` 를 판정하는 자리는 **셋**이지만, **세 사건이 서로 다르다**:

| 자리 | 조건 | 사건 |
|---|---|---|
| `next.config.ts:34-38` | `FKT_API_BASE` 미지정 | **빌드 시 미지정** ← ① 이 여기서 죽는다 |
| `lib/contract.ts:483-489` | `FKT_API_BASE_BUILD` 부재 | 「굽기가 빠진 산출물」(비정상 형상) |
| `lib/boot-check.ts:27` | `runtime && built && runtime !== built` | **빌드 값 ≠ 런타임 값**(불일치) |

`boot-check` 는 **둘 다 있고 다를 때만** 운다(`if (!runtime || !built || runtime === built) return;`).
`contract.ts` 는 빌드가 선 산출물에는 상수가 반드시 있으므로 정상 경로에서 도달하지 않는다.

**실측 확인**: 대상 ① 빌드 로그에서 「빌드 상수」 **0건** · 「부팅 중단」 **0건** — 미지정 사건에
문면은 **하나만** 떴다. → 「같은 사건에 문면 둘」 없음. **센쿠2 주장 확증.**

---

## 2. `apps/**` 소스 `:8000` 잔존

**1건** — `apps/web-console/next.config.ts:26`, 앞판 값을 설명하는 **주석**이다(허용).
🔴 발주 문면은 「주석 2건 = 허용」이었으나 **내가 센 것은 1건**이다. 계수 차이를 그대로 적는다.

빌드 산출물(`.next/**`)의 `:8000` 잔존은 ② 열에서 **0**.

---

## 3. 보조 증인 — 23:26 관측(다른 트리 · 우연)

D-73 작업 중 `levi2-d74d73`(출발 `5894204`)에서 `FKT_API_BASE` 없이 `pnpm build` 를 돌려 **rc 0** 으로
성공했고, 그 산출물로 띄운 셸이 스스로 **「빌드(정본): `http://127.0.0.1:8000`」** 을 신고했다.
`5894204` ↔ `811037f` ↔ `b6b3ab2` 의 대상 2파일 diff = **0**.
→ 대조군 결론과 **독립적으로 같은 사실**을 가리킨다(보조 증인 · 정본은 §0 의 대조군 열).

---

## 4. 안 잰 것 (이름으로)

1. 🔴 **Vercel 빌드 env 축** — 공개면 배포 파이프라인이 `FKT_API_BASE` 를 주는지는 **재지 않았다**
   (발주가 오케 승격 전 항목으로 지정). 이 처방이 배포를 깨는지 여부는 **이 문서가 말하지 않는다.**
2. **② 산출물로 셸을 띄워 보지 않았다.** 「구워진 값이 실제로 그 API 를 향하는가」는 굽힌 문자열
   실재까지만 확인했고, 런타임 왕복은 안 쟀다.
3. `contract.ts` throw 의 **도달 경로를 실측으로 만들어 보지 않았다**(코드 조건으로만 가름).
