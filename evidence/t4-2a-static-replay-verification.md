# T4-2a «정적 replay 경로» 독립 검증

> 검증 좌석 리바이2 **14대**(①~⑦) + **16대**(⑧) · lane `lane/levi2-t4-2a-verify` ·
> 🔴 **기점 2줄**: ①~⑦ = **`c0276df`**(발주 지정 sha · 발주 시 `origin/develop` 과 동일 실측) ·
> ⑧ = **`3617abe`**(= `c0276df` + `origin/develop` `de65406` 병합 · **D-5 픽스 `f3f4cec` 포함 · D-3 미포함**) ·
> 대상 서버 = ai-api **`:8061`**(코드 42/43 동일 · 갈린 1본 `probes.py` 는 §10.1·§10.8 에서 갈랐다) · 대상 = PR-a #216 + PR-b #220(코드 = #227 D-6 제외 규칙까지) ·
> 정본 = 티켓 `docs/plan/tickets/T4-2a.md`(14:12 보정본 · AC 5) + 대응표 판정 R-1~R-4 ·
> 근거 **E1**(별도 표기 외 전건 실측) · 측정 2026-08-31 20:0x~ KST.
>
> 🔴 **축별 append 로 써 나간다** — 이 파일이 곧 인계본이다.
> **진행: ①~⑧ 전 축 종결 — AC 전건 PASS.**
> **대상 결함 회부 0건.** 승계 회부 2건(브라우저 축 네트워크 · 최종 RunState 동형) 모두 해소(§5·§7).
> 미해결로 넘긴 것은 **대상의 것이 아닌 그물 부채 1건**(Q-58 = 그물 결함 · §10.7 에서 처방까지 마쳤다).

---

## §0 계측기 — 내 것부터

| # | 무엇이 거짓말했나 | 어떻게 드러났나 | 교정 |
|---|---|---|---|
| 1 | `pnpm install` 이 끝나기 «전»에 build 를 돌렸다 | `next: 명령을 찾을 수 없음` · rc=1 — 하마터면 **대상의 빨강**으로 읽을 뻔했다 | `node_modules/.bin/next` 실재를 확인해 갈랐다. 설치 완료 후 재빌드 rc=0 |

### §0.1 측정 조건 — 🔴 **자극이 둘이고 서로 다르다**

| 자극 | 무엇 | 셸 | 뜻 |
|---|---|---|---|
| **OFF** | `FKT_API_BASE` = **8099**(리슨 0 · 실측 확인) | **:3181**(그 값으로 빌드) | 연결 **거부** — 즉시 실패 |
| **LIVE** | ai-api **8061** 살아 있음 · `/api/live/status` = **`online:false`** 실측 | **:3161**(그 값으로 빌드) | 도달하지만 합성 게이트웨이 부재 |
| (별건) | **블랙홀 8074**(accept 하고 안 답함) | **:3171** | T4-1 D-3 의 자극 — 여기서는 ⑥ 값 비교에만 인용 |

- 워크트리 `_wt/levi2-t42a` · 목적지는 **빌드 값이 정본**이라 셸을 각각 따로 세웠다(Q-37).
- 🔴 **무접촉**: `fkt-senku2-t41`(8010) · 센쿠2 대조군 8004 · 13대 존치 5벌 · `t41y`(8057 = Q-52 BEFORE 표본) · 셸 3151.
- 로그 인용 절대경로 마스킹(Q-49).

---

## §1 ① OFF 축 — 「셸만으로 GS-01 replay 완주」 = **PASS**

그물 = `tests/web/t42a_static_replay_drill.mjs`(신규) · 자극 = 연결 거부.

| 무엇 | 실측 |
|---|---|
| 제안 노출 | `checking`(31ms) → `unavailable` + **정적 제안 619ms** |
| 🔴 `checking` 중 제안 | **0회**(R-3 — 아직 물어보는 중인 것을 실패로 세지 않는다) |
| 클릭 → 이동 | `/incidents/INC-2026-014?run=**STATIC-GS-01**` |
| 완주 | `run-console[data-status] = completed` |
| 이벤트 | **32 / 32** 전건 적용 |
| 단계 · 근거 · 후보 | **5 · 19 · 2** |
| TTAE | **14,513ms** |
| 🔴 자신을 LIVE 라 말하지 않는다 | `run-mode-badge[data-mode] = replay` · 출처 «정적» 표기 실재 |
| WO 링크 | **Live 전용 표기**(`work-order-draft-live-only`) · 열리는 링크 **0** — 서버 501 `replay_draft_source_absent` 와 동형 |
| 전략 비교 | **Live 전용 표기**(`to-compare-live-only`) |
| 빈 화면 | 아님 — 본문 **4,487자** |

### 1.1 되감기 · 방문자 상태 격리

| 무엇 | 실측 |
|---|---|
| 되감기 | 32 → **30** |
| 새로고침(같은 브라우저) | **30** — 그 자리를 기억한다 |
| 🔴 **새 브라우저**(다른 storage) | **32** — 남의 되감기 자리를 모른다 = 격리는 브라우저다(§14.1) |

---

## §2 ④(i) LIVE 대조군 — 「`online:false` 는 트리거가 아니다」 = **PASS**

| 무엇 | 실측 |
|---|---|
| 배지 | `checking`(11ms) → **`replay`(◑REPLAY)** 227ms — 도달은 한다 |
| 🔴 정적 제안 | **20초 관측 · 0회** |

🔴 **이 0 은 «내가 못 본 0» 이 아니다.** 같은 그물·같은 셀렉터가 OFF 축에서는 제안을 **619ms** 에
찾아냈다(참 울림 선행). 부정 판정식 앞에 세는 눈을 먼저 증명한 것이다.

⇒ R-3 이 **양면으로** 선다: 응답 실패면 제안하고, `online:false` 면 제안하지 않는다.

---

## §3 ⑤ 자산 축 — 「빌드가 sha 로 잠근다」 = **PASS**

`node apps/web-console/scripts/copy-static-replay.mjs` · **파이프 없이 rc** 로 판정.

| 자극 | rc | 문면 |
|---|---|---|
| (대조군) 무변조 | **0** | 동봉 **28건 + 이벤트 32건** → 산출물 3본 · fixture sha `3eb624c237db` · 굳힘 `2026-08-31T05:22:01Z` · ai-api build `47133a0` · 「서버가 막은 자리 **6건**은 막힌 채로 실었다」 |
| 자산 1건 변조(`plants.json`) | **1** | 「자산이 매니페스트와 갈렸다: plants.json」 + 매니페스트/실제 sha 양쪽 |
| 매니페스트 **밖** 파일 추가 | **1** | 「매니페스트에 없는 자산이 있다(1건): zz_orphan.json」 |
| 이벤트 fixture 변조 | **1** | 「fixture 가 매니페스트와 갈렸다」 + 양쪽 sha + 굳히기 재실행 안내 |

- 3건 전건 **원복 후 rc=0** · `git status` 청결 확인.
- 산출물 `lib/static-replay/generated/`(3본) = **gitignore 등재 확인**(`.gitignore:46`).

---

## §4 ⑥ 「첫 화면 ≤3s」 — **값 기록**(D-3 참조 · 중복 등재 없음)

🔴 **같은 「미연결」이 자극에 따라 두 자릿수 배로 갈린다.**

| 자극 | 제안 노출 시각 | 판정 |
|---|---|---|
| **연결 거부**(리슨 0 · 이 티켓의 OFF 정의) | **619ms** | ≤3s **충족** |
| **블랙홀**(accept 하고 안 답함) | **12,489~12,972ms**(3회) | 미충족 = **D-3**(T4-1 재검 §R3 · 여기서 중복 등재하지 않는다) |

⇒ D-3 픽스의 대상은 **블랙홀(drop) 자극**임이 이 대비로 확정된다.

---

---

## §5 ② 네트워크 축 — 「정적 경로는 live/status 만 부른다」 = **PASS**

그물 = `tests/web/t42a_static_replay_drill2.mjs`.

| 무엇 | 실측 |
|---|---|
| 🔴 세는 눈 선증명 | 같은 눈이 `GET /api/live/status` 를 실제로 **센다**(총 2~11건 · 실행별) |
| `/api` 호출 **종류** | **1종 = `GET /api/live/status`** — 그 외 **0** |
| 🔴 정적 runId 서버 송출 | **0건** — URL·본문 전수에서 `STATIC-GS-01` 을 실은 `/api` 요청 없음 |
| 첫 화면 청크 | 정적 자산 **0** · 제안 클릭 «후» 새 청크 **5건**(동적 import 실증) |
| 열람까지 마친 뒤 재측 | `/api` **10건 · 여전히 1종** — 근거 14·막힌 5·문서 열람을 다 해도 늘지 않는다 |

---

## §6 ③ 쿠키 축 — 「서버 세션을 만들지도 위조하지도 않는다」 = **PASS**

그물 = `tests/web/t42a_static_cookie_probe.mjs` · 🔴 **세 갈래로 갈라** 잰다(§0 자수 2 참조).

| 갈래 | 쿠키 실측 |
|---|---|
| ⓐ **딥링크**(`/` 무경유 · 정적 URL 직행) | **0건** — `fkt_session`·`fkt_sid` 둘 다 없다 |
| ⓑ **입장 경유**(`/` → 제안 → 정적) | 제안 «전» `fkt_session=pending:<uuid>` → 정적 진입 «후» **동일**(새 쿠키 **0건 추가**) · `fkt_sid` **끝내 없음** |
| ⓒ **LIVE 대조군** | `fkt_sid=<sid>` **실재** + `fkt_session=api:<sid>` |

🔴 **위조 0 의 실증**은 값의 «접두»에 있다: OFF `pending:` ↔ LIVE `api:`.
셸 쿠키가 자기 origin 을 스스로 말하므로, 정적 방문자가 서버 눈에 「있는 세션」인 척하지 않는다.
그리고 ⓑ 의 `fkt_session` 은 **`/` 의 입장 시도**가 남긴 것이지 정적 경로가 만든 것이 아니다 —
ⓐ 가 그것을 갈랐다.

| 무엇 | 실측 |
|---|---|
| 방문자 상태 | `localStorage["fkt.static-replay.visitor"]` = `{visitorId, cursor, visits…}`(§14.1 그대로) |
| R-1 「세 번째 origin `static`」 | **쿠키가 아니라 별 표지**로 산다 — `static-visitor-chip[data-origin="static"]`(`lib/session.ts` 는 `api`·`pending` 만 파싱). 쿠키 이름을 빌리지 않으려는 설계가 코드 주석과 실물에서 일치 |

---

## §7 ④(ii) 동형 — 「서버 replay ≡ 정적 replay 최종 RunState」 = **PASS** (승계 회부 해소)

같은 fixture 를 두 경로가 각각 재생한 뒤 **같은 축으로 정규화**해 맞댔다(runId 제외 · mode 포함).

| 축 | 정적(:3181) | 서버 replay(:3161 → 8061) |
|---|---|---|
| status · mode | `completed` · **`replay`** | `completed` · **`replay`** |
| 이벤트 | **32 / 32** | **32 / 32** |
| 단계 · 근거 · 후보 | **5 · 19 · 2** | **5 · 19 · 2** |
| TTAE | **14,513ms** | **14,513ms** |
| 질문 문면 | 동일 | 동일 |
| 후보 문면 2건 | 동일 | 동일 |
| 근거 목록 19건 | 동일 | 동일 |

⇒ **9축 + 후보 문면 + 근거 목록 전건 일치.** 구현 16대가 「계측기가 없어」 넘긴 승계 회부
「브라우저 축 최종 RunState 동형」이 이로써 **해소**된다.

---

## §8 ⑦ Live 전용 · 열람 = **PASS**

그물 = `tests/web/t42a_static_reading_drill.mjs` · 매니페스트를 정본으로 삼아 «열리는 것»과
«막힌 것»을 **둘 다** 센다.

| 무엇 | 실측 |
|---|---|
| 🔴 세는 눈 선증명 | 굳힌 근거 3건 표본이 **3/3 열린다** |
| 굳힘 규모 | evidence **14** · document **8** · 막힌 evidence **5** · 막힌 WO **1**(매니페스트 `skipped` 전건 404) |
| 그래프 경로 근거 5건 | **5/5 «막힌 채로»** — 없는 것을 있다고 하지 않는다 |
| 근거 카드 링크 | **19/19** 가 정적 꼬리표 `?run=STATIC-GS-01` 를 달고 간다(방문자가 실제로 걷는 길) |
| 문서 계열 근거 | 카드 클릭 → 열린다 |
| 추세 창 «3주» | 「추세를 가져오지 못했다: **이 창은 Live 전용입니다**」 — 빈 그래프 0 |
| 조사 시작 진입로 | 정적 화면에 **열린 채로 없다**(`start-from-alarm`·`start-from-headline` 0) |
| WO · 전략 비교 | **Live 전용 표기** · 열리는 링크 0 |
| 조용한 실패 | **0** — 「Live 전용」 낱말이 화면에 실재 |

### 8.1 렌더 분기 판정 (승계 회부 · `run-console.tsx`)

| 자리 | 무엇을 렌더하는가 | 판정 |
|---|---|---|
| `:266` `state.mode === "replay"` | 「재생본 · 원 실행 관측치」 **배지 문구** | **허용**(배지·출처 문구) |
| `:299` `isStatic && (live\|replay)` | **Live 복귀 제안** 배너 + 링크 | **허용** — 티켓 T4-2a **ⓓ 가 명시로 요구한 것**(「복귀 = 배지 전환 + 제안이지 강제 이동 아님」) |

🔴 그러므로 `t3-4-run-screen` 「코드 축 = 분기 1곳」의 red 정의가 **정본보다 좁다**.
정본이 요구한 것을 그물이 금지하는 형태이므로, **그물을 정본에 맞춰 넓히는 것**이 옳다
(검증 좌석 scope). 🔴 오케 조건 2 동봉: ⓐ 허용은 **허용 목록 1곳**(testid 로 `:299` 복귀 제안만 ·
`isStatic` 일반 허용 아님) ⓑ **「허용 목록은 자기 분기를 가린다」 대조군 1행**(허용 밖 분기를
1줄 주입 → FAIL 확인 후 원복). **⑧ 에서 처리 — 15대 몫.**

---

## §9 §0 자수 추가분 (조각 2)

| # | 무엇이 거짓말했나 | 어떻게 드러났나 | 교정 |
|---|---|---|---|
| 2 | 쿠키를 «한 항아리»에서 셌다 | `/` → 제안 → 정적으로 걸어간 뒤 세었더니 `fkt_session` 이 있어 **FAIL 을 냈다**. 그 쿠키는 **`/` 의 입장 시도**가 남긴 것이지 정적 경로의 것이 아니다 | **딥링크 갈래**를 대조군으로 세워 두 사건을 갈랐다(§6 ⓐ) |
| 3 | 정적 열람을 `?run=` **없이** 쳤다 | 근거 3건이 「열리지 않음」으로 나왔다 — 꼬리표가 없으면 그 라우트는 **Live 경로**로 서고, ai-api 가 죽었으니 당연히 못 연다. **대상이 아니라 내 URL 이 틀렸다** | 꼬리표를 달아 재측 → 3/3 열림 |
| 4 | 근거 카드의 링크를 카드 «자신»에서 찾았다 | `href` 도 `closest("a")` 도 없어 「링크 아님 19건」 **위양성 FAIL**. 링크는 카드 **안**에 있다(`run-panels.tsx`) | 자손 `a[href]` 로 다시 세어 19/19 |

---

*(⑧ 회귀 — 아래 §10 · 좌석 리바이2 16대가 승계해 종결)*

---

## §10 ⑧ 회귀 = **PASS** (검증 좌석 리바이2 **16대** · 승계본)

### 10.1 측정 조건 — 🔴 ①~⑦ 과 **기점이 다르다**

| 무엇 | 값 |
|---|---|
| ①~⑦ 기점 | `c0276df` |
| **⑧ 기점** | **`3617abe`** = lane 에 `origin/develop` 병합(**D-5 픽스 `f3f4cec` 포함** · D-3 미포함) |
| 🔴 실측 정정 | 발주문의 `ad8b2a3` 는 병합 시점의 `origin/develop` 이 **아니었다** — 실제 tip = `de65406`(PR#239). 추가분은 `.workspace/handoffs/*` + `INDEX.md` **2파일뿐**이고 `services/` · `packages/contracts/` · `tests/api/` · `data/` 갈림 **0** |
| 병합 충돌 | **0** — 내 lane 5파일(evidence 1 + `tests/web/t42a_*` 4)과 겹침 0 |

**대상 서버 · 🔴 귀속을 이름으로 믿지 않았다**

| 무엇 | 실측 |
|---|---|
| tests/api 대상 | ai-api **`:8061`**(`fkt-levi2-t35-seeded`) · 재부팅 후 `docker start` 로 되살림(rc=0) |
| 씨앗 실재 | `document_chunk` **59행** · neo4j **309 노드**(「health 초록 ≠ 데이터 있음」을 따로 셌다) |
| 코드 귀속 | `docker exec` 로 컨테이너 `/srv/app` 의 `.py` **43본 sha256** ↔ 내 lane `services/ai-api/app` 대조 = **42/43 동일** · 갈린 **1본 = `probes.py`**(Q-52 재연결 · `b43f2c3`) |
| 그 1본이 축에 닿는가 | 닿는 자리는 `/api/health` 뿐이고, 바뀐 가지는 **핸들이 없을 때**만 돈다. **대조군**: 의존 전건 UP 상태에서 `:8061` ↔ `:8071`(43/43 동일본) `/api/health` 응답 **형상·값 동일**(`postgres ok` · `neo4j ok`) ⇒ 이 실행에서 그 가지는 **돌지 않았다** |
| 트리 귀속 | `_colocation` **매 실행 ON** · `FKT_SERVER_REPO` = `_wt/levi2-t35v`(그 서버가 실제로 읽는 fixture 디렉터리) · 그 트리 ↔ 내 lane 의 `data/replay`·`tests/api` 갈림 **0** |

🔴 **왜 `:8071` 이 아닌가**(발주문 채택안에서 옮긴 이유): `:8071`(`t41r`)은 코드가 **43/43** 로 완전 일치하지만
그 스택의 postgres 에 **`document_chunk` 릴레이션이 없고** neo4j 는 **0 노드**였다 — 씨앗이 없다.
「코드가 같다」와 「데이터가 있다」는 다른 사실이고, 21종 중 다수는 뒤엣것을 필요로 한다.
그래서 **씨앗 있는 `:8061`** 로 가되, 갈린 1본이 이 실행에서 도는지를 **위 대조군으로 갈랐다**.

### 10.2 `tests/api` 21종 = **21/21 PASS · 건너뛴 행 0**

- 직렬 · `_colocation` 켬 **18/21**(불요 3 = `ci_hygiene`·`anchor_extraction_probe`·`ssot_write`) · 총 34s.
- 🔴 **「rc 0 ≠ 한 일」을 따로 셌다**: `gs01_integration` S1~S12 가 실 ID(`RUN-70e8474cc000` · `WOD-a2e65c568fd7`
  · `AUD-85ac4dd65bd3`)와 **이벤트 32 · 근거 20** 을 실어 왔고, `event_schema` 는 표본 **11종** 자기 검증 후
  S-01~S-05 를 냈다. **빈 초록 0건.**
- 🔴 **`ssot_write_drill` 기본판은 초록이 아니었다** — 스스로 「W-01 건너뜀. 🔴 초록으로 세지 않는다」를
  인쇄한다. `--run` 으로 되받아 **PASS**(`RUN-c0de9f033238` 완주 · 지문 29테이블 950,372행 · **SSOT 변화 0**).
  그래서 21종의 **건너뛴 행이 0** 이 됐다.
- 계약 정본은 **내 트리**의 것을 썼다(v0.1.9 = `run.queued` enum 8→9 포함). 서버는 그보다 앞선 코드지만
  `event_schema` 는 **관측된 이벤트가 어휘 안인가**를 묻지 `어휘 전건이 나왔는가`를 묻지 않는다 —
  미구현 T4-2b 어휘가 **거짓 빨강을 만들지 않았다**(어긋남 0).

### 10.3 🔴 `contract-surface` — 빌드 «뒤» = **PASS (계약 밖 0)** ⇒ **D-6 종결 조건 충족**

| 무엇 | 실측 |
|---|---|
| 순서 | `pnpm build`(rc=0) → **그 트리에서** `pnpm contract:surface` |
| 산출물 실재 | `lib/static-replay/generated/` 3본(`events.ts`·`manifest.ts`·`responses.ts` · 23:12 생성) |
| 결과 | rc=0 · **`계약 표면 대조: PASS (계약 밖 0)`** · fetch 호출 파일 **1**(`lib/contract.ts`) |
| 🔴 **초록이 「제외가 일한 것」인가** | 굳힌 3본 안에 `/api/` 문자열 **102개** 실재 = 제외가 없었으면 물렸을 자극 · 드릴 자기 출력 「스캔 40파일 · **굳힌 데이터 제외 3파일 · 경로 68종**」 |
| 규칙이 죽지 않았나 | PR#227 의 **0파일 불변식**(`contract-surface.mjs:159` — generated 디렉터리는 있는데 매칭 0이면 FAIL)이 코드에 실재 |

⇒ 「빌드 뒤 게이트 항상 빨강」은 사라졌고, **그 사라짐이 «규칙이 물어서» 생긴 것**임을 위 두 줄이 가른다.

### 10.4 브라우저 suite — `--workers=1` · production build

| 실행 | 셸 | 결과 |
|---|---|---|
| 1차(그물 손대기 «전») | **3191** 신설(`FKT_API_BASE=:8061` 로 빌드 · Q-37) | **85 passed · 1 failed · 1 skipped**(1.6m) |
| 2차(그물 정본화 «뒤») | 같음 | **86 passed · 0 failed · 1 skipped**(1.6m) |

- 1차의 빨강 **1건은 예고된 `t3-4:163` 하나뿐** — **새 D-n 0건**.
- 🔴 **부하 flake 0건**(13대 표·T3-5 ⓒ 3건과 다르다). 이 실행에서는 관측되지 않았다 — 없다는 뜻이 아니라 **이번엔 안 났다**.
- 건너뛴 1행 = `t3-2-screens:97`「활성 알람 **0건이면** 정상 가동 대체문」 — 씨앗에 알람이 있어 조건이 안 섰다. **초록 아님.**

### 10.5 `t34_run_failure_drill` = **6/6 PASS**

X-1 서버 `failed` ↔ 화면 `failed` · X-2 낱말 「중단됨」 · X-3 도는 단계 0 · X-4 서버가 말한 사유
(`step_failed:graph`)가 화면 문면에 실재 · X-5 경과값에 「중단 시점」 라벨 · X-0 빈 화면 0.
되감기 = `fkt-levi2-neo4j-1` 재기동 후 `ok` 확인.

### 10.6 🔴 `t3-4-run-screen` 코드 축 **그물 정본화** (오케 조건 2 이행)

**처방** — red 정의를 「**분기 1곳**」에서 「**허용 목록(testid) 밖 0**」으로 넓혔다.

| 무엇 | 값 |
|---|---|
| 허용 | `ttae-replay-note`(`:266` 배지 문구) · `live-return-offer`(`:299` = 티켓 T4-2a **ⓓ** 가 명시로 요구한 Live 복귀 «제안») |
| 🔴 허용의 열쇠 | **자리 번호가 아니라 그리는 것**(분기가 여는 블록의 `data-testid`) — `isStatic` **일반 허용 아님** |
| 함께 둔 불변식 | 허용 testid 가 **실재하지 않으면 FAIL**(「목록이 0을 물면 규칙이 죽은 것」 · PR#227 계보) |

**🔴 대조군 4본 — 전건 빨강 확인 후 원복(`git status` 0행)**

| # | 자극 | 결과 |
|---|---|---|
| 1 | 허용 밖 분기 1줄 + 낯선 testid(`zz-control-probe`) | **FAIL** — 「허용 목록 밖 모드 분기: …:294」 |
| 2 | 허용 밖 분기 1줄 + **testid 없음** | **FAIL** — 같은 문면 |
| 3 | 허용 testid 개명(`live-return-offer-RENAMED`) | **FAIL** — `:299` 가 허용 밖으로 떨어진다 |
| 4 | 분기만 정규식 밖으로(자리 소멸 · testid 유지) | **FAIL** — 「**허용 자리 live-return-offer 가 실재하지 않는다**」 = 불변식이 산다 |

⇒ §8.1 의 판정(`:299` = 정본이 시킨 것)이 **그물에 반영**됐고, 넓힌 만큼을 **네 방향으로 잠갔다**.

### 10.7 🔴 Q-58 — **재현되지 않았다.** 등급 **하** · 귀속 = **그물**(대상 결함 아님) · 비차단

| 무엇 | 실측 |
|---|---|
| 이 실행 | `t3-3-evidence.spec:186` **PASS**(6.5s) — 주입 후 뷰 `STALE` · 화면 배지 `stale`/`stale` · 대조군 `fresh` · 원복 후 `FRESH` |
| 🔴 귀속 (E1 포렌식) | 관측자(센쿠2 17대) 셸의 `.next/server` 에 **구워진 API = `127.0.0.1:8004`**(senku2 스택). 같은 자리에서 내 셸은 `:8061`. `_wt/senku2-t35`·`_wt/senku2-t42a` 둘 다 `8004`, `_wt/levi2-t35v`·`_wt/levi2-t42a` 둘 다 `8061` |
| 그물이 흔든 곳 | `FKT_PG_CONTAINER` **기본값 = `fkt-levi2-postgres-1`**(levi2 DB) |
| 그래서 무슨 일이 났나 | 「뷰가 STALE 로 전이」는 **주입한 levi2 DB 를 psql 로 되물어** 참이었고, 화면은 **자기 DB(senku2) 기준으로 옳게** `fresh` 였다. **한 측정에 두 스택이 담겼다** — 「막는 처방」과 「주지 않는 처방」이 같은 초록을 내듯, 여기서는 **다른 스택이 「침묵」처럼 보였다**(Q-40 계보) |

**처방 (검증 좌석 scope · `tests/**`)** — 판정 «앞»에 귀속을 세웠다.

- 셸은 `/api` 를 **자기 API 로 프록시**한다(실측: `GET :3191/api/live/status` → 200). 그 응답이 곧 「이 화면이 무엇을 보는가」다.
- 주입 중 `GET {FKT_WEB_BASE}/api/evidence/{TARGET_CHUNK}` 의 `stale` 이 `true` 가 **아니면**
  「화면이 침묵한다」가 아니라 **「다른 스택을 흔들었다 = 측정 불가」** 라고 말하게 했다.
- 🔴 **세는 눈 선증명**: 같은 탐침이 주입 **전** `stale=false` 임을 먼저 단언한다 —
  실측 `false → true` 로 **갈렸다**. 늘 같은 답을 하는 탐침이 아니다.

🔴 **한계(정직하게)**: **참 교차 대조군**(두 번째 «씨앗 있는» 스택으로 실제 오귀속을 재현)은 세우지 못했다.
이 머신에서 `index_build` 를 가진 postgres 는 `fkt-levi2-postgres-1` **하나뿐**이다(나머지 4벌 전건 릴레이션
없음 실측). 그래서 오귀속 갈래는 **구성상 도출**(E2)이고, 탐침이 이 DB 를 따라간다는 것만 **E1** 이다.

---

### 10.8 되받기 — Q-52 프로브 축을 **`:8071`(43/43 동일본)** 으로 1회 (오케 재계산 요구 이행)

`probes.py` 한 본이 갈린 서버로 21종을 잰 것이 판정에 섞이지 않도록, **`/api/health` 에 닿는 4종**을
코드가 **완전히 일치하는** `:8071` 에서 다시 돌렸다(`_colocation` ON · `FKT_SERVER_REPO=_wt/levi2-t41r`).

| 드릴 | `:8061`(씨앗 O · 42/43) | `:8071`(씨앗 X · **43/43**) |
|---|---|---|
| `error_shape_drill` | PASS 9/9 | **PASS 9/9** |
| `replay_fixture_drill` | PASS 어긋남 0 | **PASS 어긋남 0** |
| `dependency_code_drill` | PASS 어긋남 0 | **rc=2 측정 불가** — 기준선 D-01·D-02·D-03 이 `503 dependency_unavailable`(씨앗 부재) |
| `session_guard_drill` | PASS 어긋남 0 | **rc=2 측정 불가** — 같은 사유 |

🔴 **잴 수 있었던 자리에서는 두 서버가 같은 색을 냈다.** 못 잰 두 자리의 rc=2 는 **FAIL 이 아니라**
「씨앗이 없어 판정 안 한다」는 그물 자신의 규율이고, 이것이 `:8061` 을 대상으로 고른 사유를
**뒤에서 다시 증명한다**. ⇒ `probes.py` 갈림은 이 판정의 어느 색에도 닿지 않았다.

---

## §11 §0 자수 추가분 (⑧ · 16대)

| # | 무엇이 거짓말했나 | 어떻게 드러났나 | 교정 |
|---|---|---|---|
| 5 | 컨테이너 코드 대조에서 `sha256sum` 출력 폭을 **고정 오프셋**으로 잘랐다 | 43행이 **전건 갈림**으로 보였다 — 해시는 처음부터 같았고 경로 접두 길이만 달랐다 | 이름 정규화 후 재대조 → 43/43 일치 |
| 6 | 같은 대조에서 Windows `sha256sum` 의 `해시 *이름`(바이너리 표지)과 컨테이너의 `해시␣␣이름` 을 그대로 맞댔다 | 다시 43행 전건 갈림 | 구분자 정규화 |
| 7 | 대상 서버를 **`FKT_BUILD_SHA`(자기 신고)** 로 고를 뻔했다 | 그 값은 빌드 인자라 스스로를 증명하지 못한다 | `docker exec` 로 **실파일 43본 sha256** 을 재서 골랐다 |
| 8 | `:8071` 을 「코드가 같으니 대상」으로 정하고 발주자에게 그렇게 보고했다 | 첫 드릴에서 「없는 근거 → **503 dependency_unavailable**」이 나왔다. 실측하니 그 스택은 **`document_chunk` 릴레이션조차 없다** — 「health 초록 ≠ 데이터 있음」 | 씨앗 있는 `:8061` 로 옮기고, 갈린 1본(`probes.py`)이 도는지를 health 대조군으로 갈랐다 · 보고 정정 |
| 9 | suite 로그에서 빨강을 **문자 클래스 `[✘×✗]`** 로 셌다 | ✓ 줄이 빨강으로 잡혀 나왔다(멀티바이트 바이트 매칭) | 요약 행(`N passed / N failed`)으로 판정 |
| 10 | 발주문의 병합 대상 `ad8b2a3` 를 그대로 믿을 뻔했다 | `git merge origin/develop` 이 **`de65406`** 를 가져왔다 | 추가분 2파일을 전수 실측해 축 무접촉 확인 후 진행 · 발주자에게 정정 보고 |

