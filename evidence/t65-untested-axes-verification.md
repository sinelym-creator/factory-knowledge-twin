# T6-4/T6-5 미측 7축 판정 — 리바이2 34대 (2026-09-03)

발주 = 스자쿠 28대 16:23 KST(전임 33대가 「추정으로 초록 주지 않는다」로 남긴 7축).
무대 = 셸 로컬 **prod 빌드**(`next build` + `next start -p 3107`) · `FKT_API_BASE=http://127.0.0.1:8010`
· 대상 sha `d15b534`(origin/develop tip 실측) · 배포 ai-api `health.build=7956e0c`(내 실측 E1).

> 🔴 이 문서에서 「PASS」는 **판정선의 출처를 함께 적은 초록**만을 뜻한다. 발주가 준 판정선과
> 정본(규격) 문면이 갈리는 자리에서는 **둘 다** 적었다 — 발주선이 목적을 다 담지 못한 축이
> 실제로 하나 있었다(축 ⑤).

## 판정 요약

| 축 | 판정 | 한 줄 |
|---|---|---|
| ① 중간 재개(스텝 5 Esc → `?`) | 🔴 **FAIL** | 3회 중 2회 **투어가 아예 안 열린다**(저장은 `step:5` 보존) |
| ② 키보드 완주 | ⚠️ **부분 FAIL** | 완주·제목 포커스 PASS · **Esc 후 포커스 복귀 없음**(3회 전부 `<body>`) |
| ③ 투어 문구 경계 | ✅ **PASS** | 렌더 문면 10면 위반 0 · synthetic 명시 있음 · 대조군 4건 검출 |
| ④ reduced-motion | ✅ **PASS**(+규격 미이행 1) | 정지 링 유지·정보 손실 0 · 다만 링 두께 **2px**(규격 ④-6 은 3px) |
| ⑤ 390 바텀시트 | ⚠️ **발주선 PASS · 규격 FAIL 1** | 하단 부착·가로 스크롤 0 ✓ · **콜아웃이 대상의 98.4%를 덮는다** |
| ⑥ 「후」 50칸 시각 검수 | ⚠️ **레이아웃 3지표 0 · 대비 25행 회부** | 넘침·잘림·겹침 0 · 대비 미달 고유 25행(축 소속은 오케 판단) |
| ⑦ O-1 귀속 | 📋 **관측(미재현)** | 30회 전수 `46` · `h1` 항상 1 · **69 미재현**(계측기는 2벌을 센다) |

## ① 중간 재개 — FAIL

정본: `t6-5-guided-tour-spec.md` ⑥ 흐름 「Esc 종료 · **재개(`step` 기억)**」.

| 열 | 재개 열림 | 복원 index | 저장값 |
|---|---|---|---|
| target#1 | ✗ | — | `skipped/5` |
| target#2 | ✓ | **5** | `active/5` |
| target#3 | ✗ | — | `skipped/5` |
| target-reload(재개 뒤 새로고침) | ✓ | **5** | `active/5` |
| control(재개 직전 저장 `step→0`) | ✓ | 0 | `active/0` |

- **저장은 멀쩡하다**(`step:5` 보존). 깨지는 것은 **재개 표시 경로**다.
- 새로고침 열이 100% 열린 것이 손잡이 — `tour-provider.tsx` 의 `readState()` 가 `useEffect(…, [wants])`
  안 1회뿐인데, `?`(`/overview?intro=1&tour=1`)를 **이미 `tour=1` 인 URL**에서 누르면 `wants` 가
  true→true 라 deps 가 안 움직인다. 리마운트가 없는 회차에는 메모리의 `skipped` 가 남아 렌더 0.
- 오케 28대가 같은 근인을 코드에서 확인하고 수리(상태가 `active` 를 벗어날 때 URL 의 `tour` 도 끈다).
  **재측은 병합 뒤.**
- 🔴 자수: 1차 실행에서 `?` 클릭이 URL 을 못 바꾼 회차가 섞여 있었다(자극 미실림). 그물에
  「클릭 → URL 전이 확인 → 3회 재시도 → 끝내 실패하면 `stimulus-missing`」을 넣어 고쳤다.

근거: `t65-axis1-resume.json` · `t65-axis1-resume-step1.json` · 그물 `tests/web/t65_resume_drill.mjs`

## ② 키보드 완주 — 부분 FAIL

정본: 같은 문서 ⑤ 「Tab 순회 · Enter = 다음 · **Esc = 종료(포커스 = `?` 링크로 복귀)** · 스텝
열릴 때 포커스 = 콜아웃 제목」 · ⑥ 「Tab/Enter/Esc 만으로 완주 · 포커스 복귀」.

- **완주 PASS** — 마우스 0회. `status:"done"` 도달, 화면 전이도 정상(overview → incidents → **evidence** → overview).
  링크 스텝(3·9)은 `Tab×2+Enter`, 클릭 스텝(6)은 근거 칩 `<a>` 에 `TabInto×9+Enter` 로 진행·이동.
- **스텝 제목 포커스 PASS** — 9/9 `tour-title`.
- 🔴 **Esc 후 포커스 복귀 FAIL** — 3회 전부 `document.activeElement` = `<body>`. `stop()` 에 포커스를
  되돌리는 코드가 없다. 키보드 사용자는 Esc 한 번에 문서 맨 앞으로 떨어진다.
- 링크 스텝이 Enter 를 「직접」 받지 않는 것은 **결함 아님** — 스펙 ⑤ 가 「Tab 순회」를 말하고,
  provider 가 Enter 를 `kind==="next"` 로 한정한 것은 버튼 기본 동작과의 이중 발화를 막는 설계다.
- 🔴 자수 2건: 링크·클릭 스텝에서 **Enter 뒤 이동을 안 기다리고** 판정해 「스텝 5 막힘」·「키보드로는
  근거 화면에 못 간다」는 **위양성**을 냈다. 대기(10초) 후 둘 다 정상으로 뒤집혔다.

## ③ 투어 문구 경계 — PASS

정본: ⑤ 「문구에 내부 예외명·헤더명·경로 0 · synthetic 명시」.

- **렌더 문면**(코드 grep 아님) 10면 = 초대 카드 1 + 스텝 9. 위반 **0건**, synthetic 명시 있음.
- 🔴 대조군: **살아 있는 콜아웃 DOM 에 위반 문면을 심어** 같은 수집 경로로 재수집 → **4건 검출**
  (`HTTPError` · `X-FKT-Trace` · `/api/sessions` · `/ai-api/main`).
- 🔴 정본 인용 정정(오케 집행 확정): baseline **§15.2 원문은 「Claude Code 사용 경계」**이고 「내부
  예외명·헤더명·경로 0」은 §15.2 에 없다 — 그 규율은 **D-24 계보**의 것이다. 이 PASS 는 그 축의 것.
- 🔴 자수 1건: 완주 뒤 콜아웃이 사라진 화면에 needle 을 심으려다 그물이 죽었다 → 대조군을 스텝 0 으로 이전.

## ④ reduced-motion — PASS (+규격 미이행 1)

| 열 | transition | animation | outline | 링 |
|---|---|---|---|---|
| reduce | `0s` | `none` | `2px solid rgb(10,132,255)` | 남음 |
| no-preference | `0.4s` | `none` | `2px solid rgb(10,132,255)` | 남음 |

- 두 열이 실제로 갈렸다(계측기 생존) · 링·딤·`opacity 1` 유지 → **정보 손실 0**.
- 🔴 규격 ④-6 「pulse 대신 **정지 링 3px + 굵은 테두리**」 대비 **2px 고정** — 미이행 1건 회부.
  (근인: 인라인 `outline` 은 미디어쿼리를 못 탄다.)

## ⑤ 390 바텀시트 — 발주선 PASS · 규격 FAIL 1

- 하단 부착 ✓ `bottomGap = 12px` · **가로 스크롤 0** ✓(8칸 전수 `overflowPx = 0`).
- 🔴 스텝 2(`alarm`)에서 **콜아웃이 스포트라이트 대상의 98.4%를 덮는다**(겹침 77,400px² ·
  spot `t507 h215` vs callout `t275 h557`). **1440 열은 0%** → 390 전용.
- 근인: 콜아웃의 인라인 `top`/`width` 가 `max-md:top-auto`·`max-md:w-auto` 를 무력화(인라인이
  클래스보다 세다) → top·bottom 동시 고정 → 시트가 화면 높이의 66% → 대상을 덮는다.
- 정본 ⑤ 「모바일: 콜아웃 = <md 바텀 시트 · 대상 `scrollIntoView({block:'center'})`」의 **목적**
  (대상을 보여 주며 설명한다)이 깨진다. 발주선만 보면 초록이라 **둘 다** 적는다.

## ⑥ 「후」 50칸 — 레이아웃 3지표 0 · 대비 25행 회부

5화면 × 5뷰포트(390·768·1024·1280·1440) × colorScheme 2 = 50칸.

| 지표 | 결과 | 대조군(같은 실행) |
|---|---|---|
| body 가로 넘침 | 0칸 | `+1720px` 검출 |
| 잘린 요소 | 0건 | `0→1` |
| 겹친 쌍(≥20%) | 0건 | `0→8` |
| 대비 <4.5:1 | 470건 = **고유 25행** | `3→4` |

- 🔴 **자수 3건째 — colorScheme 축은 판정력 0**: `colorScheme:'light'` 에서도 `body background =
  rgb(0,0,0)` · root `color-scheme: dark`. **앱이 다크를 강제**한다(정본 §10). 유효 조합은 25칸 × 2 중복.
- 대비 미달 두 무리: ⓐ CTA 버튼 흰 글자 on 틴트 **3.65:1**(4행) ⓑ 틴트색 링크·근거 ID 텍스트
  **3.82:1**(21행). t6-4 ⑤ 는 「**본문** ≥4.5:1」이라 ⓐ 의 축 소속은 오케 판단 영역 — **값만 올린다**.
- `t6-4-redesign-verification.md` 에 대비 언급이 없어 기지 판정과 충돌하지 않는다(신규 관측).

## ⑦ O-1 귀속 — 관측(미재현)

- **30회 전수 `46`**(단독 12 + 부하 중 12 + 대조군 실행 6) · `h1` 값은 **항상 1** ·
  중복 testid 는 정상 반복 요소뿐(`equipment-card` 12 · `sparkline` 12).
- 시점별(0·60·150·400·1200ms)로도 46 초과 없음 — 「스트리밍 교체 창의 2벌」은 이 무대에서 안 잡힌다.
- 🔴 **계측기 판정력 증명**: overview 본문을 통째로 복제해 심자 **46 → 82 · `h1` 1 → 2** 로 올랐다.
  즉 「69 미재현」은 값이지 안 본 것이 아니다.
- 전임 관측(69)은 `c4f9ab2` 3회 중 2회 · 나는 `d15b534`. **재현 조건이 sha 또는 실행 형태에 걸려
  있을 가능성** — D-n 승격은 그 조건을 특정한 뒤가 옳다. 지금은 **관측 행 유지** 권고.

## 자수(내 그물이 만든 빨강 · 대상 결함 아님)

1. 완주 뒤 콜아웃 없는 화면에 문면 needle 을 심으려다 그물 사망 → 대조군을 스텝 0 으로 이전.
2. 링크 스텝 Enter 뒤 이동을 안 기다려 「스텝 5 막힘」 **위양성**.
3. 클릭 스텝 Enter 뒤 같은 실수 → 「키보드로는 근거 화면에 못 간다」 **위양성**.
4. 축 ⑥ 의 colorScheme 열이 판정력 0(앱이 다크 강제) — 50칸이 실질 25칸이었다.
5. 축 ① 1차 실행에서 `?` 클릭이 URL 을 못 바꾼 회차를 걸러내지 못했다(자극 미실림) → 그물 보강.

## 재측 대기(오케 발주 ⓐ~ⓓ · PR 병합 뒤)

ⓐ 축① 재개(step 5·1 × 3회 + 대조군 · URL 전이 선확인) ⓑ 축② 포커스 복귀 ⓒ 축⑤ 390 겹침 전 스텝
ⓓ 축④ `motion-reduce` 링 3px.

---

# 재측 (수리 병합 뒤 · 같은 무대 · 대상 `692278a`)

오케 28대가 4건(재개·포커스·390 겹침·링 3px) + 대비 + 겹침 재수리를 병합했고, **매 재측 전에
「대상이 처방을 실었는가」를 먼저 봤다**(병합본 코드 심볼 + 재빌드 산출물 grep).

| 축 | 재측 | 값 |
|---|---|---|
| ⓐ 재개 step 5 | ✅ **PASS** | 3/3 열림 · index 5 보존 · 대조군 여전히 0 검출 |
| ⓐ 재개 step 1 | 🔴 **대상 결함 확정** | 아래 별도 절 |
| ⓑ Esc 후 포커스 복귀 | ✅ **PASS** | 4시점(0·100·300·800ms) 전부 `intro-reopen` |
| ⓒ 390 겹침 | ⚠️ **3칸 해소 · 1칸 회부** | 아래 별도 절 |
| ⓓ reduce 링 3px | ✅ **PASS** | reduce `3px` / 대조 `2px` |
| ⓔ 대비 | ✅ **PASS** | 고유 25행 → **0** · 레이아웃 3지표 회귀 0 · 대조군 4지표 발화 유지 |
| ⓕ 발광 노드 1개 | ⚠️ **실측 2개 · 소속 회부** | `start-from-headline` · `start-from-alarm` — 둘 다 「조사 시작」 |
| ⓖ 투명도 감소 | ✅ **PASS** | 아래 별도 절 |

## ⓐ step 1 — 「`?` 를 눌러도 아무 일도 안 일어난다」(대상 결함)

한 자리에서 세 값을 함께 찍어 귀속을 끝냈다(`t65_reopen_click_attrib.mjs` · 6런).

| 열 | 값 | 뜻 |
|---|---|---|
| 링크에 click 이벤트가 닿았는가 | **6/6** | 내 클릭은 매번 발화했다 |
| 클릭 뒤 `location` 변화(2·5·10초) | **2/6** | 4회는 10초가 지나도 그대로 · 콜아웃 0 |
| 같은 href 로 직접 이동 | **4/4** · index 1 | 화면·저장·재개 로직은 멀쩡하다 |

같은 pathname 에 쿼리만 붙는 이동(`/overview` → `/overview?intro=1&tour=1`)에서만 나고, 경로가
바뀌는 이동(step 5)은 3/3 정상. **수리 전(`d15b534`)에도 같은 비율** — 회귀가 아니라 상시 거동이다.

## ⓒ 390 겹침 — 3칸 해소, 남은 1칸은 «0 이 불가능한 칸»

| 열 | 스텝별 대상 덮임(0→8) |
|---|---|
| 390 | 0 · **0** · **0** · **0** · 23.3 · 0 · 0 · — · — |
| 1440 | 0 · 0 · 0 · 0 · **16.6** · 0 · 0 · — · — |

- 390 스텝 1·2·3 이 **40.6 / 100 / 18.5 → 전부 0**(시트가 대상과 겹칠 높이면 상단으로 뒤집힌다).
- 남은 스텝 4(`candidate`)는 대상 높이 **800px / 뷰포트 844px**(1440 은 786/900) — **콜아웃을
  어디에 두어도 겹친다**. 배치 실패가 아니라 「대상이 뷰포트보다 큰 목록」이라는 사실이다.
  발주 기대(겹침 0)로는 2칸 FAIL · 규격 ⑤ 목적(대상을 보여 주며 설명)으로는 PASS.
  🔴 **권고: 판정선을 고칠 자리다** — 「덮임 0%」는 화면보다 큰 대상에는 도달 불가능한 기준이다.

## ⓖ 투명도 감소 — 자극부터 세운 축

Playwright `emulateMedia` 에 `prefers-reduced-transparency` 가 **없다**. CDP
`Emulation.setEmulatedMedia` 로 걸고 **브라우저에 `matchMedia(...).matches` 를 되물어** 자극
실림을 먼저 확인했다(reduce 열 `true` · 평상 열 `false`). 안 실렸으면 빨강이 아니라 `exit 2`.

| 대상 | 평상 열(대조군) | reduce 열 |
|---|---|---|
| `.fkt-glass` × 2 | `blur(20px) saturate(1.8)` · α 0.72 | **`none`** · α **1** |
| `.fkt-glow-ai` | `color(srgb …/0.55) 0 4px 18px -6px` | **`none`** |

## 계약 회귀 그물 — `runCompleted.candidates: minItems 1`

`tests/api/candidates_guard_drill.py`(센쿠2 회부분). 세 축을 갈라 적는다:
① 계약은 참(빈 배열을 실제로 거부) ② **방출 경로(`_emit`)는 스키마를 검증하지 않는다**
③ 집행자는 `synthesize_node` 의 「후보 0건이면 raise」 **한 줄**. 대조군(가드를 걷어낸 사본)에서
검사기가 빨강을 내는 것까지 같은 실행에서 확인한다 — 이 그물이 지키는 것은 **③이 사라지는 회귀**다.

## 자수 (재측 구간)

6. ⓐstep1 을 「내 창 오염 탓」으로 자수했다가 **오염 없는 창에서 같은 값**을 얻어 자수를 기각했다.
7. ⓑ 를 「여전히 FAIL」로 보고했는데, 오버레이 `detached` 직후 **한 시점만** 읽은 값이었다.
   시점을 열로 펼치자 **0ms 부터 이미 복귀**해 있었다 — 계측기의 순간을 제품의 성질로 적을 뻔했다.
8. 내 `tests/web/node_modules` 가 **다른 좌석 워크트리를 가리키는 junction** 이었고 그 워크트리가
   정리되며 죽었다 — 내 워크트리에 직접 설치해 끊었다.

---

# 재측 2차 (`63ca7fd` → `96e4107`)

| 축 | 결과 | 값 |
|---|---|---|
| ⓐ 재개 step 1 · step 5 | ✅ **PASS** | 두 좌표 **3/3** 열림 · index 보존 · 클릭 닿음 3/3 · (기록) URL 전이도 3/3 |
| ⓕ 발광 | ✅ **PASS** | `.fkt-glow-ai` **1개**(「조사 시작」) · 다른 1차 버튼 그림자 0 |
| ⓖ 투명도 감소 | ✅ **무회귀** | 자극 실림 확인 · glass `none`/α1 · glow `none` · 평상 열은 살아 있음 |
| ⓒ 겹침 390 | ✅ **PASS** | 머리 덮임 **69.9% → 0%** · 콜아웃 하단 복귀 · 스텝 1·2·3 무회귀 |
| ⓒ 겹침 1440 | 🔴 **FAIL** | 머리 덮임 **49.9% → 49.9%**(한 자리도 안 움직였다) |

## 판정선 개정 — 「덮임 0%」에서 「머리가 보이는가」로

대상이 뷰포트보다 크면(스텝 4: 800px / 844px) **어떤 배치도 덮임 0 에 도달하지 못한다**. 규격 ⑤ 의
목적이 「대상을 **보여 주며** 설명한다」이므로 판정선을 **대상의 머리(상단 1/3) 노출**로 바꿨다
(오케 28대 채택). 🔴 그 기준으로 재자 **같은 처방이 개선에서 악화로 뒤집힌 자리**가 드러났다 —
390 스텝 4 는 시트를 위로 보내는 수리 뒤 머리 덮임이 **0% → 69.9%** 였다. 판정선을 바꾸면
**무엇이 좋아졌는지도 다시 물어야 한다**. 그물은 이제 두 값을 같은 실행에서 나란히 찍는다
(`spotCoveredPct` · `headCoveredPct`).

## 1440 이 안 움직인 이유(E3 · 코드 대조)

수리(`targetFillsViewport`)는 **`max-md` 바텀 시트 경로**에 들어갔다. 1440 은 그 경로를 타지 않고
`top-[var(--tour-top)]` **좌표 배치**를 쓰는데, 그 계산은 `fitsBelow ? below : max(12, hole.top-232)`
라 대상이 크면 **상단 12** 로 떨어지고 머리를 덮는다. 같은 규칙을 좌표 계산에도 걸어야 한다.

## 앞판 단위 케이스(작성 완료 · 실행은 승격 뒤)

`tests/web/t65_candidates_zero_frontend.mjs` — 서버·빌드를 건드리지 않고 `page.routeWebSocket` 으로
브라우저의 WS 만 가로채 **마지막 프레임만 다른 세 열**을 넣는다: ① synthesize running ②
`run.completed` 의 `candidates: []`(계약 위반) ③ `candidates: [1건]`(대조군). ③이 정상으로 그려져야
②의 화면을 계약 위반의 결과로 읽을 수 있고, WS 를 한 번도 못 가로채면 빨강이 아니라 `exit 2` 다.

---

# 종결 재측 (`03d1a1d` · 재현 2/2)

| 지표 | 390 | 1440 |
|---|---|---|
| **머리 덮임**(개정 판정선) | 전 칸 **0** | 전 칸 **0** |
| 전체 덮임 | 0·0·0·0·23.4·0·0 | 0·0·0·24·16.6·0·0 |
| 가로 스크롤 · 링 | 9스텝 0 · 3px/2px | 9스텝 0 · 3px/2px |

- 1440 스텝 3 회귀(머리 7.6%)와 스텝 4(49.9%)가 **함께 0** 이 됐다. 근인은 「배치가 **한 프레임 늦은
  콜아웃 높이**로 계산됨」이었고(실측 좌표 632 를 역산하면 189 가 아니라 256 을 썼다), `ResizeObserver`
  로 높이를 실측 갱신하자 두 칸이 동시에 닫혔다.
- 🔴 남은 전체 덮임은 **도달 불가능한 기준의 잔값**이다 — 대상이 뷰포트보다 크면 어떤 배치도 0 이
  될 수 없고, 규격 ⑤ 의 목적은 「대상을 **보여 주며** 설명한다」이므로 머리 노출로 판정한다.
  **이 숫자를 「미해결」로 옮겨 적지 않는다.**
- 🔴 이 축에서 수리는 **세 번** 옆 칸을 움직였다(스텝 2 → 4 → 3). 그래서 매 재측을 **9스텝 전수
  두 열**로 돌렸고, 마지막 판정은 **재현 열까지 2/2** 로 세웠다.

## 오늘 총계

- 발주 7축 + 재측 5축 + 추가 2축(발광 · 투명도 감소) + 계약 회귀 그물 1본.
- **대상 결함 6건 검출 → 전건 수리·재측 완료**: 중간 재개 미개방 · Esc 후 포커스 미복귀 ·
  390 콜아웃이 대상을 덮음 · 대비 미달 25행 · 발광 노드 2개 · 데스크톱 겹침 회귀.
- **내 그물이 만든 위양성 8건 자수** — 전부 판정 전에 귀속을 세워 걸렀다.

---

# 인계 (리바이2 34대 → 다음 대)

## 그물 — `--base` 만 주면 그대로 돈다

| 파일 | 무엇을 재는가 | 대조군 |
|---|---|---|
| `tests/web/t65_resume_drill.mjs` | 투어 중간 재개(`--step N`) | 저장 진행을 0 으로 되돌린 열 |
| `tests/web/t65_keyboard_wording_drill.mjs` | 키보드 완주·포커스 복귀·투어 문면 경계 | 살아 있는 콜아웃에 위반 문면 주입 |
| `tests/web/t65_motion_mobile_drill.mjs` | reduced-motion 링 · 바텀시트 · 겹침(전체+머리) | 390/1440 × reduce/motion 4열 |
| `tests/web/t65_axis6_layout_defects.mjs` | 50칸 넘침·잘림·겹침·대비 | 네 지표 각각에 위반 주입 |
| `tests/web/t65_o1_overview_census.mjs` | testid 계수 시점별 census | 본문 복제로 계수 상승 확인 |
| `tests/web/t65_reopen_click_attrib.mjs` | `?` 클릭의 귀속(발화·전이·직접 이동) | 세 값의 조합이 귀속을 정한다 |
| `tests/web/t65_glow_transparency_drill.mjs` | 발광 1개 · 투명도 감소 | 평상 열(끌 것이 있는가) |
| `tests/web/t65_candidates_zero_frontend.mjs` | 계약 위반 이벤트의 화면(**미실행**) | `candidates:[1건]` 열 |
| `tests/api/candidates_guard_drill.py` | 계약을 «누가» 집행하는가 | 가드를 걷어낸 소스 사본 |

## 판정선 — 바꾼 것과 그 이유

- **겹침**: 「대상 덮임 0%」 → **「대상의 머리(상단 1/3)가 보이는가」**. 대상이 뷰포트보다 크면
  어떤 배치도 0 에 도달하지 못한다(스텝 4: 800px / 844px).
- 🔴 **잔값의 이름**: 그래서 남는 전체 덮임(1440 스텝3 24% · 스텝4 16.6% · 390 스텝4 23.4%)은
  **도달 불가능한 기준의 잔값**이지 **결함이 아니다**. 미해결로 되살리지 마라.
- **재개**: 「URL 이 바뀌었나」 → **「투어가 열렸나」**. 수리가 열기를 이동에서 떼어냈다.
  대신 **클릭이 링크에 닿았는지**를 capture 계수로 따로 센다(안 닿은 회차는 판정에서 뺀다).

## 무대 — 재기동 함정

- 셸은 **`next build` + `next start`** 로만. `next dev` 는 WS 프레임 0 이라 run 화면이 안 그려진다.
- 그물은 **`pwsh -WorkingDirectory <wt>/tests/web`**. cwd 없이 돌리면 앱 소스를 못 읽는다.
- `tests/web/node_modules` 를 **남의 워크트리에 junction 으로 걸지 마라** — 그 워크트리가 정리되면
  도구가 죽는다(오늘 실측). 자기 워크트리에서 직접 설치한다.
- 수리 재측 전에는 **병합본 코드 심볼 + 재빌드 산출물**에서 처방이 실렸는지부터 본다.

## 외부 판정 근거 규율

**공개 URL 을 쳤다는 사실은 「밖」의 증거가 아니다.** `curl` 의 `remote_ip` 가 `100.x`(tailnet self)면
그 창은 밖이 아니고, 그 축은 **미측**이며 화면 값은 「로컬 셸에서만 확인」으로 분리해 적는다.
오늘 공개면 판정의 근거는 `remote_ip = 64.29.17.195`(공인 IP)다.
r.jina.ai 경유는 **텍스트 리더**라 CSS/DOM 축(다크·대비·렌더)에는 못 쓴다.

## 남은 것

- **앞판 단위 케이스 미실행** — `t65_candidates_zero_frontend.mjs` 는 작성만 됐다.
  서버·빌드 무접촉(WS 만 가로챈다)이라 `--base` 하나로 돈다.
- O-1(overview testid 46/69)은 **이 무대에서 재현 불가**로 닫았다. 되살리려면 **재현 조건**
  (sha 또는 실행 형태)부터 특정해야 한다.
