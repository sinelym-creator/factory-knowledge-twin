# 묶음 독립 검증 — D-60/61 · T7-39 · O-9 · D-56 (리바이2 43대)

- **대조군** = `origin/develop` **`558995c`** · `:8144` · BUILD_ID `K0nuypGZrNjJXl9t5iixB`
- **상류** = 로컬 ai-api `:8118`(`0c2bc38` 트리) · 근거 등급 **E1**
- 산출 = `evidence/batch/*.json` · 그물 = `tests/web/batch_*.mjs`

## 0. 적재 역확인 — 🔴 양방향(대상에 «있고» 대조군에 «없다»)

| 티켓 | 대상 | 심볼 | 대상 | 대조군 |
|---|---|---|---|---|
| D-60/61 | `6b853e8` · `:8140` · `gBvz6PRc2V9PqaFDw0aBZ` | `app/incidents/page.tsx` | **있음** | **없음** |
| T7-39 | `3e9f720` · `:8141` · `eW71gTtU7n9A8Y_JnjS3X` | `data-tour-note` | **1** | **0** |
| O-9 | `0efae63` · `:8142` · `vuCSiIzU6bHg_guq8fcFc` | `x-fkt-run-reused`(허용목록) | **1** | **0** |
| D-56 | `3239cd9` · `:8143` · `3kSjxRVrWCaYyPdSArzEz` | `createPortal` | **2** | **0** |

## 1. D-56 — **PASS**(축① · 판정력 있음)

**축① 리셋 확인창 `top ≥ 0` · 5뷰포트**

| 뷰포트 | 대조군 top | 대상 top |
|---|---|---|
| 1920×900 | **-57** | **367** |
| 1280×800 | **-57** | **317** |
| 1280×400 | **-57** | **117** |
| 390×844 | **-32** | **339** |
| 390×500 | **-32** | **167** |

대상 **5/5 음수 아님** · 대조군 **5/5 음수** ⇒ 🔴 **대조군이 울었다(rc=1)** — 이 축은 판정력이 있다.
음수 값 **-57 / -32** 는 센쿠2가 낸 산수(앱바 52→중심 26→`26-83=-57` · 앱바 103→`51.5-83=-31.5`)와 **일치**한다.

**축② 창의 두 버튼 · Esc** — 대상 5/5 `Esc` 로 닫힘. 🔴 **대조군도 5/5 닫힘** ⇒ 이 축은 **아무것도 안 가른다.**
「처방이 이것을 고쳤다」가 아니라 **「처방이 이것을 깨지 않았다」**로만 읽어야 한다(회귀 없음).

**축③ 배경 차단(센쿠2가 «안 잼»으로 남긴 축)** — 창을 연 채 배경에 심은 표적을 **실제로 클릭**했다.
대상 **5/5 차단**(표적 계수 0) · 🔴 **대조군도 5/5 차단** ⇒ **portal 이동이 배경 차단을 깨지 않았다**가 이 축의 답이다.
(원래 깨져 있던 것을 고친 축이 아니므로 대조군이 안 우는 것이 정상이다.)

**축④ 토스트 `right ≤ innerWidth`** — 대상·대조군 5/5 참. 🔴 **다만 내 셀렉터는 `[role="status"]` 이고,
그것이 «리셋 토스트»인지 «Live 안내 띠»인지 가르지 않았다** ⇒ 이 축은 **판정 불가**로 적는다(§4 자수 2).

🔴 **못 믿을 값 1** — 내 그물의 `appBarHeight`(`header, .fkt-glass`)는 1310/1330/0 을 냈다. 앱바 높이가 아니다.
**셀렉터가 틀렸으므로 이 칸은 값으로 쓰지 않는다.** 판정은 `top` 만으로 냈다.

## 2. T7-39 — **PASS**(축①② · 축③ 안 잼)

**축① 「조사 보기」 걸음 꼬리** — 대상에서 **`data-tour-note="replay-offline"`** 관측 · **대조군 0건**(속성 자체가 없다) ⇒ 판정력 있음.
🔴 **두 갈래 중 하나만 봤다** — 내 무대는 `online:false` 라 **`replay` 갈래는 안 돌았다**. 그 갈래는 **안 잼**이다.

**축② 걸음 0·1·3~8 본문 «바이트» 동일** — 화면 문면이 아니라 **원본 바이트**로 물었다:
`git diff 558995c 3e9f720 -- components/tour/tour-steps.ts` = **31 insertions · 0 deletions**,
그리고 `^[-+].*(body|title):` 매칭 **0건** ⇒ 🔴 **기존 걸음의 `body`·`title` 은 한 글자도 안 바뀌었다.** PASS.

**축③ 투어 OFF 말풍선 0 · `checking` 회차 기본 문장** — **안 잼**(이번 회차에서 그 두 상태를 못 세웠다).

## 3. D-60/61 — **PASS**(축①②) · 축③④ 안 잼

**축① `/incidents`** — 대상 **200**(`data-testid` 13 · incident 링크 3) · **대조군 404** · T7-39 열도 404 ⇒ 판정력 있음.
🔴 세션 «없이» 치면 양쪽 다 **307**(내 1차 curl 실측)이라, 그 회차의 307 은 대상의 답이 아니다 — 브라우저(세션 보유)로 다시 쟀다.

**축② 조사 복원** — `/incidents` 목록에 **「이 세션의 조사」 행**이 `?run=` 을 달고 있고, 그 행을 따라가면
**같은 run 이 복원**된다: 진입 시 `RUN-cda2b24d249d` → 목록 행 `/incidents/INC-2026-014?run=RUN-cda2b24d249d`
→ 착지 `runAfter = RUN-cda2b24d249d` · `data-testid` **56**(조사 화면 전개). **PASS**.

🔴 **1차에 나는 이 축을 «틀린 길»로 쟀다** — Overview 의 incident 링크로 돌아갔고, 그 길에는 `?run=` 이 없어
**대상·대조군 모두 `null`** 이 나왔다. **대조군과 값이 같으면 내 길을 먼저 의심해야 한다**(§4 자수 1).

**축③ 행 실재 확인 · 404/401/503 자극** — **안 잼**(상한 안에서 자극 주입까지 못 갔다).
**축④ 값 불변 집합** — **안 잼**(같은 이유).

## 4. O-9 — 🔴 **안 잼(무대 미성립)** · 빨강 아님

두 열 모두 `exit 2` 로 끝냈다. 이유는 **대상 결함이 아니라 내 무대가 이 축을 세울 수 없기 때문**이다.

1. 요청 형상 — 1차에 내가 지어낸 body 는 **422** 였다. 대상의 `openapi.json` 이 `RunRequest = {sessionId, mode}` 를
   **required** 로 말한다. 대상이 부르게 해서 고쳤다(자수 3).
2. 🔴 **재사용은 `live` 전용이다** — `services/ai-api/app/routers/investigations.py:256`
   `_reusable_run(_store(request), body.sessionId, scenarioId, **"live"**)`. 내가 쏜 `mode:"replay"` 는
   **어떤 경우에도 재사용되지 않는다.**
3. 직렬 2발 → runId 갈림. **동시 2발 × 5회**(`Promise.all`) 로 바꿔도 **6회 전부 runId 갈림** ⇒ 재사용 창을 못 밟았다.
4. 로컬 무대에는 **live 를 낼 상류가 없다**(게이트웨이 없음). 공개면은 **셸이 `daf5b8d`** 라 **O-9 처방이 안 실려 있다**.

⇒ 🔴 **이 축은 「처방이 실린 셸 + live 가능한 상류」가 같이 서야 잰다.** 지금 그 조합이 세상에 없다.
   **#609 병합 → 승격 → 그 공개면에서 live 2발 동시**(재사용이면 계수 1)가 유일한 경로다. **다음 회차 발주 후보.**

## 5. 안 잼 · 자수

**안 잼** — O-9 전 축 · D-60/61 ③④ · T7-39 ③ 과 `replay` 갈래 · D-56 ④(셀렉터 불확실) · D-56 앱바 높이 칸.

**자수**
1. D-60 ② 를 **틀린 경로**(Overview 링크)로 재서 대상·대조군 모두 `null` 을 냈다. 🔴 **대조군과 값이 같으면
   내 길부터 의심**해야 하는데, 하마터면 「복원 안 된다」로 적을 뻔했다. 목록 행으로 다시 재서 PASS 가 나왔다.
2. D-56 ④ 토스트 셀렉터(`[role="status"]`)가 **리셋 토스트인지 Live 띠인지 안 갈랐다.** 참으로 나왔지만 **판정 불가**다.
3. O-9 1차 요청 body 를 **지어냈다**(422). 대상의 openapi 로 고쳤다.
4. 🔴 **내 그물의 `node_modules` 가 통째로 죽어 있었다** — 전대가 junction 으로 빌려 쓰던
   `_wt/levi2-t7-29v` 워크트리가 사라져 **모든 junction 이 매달린 링크**가 됐다. `MODULE_NOT_FOUND` 를
   대상 문제로 읽지 않고 **도구 문제로** 짚어 이 lane 에 직접 설치했다(`tests/web/node_modules`).
   🔴 **검사 도구를 남의 워크트리에 결합시키면, 그 워크트리가 사라질 때 도구가 함께 죽는다.**
5. 로컬 ai-api 는 **오케 전용 메인 체크아웃의 venv 인터프리터**로 띄웠다.
6. 표본 각 열 1회.

## 6. 판정

| 절 | 판정 |
|---|---|
| D-56 | **PASS**(축① · 대조군 울림) · ②③ = 회귀 없음 · ④ 판정 불가 |
| T7-39 | **PASS**(축①② · 대조군 울림) · ③ 과 `replay` 갈래 안 잼 |
| D-60/61 | **PASS**(축①②) · ③④ 안 잼 |
| O-9 | 🔴 **안 잼 — 무대 미성립**(빨강 아님) · 경로는 §4 에 적었다 |
