# 승격 31 — 외부 재검 (리바이2 62대)

- production = main **`509f625`**(#991 · 폐하 승인 20:20) · Vercel READY 20:20:43 `dpl_GGCMS3knGDbbgLRFXqPLiDZxKCzV` · ai-api 재생성 완료 20:21:20
- 前 열 창 = `date 20:06:17` ~ `20:06:20`(**재생성 «전»**) · 後 열 창 = `20:22:09` ~ `20:22:15`
- 🔴 **구독 0** · production **읽기만**(유일한 쓰기성 = replay run 1건 · 재생은 기록 재생이라 구독을 안 쓴다) · 근거 **E1**
- 🔴 **「밖」의 근거 = 연결 IP**: TLS peer 前 **`216.198.79.131`** · 後 **`64.29.17.195`**(둘 다 Vercel edge · TLSv1.3).
  공개 URL 을 쳤다는 «사실»은 근거가 아니다 — tailnet self 로 붙어도 URL 은 같다.

## 0. 판정 — **PASS** (새 빨강 0 · 사전 예측 2건 모두 적중)

| 축 | 前 | 後 | 판정 |
|---|---|---|---|
| ① `/api/health.build` | `4225b4f` | **`509f625`** | **PASS** — 전이 관측 |
| ② 세션 없는 `/api/live/status` | 200 `{online:true}` | 200 `{online:true}` | **PASS**(불변이 정상) |
| ② 401 baseline(`POST …/runs` · `GET /api/runs`) | 401 · 401 | **401 · 401** | **PASS** |
| ③ `?sessionId=` 상한 읽기 | `runCap 3/0/3` · `hourlyCap 3/0/3` | **동일** | **PASS** |
| ④ replay 완주 1건 | completed · 이벤트 38 · `synthesis` 키 `axis`·`model` | **동일**(`RUN` 다름) | **PASS** |
| ⑤ 안 잰 것 | — | — | §4 |

- 의존 상태 後: postgres **ok** · neo4j **ok** · embedding **ready**.
- **처방 탑재(정적)**: `40f54b8`(D-97-M) · `8373873`(D-97-M2) 둘 다 `origin/main` 의 **조상 YES**.

## 1. ① 표지를 이 창에서는 «서버 build» 로 골랐다

승격 30 재검에서는 `/api/health.build` 가 **못 쓰는 표지**였다 — 그 승격은 화면만 바꿨고 서버는 불변이 정상이었기 때문이다.
이번 승격은 **ai-api 재생성을 포함**하므로 같은 칸이 **가장 강한 표지**가 된다. 🔴 **표지는 창마다 다시 고른다** —
「지난번에 못 썼다」도 「지난번에 썼다」도 이번 창의 근거가 아니다.

## 2. 🔴 발주 문면 2건을 실측이 고쳤다 (선준비 단계에서 회부 · 오케 수용)

| 발주 문면 | 실측 |
|---|---|
| ③ 「**세션 생성 응답** 헤더 `x-fkt-run-cap-limit 3`」 | 세션 생성 응답의 cap 헤더 **0개**. `x-fkt-run-cap-*` 는 **run 생성 응답**에 찍힌다(전용 스택 §8.3 실측). 공개면에서 **구독 0** 으로 읽는 정본 경로는 `GET /api/live/status?sessionId=` 이고 거기서 3/3 이 나온다 |
| ② 「**401** baseline」 | `/api/live/status` 는 **세션 없이도 200**이다. 401 은 **run 생성·목록** 경로다 |

두 사실을 한 칸에 적으면 「공개면이 열렸다」로 오독된다 — 그래서 **두 줄로 나눠** 각각 前後를 찍었다.

## 3. ④ 사전 예측이 대조군이었다

後 열을 재기 «전»에 적어 둔 예측: **「replay 는 기록 재생이라 승격 후에도 `calls` 가 없을 것이고, 그것은 결함이 아니다」**.

- 계약 스키마: `synthesis.required = ["axis"]` · `additionalProperties: false` ⇒ **부재는 유효**, 실렸다면 `0..2`.
- 실측: 前後 모두 `synthesis` 키 = `axis`·`model` · `axis: "live"` · `calls` **없음** · 이벤트 수 **38 동일**.
- 🔴 이 축은 **빌드를 안 읽는다**. 픽스처에 적힌 대로 나오므로 「後에도 없음」이 정상이고, 만약 `calls` 가 **생겼다면** 그때가
  이상한 것이다(재생이 기록이 아닌 것을 낸다는 뜻). 예측을 «먼저» 적었기 때문에 이 초록이 사후 합리화가 아니다.

## 4. 안 잰 것 — 이름으로 남긴다

1. **live run 실적재** — 공개면에서 `calls`·`safetyRetried` 가 실제로 실리는지는 **구독을 써야** 잰다. 이 창의 허가는 구독 0 이라 **안 쟀다**.
   (같은 축은 전용 스택에서 이미 관측했다 — `evidence/d97m-verify.md` §8.3. 그 초록은 **그 무대의 것**이고 공개면의 것이 아니다.)
2. **화면(브라우저) 축** — 이번 발주 범위 밖이라 안 봤다.
3. **게이트웨이 `:8787`** — 무접촉(발주 지시).
4. **전역(서비스 전체) 시간당 상한** — `?sessionId=` 가 주는 `hourlyCap` 을 읽었을 뿐, 여러 세션으로 소진시켜 본 적 없다.

## 5. 자수

1. **前 열을 「승격 대기 중」에 먼저 찍었다** — 배포가 바뀌면 前은 영영 없다. 이건 자수가 아니라 승격 30 의 교훈을 쓴 자리지만,
   그 판단을 **발주 전에** 했다는 것은 적어 둔다(발주는 「재생성 완료 뒤 착수」였다 · 前 칸 없이는 ①이 성립하지 않는다).
2. **vantage IP 가 前後로 달랐다**(`216.198.79.131` → `64.29.17.195`). 같은 Vercel edge 군이라 「밖」이라는 사실은 안 바뀌지만,
   **같은 값이 아니므로** 이 칸을 「불변 축」으로 쓰지 않았다 — 쓸 수 있는 것은 「둘 다 tailnet self 가 아니다」까지다.
