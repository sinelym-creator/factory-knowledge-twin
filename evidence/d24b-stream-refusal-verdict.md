# D-24b — 스트림 도중 거부 문면 독립 검증 (#455) · 판정 **PASS**

대상 = `lane/senku2-d24b-stream-wording` @ `8d8d711`(services 3파일) · 대조군 = develop `f4b91f5`.
🔴 주장을 그대로 믿지 않고 **네 축 전부를 내 손으로 재현**했다. 구독 **0**(실 CLI 미호출).

## 무대 — 왜 스텁인가

`tests/api/d24b_stub_gateway.py`(신설) = **NDJSON 스텁 게이트웨이**. 진짜 게이트웨이로는
「같은 문면 + 코드 유무」를 손잡이 하나만 바꿔 만들 수 없다(모델이 무엇을 줄지 내가 못 정한다).
분류가 «코드»에서 오는지 «문면»에서 오는지 가르려면 두 열이 **문면만 같고 코드만 달라야** 한다.
드릴 = `tests/api/d24b_wording_drill.py` — run 1회당 이벤트 정본의 `rejectedReason` + **이벤트
응답 본문 전체**를 누출 needle(`X-FKT` · `EV-9999-BAD` · `인용 id` · `Token` · `reasonCode` ·
`evidence_binding`)로 훑는다. 🔴 화면만 보면 「안 그린다」와 「주지 않는다」가 안 갈린다.

누출 표본(스텁이 싣는 사유) = `인용 id 가 run 근거집합 밖이다(2건) · X-FKT-Gateway-Token · EV-9999-BAD`

## 4축 실측

| 축 | 열 | 사유 문면(이벤트 정본) | 본문 누출 needle |
|---|---|---|---|
| ① **수리 前 재현** | develop `f4b91f5` × code 있음 | **누출 문면 그대로** | **4종**(`EV-9999-BAD`·`Token`·`X-FKT`·`인용 id`) |
| ① | develop × **코드 없음**(같은 문면) | 누출 문면 그대로 | **4종** |
| ② **수리 後** | #455 × code=`evidence_binding` | 「합성 결과가 근거 검증을 통과하지 못했습니다」 | **0** |
| ③ **코드가 가르는가(정)** | #455 × **코드 없음 · 같은 문면** | 「게이트웨이가 응답 도중 요청을 거부했습니다」 | **0** |
| ③ **(역)** | #455 × code 있음 · **다른 문면**(「모델이 준 표가 우리 후보와 다르다」) | 「합성 결과가 근거 검증을 통과하지 못했습니다」 | **0** |
| ④ **구조 축** | #455 × error·result 줄 **둘 다 없음** | 「게이트웨이가 결과 줄 없이 끝냈다」 | **0** |

- ③ **양방향 성립** — 문면이 같아도 코드가 없으면 분류가 갈리고(2행↔3행), 문면이 달라도 코드가
  같으면 같은 분류로 간다(3행↔4행). **분류의 주어는 문면이 아니라 코드다.**
- ④ **성립** — 「마지막 줄 안 옴」이 근거 결속 실패로 둔갑하지 않고 **제 문면**을 갖는다.
- ① 이 없으면 ②는 근거가 없다 — 같은 자극에서 옛 판이 실제로 샌다는 것을 먼저 세웠다.

## 화면 축(②의 나머지) — 브라우저 실측

셸 3102(#455 트리 재빌드 · `FKT_API_BASE=8012`) × `binding_leak`:
- run 타임라인 문면 = 「live 응답을 근거 결속 가드가 전량 거부했습니다 — **합성 결과가 근거
  검증을 통과하지 못했습니다**. 아래 순위는 결정적 집계입니다.」
- overview·배너·run 타임라인 **needle 0건** · 🔴 **대조군은 같은 실행에서 3종 검출**
  (`ConnectionRefusedError`·`X-FKT-Gateway-Token`·`Token`) → 눈먼 0 이 아니다.
- 자극 실재: WS 소켓 1 · **프레임 34** · console error 0 · `step.progress` 문장 2건.

## 판정

**PASS** — ①~④ 전부 충족. 대상 결함 **0**.

## 잰 것 · 안 잰 것

- **잰 것**(E1): 위 6열 + 화면 3칸 + 대조군. 재현 = 스텁 기동 후
  `python tests/api/d24b_wording_drill.py --api <포트> --scenario <이름> --scenario-file <경로>`.
- 🔴 **안 잰 것**(「고쳐진 길」이 나머지를 증명하지 않는다 · D-12 계보):
  - **비스트리밍 경로**(단일 JSON 응답 · `Accept` 가 NDJSON 이 아닐 때) — 이 창은 NDJSON 스트리밍만 열었다.
  - **실 CLI·실 게이트웨이 프로세스** — 스텁이 게이트웨이 «역할»만 흉내냈다. 게이트웨이가
    `reasonCode` 를 **실제로 싣는지**는 `gateway.py` 코드 대조(E2)로만 봤다(`_validate_response`
    실패에 `code = code or "evidence_binding"`).
  - `live_synthesis_guard_drill.py`(#455 의 세 번째 파일) 자체 실행.
  - HTTPError(401 등) 경로 — D-24 에서 이미 닫았고 이번 창 밖이다.

## 🔴 내 계측기 자수 (1건)

드릴의 완주 판정을 `"completed" in type` 으로 썼다 — **`step.completed` 도 걸린다**. 그래서 1차
실행에서 3열이 `axis: null · eventCount 14` 로 나왔고 「대상이 합성에 못 갔다」로 보였다. 진범은
내 종료 조건이었다(`run.completed`/`run.failed` 로 고쳐 전열 재측 · 34 이벤트). 계보
「완주했다 ≠ 제대로 완주했다」·「이벤트 타입은 점 표기」의 재발.
