# 승격 26 — 외부 재검 (공개면)

- **검증 좌석** 리바이2 58대 · 발주 = 오케 외부 재검 「가」(11:56) · 상한 25분
- **대상** production 공개면 `https://factory-knowledge-twin.vercel.app` · main sha **`30274ba`** · Vercel deployment `dpl_78KwrwMEFprbhqrTXC7TFcQH7A5F`(target production · READY)
- **읽기 전용** — 공개면을 치는 것 외에 production 에 아무것도 쓰지 않았다. 구독 0.
- **측정 모델** 값은 대상의 것이다. 내 도구가 흔들린 자리는 자수 절에 적었다.

## 0. 기준선 — 자극 «전»에 찍어 두었다 (E1)

승격 착수 **전** `11:53:23` 공개면 실측:

```
/api/health -> 200 · "build":"99d3cab"
Server: Vercel · X-Vercel-Id: icn1::iad1::… · X-Matched-Path: /api/[...path]
```

🔴 **main sha `30274ba` ≠ 기준선 `99d3cab`** 였으므로, 재검에서 `30274ba` 가 보이면 그것은 **승격이 공개면에 닿은 결과**다. 기준선을 자극 전에 찍지 않았으면 같은 값을 보고도 「원래 그랬다」와 못 갈랐다.

## 1. 판정 — 10축 fail 0 (3회 연속)

| 축 | 실측 | 판정 |
|---|---|---|
| build sha (직접) | 200 · **`30274ba`** | **PASS** |
| build sha (`r.jina.ai` vantage A) | **`30274ba`** | **PASS** |
| build sha (`r.jina.ai` vantage B · no-cache) | **`30274ba`** | **PASS** |
| `/api/scenarios` 세션 없음 | **401** | **PASS** |
| D-87 `/openapi.json` | **404** · ai-api 층 | **PASS** |
| D-87 `/docs` | **404** · ai-api 층 | **PASS** |
| D-87 `/redoc` | **404** · ai-api 층 | **PASS** |
| D-87 `/api/docs` | **404** · ai-api 층 | **PASS** |
| 공개 화면 SSR | `/` **200** · testid **11** 렌더 | **PASS** |
| E-1·E-3 리셋 | 투어 키 **null** · 안내 카드 **1** | **PASS** |
| E-5 첫 화면 복귀 | **`/overview`** | **PASS** |
| E-2·E-4 투어(412 폐하 경로) | ring **top 194** · bottom 721 · h 527 · 제목 「다섯 단계가 실제로 지나간 자리」 | **PASS** |

- 전이 판정: **`99d3cab` → `30274ba`** — 승격이 공개면에 **닿았다**.
- 착지 근거: `Server: Vercel` · `X-Vercel-Id: icn1::iad1::…` · `X-Matched-Path: /api/[...path]`. (`remote_ip` 마커는 이 트리에 없다 — 기지 사항.)
- 투어 걸음 자취: `tour-next → tour-next → **tour-goto**`(index 2 가 link 걸음). 걸음 **제목**과 **링 높이 527** 로 정체를 함께 찍었다.

## 2. D-87 — 층이 갈린다 (E1 · 값의 뜻)

같은 네 경로가 **어떻게 묻느냐에 따라 다른 답**을 낸다.

| 묻는 방식 | `/openapi.json`·`/docs`·`/redoc` | `/api/docs` |
|---|---|---|
| 문서 네비게이션(curl · 주소창) | **307** — 셸 가드가 돌려보낸다 | 404 |
| 같은 오리진 `fetch`(세션 있음) | **404** — ai-api 층까지 가서 라우트가 없다 | 404 |

🔴 **두 값 모두 「표면을 내주지 않는다」이지만, 처방을 시험한 것은 404 열뿐이다.** 307 은 셸이 앞에서 막은 것이라 D-87(ai-api 라우트 부재)에 대해 아무 말도 하지 않는다. 판정문에 **차단 층을 이름으로** 적는다.

🔴 **자수 17-a**: 처음에 curl 값(307)만 보고 **판정선을 307 기대로 옮겼다**. 그러자 브라우저 열이 404 를 내어 「FAIL」이 3칸 났다 — **내가 옮긴 기준이 낸 빨강**이다. 두 도구가 갈릴 때는 「둘이 같은 것을 물었는가」부터 봐야 했다.

## 3. 자수

🔴 **자수 17-b — 고정 대기창이 칸을 흔들었다.** E-1·E-3 열이 4회 중 **2회 FAIL**(`tour=kept`)이었다. 대상의 비결정이 아니라 **내 2.8초 고정 대기**가 공개면 왕복보다 짧았던 것이다. 관측을 「결과를 기다리는」 방식(`waitForFunction`)으로 바꾸자 **3회 연속 PASS**. 🔴 흔들리는 칸을 그대로 회부했으면 **없는 결함**을 올릴 뻔했다.

🔴 **자수 17-c** — 공개면의 서빙 화면은 `/overview` 가 아니라 **`/`** 다(가드가 그리로 보낸다). 내 첫 기대가 틀려 FAIL 이 하나 났다.

## 4. 미측

- ⑤ `evidence.flagged` 배지 브라우저 실렌더 — 승격 26 판정문(#906)에 적힌 그대로 **미측**이다. 공개면에서도 표지가 붙는 run 을 만들 경로가 없어 이 재검에서도 닫지 못했다.

그물 = `tests/web/promo26_public_recheck.mjs`(공개면 5축 + 투어 4축 · 읽기 전용).
