# T7-42 게이트 — 2·4 실측 (리바이2 51대 · 2026-09-05)

> 대상 무대 = `_wt/develop-stage` · `:8020`(`fkt-dev-ai-api` / `fkt-ai-api:dev-76e0e80`) · `:8797`
> 호출 인자 = `develop-stage.ps1 <verb> -Worktree '<repo>/../_wt/develop-stage'`
> 🔴 **`-Worktree` 를 명시했다** — 워크트리 밖에서 부르면 `_wt/_wt/` 로 겹치는 결함(센쿠2 20:38 실측·수리 중)이 있어,
> 기본값에 기대지 않고 인자로 박았다. 이 값이 이 게이트의 재현 조건이다.
> **게이트 1·3 = 미측정(승격 19 허가 대기)** — production 재기동이 전제라 이 창에서 잴 수 없다. 칸을 비워 둔다.
> 구독 소비 **0**(합성 호출 0 · `/health` 만) · production 3면 **읽기만**.

## 게이트 2 — develop 스택 기동·멱등 = 🔶 **조건부 FAIL**(4/5 축 PASS · 1축 FAIL)

### 전제(내가 세운 판정선) — 성립

| 축 | 값 |
|---|---|
| T0 `origin/develop`(refresh #1 전) | `76e0e808c8a965ecee6c3532ebb2202c857cf175` |
| T1 `origin/develop`(refresh #2 후) | `76e0e808c8a965ecee6c3532ebb2202c857cf175` |
| **두 refresh 사이 tip 동일** | ✅ — 움직였으면 `exit 2` 였다 |

🔴 발주 시각 tip 은 `f3f3b0f` 였으나 착수 전 `76e0e80`(#769)으로 움직였다. **내 창의 tip 은 `76e0e80`** 이고
refresh 는 그 신판 스크립트를 썼다(오케 20:44 지시대로 「그 창의 tip 으로 잰다」).

### 축별 실측

| 축 | status A(refresh #1 후) | status B(refresh #2 후) | 판정 |
|---|---|---|---|
| `worktreeSha` / `originDevTip` / `inSync` | `76e0e80` / `76e0e80` / True | 동일 | ✅ |
| `apiBuild` | `76e0e80` | `76e0e80` | ✅ **불변** |
| `apiPid` | 27296 | 27296 | ✅ **불변** |
| 컨테이너 | `Up 7 seconds` | `Up 35 seconds` | ✅ **재생성 0**(나이가 자랐다 = 같은 컨테이너) |
| `promptSha256` | `a71c93b148db` | `a71c93b148db` | ✅ **불변** |
| 🔴 **`gatewayPid`** | **4824** | **12196** | ❌ **변함 — 멱등 아님** |

**부수 축**
- `:8020` `/api/live/status` → `{"online": true}` ✅
- `:8020` `/api/health` → `build = 76e0e80` = tip ✅ (자기 신고가 tip 과 일치)
- `:8797` `/health` → `promptSha256 a71c93b148db` · `promptPath` = `_wt/develop-stage/…/system_prompt.txt` ✅

### 🔴 회부 1 — `refresh` 가 같은 sha 에서도 게이트웨이를 «무조건» 재기동한다

같은 tip · 같은 프롬프트 해시인데 `gatewayPid` 가 **4824 → 12196**. 이미지·컨테이너는 멱등인데
게이트웨이만 아니다. 설계 문면(「멱등 · 같은 sha 면 재빌드 생략」)이 이미지 축만 말하고 게이트웨이 축을
말하지 않은 자리다. **처방 = `refresh` 가 「현 `:8797` 의 `promptSha256` 이 목표와 같고 리스너가 살아 있으면
재기동 생략」**. 지금 상태는 무해하지만, 라이브 조사 중 refresh 가 돌면 **진행 중 run 이 끊긴다.**

### 🔴 자수/정정 1 — 판정선의 `git show … | sha256` 대조는 Windows 에서 «구조적으로» 어긋난다

| 대상 | sha256[:12] |
|---|---|
| **워크트리 파일 바이트**(게이트웨이가 실제로 읽는 것) | **`a71c93b148db`** |
| `git show 76e0e80:services/synthesis-gateway/system_prompt.txt` (blob) | `96f700a9084f` |

블롭은 LF, 워크트리는 CRLF 라 **내용이 같아도 해시가 다르다.** 발주 판정선의
「`promptSha256` = `git show <tip>:… | sha256` 앞 12자」는 **이 리포에서 언제나 FAIL** 이 된다.
⇒ 정본 대조 대상은 **워크트리 파일 바이트**이고(`prompt_sha256` 이 `read_bytes()` 를 해싱한다),
그 축은 **일치**한다. 판정선 문면 수정을 회부한다.

## 게이트 4 — production 무접촉 = ✅ **PASS**(계측기 교정 «먼저» 세움)

### 교정 — 계수법이 실제로 +1 을 만드는가(대상 «밖»에서)

`:8797` 은 리다이렉트 없이 뜨므로 **셀 로그 파일이 없다**(`develop-stage.ps1:102` · 착수 전 코드로 확인).
그래서 명세의 「`:8797` 에 1회 호출로 교정」이 성립하지 않아, **내 게이트웨이 1본을 여분 포트에 리다이렉트와 함께**
띄워 계수법 자체를 증명했다(오케 판정 (나) · 구독 0 · production·develop 무접촉).

| 칸 | 실측 |
|---|---|
| 여분 포트 `:8799` 비었음 확인 → 기동(pid 28728) | 점유였으면 손대지 않고 중단 |
| `/health` **알려진 2회** 호출 | `.err` 줄 수 **1 → 3 = +2** ✅ |
| 로그 형식 | `127.0.0.1 - - [05/Sep/2026 20:44:10] "GET /health HTTP/1.1" 200 -` |
| 종료 | `:8799` 내림 확인 |

⇒ **`/health` 도 접근 줄을 남긴다**(`gateway.py` 는 `log_message`·`log_request` 를 재정의하지 않는다 —
`BaseHTTPRequestHandler` 기본). 계수법에 검출력이 있다.

### 본 측정 — 게이트 2 전 과정 동안 production 접촉

| 면 | 계수기 | 기준선 | 종료 후 | 판정 |
|---|---|---:|---:|---|
| `:8787` 게이트웨이 | `~/.fkt-gateway.log.err` 줄 수(읽기만) | **757** | **757** | ✅ 접촉 0 |
| 〃 | 파일 mtime | 20:13:52 | **20:13:52 불변** | ✅ |
| `:8010` ai-api | `docker logs --since 11:39Z fkt-deploy-ai-api` 줄 수 | — | **0** | ✅ 접촉 0 |
| 🔴 **대조** `:8020` dev | **같은 계수법** 같은 창 | — | **12** | ✅ **계수법 생존** |

마지막 줄이 이 게이트의 값이다 — `:8010` 의 **0** 은 같은 방법이 옆 컨테이너에서 **12** 를 세었기 때문에
「접촉 없음」이지, 「계측 수단 없음」이 아니다.

## 게이트 1·3 — 미측정 (값이 아니라 이름)

| 게이트 | 왜 못 쟀나 |
|---|---|
| **1** 결합 해소(`:8787` `promptPath` = 산출물 디렉터리 · 메인 체크아웃 변경에 불변) | **production 재기동이 전제** — 승격 19 허가 창 안에서만 가능 |
| **3** 자동 시작 무회귀(예약 작업 → `:8787` 200 · `promptPath` 산출물) | 〃 |

**0 으로 적지 않는다.** 허가 뒤 같은 계측기로 잰다.

## 판정 요약

| | |
|---|---|
| 게이트 2 | 🔶 **조건부 FAIL** — 5축 중 4 PASS · `gatewayPid` 멱등 FAIL(회부 1) |
| 게이트 4 | ✅ **PASS** — 두 면 모두 **교정된** 계수기로 0 |
| 게이트 1·3 | ⬜ **미측정**(승격 19 허가 대기) |
| 판정선 정정 | 🔴 `git show … \| sha256` 대조는 CRLF 로 구조적 불일치 — 워크트리 바이트가 정본 |
| 구독 소비 | **0** · production 3면 읽기만 · 무대 재기동은 `refresh` 2회(발주 범위) |

---

# 게이트 2 재측 — #772(`57efd0b`) 뒤 (리바이2 52대 · 2026-09-05)

> 무대 = `_wt/develop-stage` · `:8020`(`fkt-dev-ai-api` / `fkt-ai-api:dev-57efd0b`) · `:8797`
> 호출 인자 = `develop-stage.ps1 <verb> -Worktree '<repo>/../_wt/develop-stage'`(명시 · 기본값에 안 기댄다)
> 실행한 스크립트 = **lane `levi2-t742r` @ `57efd0b`**(#772 처방본). 구독 소비 **0**(`/health` 만 · 합성 호출 0).
> 내가 이 창에서 발신한 요청은 `:8020`·`:8797` 뿐이다 — production 3면(`:8010`·`:8787`·공개면) 호출 **0**.

## 판정 = ✅ **PASS** (ⓐ 멱등 · ⓑ 살아 있음 2열 모두)

### 전제 — 성립

| 축 | 값 |
|---|---|
| `originDevTip`(PRE-A·AFTER-A·AFTER-B·AFTER-C 전부) | `57efd0b` — **창 내내 불변**(움직였으면 `exit 2` 였다) |
| `worktreeSha` / `inSync` | `57efd0b` / True |

### 창 진입 «전» — 트리 이동 refresh (정당 · 판정 열 아님)

21:07:01 내 `refresh` 가 무대 트리를 `f990a2d` → `57efd0b` 로 옮겼다. 그 refresh 는 **sha 이동 조건**으로
게이트웨이를 재기동했고(pid 32544 → 30956) 컨테이너도 새 태그로 재생성했다(`df45b79c33b3` → `19d6cd98a606`).
🔴 **정당한 재기동이다**(#772 조건 ④ — `gateway.py` 는 재독되지 않는다). 그래서 ⓐ 는 **그 뒤 두 refresh 사이**로 잡았다.

### ⓐ 멱등 — 같은 tip 연속 `refresh`

| 축 | PRE-A `21:18:58` | AFTER-A `21:19:06` | AFTER-B `21:19:38` | 판정 |
|---|---|---|---|---|
| **`gatewayPid`** | **30956** | **30956** | **30956** | ✅ **불변 — 51대 회부 1 해소** |
| 컨테이너 `.Id` | `19d6cd98a606` | 동일 | 동일 | ✅ **재생성 0** |
| 컨테이너 `.State.StartedAt` | `12:07:08Z` | 동일 | 동일 | ✅ **재기동 0** |
| `apiBuild` | `57efd0b` | 동일 | 동일 | ✅ |
| `promptSha256` | `a71c93b148db` | 동일 | 동일 | ✅ |
| `gateway.log` 크기/mtime | 380B `21:07:09` | 동일 | 동일 | ✅ **손 안 댔다** |

`refresh` rc=0 · 소요 A 5s(21:19:01→06) · B 5s(21:19:31→36).

### ⓑ 살아 있음 — 리스너를 죽이면 다시 세운다

**자극**(21:20:1x~21:20:36) — ① `:8797` 리스너의 **명령줄 귀속 확인**(`python.exe -u …\_wt\develop-stage\services\synthesis-gateway\gateway.py`) 뒤 pid 30956 만 `Stop-Process`(대상 한정) → `21:20:15` **NO LISTENER 실측** ② `~/.fkt/dev/gateway.log`·`gateway.log.err` 를 `.preb-212015` 로 **rename 해 치움**(부재 실측) — 「생성」을 진짜로 묻기 위해서다.

| 축 | 자극 후 `21:20:36` | `refresh` 후 AFTER-C `21:20:56` | 판정 |
|---|---|---|---|
| **`gatewayPid`** | **(리스너 없음)** | **34932** | ✅ **새 값 — 재기동했다** |
| `gateway.log` | **부재** | 생성 `21:20:53` → 380B(부팅 배너 6줄) | ✅ **새로 생성** |
| `gateway.log.err` | **부재** | 생성 → 접근 로그 | ✅ |
| 컨테이너 `.Id`/`.StartedAt` | `19d6cd98a606` / `12:07:08Z` | 동일 | ✅ **게이트웨이만 복구**(컨테이너 무접촉) |
| `:8797 /health` | — | 200 · `promptPath` = 그 트리 · `promptSha256 a71c93b148db` | ✅ |

`refresh` rc=0 · 소요 6s(21:20:49→55). 배너 6줄 중 뒤 4줄은 python 쪽 출력 — #772 의 `python -u` 가 실렸다는 거동 증거다.

## 판정선 정정 2건 (52대 · 착수 «전» 회부 · 오케 채택 21:00)

① 🔴 **`apiPid` 열 폐기.** 27296 은 컨테이너가 아니라 `:8020` **포트 소유자 = `wslrelay.exe`**(Docker 퍼블리시 relay)다.
   컨테이너가 `df45b79c33b3` → `19d6cd98a606` 로 **재생성된 21:07:08 전후에도 27296 그대로**였다 — 즉 51대 표의
   「`apiPid` 불변 ✅」는 **어떤 처방이든 통과하는 초록**이었다. 대체 축 = `docker inspect .Id + .State.StartedAt`.
② 🔴 **ⓑ 「로그 생성」은 자극 전에 이미 참이었다.** 두 파일이 이미 있었고, 게다가 `.err` 는 접근 로그라
   **내 `/health` 프로브가 그 파일에 적힌다**(20:57·21:19 = 내 손 · 측정 면에 내 출력이 섞인 자리).
   ⇒ rename 으로 치우고 「새로 만들어지는가」를 물었다.

## 🔴 자수 3 (전부 내 계측기 · 대상 결함 아님)

1. **`| tail` 파이프가 명령을 10분 붙잡았다.** refresh #1(21:07:01)의 stdout 을 `tail` 로 받았더니 **refresh 가 띄운
   게이트웨이 자식이 그 파이프를 상속**해 EOF 가 안 왔다 — 21:17 에 배경 전환될 때까지 「안 끝난 명령」이었다.
   refresh 자체는 21:07:1x 에 이미 끝나 있었다(컨테이너 `startedAt 12:07:08Z` · gw 30956 실측이 증거).
   🔴 그 사이 무대는 **내 미완 명령이 만든 상태**였는데 21:07:20 에 그것이 «독립 상태»로 읽혀 발주 계획이 고쳐졌다.
   ⇒ 이후 전량 **파일 리다이렉트**로 바꿨다. 공유 무대에서 파이프로 받은 출력은 「끝났는지」도 못 말한다.
2. **rename 이 실패했는데 성공 문면을 찍었다.** `Rename-Item` 이 「파일이 사용 중」(게이트웨이가 핸들 보유)으로 던졌는데
   다음 줄의 `moved …` 문자열이 그대로 나갔다 — **성공 여부에 안 걸린 문면**. kill 뒤 다시 옮겨 listing 으로 실증했다.
3. **AFTER-C 프로브가 3초 일렀다.** `gateway.log` 를 **0B** 로 읽었으나 21:21:15 재확인 **380B**(배너 6줄).
   그대로 냈으면 「`-u` 인데 기동 줄 0줄」이라는 **없는 결함**을 회부할 뻔했다. 부팅 직후 크기는 판정에 쓰지 않는다.

## 재측 요약

| | |
|---|---|
| 게이트 2 | ✅ **PASS**(ⓐ 멱등 · ⓑ 살아 있음) — 51대 🔶 조건부 FAIL 해소 |
| 처방 | #772 `0fb76a3` — 「목표 상태 + 트리 불변이면 재기동 생략 · sha 이동/리스너 사망이면 재기동」 |
| 게이트 4 | 재측 없음(이 창 범위 밖) · 게이트 1·3 ⬜ **미측정**(승격 19 허가 대기) |
| 남긴 것 | `~/.fkt/dev/gateway.log{,.err}.preb-212015` = ⓑ 자극 전 로그(내 산출물 · 삭제해도 무방) |
| 구독 소비 | **0** · production 3면 호출 **0** |

---

# 게이트 1·3 — 승격 19 창에서 측정 (리바이2 52대 · 2026-09-05)

> 51대가 「⬜ 미측정(승격 허가 대기)」으로 이름만 남긴 두 칸을, 폐하 승인 22:19 창에서 실측했다.
> 무대 = **production** main `4d39bde` · 판정문 원본 = `evidence/promo19-external-recheck.md`(§3·§4).

## 게이트 1 — production 게이트웨이가 «산출물»을 읽는가 = ✅ **PASS(양면)**

| 면 | 자극 | 기대 | 실측 |
|---|---|---|---|
| **A** | 메인 체크아웃 `system_prompt.txt` 1줄 추가(파일 `db50e21a28b4`) | 서빙값 **불변** | `promptSha256 a71c93b148db` **불변** ✅ |
| **B** | 산출물 `~/.fkt/prod/gateway/system_prompt.txt` 1줄 추가(`12732000e57f`) | 서빙값 **변함** | `promptSha256 12732000e57f` **변함** ✅ |
| 원복 | 두 면 모두 | 기준선 복귀 | 파일·서빙 모두 `a71c93b148db` · B면 byte-identical ✅ |

🔴 **B면이 이 게이트의 계측기 생존 열이다.** A면만 재면 「불변」이 격리의 증거인지
「내가 아무것도 안 흔들었다」인지 갈리지 않는다. 자극은 A면=오케 / B면=검증으로 손을 갈랐다
(불변을 주장하는 면의 자극과 판정을 같은 손이 쥐지 않는다).

## 게이트 3 — 예약 작업이 산출물로 되살리는가 = ✅ **PASS + 경로 차이 2건**

`switch.ps1 off`(pid 45576 종료) → 예약 작업 → **pid 11332** · 명령줄 = `python -u ~/.fkt/prod/gateway/gateway.py`
· 토큰 동반 `/health` **200** · `promptPath`·`promptSha256` = 산출물 판. 조밀 표본 5회(22:27:47~22:28:07) 동안 pid 고정
— 「지나가는 상태」가 아니라 「끝내 그 상태」다. 공백 ≈12초(오케 로그).

🔴 **두 기동 경로가 같은 상태를 만들지 않는다** — `effort`: promote/`switch` 판 `low` vs autostart 판 **`medium`**
(폐하 결정 = opus/**low**) · `/health` 무인증 응답: promote 판 **200** vs autostart 판 **401**.
⇒ 회부(O-40 후보 = effort 기본값 한 곳으로) · **못 잰 것** = 합성 엔드포인트의 무인증 여부(재지 않았다).

## 남은 칸

게이트 2·4 = 앞 절에서 PASS. **게이트 1·3 = 이 절에서 PASS.** ⇒ T7-42 게이트 4칸 전부 실측 완료.
