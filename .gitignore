# secrets / credentials — never commit (baseline §34.6)
.env
.env.*
# 예시 파일은 키 목록만·값 0 이므로 커밋 대상 (baseline §34.6)
!.env.example
*.pem
*.key
credentials*
secrets*

# python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/
*.egg-info/

# node / next.js
node_modules/
.next/
out/
.vercel/

# db volumes / model caches (baseline §34.6)
volumes/
*.dump
models/
.cache/

# workspace scratch / 세션 내부 파일 (공개 비대상)
.workspace/scratch/
.claude/

# os / editor
.DS_Store
Thumbs.db

# T1-0 — 로컬 인프라 볼륨(리포 미포함) · D-1 VOLUME_ROOT 파라미터화 대응(D-3): 변형 경로 전부 차단
.volumes*/

# T1-0 — python venv
.venv/
__pycache__/

# tests/graph/graph_verify.py 가 GRAPH_DUMP_DIR 미지정 시 cwd 에 떨구는 덤프(러너는 임시 폴더를 준다)
graph-dump.txt

# O-21 — tests/web 는 npm 층(package-lock.json 추적) · pnpm 이 만든 lockfile 은 산출물
tests/web/pnpm-lock.yaml

# 답변 축 채점기 --gate-only 무인자 scratch(#780 · 매번 재생성 · 정본 아님)
benchmarks/eval-answer-gate-latest.json
