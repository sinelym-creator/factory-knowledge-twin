# T6-5 가이드 투어(게임식 튜토리얼) 규격서 v1.0

> 하명 09-03 13:45~13:47 「게임 튜토리얼처럼 · 초기에 사용 여부 물어보고 · 차근차근 메커니즘 설명 · OFF = 평상시 직접 조작 · 다음에 누를 곳을 동적 효과로 유도」. 리서치 = `.workspace/`(스자쿠 26대 서브에이전트 · 출처 16 · E2/E3 병기 · 요지는 §1). 대상 `apps/web-console`. 새 의존성 0 · CSS 전용 모션 · 서버 무접촉 · 상태 = 브라우저. T6-4 토큰·유틸(`--fkt-*` · `.fkt-*`) 위에 얹는다.
> 🔴 **무변경**: 기존 `data-testid` · 문구 · role/aria · DOM 순서 · contracts · 서버 · `tests/**`. 투어 OFF 일 때 화면·동작 변화 **0**(폐하 13:46).

## ① 원칙(리서치 요지 → 규칙)

| # | 원칙 | 규칙 |
|---|---|---|
| 1 | 보여 주고 시킨다(show, don't tell) | 슬라이드 0 · 매 스텝 = 실제 요소 1개를 «실제로» 누르게 한다(E2 HIG·NN/g) |
| 2 | 한 스텝 = 한 개념 | 스포트라이트 1개 · 주 동작 1개 · 설명 ≤2줄(「무엇을 했나 / 왜」) |
| 3 | 막지 않는다 | 첫 방문 = 모달이 아니라 **초대 카드**(우하단 · 배경 조작 가능) · Esc 로 언제든 종료 · 앱바 `?` 로 다시 보기 |
| 4 | 안전한 샌드박스 | 투어는 **REPLAY**(녹화 재생)로만 돈다 — 실패·지연·구독 소모 0 · 스텝 동기화 결정적 · LIVE 는 마지막 스텝 「이제 직접 돌려보기」로 넘긴다 |
| 5 | 막힐 때만 돕는다 | 스텝 대상에 8초 무반응 → 콜아웃 강조(pulse 2회 재생) · 그 전엔 조용히 |
| 6 | 판단하지 않는다 | 「건너뛰기」에 죄책감 문구 0 · 완료·건너뜀 기억 · 진행률 점 표시 · 완료 축하 1회 짧게 |
| 7 | 측정-주장 경계(§0.2) | 첫 스텝 1줄 「이 재생은 synthetic 데이터입니다」 · 수치는 화면이 보여 주는 실값만 인용 |

## ② 흐름(대표 시나리오 1개 = GS-01 · 5화면 순회 금지)

| 스텝 | 화면 · 대상(`data-testid`) | 사용자가 하는 것 | 메커니즘 설명(≤2줄) |
|---|---|---|---|
| 0 | overview · 초대 카드(신규 `tour-invite`) | 「둘러보기 시작」/「나중에」/「다시 보지 않기」 | — |
| 1 | overview · `headline` | 읽기 → 「다음」 | 「가장 심각한 활성 알람 1건을 서버가 골라 문장으로 만든다 — 순위는 서버, 화면은 첫 줄만」 |
| 2 | overview · `alarm-card` | 알람 카드 보기 → 「다음」 | 「임계 6.3 → 관측 6.30 — 알람은 센서 임계표가 아니라 «울린 행»의 값이 앵커다」 |
| 3 | overview · `start-from-alarm` | **직접 클릭**(REPLAY 로 시작) | 「조사 시작 = 에이전트가 5단계(검색→순위→합성→근거→작업지시 초안)를 돈다 · 지금은 녹화 재생」 |
| 4 | incident · 타임라인 단계 카드 | 단계가 «실제로 도착할 때» 옆에 힌트(just-in-time) | 검색: 「문서·센서·정비 이력을 온톨로지 관계로 끌어온다」 / 순위: 「결정적 규칙으로 후보를 세운다」 / 합성: 「LIVE 면 LLM 이 근거를 인용해 문장을 쓴다 · 인용 없는 문장은 전량 거부」 |
| 5 | incident · 후보 카드 → 근거 링크 | **직접 클릭** | 「후보마다 근거 ID 가 붙는다 — 화면의 모든 주장은 근거로 돌아갈 수 있다」 |
| 6 | evidence · `trust-header` | 읽기 → 「다음」 | 「출처·시각·신선도(STALE) 표시 — 오래된 문서는 그렇다고 말한다」 |
| 7 | work-order · 초안 | 읽기 → 「다음」 | 「초안은 사람이 승인해야 효력 — AI 는 제안까지」 |
| 8 | 완료 카드 | 「LIVE 로 직접 해보기」/「닫기」 | 축하 1줄 · 재생 = 앱바 `?` |

compare 화면은 투어에서 제외 · 첫 진입 시 컨텍스트 힌트 1개(「두 run 을 나란히 — 같은 입력, 다른 축」).

## ③ 컴포넌트(신규 · `apps/web-console/components/tour/`)

| 파일 | 역할 |
|---|---|
| `tour-provider.tsx` | 상태(`localStorage` `fkt.tour.v1` = `{v:1, status:'never'\|'active'\|'done'\|'skipped', step:n}`) · 스텝 정의 · `?tour=1` 재개 · Esc 종료 · 포커스 복귀 |
| `tour-invite.tsx` | 초대 카드(우하단 · `fkt-card fkt-rise` · 버튼 3) · `status==='never'` 이고 overview 일 때만 |
| `tour-spotlight.tsx` | 대상 요소 `getBoundingClientRect` → 딤(`box-shadow: 0 0 0 9999px var(--fkt-scrim)` · `pointer-events:none` · `aria-hidden`) + **틴트 링**(`outline 2px var(--fkt-tint)` + `.fkt-pulse` 링 1겹) · 대상은 실제 조작 가능(딤 위에 구멍) |
| `tour-callout.tsx` | 콜아웃(`fkt-card fkt-sheet` · 화살표 · 제목 1줄 + 설명 ≤2줄 + 진행 점 n/8 + 「다음/건너뛰기」) · `role="region" aria-label="가이드 투어"` · `aria-live="polite"` · 대상 옆 자동 배치(위/아래/좌/우 · 뷰포트 안) |
| `tour-steps.ts` | 스텝 표(②) — 대상 testid · 진행 조건(`click`\|`next`\|`event`) · 문구 |

셸 접점 = `app-shell.tsx` 에 `<TourProvider>` 1줄 + 앱바 `?` 링크가 `/overview?intro=1&tour=1` 로도 열림(안내 카드는 그대로 · 투어는 카드 안 「둘러보기」 버튼으로도 시작). 그 외 기존 컴포넌트 무변경(대상은 testid 로 찾는다).

## ④ 동적 효과(폐하 13:47 「다음 순서 유도」)

| # | 효과 | 구현 |
|---|---|---|
| 1 | 스포트라이트 이동 | 링·딤 구멍이 이전 대상 → 새 대상으로 `transform`/`inset` 전이 `--fkt-dur-3` spring |
| 2 | 대상 링 pulse | `.fkt-pulse` 변형(scale 1→1.06 · opacity) 1.6s · 무반응 8초 뒤 2회 강조 |
| 3 | 콜아웃 등장 | `fkt-sheet`(scale .96→1) · 화살표는 대상 방향 |
| 4 | 진행 점 | 8점 · 현재 점 `fkt-pop` · 완료 점 틴트 채움 |
| 5 | 완료 | 완료 카드 `fkt-rise` + 체크 아이콘 stroke-dashoffset 1회(0.6s) |
| 6 | reduced-motion | 전역 규칙이 전부 0 → pulse 대신 **정지 링 3px + 굵은 테두리** · 정보 손실 0 |

## ⑤ 접근성·경계

- 키보드: Tab 순회 · Enter = 다음 · Esc = 종료(포커스 = `?` 링크로 복귀) · 스텝 열릴 때 포커스 = 콜아웃 제목.
- 스크린리더: 콜아웃 `aria-live="polite"` · 딤·링 `aria-hidden`.
- 모바일(⑧): 콜아웃 = <md 바텀 시트 · 대상 `scrollIntoView({block:'center'})` · 터치 타깃 ≥44.
- 공개 경계(§15.2): 문구에 내부 예외명·헤더명·경로 0 · synthetic 명시.
- OFF 상태: 렌더 0(`status!=='active'` 이면 Provider 가 null · 초대 카드만 조건부).

## ⑥ 검증 축(리바이2)

| 축 | 기준 |
|---|---|
| OFF 무영향 | 투어 OFF 에서 e2e 133/0/3 그대로 · DOM diff 0(스냅샷) |
| 흐름 | 스텝 0→8 완주(REPLAY) · 각 스텝 대상 실재 · 「직접 클릭」 스텝은 클릭 없이는 진행 안 됨 · Esc 종료 · 재개(`step` 기억) · 완료·건너뜀 뒤 초대 카드 0 |
| 키보드 | Tab/Enter/Esc 만으로 완주 · 포커스 복귀 |
| 모션 | reduced on/off 스크린샷 · 정지 링 존재 |
| 모바일 | 390 바텀 시트 · 가로 스크롤 0 |
| 경계 | 문구 스캔(예외명·헤더명·경로 0) |

## ⑦ PR 분할(스자쿠 직접 · 폐하 13:43)

| PR | 내용 | 완료 조건 |
|---|---|---|
| 1 | Provider + 상태 + 초대 카드 + 스포트라이트/콜아웃 골격 + 스텝 0~3(overview) | OFF 무영향 · 스텝 3 클릭 진행 · Esc |
| 2 | 스텝 4~8(incident just-in-time · evidence · WO · 완료) + compare 힌트 | 완주 · 재개 · 키보드 |
| 3 | 모션 마감(④) + 모바일 + reduced + 검증 수정 | ⑥ 전 축 |

순서 = T6-4 PR 2b 뒤(같은 셸 파일 접점) · 상한 09-04.
