---
ticket: T2-3
title: LangGraph 조사 워크플로우 — plan 5단계 · GS-01 대본 결속 · step 이벤트
owner: 구현
status: 발주(08-30 01:52 · 센쿠2 10대 — 발주문 = 팀 채널 1543302218277593139)
due: 독립 검증 PASS 시 완결
---

# T2-3 — LangGraph 조사 워크플로우

## 목표

조사 실행의 5단계 plan(`structured → vector → graph → synthesize → draft_work_order`)을 LangGraph로 세우고, GS-01 대본에 결속된 실행 실물 + **step 이벤트 스트림(agent-events 스키마)**을 낸다. 🔴 **Claude 구독 공개 API 노출 금지(§15.2)** — synthesize의 LLM 호출은 «로컬 실행 경계» 안에서만이다.

## 단계 게이트 (순서 고정)

1. **계약·스키마 실독 보고 선행** — agent-events 스키마(`packages/contracts/`) + runs 표면(`POST /scenarios/{id}/runs` · `GET /runs/{runId}` · `/events` · `POST /runs/{runId}/stop` · `WS /ws/runs/{runId}`) 실독 → 🔴 «이 티켓이 해제하는 라우트 범위»를 보고로 올리고 판정 받는다(runs 표면 전부인지, WS는 이월인지 — 오케 판정 사안). `/graph/paths` 귀속 재판정(Q-18)도 이 게이트에서.
2. **LLM 없는 3단계 먼저** — structured·vector·graph 노드는 T2-1 retrieval 경로 재사용(새 검색 경로 금지 — 있으면 회부). 단계별 이벤트 산출이 완료 증거.
3. **synthesize = Claude 로컬 호출 경계** — 🔴 자격 증명이 서버 표면·응답·로그·이벤트에 노출 0 · 공개 API 경로에서 synthesize 트리거가 «구독 프록시»가 되지 않는 구조(운영자 로컬 실행/사전 녹화 축과의 관계를 게이트 1 보고에 소견으로) · live 불가 시 replay 강등은 계약 성문분.
4. **draft_work_order** — 초안 «생성»까지만(승인·CRUD 표면 = T2-5). R12 REQUIRES(안전 조치) 필드가 초안에 실리는 것까지 — 서버 강제는 T2-5.
5. **GS-01 대본 결속 실행 실물** — 대본 단계↔step 이벤트 대응 + 근거 evidenceId가 T2-2 표면으로 해석 가능함을 실증. 🔴 Q-9 결속: 착지 시 실물 rank2 후보를 보고(화면·대본 재바인딩은 오케 패스).

## AC (발주 시 확정 — 선작성 초안)

- [ ] 계약 표면 green · 해제 범위 = 게이트 1 판정분과 1:1 · 계약 밖 경로 0.
- [ ] 5단계 각 노드의 step 이벤트가 agent-events 스키마 준수(seq 단조 증가 · 검증 좌석이 독립 파싱 가능).
- [ ] GS-01 실행 실물 — 대본 기대 단계 전건 + 근거 evidenceId 전건이 kind doc-chunk|record로 해석됨(E1).
- [ ] 🔴 Claude 자격 증명·내부 경로 노출 0(응답·이벤트·로그 전수) · SQL/Cypher 문자열 조립 0 · blocking 0(LLM 호출은 비동기 경계 — 실측 병기).
- [ ] 폴백 침묵 금지 — 단계 실패는 이벤트로 «보이게» 실패(다른 전략 결과로 조용히 채우지 않는다 · T2-1 계보).

## 판정 append (08-30 게이트 1 통과 — J-1~J-6 · 회신 = 팀 채널 1543308067666395307)

- **J-1 해제 범위** = runs 표면 5건 전부(`POST /scenarios/{id}/runs` · `GET /runs/{runId}` · `/events` · `stop` · `WS /ws/runs/{runId}`) — stop은 실행기 성질·WS는 이벤트 로그 fan-out. `/incidents`·work-orders = 범위 밖(조회 계층 귀속 = 후속 발주 시 판정). `/live/status online` 의미 = **(b) 로컬 synthesize 게이트웨이 도달 가능**(계약 v0.1.2 append 성문 — 공개 Sandbox false = 참).
- **J-2** `/graph/paths` = **T2-3 해제 확정**(Q-18 갱신) — S5 기대 evidence(graph path 시각화)를 푸는 라우트 강제 · 고정 template 조회만 · Cypher 조립 0 · graph-path evidenceId 소비처 = `?byRun`.
- **J-3 저장소** = 프로세스 내 세션 스코프 · **SSOT 쓰기 0** — 재기동 소실은 성문된 대가(내구 축 = T2-4 replay fixture). incidentId = seed `INC-2026-014` 참조(쓰기 0).
- **J-4 structured** = (b) 별도 읽기 전용 온톨로지 조회(compare 무접촉 · 출력 바이트 동일) — 상한 = 종류별 + 대본 기대(MR-2025-0087 = 최신순 3번째) 포섭 실측 근거 병기.
- **J-5** = 두 구현 분리(live 노드는 게이트 env 부재 시 미등록 — 구조적 프록시 차단) + 🔴 egress 차단 부팅 assert(LANGSMITH/LANGCHAIN_TRACING 강제 false).
- **J-6** = langgraph 1.2.11 승인(+websockets 16.1.1 강등 · requirements 고정값 변경 승인) — 조건: 공유 venv 설치 직전 채널 신호 + 설치 직후 WS 실측·contract_surface 재확인. WinError 4551 사정거리 축소 성문: «scipy/sklearn/torch 계열의 사실»(langgraph 계열 8건 import OK 실측 E1).

## 컨텍스트 (이것만)

- `packages/contracts/`(agent-events·runs 표면) · `docs/product/golden-scenario-spec.md`(대본) · T2-1 retrieval(재사용) · T2-2 evidence 표면(해석 대상) · 원장 Q-9(rank2 재바인딩)·Q-18(/graph/paths).
- 스택 규율·lane 규율·멘션 규율 = T2-1·T2-2와 동일. lane: `lane/senku2-t2-3`.
