---
ticket: T1-4
title: ingestion·chunk·임베딩·pgvector 색인
owner: 구현
status: 발주(내부 게이트 = chunk 정책 실측 → 오케 동결 후 빌드)
due: 독립 검증 PASS 시 완결
---

# T1-4 — ingestion·chunk·임베딩·pgvector 색인

## 목표

T1-2 seed·T1-3 문서 셋을 검색 가능한 색인으로 세운다 — **seed→색인 재생성 멱등**(원장 AC). 🔴 chunk 정책은 «실측이 먼저»다 — **정책이 앵커를 정한다**(전임 실측: 같은 문서가 고정 400tok 10개 ↔ 절경계 400tok 14개, 2배 진동). 분량을 숫자에 역산하지 않는다.

## 단계 게이트 (순서 고정)

1. **chunk 정책 실측** — T0-6 §8 Q2(400~600 token · 고정/절경계) 후보안별 실측: chunk 수 · 경계 품질 · 🔴 GS-01 S4·S7 기대 인용 문장의 chunk 내 온전성(절단 여부). 실측표를 «보고»로 올린다.
2. 🔴 **오케 동결 게이트** — 실측표 판정 후 `chunking_policy_version=1` 동결(§8 Q2 처리 원문 — 스펙 반영은 오케 scope). **동결 전 색인 빌드 금지.**
3. **임베딩 실측 택일** — sentence-transformers vs fastembed: 이 환경(win · py3.14) 설치 가능성 · 색인 시간 · 차원 실측 → 근거 병기 택일. 「최신이라서」는 근거가 아니다 — «설치되는가·재현되는가»다(asyncpg 0.31.0 선례).
4. **색인 빌드** — pgvector 적재(T1-1 스키마) · seed→색인 재생성 멱등.
5. **앵커 실값 «보고»** — wireframes #014·#009·#007·#003이 요구하는 앵커의 실값 목록을 보고로 올린다. 🔴 wireframes.md 수정은 docs = 오케 scope — 직접 수정 금지, 실값 바인딩은 오케가 집행.

## AC

- [ ] chunk 정책 실측표(후보안별 · GS-01 인용 온전성 포함) + **오케 동결 확인 후** 빌드 — 순서 위반 시 FAIL.
- [ ] seed→색인 재생성 멱등 — 연속 2회 재생성 diff 0 실측(E1).
- [ ] GS-01 S4·S7 기대 인용 문장이 각각 «한 chunk 안에» 온전 실재 — chunk 좌표 병기(E1).
- [ ] 임베딩 택일 근거(설치·시간·차원 실측) 병기 · 새 의존 추가는 이유 명시.
- [ ] 토큰 환산 명시: 전임의 1tok≈1.3자는 E3다 — 실 토크나이저 도입 시 «재실측 값» 병기(정책 동결값과 어긋나면 즉시 보고).
- [ ] 문서를 손대는 경우 `python -m data.generators.generate` **선실행**(인용 9건 · D-2 4축 · SOP 절 구조 · MR 연도 그물이 매 실행 대조).

## 범위 밖 (명시)

- `/api/evidence`·`/api/documents` 501 해제 = **본 티켓 범위 밖**(후속 발주) — `services/ai-api/` 라우트를 손대지 않는다. 라우트를 만지게 되는 상황이 오면 착수 전 오케 선확인(`python -m tools.contract_surface` 대조 — 계약 밖 경로 exit 1).
- 계약(`packages/contracts/` 동결)·docs 수정 = 오케 scope.

## 컨텍스트 (이것만)

- `docs/product/data-ontology-spec.md` §3.3·§8 Q2(🔴 v0.1 동결 — 본문 무수정 · 정오표 append only) · `data/generators/`·`data/documents/`(T1-2·T1-3 착지분) · T1-1 스키마(pgvector) · `docs/product/dev-environment.md`(스택 기동·격리).
- lane: worktree `lane/senku2-t1-4` · PR 1건 · 경로 지정 add만 · ctx% 병기.
