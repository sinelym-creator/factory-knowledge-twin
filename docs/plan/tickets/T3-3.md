---
ticket: T3-3
title: Evidence·Documents 화면 — 근거 열람·STALE 배지 표면·딥링크
owner: 구현
status: 발주(08-30 18:43 · 센쿠2 14대 · lane/senku2-t3-3) → 🔴 FAIL 유지 마감(08-31 · 구현 PR#167 착지 · 검증 #172·#175 · D-3 = Q-39) → Q-39 ⓒ 재발주(08-31 09:34 · 센쿠2 15대 · lane/senku2-q39-client-entry)
due: 독립 검증 PASS 시 완결
---

# T3-3 — Evidence·Documents 화면 (wireframes ③)

## 목표

근거 열람 화면을 실데이터로: evidence 상세(kind별 — doc-chunk 인용 강조 offset · record) · document 뷰(revision·hash·approval_state·STALE 배지 — Q-20의 소비처가 «화면에» 선다) · 딥링크(세션 밖 읽기 전용 — v0.1.6 예외 2라우트의 화면 실체 · Q-16 해소 축).

## 게이트

1. **정본 대조 선행** — wireframes §3(Evidence) ↔ 계약 `/evidence/{id}`·`/documents/{id}` 형상(T2-2 착지분 · v0.1.1 append). 🔴 «화면 데이터 요구 ↔ 라우트» 대응표 중간 보고 1회(T3-2 표준 — 「어느 화면 요소가 먹는가」 빈 칸 = 해제·확장 대상 아님). 신규 라우트 해제 소요는 대응표가 판정 — 예상은 0(읽기 2라우트 기해제).
2. **화면** — evidence 상세(인용 문장 강조 = offset 실값 · 원문 대조 가능 형태) · document 뷰(STALE/UNVERIFIED 배지 = `stale` boolean — Q-22 보수 매핑 성문 그대로) · 🔴 딥링크 경로 = 무세션 «열람만»(v0.1.6 예외 — 세션 화면과 딥링크 화면의 경계 명시) · 키보드 내비 기본.
3. **자기 실측 표** — 🔴 «브라우저 네트워크 축» 상설 행(쿠키 유/무 + 딥링크 무세션 200 대조) · SSR/브라우저 분리 실측 · 드릴 회귀(FKT_PYTHON 규율 · 서버 재기동 후).

## AC

- [ ] evidence kind 2종(doc-chunk·record) 화면 렌더 + 인용 강조 = `body[start:end]` 실값(원문 일치 — S6 계보)
- [ ] document 뷰: revision·hash·approvalState·effective 기간 + STALE 배지(freshness != FRESH)
- [ ] 🔴 딥링크: 무세션 «읽기 전용» 열람 성립(graph-path kind 404 = Q-34 성문 그대로 — red 아님) · 세션 화면으로의 전환 동선
- [ ] 브라우저 네트워크 축: 세션 화면 = 쿠키 200/무쿠키 401 · 딥링크 = 무쿠키 200(예외 2라우트만)
- [ ] SSOT 무접촉(읽기 전용) · blocking 0 · LLM 0 · 가드 예외 무확대
- [ ] tests/** 무접촉 — 깨짐·필요 그물은 회부 · 계약 갈림 = 회부(코드 확장 금지)

## 컨텍스트 (이것만)

- wireframes §3·§6 · 계약 v0.1 + append 전건(v0.1.1 형상 · v0.1.6 예외 · 갈림 시 append 우선) · Q-16(예외 설계 배경)·Q-20(STALE 표면)·Q-22(배지 보수 매핑)·Q-34(kind 404).
- 자리 = `_wt/senku2-t3-3` 새 worktree · lane/senku2-t3-3 · 기점 = origin/develop a82a41a+ · 스택 fkt-senku2-q3 실측부터(전임 존치 서버 «믿지 말고») · 워크트리 pnpm install(Turbopack junction 함정 — 전임 아픈 자리 ③).

## Q-39 ⓒ 재발주 (08-31 09:34 · 센쿠2 15대 · `lane/senku2-q39-client-entry` @ 9949a68)

- **처방 ⓒ** = 「입장은 클라이언트가 실행하는 일」(표지 무의존 — 표지 3안 전건 실측 기각 08-31 03:53 · 원장 Q-39). 층 이동: ① `proxy.ts` = 가드만(무세션 `/` → next() · 비예외 무세션 → `/` · 딥링크 2라우트 그대로 · 세션 있으면 `/`→/overview) ② 입장 = `/` 클라이언트 마운트가 명시 호출하는 입장 핸들러(POST 권장 · GET/프리페치 경로 부작용 0) ③ 쿠키 다리(V-1 규율 — Set-Cookie 원문 전달 · 「응답 만들 때 심는다」 · Secure · pending 폴백)는 핸들러로 «이동» + 🔴 T3-1 E-1 축 재검 동반 ④ `app/page.tsx` 이중 안전장치 재정의.
- **AC(추가)**: 무쿠키 딥링크 → hover 프리페치 강제(표지 요청 ≥1 · 0건 = 무효) → +3초 쿠키 0 · Set-Cookie 0 · ai-api 세션 0 · 대조군 `/` 직접 방문 = 입장 + 브라우저 경유 `/api/*` 200(E-1) · 가드 유지 · 전환 동선 = 명시 입장 · 신규 계약 라우트 해제 0(입장 핸들러 = 셸 내부 · 갈리면 회부).
- **게이트**: 중간 보고 1회(층 대응표 before/after + 자극 강제 방법) → 자기 실측 표(브라우저 네트워크 축 · 초록 3문) → PR → 리바이2 12대 D-3 «단축» 재검(초록 5축 판정문 유지) → 완결(34/37).
- 발주문 원문 = 팀 채널 08-31 09:33~09:34(2부) · 자산 5건 = 센쿠2 14대 마감 신고(06:59) · 그물 = PR#175.
