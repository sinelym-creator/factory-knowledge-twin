---
ticket: T3-1
title: 세션 실체화 — /sessions 해제 · browser session sandbox · 세션 가드 전면 · 소유권
owner: 구현
status: 발주(08-30 16:08 · 담당 = 센쿠2 13대 · lane/senku2-t3-1)
due: 독립 검증 PASS 시 완결
---

# T3-1 — 세션 실체화 (Phase 3 기반 축)

## 목표

방문자 격리의 기반 축을 세운다: `POST /sessions`·`POST /sessions/{sid}/reset` 501 해제, 전 라우트 세션 가드(계약 v0.1.6 — 정본), run·WO 초안·승인 이력의 세션 소유권, 셸 세션 칩 실데이터 결선. 화면 4장(T3-2~T3-5)이 이 위에 선다.

## AC

- [ ] `POST /sessions` → `{ sessionId }` + HttpOnly 쿠키 · `POST /sessions/{sid}/reset` = 자기 세션만 · 그 세션의 run·초안·이력만 초기화(SSOT 무접촉 불변 — 지문 실측 동봉)
- [ ] 가드 = 🔴 v0.1.6 규칙 «그대로»: 제외 3라우트 · 읽기 예외 2라우트 · 무세션 `401 session_required` · 쿠키/본문 상충 `422` — 라우트별 표로 실측(가드 자체의 형제 세기: 새 라우트가 가드를 «빼먹는» 구조 금지 — 기본 가드 + 명시적 예외 목록 방식)
- [ ] 소유권: 타 세션 run·초안·승인 = `404`(존재 은닉) — Q-25 폐쇄. 자기 세션 = 기존 거동 회귀 무결
- [ ] 세션 스토어 = 프로세스 내 + TTL 명시(세션 스코프 성문 대가 유지 — 내구성 주장 금지)
- [ ] 셸 세션 칩(T1-9 골격) = 실 sessionId 결선
- [ ] ⓔ 품질: blocking 0 · SQL 조립 0 · LLM 0
- [ ] 🔴 기존 tests/api 드릴이 가드로 깨지는 목록 = «회부»(tests/ = 검증 scope — 직접 수정 금지). 검증 좌석이 세션 어댑터를 병행 선작성 중이다(T3-6 선행 소조각)

## 컨텍스트 (이것만)

- 계약 v0.1 §1(sessions 2행·run/compare의 sessionId) + 🔴 **v0.1.6 append = 정본**(가드·예외·소유권·reset — 갈림 시 append가 이긴다) · Q-16(§3:244 예외 배경) · Q-25(T2-5에서 열린 쓰기 축) · wireframes §6.
- 자리 = `_wt/senku2-t3-1` worktree(새로) · lane/senku2-t3-1 · 기점 = origin/develop 최신 · FKT_PYTHON = services/ai-api/.venv/Scripts/python.exe · 스택 fkt-senku2-q3(5538/7591) 실측부터.
