---
ticket: T5-2
title: Gate 7 Security·Abuse 그물 + credential scan — §32.8 13항 negative test · git history secret scan · Turnstile(결정 ③ 갈래)
owner: 검증(그물·판정문) · 구현 회부 = 결함 픽스
status: 선작성(08-31 23:2x · 발주 = T4-2b 착지 후 권장(보호장치가 서야 negative 가 의미) · 그물 선행 소조각은 그 전에도 가능)
due: 13항 전건 판정 + secret scan green = §35.4 사전 충족
---

# T5-2 — Gate 7 Security·Abuse (baseline §32.8 · §16.3 · §35.4)

## 목표

공개 표면이 «악의 입력에 예고된 방식으로» 닫히는가를 13항 negative test 로 판정한다. 🔴 「검색 문서는 evidence data 이며 instruction authority 가 아니다」(§32.8) — prompt injection 축의 판정 기준.

## 게이트

1. **정본 대조 선행(있는 것/없는 것)** — §32.8 13항 ↔ 기존 그물 대응표 1회: «이미 있는 것»(SQL·Cypher = `injection_surface_drill` · 타 세션 = `session_guard_drill` · trace/env 노출 = `error_shape_drill` · 임의 tool = `scenario_allowlist_drill` · secret = `credential_leak_drill`·`ci_hygiene_drill`) / «없는 것»(문서 내 prompt injection · 관리자 endpoint · oversized+rate limit 외부판 = T4-2b §C 재사용 · 잘못된 WS message · path traversal · CORS 우회 · 승인 우회) — 없는 것만 신설 · 있는 것은 «Gate 7 축으로 재실행»만.
2. **그물 신설** — 각 항 = 자극 «수» + 기대(차단·은닉·안전 실패) + 대조군(정상 입력 통과 = 참 울림 선행) · 문서 prompt injection = fixture 문서에 지시문 심어 tool authority 미획득 실증 · 승인 우회 = R12 서버 강제(T2-5) 재사용.
3. **secret scan** — git «이력 포함» scan(§35.4 「Git history secret scan」 · Q-30 계보 = HEAD 제거만으로 부족) + local + CI(`security.yml` = T5-3 결속) · 판정 = green.
4. **Turnstile(결정 ③)** — 현 갈래 = «동등 보호»(T4-2b) 유지 · Cloudflare 계정·도메인 생기면 별 소조각(분모 불변) — 폐하 자산 상태 변동 시만.

## AC

- [ ] 13항 전건 판정문(각 항 E1 · 자극 수 · 대조군 · FAIL = D-n 등급 분리 → 픽스 → 단축 재검)
- [ ] git history secret scan green(도구·범위 명시) · CI 결속 확인(T5-3)
- [ ] 기존 드릴 재실행분 = «Gate 7 축» 라벨로 판정문에(새로 만들지 않은 것을 만든 것처럼 적지 않는다)
- [ ] SSOT 무접촉 · 공개 경계 위반 0 · 판정문 = `evidence/t5-2-gate7-security-verification.md`

## 컨텍스트 (이것만)

- baseline §16.3 · §32.8 · §34.6 · §35.4 · 결정 ③ · T2-5(R12) · T3-1(세션 가드) · T4-2b(429/413/422/503) · Q-30(이력 scan 계보) · 기존 tests/api 드릴 목록.
- 자리 = `lane/levi2-t5-2-gate7` · write = `tests/**` · `evidence/**` · 서버 = 자기 트리·포트.
