---
ticket: T3-5
title: 작업지시서 편집·승인 화면 «최소 형상» — 편집(PATCH 자동 저장) · 안전 조치 불변(R12) · 승인/반려 · 근거 패널
owner: 구현
status: 선작성(08-31 10:15 · 🔴 최소 형상 = Feature Freeze 조기 선언 · 폐하 재가 09:57 ⓑ) → 발주 = T3-4 착지 후(또는 T4-1과 교차)
due: 독립 검증 PASS 시 완결
---

# T3-5 — 작업지시서 편집·승인 화면 (wireframes ④) · «최소 형상»

## 목표

GS-01의 끝 = 사람이 «승인»하는 장면(§16.4 human-in-the-loop). ④ `/work-orders/[woId]`(현 Placeholder)를 T2-5 착지분(WO 초안 CRUD·승인·반려·R12 서버 강제 · 세션 소유권 Q-25) 위에 세운다. 🔴 Feature Freeze 조기: 아래 «최소 형상»만 — 그 밖은 plan §3 배포 후 트랙.

## 최소 형상 (이것이 전부)

| 축 | 포함 | 이연(배포 후 트랙) |
|---|---|---|
| 표시 | `GET /work-orders/{woId}` = **계약 v0.1.4/5 12필드**(`workOrderDraftId·incidentId·equipmentId·title·failureModeId·procedures·safetyMeasures·parts·evidenceIds·gaps·note·approvalState` — 실측 일치 · 08-31 12:20 대응표) · 상태 배지(approvalState) · 출처 incident 링크 · 대상 설비 «고정». 🔴 wireframes §4 목업의 `priority·planned_at·assignee_role·estimated_minutes`는 계약에 «없음» → 그리지 않는다(빈 칸도 아님 — 오케 티켓 오기 정정 12:22) · 낱말 = 서버 낱말 | — |
| 편집 | 🔴 서버가 여는 필드 = **`title`·`parts` 뿐**(계약 v0.1.4 서버 문구 정본) → `PATCH /work-orders/{woId}`(디바운스 자동 저장 · 「변경 n건 · 자동 저장됨」) · parts 항목 추가/삭제 · `procedures`는 «읽기 전용 + 사유 문구»(403 `safety_basis_immutable` — 안전 조치의 근거) · 미지 필드 403 `field_not_editable` | 드래그 정렬 · procedures 편집(계약 회부 사안) |
| R12 | 🔴 안전 조치(`safetyMeasures`) = 편집·삭제 «불가»(서버 강제 403 `safety_measure_immutable` · mandatory 여부와 «무관» = Q-31 안전한 방향 불일치) · 툴팁 문구 = 「안전 조치는 SOP가 요구하는 항목이라 편집·삭제할 수 없습니다」(조건절 없음 — 화면이 서버보다 느슨하게 말하지 않는다 · 3코드 = 3문구) | 근거 툴팁 확장 축(Q-32 — 응답에 `sourceSopId` 없음 · 목업 SOP 인용 지어내지 않음) |
| 승인·반려 | 「승인」 → 확인 모달 → `POST …/approve` → `{status:"approved", auditId}` 표시 · 「반려」 → **사유 필수 = «화면 규칙»**(서버 `{comment?}`는 선택 — 화면이 더 엄격한 것은 허용 · 판정문 표에 그 사실 성문) → `POST …/reject` · 이후 읽기 전용(서버 409 `work_order_not_editable`·`approval_state_terminal` 표면) + 이력(세션 내 화면 보관 · 「새로고침 시 소멸」 한계 문구 — 이력 조회 라우트 없음) | — |
| 근거 패널 | `evidenceIds[]` → 카드(kind 아이콘·sourceId·요약) · 클릭 → ③(T3-3 착지분) | — |

## 게이트

1. **정본 대조 선행** — wireframes §4 ↔ 계약 WO 표면(T2-5 착지 · append v0.1.4~5 계열 · R12 오류 형상) — «화면 데이터 요구 ↔ 라우트» 대응표 중간 보고 1회(빈 칸 = 해제·확장 대상 아님 · 예상 신규 해제 0).
2. **화면** — 위 표 그대로 · 세션 소유권(Q-25 — 남의 세션 WO = 404 은닉) 회귀 0 · 승인 후 재편집 시도 = 서버 거절 형상 그대로 표면(코드에서 우회 0).
3. **자기 실측 표** — 🔴 초록 3문 · 편집→자동 저장→새로고침 일치 · 안전 항목 삭제 시도 → 거절 + 툴팁 · 승인 흐름(모달→approve→auditId) · 반려 흐름(사유 필수) · 🔴 브라우저 네트워크 축(PATCH/POST 형상 · 무쿠키 401/`/`) · 드릴 회귀(wo_shape·r12_enforcement·approval_transition — FKT_PYTHON 규율).

## AC

- [ ] WO 계약 12필드 표시(목업 4칸 «없음») + 대상 설비 고정 + 상태 배지 · 출처 incident 링크
- [ ] 편집 = `title`·`parts`만 → PATCH 자동 저장(디바운스) · parts 추가/삭제 · 새로고침 일치 · `procedures` 읽기 전용 + 사유
- [ ] 🔴 403 3코드(field_not_editable · safety_basis_immutable · safety_measure_immutable) = 3문구 표면 + R12 툴팁(조건절 없음 · Q-31)
- [ ] 승인(모달·approve·auditId) · 반려(사유 필수 = 화면 규칙 · 서버 200 대조군 병기) · 이후 읽기 전용(409 두 종) + 세션 내 이력(소멸 한계 문구)
- [ ] 근거 패널 → ③ · 세션 소유권 회귀 0(Q-25) · 브라우저 네트워크 축
- [ ] SSOT 무접촉 · blocking 0 · LLM 0 · 가드 예외 무확대 · tests/** 무접촉 · 계약 갈림 = 회부

## 컨텍스트 (이것만)

- wireframes §4·§9(조작 경계) · 계약 WO 표면 + append(R12 형상) · T2-5(구현 PR#134 · 검증 #137 · 그물 wo_shape·r12·approval_transition) · Q-25·Q-31·Q-32 · T3-3 ③ 링크 · T3-4 「작업지시서 초안 보기」 진입점.
- 자리 = 새 워크트리 · `lane/senku2-t3-5-wo-screen` · 기점 = develop(발주 시 sha) · 스택 fkt-senku2-q3 · 포트 규율 · 좌석 계보 3문.
