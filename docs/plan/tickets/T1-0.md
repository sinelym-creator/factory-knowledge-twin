---
ticket: T1-0
title: 개발 환경 기반 실측·골격 (S2 선행 — 게이트 무관)
owner: 구현
status: 발주
due: AC 실측 충족 시
---

# T1-0 — 환경 기반 실측·골격

## 목표
S2(skeleton) 착수 전 환경 불확실성을 지금 소거 — Phase 0 게이트와 무관한 준비 작업(기능 구현 아님).

## 산출물
1. **버전 실측표**: node·패키지 매니저·python·uv/pip·docker(compose) — 로컬 실측값(E1) + S2에 쓸 선택(예: pnpm vs npm · uv vs pip) 근거 1줄.
2. `docker-compose.yml` 초안: postgres(+pgvector)·neo4j 2서비스 — **로컬 기동 실측**(볼륨은 .gitignore 경로 · 포트·헬스체크 포함).
3. `.env.example`: 키 «목록만»(값 0 — baseline §34.6) + 각 키 1줄 설명.
4. 디렉토리 골격: `apps/web-console/`(Next.js 최소 생성·boot 확인) · `services/ai-api/`(FastAPI hello·boot 확인) — 기능 코드 0 · 부팅만.
5. `docs/product/dev-environment.md`: 위 전부 + 재현 절차(clone→up→boot 3명령 이내 목표).

## AC (판정 = 오케)
- [ ] `docker compose up` 실측 로그: pg(pgvector 확장 확인)·neo4j 기동 E1.
- [ ] Next.js dev·FastAPI uvicorn 부팅 E1(응답 1건 캡처).
- [ ] 시크릿·절대경로·실데이터 커밋 0(공개 경계 — push 전 스캔).
- [ ] 기능 코드 0 — 스캐폴드·환경뿐(게이트 원칙 준수).

## 컨텍스트
- `docs/product/system-architecture.md` §2·§6 · 품질 원칙 §7(비동기 스택 선택에 반영: asyncpg·httpx 등은 S2에서).
- 순서: T0-10 먼저(작음) → 본 티켓. worktree 새로 생성(티켓 단위 생애주기).
