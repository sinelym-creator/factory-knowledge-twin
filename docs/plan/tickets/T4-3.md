---
ticket: T4-3
title: Tunnel·Vercel 공개 RC — Tailscale Funnel(초기 · 갈래 (b)) · Vercel Hobby 셸 배포(🔴 공개 행위 · 착수 시 확인 1회) · 노트북 운영 조건 §14.4 · 외부 네트워크+모바일 접속
owner: 오케(Vercel 배포 = 폐하 재가분 · 오케 직접) + 구현(Funnel runbook · 자동 시작 · env 갈래 · CORS origin) · 판정 = T4-4
status: 선작성(08-31 23:2x · 발주 = T4-1 완결 + T4-2b PR-1 착지 후 · 🔴 착수 «직전» 폐하 확인 2건)
due: Public RC URL + 외부 접근 E2E PASS(plan S7 Exit · 판정 = T4-4)
---

# T4-3 — Tunnel·Vercel 공개 RC (S7 · baseline §14 · §34.1 공개 원칙)

## 목표

공개 형상을 «실제로 밖에서» 서게 한다. Vercel Hobby 에 셸을 올리고(노트북 OFF 축 = T4-2a 정적 replay 경로가 이미 셸 안에 있다), 노트북 ai-api 를 Tailscale Funnel 로 HTTPS 노출해 셸 `FKT_API_BASE` 가 그 URL 을 «빌드 타임»으로 굽는다(T4-1 Q-37 정식화 재사용 · 「목적지 변경 = 재빌드」). 외부 네트워크·모바일에서 접속 = §21 Phase 4 완료 증거 ①.

## 🔴 폐하 결정 영역 (착수 «직전» 확인 · 티켓은 구도만 올리고 대신 정하지 않는다)

- **① 공개 행위 확인 1회**(결정 ② · 주법 §5): GitHub Public 개설·push + Vercel 배포 = 공개 행위 → 착수 직전 재상신. 동봉 = 공개 경계 체크(§15.2·§16·§34.6): synthetic only · secret 0 · 절대경로 0 · 실데이터 0 · Claude 구독 노출 0 · `credential_leak_drill`·`ci_hygiene_drill` green 실측치.
- **② Tunnel 갈래 = (b) Tailscale Funnel**(폐하 확정 08-31 10:05 · Cloudflare 계정·도메인 «없음») — Cloudflare Named 로의 전환은 도메인이 생길 때 별 티켓(등재만 · 분모 변경 없음 · §14.3 「최종 공개 Live endpoint」).
- **③ 운영자 자산 청구(변한 것만 · 정본 = 08-31 10:08 준비 상태)**: Vercel 계정·GitHub 연결·env 1개(`FKT_API_BASE`) · Tailscale Funnel 활성(HTTPS certificates · Funnel ACL = 폐하 계정 화면 · Tailscale 1.102.2 설치·로그인은 이미 됨) · 노트북 절전/최대 절전 끔(§14.4 · 기기 설정). 🔴 토큰·키는 채팅 금지 — 경로(`~\_secrets\` 또는 환경변수)만.

## 게이트

1. **정본 대조 선행** — §14.1~14.4 · §34.1·34.6·34.7 ↔ 현 상태(T4-1 착지본 compose `ai-api` · `FKT_API_BASE` 빌드 주입 갈래 · CORS allowlist env · README boundary) 대응표 1회: «Vercel 층 / Funnel 층 / 노트북 층 / 없음» — 빈 칸 = 이 티켓 범위 아님(T5-4 회부). 🔴 공개 금지 artifact(§34.6) 스캔 green 을 착수 조건으로.
2. **구현**(구현 좌석 · write = `infra/**` · `apps/web-console/**` · `services/ai-api/**` env 만 · runbook 문서 = `docs/` 오케) — ⓐ Funnel runbook: `tailscale funnel` 로 compose ai-api 를 HTTPS 노출 · ts.net 호스트명 고정성 확인 · 서비스 자동 시작 · 재부팅 후 health 스크립트(§14.4 · T5-4 초안 겸용) ⓑ 셸 env 갈래: Vercel 프로젝트 env `FKT_API_BASE=<Funnel URL>` → production 빌드 · 로컬 갈래 유지 · «잘못 주입 = 눈에 보이게 실패»(T4-1 AC 재사용) ⓒ ai-api CORS allowlist 에 Vercel origin 추가(env · 기본 0) · `X-Forwarded-*` 신뢰 범위 = Funnel 프록시 «한 스위치»(T4-2b 429 XFF env 와 동일 env 로 켠다 — 두 스위치가 따로 있으면 한쪽만 켜는 날이 온다) ⓓ 노트북 OFF 시 셸 = 정적 replay(T4-2a) — 여기서 새로 만들지 않는다 · 확인만.
3. **배포(오케 직접 · 폐하 재가분)** — Vercel Hobby 프로젝트 생성 · GitHub Public 연결 · env 1개 · preview 배포 URL 로 먼저 1회(공개 «전» 대조군 · 팀 채널 보고) → production. 🔴 GitHub Public push 는 ① 확인 «뒤».
4. **자기 실측 표(구현 · 초록 3문 선행)** — 외부 망(모바일 데이터 · 노트북과 다른 망)에서 Vercel URL 첫 화면 ≤3s(Q-50 결속 · 값) · `/api/live/status` Funnel 경유 200 `online:true` · Live 조사 1회 완주(WS 가 Funnel 을 지난다 — WS 핸드셰이크 429 제외 규칙 실측) · ai-api 정지 → 셸 «미연결» 배지 + 정적 replay 제안(bounded timeout 실측 초) · Tunnel OFF(`tailscale funnel` off) → bounded timeout 후 Offline 판정(Gate 6 5행) · 모바일 viewport 1회.

## AC

- [ ] Public RC URL(Vercel production) + Funnel HTTPS URL 실재 · README boundary(§34.7)에 공개 경계 문구와 함께 성문 · «잠정 목표» 표기 유지(§0.2 — synthetic PoC 실측이지 실제 공장 ROI 아님)
- [ ] 외부 네트워크+모바일 접속 실측(E1 · 스크린샷 + 시각 + 망 종류) · 첫 화면 ≤3s(값)
- [ ] Live 조사 1회 외부 완주(WS 경유) · 노트북 OFF / ai-api OFF / Tunnel OFF 3축 = Replay 전환·Offline 판정(Gate 6 1·2·5행 · 값) — 본 판정 = T4-4
- [ ] §14.4 운영 조건 runbook(자동 시작 · 재부팅 후 health · 절전 끔 «확인 방법») + 재부팅 1회 실측(「존치 믿지 마라」 · compose 자동 복귀 / `docker run` 계열 Exited 갈림 성문)
- [ ] 공개 경계 §15.2·§16·§34.6 위반 0(스캔 green) · SSOT 무접촉 · 계약 표면 변경 0(갈림 = 회부) · tests/** 무접촉(그물 = T4-4)

## 컨텍스트 (이것만)

- baseline §5 · §14 · §15.2 · §16.3 · §17.1 · §21 Phase 4 · §32.7 Gate 6 · §34.1·34.6·34.7 · plan S7 · 결정 ①~⑤(08-31 09:55) · 갈래 (b)(10:05) · 운영자 자산 준비 상태(10:08 · 변한 것만 청구) · T4-1 착지본(Q-37 · CORS · 헤더 · `/live/status` 외부축) · T4-2a(정적 경로) · T4-2b(429 XFF · 503 queue · Q-48 강등).
- 자리 = 구현 lane `lane/senku2-t4-3-public-rc` · 오케 배포 = Vercel(로컬 git 만 · Public push 는 ① 확인 뒤) · 판정문 = `evidence/t4-3-public-rc-verification.md`(T4-4 검증 좌석이 쓴다 · 구현 자기 실측은 PR 본문).
