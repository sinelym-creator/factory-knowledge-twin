# T7-44 — 조사 실행(run)의 문서 검색 단계에 하이브리드 검색 반영 (O-36 ⓑ)

- 등재: 2026-09-06 00:18 · 근거 = 폐하 결정 「b」(00:17 DM · O-36 상신 선택지 ⓑ = 넣고 기준선 재정의 · 계약 v0.2 · 재검 live n=3 구독 3회 승인)
- 좌석: 설계·계약 = 오케 · 구현 = 센쿠2 · 검증 = 리바이2
- 선행: O-41/O-34(#senku2-ci2) 착지 뒤 구현 착수

## 1. 실물(E1 · 2026-09-06 00:18 오케 읽음)

- `services/ai-api/app/investigation/workflow.py:131~140` — `vector_node` 가 `retrieval.vector.search(pool, embed_query(question))` 만 부른다. 같은 패키지의 `retrieval/hybrid.py` 는 `POST /retrieval/compare`(`retrieval/service.py:139`) 에서만 쓰인다.
- 답변 축 기준선 = `benchmarks/eval-answer-raw-v0.4.jsonl` 계열(D-85 재검 뒤) · 채점기 게이트 16 · 지표 7 2/2·2/2·1/2.

## 2. 처방(안 A · 채택)

1. `vector_node` 의 검색 호출을 **전략 선택 가능**하게: 설정 `FKT_RUN_RETRIEVAL_STRATEGY` ∈ {`vector`,`hybrid`} · **기본 `hybrid`**(ⓑ 결정) · `hybrid` 는 `retrieval/hybrid.py` 의 기존 함수를 그대로 부른다(새 검색 로직 0 · 코드 중복 0).
2. run 이벤트 `step.started`/`step.completed`(kind=vector) 의 payload 에 `strategy` 1키 추가 → 화면·채점기가 어느 전략의 근거집합인지 안다. 계약 = `packages/contracts/rest-api-v0.1.md` **v0.2.0** 개정(오케) + 케이스(리바이2).
3. 근거집합 크기·순위 규칙은 hybrid 함수의 현행 그대로(TOP_K 변경 0). graph 단계·doc-chunk 방출(O-33 안 A)·`graphDocumentCitations`(D-85) 는 무변.
4. 단위 = `vector_node` 가 설정값대로 함수를 고르는지 2케이스(vector/hybrid) + 잘못된 값 = 기동 거부 1케이스.

## 3. 검증(리바이2 · develop 무대 `:8020` · 구독 3회)

- GS-01 live run n=3 · 실행기 `benchmarks/run-eval-answer-v0.3.mjs --score-raw` · **새 기준선 = v0.5 raw + report**(기준선 재정의 · 옛 v0.4 는 «vector 기준선» 이름으로 보존).
- 판정선: ① `strategy=hybrid` 가 이벤트에 실린다 ② 근거집합에 어휘 다리로만 잡히는 청크가 **≥1 등장하는 run 이 있다**(대조군 = 같은 질문 `vector` 설정 run 1회 · 구독 +1 = 총 4회는 승인 밖 → **대조군은 옛 v0.4 raw 로 대체**) ③ 지표 1·5·6·7 이 v0.4 대비 **하락 0**(하락 시 회부 · 기준선 재정의는 하락을 덮는 도구가 아니다) ④ dup 0 · 게이트 16/16.
- 못 잴 것을 이름으로: 검색 품질 향상량(n=3 · 질문 1건이라 주장 안 함).

## 4. 완료 정의

구현 PR + 계약 v0.2 PR + 검증 PASS(#…) → develop 종결 → 승격 22 청구(production 반영 = ai-api 재빌드 · 컨테이너 재생성 · 배포 행위 = 폐하 허가).
