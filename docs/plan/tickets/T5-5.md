---
ticket: T5-5
title: Phase 5 통합 독립 검증 — Gate 1~8 «전건» 정밀 판정 · restart recovery · clean env 타 경로 재현 · Release checklist §35.1~35.5 사전 점검
owner: 검증
status: 선작성(08-31 23:2x · 본 판정 = T5-1~T5-4 + T4-4 착지 후 · 점검표 골격은 선행 가능)
due: PASS = Release 후보 판정 → 폐하 직보(§35.7 최종 판정은 폐하 관문)
---

# T5-5 — Phase 5 통합 독립 검증 (baseline §32 전문 · §35 · plan §2 Exit)

## 목표

Release 직전의 «전건» 정밀 판정 — 개별 티켓 PASS 의 합이 아니라 Gate 1~8 을 한 자리에서 다시 세고, §35.1~35.5 체크리스트를 실측으로 채운다. 🔴 §35.7 「최종 Release 판정」 = P0 완료 + GS E2E + 독립 검증 + Security Gate + Public Offline Fallback 전건 — 하나라도 미충족이면 후보 아님.

## 게이트

1. **Gate 1~8 전건** — Gate 1 Contract(계약 밖 0 · tests/api 전종) · Gate 2 SSOT(지문 불변) · Gate 3 Retrieval(4전략 raw = T5-1 artifact 재검) · Gate 4 Agent(GS-01 13행) · 🔴 Gate 5 Live·Replay equivalence «재실증»(live 1회 신규 녹화 ↔ replay 논리 일치 · 비결정 필드 제외 · audit summary 동반) · Gate 6(= T4-4 판정 승계 + 재부팅 축 재확인) · Gate 7(= T5-2 판정 승계) · Gate 8 Portfolio Claim(주장 ↔ Evidence 대응표 — README·report 의 모든 수치에 근거 파일 경로).
2. **restart recovery** — 재부팅 → 자동 복귀(T5-4 스크립트) → 진행 중이던 세션·run 의 상태가 «예고된 방식»(만료·404 은닉·replay 제안)으로 정리되는지.
3. **clean env** — 🔴 타 경로 «새 클론»(기존 트리 무접촉)에서 README 만으로 seed → index → run → GS-01 완주 · 걸린 시간·막힌 자리 전건 기록(막힌 자리 = README 결함으로 회부).
4. **§35.1~35.5 사전 점검표** — 각 항 = 실측 근거 경로(파일·스크린샷·run 링크) · «못 채운 항 = 빈 채로»(§0.2 — 채우기 위해 낮추지 않는다) · §35.6 Portfolio(영상 등) = 폐하·오케 몫으로 분리 표기.

## AC

- [ ] Gate 1~8 판정문(각 Gate = E1 근거 · 승계 Gate 는 원 판정문 링크 + 재확인 축 명시)
- [ ] restart recovery + clean env 타 경로 재현 PASS(시간·막힌 자리 기록)
- [ ] §35.1~35.5 점검표 = 전항 근거 경로 또는 «미충족» 명시 · Target/Actual 분리 · 실제 공장 KPI 문구 0
- [ ] 판정 = PASS → «Release 후보» 직보(최종 판정 = 폐하) / FAIL = D-n → 픽스 → 단축 재검
- [ ] 판정문 = `evidence/t5-5-phase5-integration-verification.md` · SSOT 무접촉

## 컨텍스트 (이것만)

- baseline §32 전문 · §35 전문 · §0.2 · plan §2(S8 Exit) · T2-6·T3-6·T4-4(통합 판정 표준 계보) · T5-1 artifact · T5-2·T5-3·T5-4 판정문 · Q-43(표면 비대칭) · Gate 5 원 판정(T2-4 계열).
- 자리 = `lane/levi2-t5-5-verify` · write = `tests/**` · `evidence/**` · clean env = 타 경로(리포 밖 · 산출물만 evidence 로).
