---
ticket: T4-2a
title: 정적 replay 경로 — 노트북(ai-api) OFF 에서도 셸만으로 GS-01 replay 완주(§14.1·§6.2 · Q-51)
owner: 구현
status: 선작성(08-31 12:30 · T4-2 단위 분해 ⓐ — 발주 시 「분모 46→47」 선행 선언 · 발주 = T3-5 착지 후)
due: 독립 검증 PASS 시 완결(T3-6 증거 ① 「노트북 OFF GS 완주」 · T4-4 Gate 6 1행이 검증)
---

# T4-2a — 정적 replay 경로 (baseline §14.1 · §6.2 · §21 Phase 3 완료 증거 ① · Gate 6 1행)

## 목표

지금 replay는 ai-api가 fixture를 서버측에서 재생한다(`investigations.py` replay 분기 · T2-4) — 노트북(ai-api) OFF = replay 불가 = 「기본 공개 체험은 cloud DB 장애·pause에도 동작」(§14.1)·「Live 실패 시 Replay 제안 · 상태형 Sandbox」(§6.2) 미달(원장 Q-51). 셸이 fixture를 «정적 자산»으로 들고 ai-api 없이 GS-01을 재생·되감기할 수 있게 한다. Feature 아님 — baseline P0(§12.1 Replay Sandbox).

## 게이트

1. **정본 대조 선행** — §14.1(정적 fixture·graph snapshot = Vercel static · 방문자 상태 = Browser storage) · §6.2 · 계약 README 원칙(「replay fixture 이벤트는 live 이벤트와 동형」) · T2-4 fixture 32건 형상 · T3-4 `reduceEvents`·run-console · T3-2/T3-3 조회 화면의 데이터 요구(evidence·document·incident·equipment 정적 사본 필요 범위). 🔴 「어느 화면 요소가 정적 경로에서 무엇을 먹는가」 대응표 중간 보고 1회 — 정적으로 «못 주는 것»(예: 세션 소유 WO 승인)은 «빈 칸이 아니라 Live 전용 표기».
2. **구현** — ⓐ fixture 32건(+ GS-01 replay에 필요한 evidence·document·incident 정적 사본)을 빌드 시 셸 static asset으로 동봉(SSOT·ai-api 무접촉 · 빌드 스크립트로 «복사» — 손 복제 0). 🔴 **원본 2원화(판정 08-31 14:12 · 대응표 R-4)**: 이벤트 원본 = `data/replay/gs-01.events.jsonl`(파일 1본 · 이벤트 32건) · 조회 사본 원본 = `data/replay/static/`(**2단 구조** — ① 굳히기 도구 `apps/web-console/scripts/`가 «현 트리» ai-api 프로세스 + seed DB를 «읽기만» 해 응답 원문 무가공으로 굳힘 + 매니페스트 1본(라우트·빌드 sha·시각·건수·sha256) · 사람 1회 실행 · 재실행 diff 0(휘발 필드 명시) · 합성 데이터만 · 절대경로·secret 0 · README 「담지 않는 것」 갱신 → 커밋 / ② 빌드 복사 = ai-api 무접촉 · 매니페스트 sha 검증) · 필요 범위 = 대응표 28행(incident 1 · overview 1 · plants 1 · equipment 1 · scenarios 1 · sensor series 1 · evidence 14 · document 3 · WO 0~1 = R-2 실측 후) ⓑ Live 미도달(🔴 정의 = `liveStatus()` **응답 실패**(연결 거부·2s 상한 초과 → unavailable)«만» · `online:false`(도달 + 합성 게이트웨이 부재)는 서버 replay 정본 = 정적 제안 0 · `checking` 중 제안 0 — R-3) 시 «정적 replay» 자동 제안 → 실행 = 정적 이벤트를 `reduceEvents`로 재생(live/replay 무차별 렌더 유지 · 배지 = REPLAY · 「재생본 · 원 실행 관측치」 · 출처 «정적») ⓒ 방문자 상태(세션 칩·되감기 위치·열람 이력) = browser storage(§14.1 — 서버 세션 없음 · 격리 = 브라우저 · 🔴 R-1: 세션 origin 3값 `static` 승인 — «서버 눈에 무세션» · `fkt_session`/`fkt_sid` 쿠키 위조·재사용 0 · 가드 통과는 별 표지 · Live 복귀 시 정식 입장 `POST /enter` 재실행 = T3-1 E-1 회귀 0) ⓓ ai-api가 돌아오면 Live 경로로 «복귀»(모드 배지 polling 그대로 · 복귀 = 배지 전환 + 제안이지 강제 이동 아님 — 되감기 상태 보존) ⓔ 정적 경로에서 Live 전용 동작(조사 시작·WO 승인)은 «Live 전용» 안내(빈 화면 0 · 조용한 실패 0).
3. **자기 실측 표** — 🔴 ai-api «없는» 조건(대조군 프로세스 종료 + 셸 `FKT_API_BASE` = 리슨 0 포트 — `fkt-senku2-t41`은 옛 빌드·빈 DB·Q-52 증거물이라 대조군 아님·무접촉)에서 셸만 기동 → GS-01 replay 완주(이벤트 32 · 단계 5 · 근거 19 · 후보 2 · 되감기 seq 복원) · 첫 화면 ≤3s(§17.1 · Q-50 결속) · 대조군 = 현 트리(발주 sha) ai-api 프로세스 + q3 DB에서 Live 경로 그대로 + (i) `online:false` → 서버 replay / (ii) 미도달 → 정적 두 행 + 서버 replay vs 정적 replay 최종 RunState 동형 1행(runId 제외 · mode 포함) · 정적 자산 = 빌드 산출물에서 실재(sha = 매니페스트 일치) · 브라우저 네트워크 축(🔴 정적 경로 허용 /api 호출 = `GET /api/live/status` polling 1종뿐 · 그 외 0) · 자극 «수»(프로세스 내림·제안 노출 횟수) · 초록 3문 · R-2 = 서버 replay에서 WO 초안 링크 실측 1행(사전 판정 (a) 우세 · 기본 사본 0 + 「Live 전용」 안내).

## AC

- [ ] ai-api OFF(미도달 = 대조군 프로세스 종료 + 리슨 0 포트)에서 셸만으로 GS-01 replay 완주 + 되감기 상태 복원(§21 Phase 3 증거 ① · Gate 6 1행)
- [ ] 정적 자산 = 빌드 시 data/replay(이벤트) + data/replay/static/(굳힘 사본 + 매니페스트)에서 복사(손 복제 0 · sha = 매니페스트 일치) · SSOT·ai-api 무접촉 · 정적 경로 화면 데이터 /api 호출 0(허용 = `GET /api/live/status` polling 1종)
- [ ] Live 미도달(응답 실패만 · `online:false` ≠ 트리거) → 정적 replay 자동 제안(§6.2) · 첫 화면 ≤3s(Q-50) · ai-api 복귀 시 Live 경로 복귀(제안 · 강제 이동 0) · 정적 화면이 자신을 LIVE라 말하지 않는다(mode→replay 치환)
- [ ] 방문자 상태 = browser storage(§14.1) · Live 전용 동작은 «Live 전용» 안내(조용한 실패 0)
- [ ] live/replay 렌더 분기 0 유지(배지·출처 문구만) · tests/** 무접촉 · 계약 갈림 = 회부

## 컨텍스트 (이것만)

- baseline §6.2 · §12.1 · §14.1 · §17.1 · §21 · §32.7(Gate 6) · 계약 README 원칙 · T2-4 fixture · T3-4 run-console/reduceEvents · Q-43(replay graph path 501 — 정적 경로는 evidence 축이라 무관) · Q-50 · Q-51.
- 자리 = 새 워크트리 · `lane/senku2-t4-2a-static-replay` · 기점 = develop(발주 시 sha) · 포트 규율 · 초록 3문 · 「rc 0 ≠ 한 일」(빌드 산출물 실재로).
