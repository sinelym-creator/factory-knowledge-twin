---
ticket: T4-1
title: 공개 형상 골격·env 주입 — compose ai-api 등재 · 배포 빌드(Q-37 해소) · CORS/헤더/CSP · /live/status 외부축
owner: 구현
status: 선작성(08-31 10:00 · 분모 37→46 재가 시 등재 · 발주 = Q-39 ⓒ 착지 후)
due: 독립 검증 PASS 시 완결(T4-4 통합 검증 결속)
---

# T4-1 — 공개 형상 골격·env 주입 (S7 · baseline §14)

## 목표

«공개 형상»(Vercel 셸 + Tunnel 뒤 노트북 ai-api)을 로컬에서 1커맨드로 세우는 골격. 지금 compose는 postgres·neo4j 2서비스뿐이고 ai-api·셸은 좌석이 손으로 띄운다 — 그 손을 코드로 옮긴다. 🔴 Q-37(FKT_API_BASE가 «빌드 시점»에 구워진다 → start에만 주면 남의 서버를 잰다)을 여기서 해소한다.

## 결정 결속 (폐하 승인 08-31 09:55)

- ① Tunnel = 초기 Tailscale Funnel(호스트 서비스 — compose 밖 · 자리·절차만 문서화) → 최종 Cloudflare Named(T4-3).
- ③ bot protection = S7은 동등 보호(T4-2) · Turnstile은 T5-2. ④ Cloud DB 미사용 — 공개 체험 = 정적 snapshot + 노트북 Live. ⑤ `infra/**`·`.github/**` = 구현 좌석 write scope.

## 게이트

1. **정본 대조 선행** — baseline §13.1(Public API 표면) · §14.1~14.4 · §16.3 ↔ 현 코드(compose · `apps/web-console/next.config` rewrite · `lib/contract.ts` apiBase · `contract-surface.mjs` 허용 목록). 🔴 「어느 항목이 어느 «층»에서 서는가」 대응표 중간 보고 1회(셸 빌드 / 셸 런타임 / ai-api / Tunnel / 없음) — 빈 칸 = 이 티켓 범위 아님(T4-2·T4-3 회부).
2. **구현** — ⓐ compose에 `ai-api` 서비스 등재(healthcheck = `/health` 프로브 · 의존 postgres·neo4j · env 주입 = environment variable만 · secret 커밋 0) ⓑ web-console 배포 빌드: `FKT_API_BASE` «빌드 타임» 주입 정식화(Vercel env → build) + 공개 API base 갈래(로컬 / Tunnel URL) — 「목적지 변경 = 재빌드」를 README·주석에 성문(Q-37 종결) ⓒ ai-api CORS allowlist(env · 기본 = 셸 origin만) · security header · CSP(셸 측 · Next headers) · HTTPS 가정(Secure 쿠키는 이미 스킴 기반 — 프록시 뒤 `X-Forwarded-Proto` 신뢰 범위 명시) ⓓ `/live/status` 외부축: 셸 polling에 bounded timeout(미도달 = «미연결» 상태 · 30s poll 유지 · §6.2 빈 화면 금지).
3. **자기 실측 표** — 🔴 초록 3문 세 줄 선행(내 프로세스가 답했나 · 지금 세도 되나 · 자극이 났나) · 1커맨드 boot → `/health` 200 · 계약 회귀 22종(tests/api · `FKT_API_BASE` 빌드 주입본) · 🔴 브라우저 네트워크 축: 타 origin에서 `/api/*` 프리플라이트 허용/차단 «대조군» · 헤더 실측(CSP·HSTS·nosniff) · Tunnel 미기동 상태에서 «미연결» 표시(timeout 실측 초).

## AC

- [ ] `docker compose up` 1커맨드로 ai-api(+DB) boot · `/health` 200 · 재부팅 후 자동 시작(restart 정책 실측 — 「존치 믿지 마라」 계보)
- [ ] `FKT_API_BASE` 빌드 타임 주입 정식화 + 갈래 문서 = Q-37 종결 · 잘못 주입 시 «눈에 보이게» 실패(기본값이 남의 서버를 가리키지 않게)
- [ ] CORS allowlist(허용 origin 200 · 비허용 origin 차단 대조군) · security header·CSP 실측 · HTTPS 가정 성문
- [ ] `/live/status` 외부축 bounded timeout(미도달 = 미연결 · 빈 화면 0)
- [ ] SSOT 무접촉 · 계약 표면 변경 0(갈림 = 회부) · secret/절대경로 커밋 0(CI hygiene green) · tests/** 무접촉(그물 회부)

## 컨텍스트 (이것만)

- baseline §6.2 · §13.1 · §14 · §16.3 · §17.1 · Q-37 · 결정 ①③④⑤ · T3-1 쿠키 다리(Secure 스킴 규율) · Q-39 ⓒ 착지본(입장 층).
- 자리 = 새 워크트리 · `lane/senku2-t4-1-public-shape` · 기점 = develop(ⓒ 착지 후 sha 명시) · 스택 fkt-senku2-q3 · 포트 = 기동 전 «누가 듣나» + 기동 후 «리슨 1개» · 병행 = T4-2와 lane 분리(services 동일 파일 충돌 시 회부).
