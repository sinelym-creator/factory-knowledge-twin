---
ticket: T1-6
title: contract test harness 승격 (검사기 → 정식 테스트)
owner: 검증
status: 발주
due: AC 충족 시(오케 판정)
---

# T1-6 — contract test harness 승격

## 목표
네 event-binding 검사기(22케이스·양방향)를 일회성 검증 도구에서 **재사용 가능한 정식 contract test**로 승격 — S2부터 모든 커밋이 계약 위반을 자동으로 잡게 한다.

## 산출물
- `tests/contract/` 정리: ① 검사기 인자화(스키마 경로·케이스 파일 분리 — 하드코딩 0) ② exit code 규율(전건 PASS=0 · 실패=1 · 케이스 목록 출력) ③ OBSOLETE 스크립트는 `tests/contract/archive/`로 이동(이력 보존).
- 케이스 확장 여지 구조화: 케이스를 데이터(JSON)로 분리 — 이후 REST 응답 검증 케이스가 같은 러너에 얹히게.
- `tests/contract/README.md`: 실행법(1명령)·케이스 추가법·판정 규칙 3줄.

## AC (판정 = 오케)
- [ ] node 단독 실행(외부 의존 0 유지) · 1명령으로 전건 실행.
- [ ] 기존 22케이스 전량 이관·통과 + 양방향(통과/거부) 구조 유지.
- [ ] 잘못된 스키마를 주면 실패하는 «자기 검증» 케이스 1건(러너가 항상 초록인 함정 방지).
- [ ] CI 편입 준비 완료 신고(잡 추가 자체는 .github = 오케 lane — 네 «보고» 받고 내가 편입).

## 컨텍스트
- `tests/contract/event-binding.check.js`(네 것) · `packages/contracts/agent-events-v0.1.schema.json`(동결 v0.1 — 이제 기준면 고정).
- worktree 새로 생성(티켓 단위). 서브에이전트 규율 적용(판정 위임 금지).
