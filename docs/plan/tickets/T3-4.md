---
ticket: T3-4
title: 조사 실행·전략 비교 화면 «최소 형상» — run 실행·타임라인(WS) · replay 재생(상태형) · /compare · graph path 텍스트 체인
owner: 구현
status: 선작성(08-31 10:12 · 🔴 최소 형상 = Feature Freeze 조기 선언 · 폐하 재가 09:57 ⓑ) → 발주 = Q-39 ⓒ 착지 직후
due: 독립 검증 PASS 시 완결
---

# T3-4 — 조사 실행·전략 비교 화면 (wireframes ② 실행 축 + ⑤) · «최소 형상»

## 목표

Golden Scenario(GS-01)가 «화면에서» 실행되고 재생된다. ② Incident 화면(T3-2 착지 = 조회 축)에 **실행 축**을 얹고, ⑤ /compare(현 Placeholder)를 세운다. 🔴 Feature Freeze 조기(폐하 재가 08-31 09:57): 아래 «최소 형상»만 이 티켓 — 그 밖은 plan §3 배포 후 트랙(UX 폴리시 패스)으로 넘긴다. 범위를 코드에서 넓히지 않는다.

## 최소 형상 (이것이 전부)

| 축 | 포함 | 이연(배포 후 트랙) |
|---|---|---|
| 실행 | [실행] → `POST /runs`(`startRunBrowser` 기착지) → WS `/ws/runs/{runId}` 소비 · [중지] · 진행 인디케이터(5단계) · 타임라인(`step.started/completed{summary,elapsedMs}`) · 원인 후보(`run.completed.candidates[]`) · Evidence 스트립 실시간 누적(`step.evidence`) · 🔴 TTAE 표시행 §2.2(`elapsedMs` 누적 → `totalElapsedMs` 확정 · 수작업 대비 = «잠정 목표 · 미실측» 꼬리표 · 단축률 % 미표기) · 「작업지시서 초안 보기」 활성(`workOrderDraftId` → T3-5 링크) | 타임라인 단계 클릭 필터 · 후보 「근거 보기」 필터(③ 열기 링크는 T3-3 착지분 재사용) |
| replay | replay 모드 = fixture 이벤트를 `seq` 기준 «상태형» 재생(§6.2 영상 아님 — 재생/일시정지/처음으로) · 🔴 live/replay 무차별 렌더(`mode`는 배지만 · 렌더 분기 0) · 재생 중 되감기 시 화면 상태가 그 `seq`로 «돌아간다» | 배속 다단(1x 고정 허용) · 스크러빙 |
| ⑤ /compare | 승인 질문 목록 선택 · 전략 체크 다중(1~3열) · `POST /retrieval/compare` → 열별 hits(evidenceId·score·excerpt·elapsedMs) · 규칙 기반 «차이 요약» 라벨(안전 규정/관계 도달 여부 — 우열 단정 0) · 🔴 측정-주장 각주 상시(§0.2) · hit 클릭 → ③ | 자유 입력(길이·rate 제한은 T4-2 뒤) |
| graph path | `/graph/paths?from&to` 소비 = 경로를 «텍스트 체인»(EQ→CP→FM→SOP→SAF)으로 표시(Evidence 스트립 graph-path 카드·후보 근거) · Q-26(from&to) 재판정 회부 | 그래프 도형 시각화 |

## 게이트

1. **정본 대조 선행** — wireframes §2(실행 축·§2.1 도크·§2.2 TTAE)·§5 ↔ 계약(runs 표면 5 + WS + `/retrieval/compare` + `/graph/paths` — T2-1·T2-3 착지분 · append 전건). 🔴 «화면 데이터 요구 ↔ 라우트» 대응표 중간 보고 1회(T3-2 표준) — 빈 칸 = 해제·확장 대상 아님 · 신규 해제 소요는 대응표가 판정(예상 0~1 · Q-26·Q-27 판정 회부).
2. **화면** — 위 «최소 형상» 표 그대로. 🔴 WS 규율: 동일 run 이벤트 중복 처리 0 · 종료 코드(4000~4999) 처리 = 화면 문구 · 끊김 시 `GET /runs/{runId}` 재조회로 상태 복구(§17.2 — T4-2가 기전, 여기선 «빈 화면 0»만). 세션 가드·소유권(T3-1) 회귀 0.
3. **자기 실측 표** — 🔴 초록 3문(내 프로세스 · 시점 · 자극) · GS-01 live «실행→완주» 1회(이벤트 수·단계 5·후보·evidence 누적·TTAE 실측값) + replay «재생→되감기» 1회(seq 되돌림 실증) · /compare 3전략 열 · 🔴 브라우저 네트워크 축(세션 쿠키 200 / 무쿠키 → `/` · WS 핸드셰이크) · 드릴 회귀(FKT_PYTHON 규율 · 서버 재기동 후 · FKT_API_BASE 빌드 시점 Q-37).

## AC

- [ ] ② 화면에서 GS-01 live 실행 → 5단계 완주 → 후보·evidence·TTAE 행 표시(§2.2 규율 · 단축률 미표기) · [중지] 동작
- [ ] replay 모드: fixture 재생 → 되감기 시 화면 상태 복원(상태형) · live/replay 렌더 분기 0(배지만)
- [ ] ⑤ /compare: 승인 질문 + 전략 다중 → 열 1~3 + 차이 라벨 + 각주 상시 · hit → ③
- [ ] graph path 텍스트 체인 표시(`/graph/paths?from&to` 소비) · Q-26 재판정 회부 1건
- [ ] WS 이벤트 중복 0 · 종료 코드 문구 · 끊김 시 빈 화면 0(재조회) · 세션 가드 회귀 0
- [ ] SSOT 무접촉 · blocking 0 · LLM 0 · 가드 예외 무확대 · tests/** 무접촉(그물 회부) · 계약 갈림 = 회부

## 컨텍스트 (이것만)

- wireframes §2·§2.1·§2.2·§5·§8(GS 흐름) · 계약 v0.1 runs/WS/compare/graph + append 전건 · T2-3(WS·egress 가드) · T2-4(replay fixture 32건 · `seq`) · T3-2 Incident 화면 착지분 · T3-3 ③ 링크 · Q-26·Q-27 · Q-39 ⓒ 착지본(입장 층).
- 자리 = 새 워크트리 · `lane/senku2-t3-4-run-screen` · 기점 = develop(ⓒ 착지 sha — 발주문에 명시) · 스택 fkt-senku2-q3 · 포트 규율(기동 전 «누가 듣나» · 기동 후 «리슨 1개») · 좌석 계보 3문.
