---
ticket: T0-3
title: P0 핵심 화면 5종 wireframe + route/interaction 목록
owner: 구현
status: 발주
due: D1(08-29)
---

# T0-3 — 핵심 화면 wireframe

## 목표
P0 5화면의 구조·라우트·상호작용을 구현 착수 가능한 수준으로 확정.

## 산출물
- `docs/product/wireframes.md` — 화면별: 레이아웃 블록 구성(ASCII/mock) · 표시 데이터 항목 · 주요 인터랙션 목록.
- 5화면: ① Factory Overview ② Incident 조사(Agent 진행 표시 포함) ③ Evidence 뷰(graph path+문서 인용) ④ 작업지시서 편집·승인·반려 ⑤ 검색 전략 비교(Vector/Hybrid/GraphRAG).
- route 목록(경로·파라미터·화면 매핑) 1표.

## AC (판정 = 오케)
- [ ] 5화면 전부 레이아웃+데이터 항목+인터랙션 3요소가 있다.
- [ ] baseline §12.1 공개 Sandbox 11항이 5화면 어딘가에 전부 매핑된다(매핑표 포함 · 누락 = FAIL).
- [ ] Golden Scenario(T0-4 대본) 흐름을 화면 전환으로 재현 가능하다 — 대본 도착 후 교차 1회.
- [ ] 세션 격리·리셋(one-click demo reset) 진입점이 표시된다.

## 컨텍스트
- T0-2와 동일 + `docs/baseline/poc-baseline-v0.2.md` 718~726행(공개 가능한 조작 — 화면 조작 경계).
- 선행: T0-2 방향과 무관하게 구조 설계 가능 — 병행하되 T0-2 확정 후 시각 정합만 재확인.

## 태스크
1. P0 11항 → 화면 매핑표 → 2. 화면별 wireframe → 3. route 표 → 4. T0-4 대본 교차 → 5. «보고» 회신.
