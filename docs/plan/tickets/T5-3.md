---
ticket: T5-3
title: CI 게이트 §34.3 — ci.yml 확장 · security.yml · benchmark-smoke.yml · release-evidence.yml · §34.4 runner 경계
owner: 구현(`.github/**` = 결정 ⑤ 구현 write scope)
status: 선작성(08-31 23:2x · 발주 = T5-1 smoke 부분집합 추출 후(benchmark-smoke 입력) · ci.yml 확장분은 그 전에도 가능)
due: 4 workflow green + runner 경계 성문 = §35.5 「required check 통과」 사전 충족
---

# T5-3 — CI 게이트 (baseline §34.3 · §34.4 · §35.5)

## 목표

§34.3 의 4 workflow 를 «현 ci.yml 과의 델타»로 세운다. 🔴 §34.4 경계가 헌법: 개인 노트북을 Public PR 이 실행 가능한 self-hosted runner 로 등록하지 않는다 — Public PR = GitHub-hosted 만 · Full benchmark = 신뢰한 commit 에서 노트북 수동 실행 → manifest·hash → 검토 후 result commit.

## 게이트

1. **정본 대조 선행(델타표)** — §34.3 항목 ↔ 현 `.github/workflows/ci.yml` 실물(현재 1 job `hygiene` = required files(§34.2) · contract tests(`tests/contract/run.js`) · public boundary scan(§34.6)) — 있는 것 3 · 없는 것 = FE/PY lint·typecheck·test · replay fixture schema · SSOT manifest · ontology validation · Docker build · Replay E2E smoke + security.yml·benchmark-smoke.yml·release-evidence.yml 전부.
2. **ci.yml 확장** — 항목별 job 분리(한 job 이 죽으면 어느 축인지 보이게) · Replay E2E smoke = GitHub-hosted 에서 도는 부분집합(서버 replay 또는 정적 replay — T4-2a 산출 재사용 · 로컬 전용 축은 명시 제외) · 캐시 규율(pnpm·pip) · 🔴 기존 hygiene 3 step 회귀 0.
3. **security.yml** — local secret scan 보완 · CodeQL · dependency audit · container scan · license inventory · public endpoint policy test(§13.2 관리자 표면 부재 실증) — T5-2 그물과 «층 분리»(CI = 정적/조립 · T5-2 = 런타임 negative).
4. **benchmark-smoke.yml** — T5-1 추출 8~10문(Direct·Multi-hop·Safety·Unanswerable) · full benchmark 아님 = «regression gate» 문구 성문 · 결과는 판정이 아니라 «드리프트 알람».
5. **release-evidence.yml** — test summary · SBOM/dependency inventory · benchmark manifest validation · README KPI ↔ result file 일치 · artifact hash.

## AC

- [ ] 4 workflow 실재 + 최근 run green(각 1회 실측 링크) · required check 지정
- [ ] §34.4 경계 성문(workflow 주석 + runbook) · self-hosted runner 등록 0 실측
- [ ] 기존 hygiene 3 step 회귀 0 · Public PR 에서 노트북 자원 접근 경로 0
- [ ] 🔴 CI 는 «게이트»지 «판정»이 아니다 — acceptance = 독립 검증(§32.1) 문구 유지
- [ ] SSOT 무접촉 · secret 커밋 0 · workflow 내 절대경로 0

## 컨텍스트 (이것만)

- baseline §34.2~34.4 · §35.5 · 결정 ⑤ · 현 ci.yml 실물 · Q-30(CI 2층 계보) · T5-1(smoke 입력) · T5-2(층 분리) · T2-4(fixture schema) · T0 계열(ontology validation 스크립트 실물 대조).
- 자리 = `lane/senku2-t5-3-ci-gates` · write = `.github/**` · `scripts/**` · 검증 = 러너 실행 로그 실측(「rc 0 ≠ 한 일」 — job 이 «돌았는가»를 run 페이지로).
