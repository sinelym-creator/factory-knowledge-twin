---
ticket: T4-4
title: Phase 4 통합 독립 검증 — Gate 6 축소판 8행 장애 매트릭스(외부 축) · §21 Phase 4 완료 증거 4종 · 그물 선행 소조각
owner: 검증
status: 선작성(08-31 23:2x · 그물 선행 소조각 = T4-1 완결 + T4-2b PR-1 착지 후 즉시 발주 가능 · 본 판정 = T4-3 RC URL 실재 후)
due: Phase 4 게이트 = 본 판정 PASS → 폐하 직보(Phase 4 완결 · S7 Exit = Public RC URL + 외부 접근 E2E PASS)
---

# T4-4 — Phase 4 통합 독립 검증 (S7 Exit · baseline §21 Phase 4 · §32.7 Gate 6 · §17.2)

## 목표

Phase 4(T4-1 · T4-2a · T4-2b · T4-3)가 «밖에서» 하나의 공개 서비스로 서고, 죽을 때 «예고된 방식으로» 죽는가를 독립 검증한다 — 티켓별 PASS 의 합이 아니라 Gate 6 8행 전건 + §21 완료 증거를 외부 축(다른 망 · 모바일)에서 실측한다. T3-6(Phase 3 통합)과 같은 표준: «끊긴 곳 0» + 각 축 command · raw · hash/screenshot.

## 완료 증거 4종 (§21 Phase 4 3행 + 원장 T4-4 행이 결속한 ④ = T4-2 §21 완료 증거 ④)

| # | 증거 | 축 | 선결 |
|---|---|---|---|
| ① | 외부 네트워크와 모바일에서 접속 | 노트북과 다른 망(모바일 데이터) + 모바일 viewport · Vercel URL 첫 화면 ≤3s(값) · `/api/live/status` Funnel 경유 `online:true` | T4-3 RC URL |
| ② | Live 조사 실행 | 외부에서 GS-01 live 1회 완주(WS 가 Funnel 경유 · 이벤트 32 · `run.completed`) · T2-6 13행 «끊긴 곳 0» 표준의 외부판 | T4-3 · T4-2b |
| ③ | 노트북 종료 시 Replay 전환 | 🔴 실제 종료(ai-api 정지 → 노트북 절전/OFF 1회)에서 Vercel 셸 «미연결» 배지 + 정적 replay 제안 → GS-01 replay 완주(T4-2a 외부판) · bounded timeout 값 | T4-2a · T4-3 |
| ④ | 동시 요청 제한·timeout | 외부에서 동시 3요청 → 2 실행 + 1 큐(`run.queued`) · 4번째 503 `live_capacity_exhausted` + Retry-After · timeout env → `run.stopped(timeout)` + Replay 안내 · 429 두 축(Funnel 뒤 XFF 신뢰 스위치 ON = 방문자별 분리 대조군) | T4-2b §C PASS |

## Gate 6 축소판 (§32.7 8행 · 외부 축 · 각 행 = 자극 «수» + 기대 결과 + 값 + «원복 후 정상» 대조군)

| 장애(§32.7) | 자극 | 기대 결과(§32.7 원문) · 판정 축 |
|---|---|---|
| 노트북 OFF | ③ 겸 | Public UX 와 Replay 정상 |
| FastAPI OFF | ai-api 컨테이너 stop | Offline 표시와 Replay 전환(배지 + 제안 · 빈 화면 0) |
| PostgreSQL OFF | pg stop | Live 원인 표시 · Public UX 유지(Q-48 시작 전 판정 = 200 `mode:"replay"` 강등 / fixture 없으면 503 `dependency_unavailable`) |
| Neo4j OFF | neo4j stop | Graph 단계 제한 또는 명확한 실패(시작 전 강등 또는 `run.failed` + `payload.fallback:"replay"`) |
| Tunnel OFF | `tailscale funnel` off | bounded timeout 후 Offline 판정(값 · Gate 6 5행) |
| WebSocket 중단 | 외부 WS 강제 절단 | 재연결 또는 `GET /runs/{id}` 상태 재조회 · 이벤트 중복 처리 0(§17.2 · T4-2b PR-2 ⓕ) |
| Model timeout | timeout env 1s | 안전 종료와 Replay 안내(④ 겸 · 리소스 해제 실측) |
| 동시 요청 초과 | ④ 겸 | queue 또는 Replay 안내 |

## 그물 선행 소조각 (T4-1 완결 + T4-2b PR-1 착지 후 즉시 발주 가능 — T2-5 패턴)

- Gate 6 8행 «로컬판» 드릴(`tests/api/gate6_failure_drill.py` 가칭 · 컨테이너 stop/start 자극 · 초록 3문 · 자극 «수» · 원복 확인) — 외부판은 같은 그물에 base URL 만 바꾼다(그물은 base URL 파라미터 · 「포트만 바꿔 재사용 불가」는 셸 «빌드» 얘기다).
- ② 외부판 13행 표(T2-6 표준 → 외부 축 열 추가) · ③ bounded timeout 계측기(`q50_entry_badge_timing.mjs` 재사용 · base URL 파라미터) · ④ 동시성 그물 = T4-2b §C 에서 만든 것 재사용.
- 모바일 viewport 스크린샷 하네스(Playwright device 프로필 1~2종) · 값 표 서식 = Target / Actual / PASS·FAIL / Evidence(§0.2).

## AC

- [ ] 완료 증거 ①~④ 판정문(각 축 E1 · 망 종류 · 기기 · 시각 명시 · 스크린샷 hash)
- [ ] Gate 6 8행 전건 = 기대 결과 일치(FAIL = D-n 등급 분리 → 픽스 → 단축 재검) · 각 행 «원복 후 정상» 대조군
- [ ] «끊긴 곳 0» 외부판 통합 표 · 회귀: 브라우저 suite · tests/api 전건 · 계약 밖 0 · T3-6 증거 ① 「측정 가능」 전환 확인(T4-2a 결속)
- [ ] 🔴 측정-주장 경계(§0.2): 값은 synthetic PoC 외부 접속 실측 · «잠정 목표» 표기 유지 · 실제 공장 ROI 문구 0
- [ ] 판정 = PASS → Phase 4 게이트 직보(S7 Exit) / FAIL → 결함 목록 → 픽스 → 단축 재검

## 컨텍스트 (이것만)

- baseline §0.2 · §6.2 · §14 · §16.3 · §17.1~17.2 · §21 Phase 4 · §32.7 · plan S7 Exit · T4-1~T4-3 판정문 · T2-6 통합 판정문(13행 표준) · T3-6(증거 ① 결속) · Q-48 · Q-50 · Q-51 · Q-56.
- 자리 = `lane/levi2-t4-4-verify` · 그물 `tests/api/**` · `tests/web/**` · 판정문 `evidence/t4-4-phase4-integration-verification.md` · 🔴 외부판 서버 = RC 스택(T4-1 compose · 이 노트북) — 좌석 검증 트리·포트가 «아니다» · 자극(컨테이너 stop · Tunnel off · 절전)은 RC 운영 창 밖 · 폐하 예고 후 · 원복 실측까지가 한 행.
