---
ticket: T2-1
title: retrieval 3전략 API(vector·hybrid·graphrag) — /retrieval/compare 501 해제 계열
owner: 구현
status: 발주(08-30 · 센쿠2 9대)
due: 독립 검증 PASS 시 완결
---

# T2-1 — retrieval 3전략 API (vector·hybrid·graphrag)

## 목표

동결 계약 v0.1의 retrieval 표면(`/retrieval/compare` 계열)을 501에서 해제한다 — **동일 질문 1건을 3전략(vector·hybrid·graphrag)으로 실행한 실물이 완료 증거**(원장 AC). 🔴 **본 티켓은 «retrieval만»이다** — LLM 합성(synthesize)은 T2-3이며, 여기서 Claude 호출은 0이다.

## 단계 게이트 (순서 고정)

1. **계약 표면 실독 선행** — `packages/contracts/`(🔴 v0.1 동결 · 수정 = 오케 scope)에서 retrieval 계열 라우트·요청/응답 스키마를 실독하고, 착수 «전» 해제 대상 라우트 목록을 «보고»로 올린다(`python -m tools.contract_surface` 대조 — 계약 밖 경로 exit 1). AC 문장보다 계약이 길면 계약이 이긴다(6대 계보: AC가 정본보다 짧은 자리를 먼저 찾아라).
2. **vector 전략** — T1-4 색인(`document_chunk.embedding` · chunking_policy_version=1 동결) 위 pgvector 검색 경로. 질의 임베딩은 색인과 동일 모델·차원(어긋나면 즉시 보고).
3. **hybrid 전략** — 구조화 축(SQL) + vector 결합. 결합 방식(가중·rerank 등)은 후보 실측 후 근거 병기 택일 — «잰 것»만 채택 근거다.
4. **graphrag 전략** — T1-5 Neo4j 투영(노드 309·관계 448 · R03·R07·R08·R11·R12) traversal. GS-01 4-hop 경로가 결과에 실재해야 한다.
5. **동일 질문 3전략 실행 실물** — GS-01 계열 질문 1건 최소. 각 전략 응답에 인용(chunk 좌표 = 인용 추적용 · U-1 판정: 정본 입도는 문서·revision) 포함. 실행 실물(요청·응답)을 «보고»에 병기.

## AC

- [ ] 계약 표면 대조 green — 해제 라우트 전건이 계약 v0.1 스키마 1:1 · 계약 밖 신규 경로 0 (`tools.contract_surface` E1).
- [ ] 3전략 각각 독립 실행 경로 실물 — 전략 간 폴백으로 눙치지 않는다(전략별 실행 증거 분리 · E1).
- [ ] 동일 질문 1건 3전략 응답 실물 + 인용 chunk 좌표 — GS-01 S4·S7 기대 인용 문장이 vector/hybrid 결과에 실재(E1).
- [ ] 🔴 사용자 입력 → SQL·Cypher **문자열 조립 0** — 파라미터 바인딩·서버측 화이트리스트 경로만(공개 경계 §34.6 · 임의 실행 경로 금지). 위반 1건 = FAIL.
- [ ] 🔴 Claude·외부 LLM 호출 0 — 임베딩 모델은 T1-4 로컬 계열만.
- [ ] blocking 0 원칙 유지(T1-8 계보 — async 경로에서 동기 DB/드라이버 호출 금지 · 실측 병기).
- [ ] 색인 신선도(007·008 계열 STALE/UNVERIFIED)와 retrieval 읽기 경로의 결합 여부 **실측 소견 보고** — 판정은 오케(조용히 결정하지 않는다).
- [ ] Q-5 판정 입력: `MaintenanceRecord.note`·`FailureMode.description` 임베딩이 3전략에 실제 필요한지 실측 소견 «보고»(판정 = 오케 · 원장 Q-5).

## 범위 밖 (명시)

- `/documents`·`/evidence` 501 해제 = T2-2 · LangGraph·합성 = T2-3 · 계약·docs 수정 = 오케 scope.
- 프론트(`apps/web-console`) 연결 = 범위 밖 — 셸의 pending 칩 정직성 회귀는 검증 축이 T2-1 착지 후 잰다(6대 예고 ③).

## 컨텍스트 (이것만)

- `packages/contracts/`(동결) · `docs/product/dev-environment.md`(스택 기동·격리) · `services/ai-api/`(T1-8 골격) · `services/indexer/`(T1-4) · `services/projector/`(T1-5) · 원장 Q-5·Q-9(T2-3 결속 — 본 티켓에서 손대지 않는다).
- 🔴 재부팅 직후다 — 컨테이너 전량 다운(볼륨 생존). 자기 스택 재기동부터: `COMPOSE_PROJECT_NAME` 인라인 지정 · `VOLUME_ROOT`는 절대경로 · `$env:PYTHONUTF8='1'` · `$env:PGPORT` 자기 포트 명시(남의 스택 물지 않기).
- lane: worktree `lane/senku2-t2-1` · PR 1건 · 경로 지정 add만 · ctx% 병기 · 30분+ 조각은 30분 신호.
