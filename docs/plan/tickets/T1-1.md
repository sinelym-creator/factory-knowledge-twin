---
ticket: T1-1
title: PG 스키마·마이그레이션 (온톨로지 → DDL)
owner: 구현
status: 발주
due: 독립 검증 PASS 시 완결
---

# T1-1 — PG 스키마·마이그레이션

## 목표
동결된 T0-6 v0.1을 PostgreSQL 실물 스키마로 — T1-2(seed)·T1-4(색인)·T1-5(그래프)의 유일 기반.

## 산출물
- `services/ai-api/db/migrations/*.sql` — 번호 순 적용·**재실행 멱등**(IF NOT EXISTS 계열) + 적용 스크립트(1명령).
- T0-6 §1 entity 16종 전부의 테이블 + §3 ID 체계(PK·prefix CHECK) + §3.3 문서 revision(approval_state·effective_from/to·content_sha256) + 관계 FK(§2 카디널리티 반영) + pgvector 청크 테이블(벡터 칼럼 — 차원은 모델 미확정이라 **파라미터화**·주석).
- `docs/product/dev-environment.md`에 적용 절차 1절 추가.
- **스펙 대조표**: T0-6 항목 ↔ DDL 위치(테이블·칼럼) 1:1 — 검증 좌석이 이 표로 대조한다.

## AC (판정 = 오케 · 독립 검증 = 검증 좌석)
- [ ] entity 16/16 · relation 전부(§2 표) 반영 — 대조표 누락 0.
- [ ] 마이그레이션 2회 연속 적용 = 오류 0(멱등 실측 로그).
- [ ] ID unique·필수 속성 NOT NULL 제약 실재(위반 INSERT가 실제로 거부되는 실측 1건).
- [ ] 기능 코드 0 — 스키마·적용 스크립트만.

## 컨텍스트
- `docs/product/data-ontology-spec.md`(동결 v0.1 — 유일 원천) · compose는 T1-0 착지분 사용.
