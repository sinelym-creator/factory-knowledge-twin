---
ticket: T5-1
title: Evaluation Lab — dataset 40문(§30.2 분포) · 4전략 실행기(§30.4 · 3전략 API + SSOT 층) · deterministic 지표 · 5회 반복 · benchmark artifact(§30.10) · evaluation report
owner: 검증(dataset·실행기·지표·report = benchmarks/**) · 구현 회부 = API 갈림 시만
status: 선작성(08-31 23:2x · 폐하 하명 「설계 미리 준비」 · P4 흐름 보며 즉시 병행 가능 — plan §4)
due: 독립 재현(재현 메타 4종으로 제3자 재실행 가능) + report 착지 시 완결 · §35.2 사전 충족
---

# T5-1 — Evaluation Lab (S8 · baseline §18 · §30 · §35.2)

## 목표

§30 Benchmark Protocol 을 «측정 가능»한 실체로 세운다: 40문 dataset → 4전략 × 5회 = 800회 raw → deterministic 지표 → Target/Actual 분리 report. 🔴 §0.2 측정-주장 경계가 이 티켓의 헌법이다 — 실측 전 모든 칸은 `Not measured`, 실측 후에도 «synthetic PoC 실측»이지 실제 공장 KPI 가 아니다.

## 게이트

1. **정본 대조 선행(분모 선언)** — 🔴 §30.4 전략 = **4개**(Vector-only · Hybrid · GraphRAG · GraphRAG+SSOT) ↔ 실물 = retrieval 3전략 API(`services/ai-api/app/retrieval/` vector·hybrid·graphrag · T2-1) — 4번째(+SSOT Validation)가 «층»으로 실재하는지 실물 대조 · 없으면 **회부**(코드에서 넓히지 않는다 · §18.1 「3전략」과 §30.4 「4전략」의 갈림도 이때 오케 재계산 성문). Ground truth 스키마 = §30.3 그대로(필드 신설 0).
2. **dataset 증설** — 🔴 현 실물 = `benchmarks/datasets/eval-questions-draft.md` **10문 v0.2**(T0-8 · T0-6 ID 재바인딩 완료)이지 40문이 아니다 → §30.2 분포(직접 8 · 식별 8 · multi-hop 8 · revision 충돌 6 · 안전 5 · 답변 불가 5)로 30문 증설 · 전 문항 T0-6 확정 ID 앵커(baseline 예시 ID 금지 — v0.1 stale 계보) → `questions.jsonl` + `ground-truth.jsonl`(§30.3) + version·hash 고정.
3. **실행기·지표** — configs 4본(§30.10) · 같은 질문·snapshot·embedding·hardware(§30.4) · 지표 = §30.5(Hit@K·Recall@K·Precision@K·MRR·nDCG@5) 자동 계산 + §30.8 6종(Root Cause Top-3 · Citation Validity = ID·revision·원문 exact · Graph Path strict/logical 분리 · Abstention · Safety Omission · Unsupported Claim) · 🔴 LLM-as-judge = 보조 분석만 · acceptance authority 0(§30.9).
4. **반복·artifact** — 40×4×5 = 800회 · seed 고정 · Cold/Warm 분리 · P95 주지표 · `benchmarks/results/v0.1.0/`(summary.json·summary.md·retrieval/answer/performance-results.jsonl) · 실패 결과 미제외 · 재현 메타 4종(dataset version · source hash · model/embedding version · retrieval parameter).

## AC

- [ ] dataset 40문 = §30.2 분포 일치 · T0-6 ID 앵커 100% · version·hash 고정 · answerable 분리
- [ ] 4전략 × 5회 raw 800회 보존(실패 미제외) · 지표 raw 에서 자동 재계산 가능(§35.2)
- [ ] report = Target(§30.7~30.8 기존 Target 열)/Actual 분리 · 같은 열에 섞지 않음 · «잠정 목표» 문구 · 실제 공장 KPI 오인 문구 0
- [ ] 🔴 smoke 부분집합 8~10문 추출(Direct·Multi-hop·Safety·Unanswerable 포함) = T5-3 `benchmark-smoke.yml` 입력으로 성문
- [ ] SSOT 무접촉 · services 코드 무접촉(갈림 = 회부) · LLM judge 단독 판정 0

## 컨텍스트 (이것만)

- baseline §0.2 · §18 · §29.2~29.4 · §30 전문 · §35.2 · T0-8 초안 v0.2(10문 · 증설 기점) · T0-6 ID 체계 · T2-1(3전략 API) · T2-2 계열(SSOT validation 층) · /retrieval/compare(T3-4 소비처).
- 자리 = `lane/levi2-t5-1-eval-lab` · write = `benchmarks/**`(+ 판정문 evidence/) · 서버 = 자기 트리·포트 · 초록 3문 · 「rc 0 ≠ 한 일」(800회 = 행수 실측).
