---
ticket: T1-7
title: seed→index 재현성·무결성 검증 + 자기 점검 그물 완결
owner: 검증
status: 발주(🔴 2단 분리 — B단은 T1-4 게이트 ④ 착지가 선행)
due: 독립 검증 PASS 시 완결(판정 = 오케)
---

# T1-7 — seed→index 재현성·무결성 + 자기 점검 그물

## 목표

Phase 1 데이터·색인 층의 무결성 검증 완결(원장 AC: ID unique·hash·재생성 diff 0) + 계승 부채 3축(F-2·G-4b·G-2)을 닫는다. 🔴 색인이 아직 없다(T1-4 ④ 미착지) — **잴 수 없는 축을 «지금 잰 것처럼» 닫지 않는다.** 그래서 2단이다.

## A단 — 즉시 착수 가능 (index 무관)

1. **소조각**: `benchmarks/datasets/eval-questions-draft.md` §7 「◇ 5건이 여전히 E4」 stale 정정(같은 파일 §5는 E1 충족 — 리바이2 3대 지적 ⓐ · 오케 승인 완료).
2. **F-2 known gap 해소** — 자기 점검 바인딩 구멍: `tests/data/selfcheck_mutation.py`의 알려진 미감지 1건을 감지 가능으로 전환(주입 11 → 감지 11) **또는** 전환 불가 사유를 evidence에 성문. 🔴 감지 «사유» 대조 유지(「잡혔다 ≠ 그 검사가 잡았다」).
3. **G-4b·G-2 «비강제 의도» 검사** — 스키마가 의도적으로 강제하지 않은 축을 «무엇이 대신 지키는가»로 성문·실측(expectSemantics: 대신 지키는 것 없는 accept = 눈감기). 대신 지키는 검사가 없으면 신설(tests/** = 네 scope).

## B단 — 🔴 T1-4 게이트 ④(색인 빌드) 착지 후

4. **seed→index 재현성**: 재생성 연속 2회 diff 0 · chunk ID unique · `index_build.source_sha256` 대조(GS-01 S6 축).
5. **스키마↔spec 재대조에 `index_build` 포함**(003 마이그레이션 착지 후 — 오케 확정 ② 2026-08-29 04:20).
6. **V-1 인수**(T1-3 이월 축): wireframes 4좌표가 `document_chunk`에 실재하고 그 chunk 본문이 해당 인용을 포함하는가(양방향 · 🔴 표기 규칙 = #NNN ≡ chunk_index 0-based).

## AC

- [ ] A단 3건 각각 E1 실측 + evidence(Target/Actual/판정 분리).
- [ ] B단 3건 E1 실측 — B단 착수 선언은 T1-4 ④ 착지 확인 후(원장 기준).
- [ ] 판정 근거 전부 자기 실행 출력(E1) · 도구 생존 확인 선행(계보 규범).

## 컨텍스트 (이것만)

`tests/data/`·`tests/schema/` README(판정 규칙 정본) · `evidence/t1-2-*`·`t1-3-*` · spec `docs/product/data-ontology-spec.md` §3.3·§4·정오표 E-3 · 오케 확정 3건(팀 채널 1542977089366401125). lane/levi2-* · PR 단위 = A단 1건·B단 1건 허용 · ctx% 병기.
