# D-89 FIX3 — 「도달했다」를 래치로 굳히던 가드를 뺀다 (센쿠2 55대)

base = `origin/develop` **05d73650f8721c23b2b6910d313424983a1fff0f**(실측) · lane `lane/senku2-d89`
무대 = `:8807`(이 트리 prod 빌드 · API 참조 = develop 스택 `:8020`) · 🔴 구독 호출 **0**(정적 replay run)

## 1. 고친 것 (1파일 1줄 + 주석)

`apps/web-console/components/tour/tour-overlay.tsx` — 바깥 가드에서 **`!settled` 제거**:

```diff
-      if (!settled && !userMoved) {
+      if (!userMoved) {
```

안쪽은 그대로다: 매번 `settled = okTop && okBottom` 을 **다시 계산**하고, 제자리면 `return` 한다.
바뀌는 것은 「밀려난 뒤에 다시 본다」 하나다 — 그리고 그것이 바로 `:231~236` 주석이 이미
약속하고 있던 거동이다(문면과 거동을 일치시켰다).

멈춤 보장 3중 **유지**: ① 사람이 움직이면 `userMoved` 로 즉시 중단 ② 폴링 8초 뒤 종료
③ 스크롤 간격 450ms.

## 2. 🔴 前 재현율이 1 이 아니다 — 그래서 «비율»로 쟀다

M-2 는 같은 조건에서 3/3 FAIL 이었는데, **이 트리·이 무대에서는 3회 중 1회만 FAIL** 이었다.
경합이라 회차 수가 적으면 재현율이 흔들린다 ⇒ 判定을 **8회 비율**로 바꿨다.
(「3/3 → 3/3」 판정선을 그대로 썼다면 前 이 이미 초록이라 **수리를 증명할 수 없었다**.)

## 3. 실측 — direct × 412×600 · 판정식 `bottom ≤ vh && top ≥ 16`

| | PASS 비율 | 관측값 |
|---|---|---|
| **前**(`05d7365`) | **4/8** | PASS 회차 top 36 · bottom 563 / FAIL 회차 top **88** · bottom **615 > 600** |
| **後**(이 lane) | **8/8** | 전 회차 top 36 · bottom 563 · y 519 |

### 3.1 회귀 (後 · 3회 · 전 회차 동일)

| 뷰포트 | 경로 | top | bottom | 판정 |
|---|---|---|---|---|
| 412×915 | direct | 246 | 773 | PASS 3/3 |
| 412×915 | operator | 194 | 721 | PASS 3/3 |
| 412×600 | direct | 36 | 563 | PASS 3/3 |
| 412×600 | operator | 36 | 563 | PASS 3/3 |

### 3.2 🔴 사람 입력 보장 — 대조군을 **양쪽으로** 세웠다

| 축 | 자극 | 기대 | 실측 |
|---|---|---|---|
| `user` | `mouse.wheel(0,120)` = **사람 입력** | 보정 **0**(자리를 빼앗지 않는다) | y 587 → 587 · **2/2 보정 0** |
| `prog`(known-true) | `window.scrollBy(0,120)` = **사람 입력 아님** | 보정 **있음** | y 639 → **519** · **2/2 보정 있음** |

🔴 `user` 의 0 은 **`prog` 가 참을 낸 뒤에야** 값이다 — 고장 난 계측기는 어느 축에서나 0 을 낸다.

### 3.3 계측기 자수 2 (값을 적기 «전»에 잡았다)

1. `page.mouse.wheel` 은 **스크롤이 적용되기 전에 돌아온다**. 바로 읽었더니 휠 자신의 +120 이
   「보정」으로 잡혀 **`user` 축이 0/3 FAIL** 로 나왔다 — 그대로 적었다면 「수리가 사람 입력
   보장을 깼다」는 **거짓 빨강**이었다. 휠이 앉은 뒤(700ms)를 기준선으로 고쳤다.
2. `prog` 축을 700ms 뒤에 읽었더니 **「이동 직후」가 이미 보정된 값**(519)이라 보정이 0 으로
   보였다. 보정이 700ms보다 빠르다는 뜻이었다 — 민 «직후»를 같은 evaluate 안에서 읽어 창을 좁혔다.

## 4. rc

| 축 | 값 |
|---|---|
| `pnpm lint` | **0** |
| `pnpm build` | **0** |
| `npx tsc --noEmit`(build 뒤) | **0** |

## 5. 안 잰 것

- 다른 걸음(index 0·1·2·4)·다른 폭에서 같은 래치가 노출되는지 = 이 lane 밖(같은 가드라 같은
  방향으로 나아질 것으로 **기대**하나 측정 0 = E4).
- 실기기·다른 브라우저 엔진.
