# T7-18 1단계 — 열린 결함 7건 «실물» 대조

- 센쿠2 38대 · 2026-09-04 07:2x · **원장을 믿지 않고 지금 코드·화면을 열어서** 확인
- 무대 = **`:8799` 신설**(현 `develop` `728b66f` prod build). 🔴 `:8797`(`ea89b36`)·`:8798`(리바이2) 무접촉.

## 결론 — 🔴 **진짜 남은 것은 2건**

| # | 판정 | 근거(실측) |
|---|---|---|
| **D-26** 터치 44px | 🔴 **ⓑ 남음** | 히트 실측(아래 §1) — 1440 **1건** · 390 **4건** |
| **D-32** `text-[Npx]` 11건 | ✅ **ⓐ 고쳐짐** | `git grep "text-\[[0-9.]*px\]" origin/develop -- apps/web-console/{components,app}` = **0건** |
| **D-33** coarse 가로 1px 넘침 | ✅ **ⓐ 고쳐짐** | 390 coarse: `scrollWidth 390 / clientWidth 390` = **넘침 0px**(원장 실측은 391/390) · 1440 coarse·fine 도 0 |
| **D-38** 「잠깐 끊기」=「다시 안 보기」 | ✅ **ⓐ 고쳐짐** | `tour-later`·`tour-never` 실재 + 오늘 실측: 「건너뛰기」 → **「나중에」/「다시 보지 않기」 두 갈래** |
| **D-39** 포커스가 말풍선에 완전 가림 | ✅ **ⓐ(사정거리 좁음)** | 6걸음 × Tab 5회 = **30회 중 «완전히 가림» 0건** |
| **D-40** 말풍선 뷰포트 이탈 | ✅ **ⓐ(사정거리 좁음)** | **6걸음 전부 뷰포트 안**(이탈 0) |
| **D-41** 레일·바 같은 `data-testid` | 🔴 **ⓑ 남음** | `shell-nav.tsx` 46줄(rail)·74줄(bar) 둘 다 `data-testid={n.testid}` |

## §1 D-26 — 🔴 «층을 바꿔서» 다시 쟀다

- 처음에 **layout box** 로 세니 coarse 390 에서 **12/16 미달**이 나왔다. 🔴 **그 수는 쓸 수 없다** — 이 리포는 히트 영역을 `::before` 로 넓히고, **의사 요소는 `getBoundingClientRect` 에 안 잡힌다.**
- 그래서 **`elementFromPoint` 로 중심 ±21px 을 짚어 «실제로 눌리는가»**를 쟀다.

| 폭 | 화면 안 대상 | 실패 | 무엇이 |
|---|---|---|---|
| **1440** | 14 | **1** | 배너 `✕` **44x20 · 세로 실패** |
| **390** | 10 | **4** | `nav-overview`·`nav-incidents`·`nav-compare`(각 **72~75x28** · 세로 실패) · `tour-start`(**127x44** · 세로 실패) |

- 🔴 **원장의 「19건」과 수가 다르다.** 다른 그물·다른 형상이므로 **같은 수를 기대할 수 없다.** 이 표는 **「이 형상에서 지금 이만큼」**이다.
- 🔴 **`tour-start` 는 상자가 이미 44 인데 세로가 실패**한다 ⇒ **크기 문제가 아니라 «다른 것이 덮고 있을» 가능성**이 있다. **크기 미달과 가림은 처방이 다르므로 갈라야 한다** — 아직 안 갈랐다.
- 🔴 **못 잰 것**: 390 에서 **5건이 화면 밖**(스크롤 아래) · 1440 에서 1건. **0 이 아니라 «안 쟀다».**

## §2 D-39 / D-40 — ⓐ 로 적되 «무엇 안에서» 참인지

- 잰 범위 = **1440 · chromium · 투어 6걸음 · 걸음마다 Tab 5회**. 🔴 **7~9번째 걸음은 사람이 눌러야 넘어가 못 밟았다.**
- 그 범위 안에서 **가림 0 · 이탈 0**. 🔴 **좁은 폭(<md)은 바텀 시트라 다른 경로**이고, 안 쟀다.
- ⇒ **「닫아도 된다」가 아니라 「이 범위에서는 재현 안 된다」**가 정확한 문장이다. 원장에서 지울지는 오케 판단.

## §3 D-41 — 남았지만 「측정 불가」는 우회됐다

- 코드에 **그대로 남아 있다**(rail·bar 같은 testid).
- 다만 원장 문면의 **「좁은 폭 nav 히트 측정 불가」는 이번에 성립하지 않았다** — `display:none` 인 쪽을 걸러내면 보이는 쪽만 잡힌다(그래서 §1 의 390 수치가 나왔다). **결함은 남고, 그 «증상» 하나는 우회 가능하다.**

---

# T7-18 2단계 — 수리 «전/후» + 🔴 회부 2건

무대 `:8799` 를 **수리 전 빌드 → 수리 후 빌드**로 갈아 끼우고 **같은 그물**로 다시 쟀다.

## §4 먼저 «크기 미달 vs 덮임»을 갈랐다 (오케 지시 · 고치기 «전»에 수행)

| 대상 | 폭 | 확장(`fkt-hit`) | 짚은 점이 자기 상자 안인가 | 무엇이 잡혔나 | 판정 |
|---|---|---|---|---|---|
| `nav-overview`·`nav-incidents`·`nav-compare` | 390 | **없음** | 아니오 | 옆 텍스트/배지 | **크기 미달**(확장 자체가 없었다) |
| `tour-start` | 390 | 없음 | 🔴 **예** | 🔴 **`BUTTON#tour-never`** | **겹침**(다른 «버튼»이 위에 있다) |
| 배너 `✕` | 1440 | 있음 | 아니오 | 🔴 **`HEADER#app-bar.fkt-glass.sticky`** | **덮임**(확장분을 앱바가 덮는다) |

⇒ 셋이 **서로 다른 병**이었다. **크기를 키우는 처방은 아래 둘에 안 듣는다.**

## §5 전/후 (같은 그물 · `elementFromPoint` 중심 ±21px)

| 폭 | 전 | 후 | 남은 것 |
|---|---|---|---|
| **1440** | 화면 안 14 중 **실패 1** | 🔴 **실패 0** | — |
| **390** | 화면 안 10 중 **실패 4** | **실패 2** | `nav-incidents` · `tour-start` |

- **고쳐진 것**: 배너 `✕`(1440) · `nav-overview`·`nav-compare`(390).
- 🔴 **눈에 보이는 변화 1건** — **배너 줄 높이 36.5 → 44 (약 7.5px 더 두꺼워진다).** 글리프·글자 크기는 그대로다. `py-2` 를 `min-h-11` 로 바꿨으므로 두 번 커지지 않는다.
- **D-41 은 화면이 안 바뀐다** — `data-testid` 는 **그대로 두고** `data-nav-variant="rail|bar"` 를 **더했다**. 기존 선택자·증거가 쓰는 값을 안 건드리므로 **e2e 가 깨지지 않는다.**

## §6 🔴 회부 2건 — 「한 자리를 고치려고 다른 자리를 죽이지 않기」

1. **`nav-incidents`(390)** — 확장을 붙였더니 이제 **`A#intro-reopen`(다른 조작 요소)이 그 띠를 차지**한다. 🔴 **짚은 점은 자기 상자 «밖»이므로 보이는 72x28 을 누르는 것은 여전히 자기 자신**이다(전에도 그 띠는 남의 것이었다 — 새 피해는 아니다). **다만 44 가 서려면 이웃과의 «리듬»을 같이 바꿔야 한다** — 좁은 폭 레이아웃 판단이라 **단독으로 안 바꾼다.**
2. **`tour-start`(390)** — 🔴 **자기 상자 «안»에서 `tour-never` 가 위에 있다.** 크기 문제가 아니라 **두 조작 요소가 겹친 것**이고, 안내 카드의 배치를 바꿔야 한다. **시안 판단이라 회부.**

## §7 못 잰 것 (🔴 0 이 아니다)

- **390 에서 5건 · 1440 에서 1건이 화면 밖**(스크롤 아래) — **안 쟀다.**
- 좁은 폭은 **390 한 점**만 봤다. 그 사이 폭은 안 봤다.
- 게이트: `pnpm build` 성공. `tsc --noEmit` 단독은 이 리포에서 `generated/*`·`PageProps` 로 항상 5건을 내므로 **빌드가 정본**이다.
