# T7-19 — D-1 재개방 수리 「안내 카드는 배경이 아니다」

- 센쿠2 38대 · 2026-09-04 07:4x · 무대 **전 `:8801`**(현 develop 빌드) ↔ **후 `:8802`**(본 lane) · 🔴 `:8799`·`:8102` 무접촉(검증 것)
- 처방 = **ⓑ 채택** — 투어가 열려도 **안내 카드를 `inert` 밖**에 둔다.

## 1. 🔴 두 경로 × 전/후 네 칸 — «눌러서» 봤다(스캔 아님)

| 경로 | 전(`:8801`) | 후(`:8802`) |
|---|---|---|
| **ⓐ 첫 진입** | 격자 **20/25** 가 카드 것 · `inert` 뒤 아님 · **클릭 성공 · 카드 사라짐** | **20/25 · 클릭 성공 · 카드 사라짐 — 그대로** |
| **ⓑ 「?」 재진입** | 🔴 격자 **0/25**(셸 본문 `DIV.flex.min-w-0.flex-1` 이 전부 먹음) · 닫기가 `inert` **뒤** · **클릭 6초 타임아웃 = 못 누름** | 🔴 격자 **25/25** · `inert` 뒤 **아님** · **클릭 성공 · 카드 사라짐** |

🔴 **「안 눌린다」를 실제로 눌러 보고 실패로 확인했고, 「눌린다」도 눌러서 확인했다.**

## 2. 필수 조건 3 대조

| # | 조건 | 결과 |
|---|---|---|
| 1 | **투어 OFF 면 화면 변화 0** | 전·후 **완전 동일** — `inert` 루트 **0/0** · 투어 없음 · 조작 요소 **19/19** |
| 2 | **첫 진입이 안 나빠졌는가** | **20/25 → 20/25** · 클릭 성공 유지 ✅ |
| 3 | 닫은 뒤 `?intro=1` 이 주소에 남는가 | 닫은 뒤 주소 = `/overview` — **안 남는다** |

- ⚠ **3번 사정거리**: 관측 시점(「?」 클릭 2.2초 뒤)에 **이미 주소에 쿼리가 없었다.** 즉 **「닫기가 지웠다」까지는 이 측정이 말하지 않는다** — 「닫은 뒤에도 없다」만 참이다.

## 3. 읽을 때 오해하기 쉬운 수 1개

- 재진입 시 `inert` 루트가 **9 → 15 로 늘었다.** 🔴 **더 나빠진 게 아니다** — 카드를 허용 노드로 두면 그 조상 사슬을 «통과»하며 형제를 더 잘게 덮으므로 **루트 수가 늘고 덮는 «범위»는 오히려 좁아진다.** 판정선은 루트 수가 아니라 **「닫기가 눌리는가」**다.

## 4. 못 한 것

- **단위 케이스 미첨부** — 투어의 `inert` 걷기는 아직 `tour-overlay.tsx` **안에 인라인**이다. 단위로 못 박으려면 **모달 쪽처럼 순수 함수로 떼어내는 일이 먼저**이고(T7-3 의 U-04 계열), 그걸 이 커밋에 같이 넣으면 **행위 불변을 두 축에서 동시에 증명**해야 한다. **분리 권고.**
- **1440 · chromium 한 형상**만 봤다. 좁은 폭은 안 쟀다.
