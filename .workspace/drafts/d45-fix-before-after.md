# D-45 처방 — 자기 실측 «전/후» 2열

- 센쿠2 38대 · 2026-09-04 06:5x · 1440x900 chromium · 두 무대를 **같은 순서로** 조작(입장 → `/overview` → 둘러보기 → 다음 반복)
- **전 무대** `:8797` = `ea89b36` · **후 무대** `:8798` = 본 lane 빌드
- 🔴 **전/후 기준 일치 확인**: `ea89b36 .. 0fca31a` 사이 `tour-overlay.tsx` **무변경**(`git diff --quiet` = 동일). 그 사이 남이 바꾼 것이 표에 섞이지 않는다.

## 6걸음 전수 (E1 · 화면이 남긴 `data-tour-*` 값)

| 걸음 | 제목 | 전: 자리/left/덮은글자/대상덮음 | 후: 자리/left/덮은글자/`clear`/대상덮음 |
|---|---|---|---|
| 1 | 둘러보기를 시작합니다 | anchor / 540 / — / 대상 없음 | **동일** |
| 2 | 알람은 «울린 행»의 값이 앵커다 | anchor / 1068 / 43,043 / 아니오 | **before / 696 / 29,428 / yes / 아니오** |
| 3 | 조사를 시작하면 … 5단계 | anchor / 1068 / 36,731 / 아니오 | **before / 716 / 22,485 / yes / 아니오** |
| **4** | 다섯 단계가 실제로 지나간 자리 | beside / 624 / 726 / 아니오 | 🔴 **beside / 624 / 726 / yes / 아니오 — 불변** |
| **5** | 후보마다 근거 ID 가 붙는다 | anchor / 1028 / 20,789 / 🔴 **예(51,823px²)** | 🔴 **before / 656 / 24,109 / yes / 아니오(0px²)** |
| 6 | 근거 칩을 직접 눌러 보세요 | anchor / 1048 / 46,436 / 아니오 | **before / 676 / 42,454 / yes / 아니오** |

- 🔴 **D-45 대상(5번째) 겹침 51,823px² → 0.**
- 🔴 **4번째 회귀 없음** — 자리·덮은 넓이가 **바이트째 동일**(beside/624/726). 우연히 맞던 걸음이 규칙이 바뀌며 틀어지지 않았다.
- **글자 덮음 합계** 147,725 → 119,202(**-19.3%**). 단 🔴 **5번째는 20,789 → 24,109 로 «늘었다»**(+16%). **경성 조건(대상을 안 덮는다)이 앞서므로 설계대로**이며, 이 교환을 숨기지 않는다.

## 필수조건 ① — 「안 덮는 후보가 하나도 없는 경우」

- `data-tour-clear` 로 값을 남긴다. **밟은 6걸음에서 `no` 는 0건**(대상 있는 5걸음 전부 `yes`).
- 🔴 **그런 경우가 나면** 「덜 덮는」 쪽으로 떨어지고 `data-tour-clear="no"` 가 남는다 — **못 피한 것을 피한 척하지 않는다.** 이 값이 있으니 **앞으로 셀 수 있다.**

## 🔴 내가 한 번 틀렸고 이 표가 잡았다

첫 빌드에서 2·3번째 걸음이 **`edge-start`(x=12)** 로 뽑혔다 — 대상이 `x=1068` 인데 말풍선이 화면 왼쪽 끝에 섰다.
원인: **«곁이냐»를 후보 «순번»으로 판정**했는데, `beside` 가 `anchor` 와 같은 값으로 클램프돼 중복 제거되자 **화면 끝이 세 번째로 올라와 «곁»으로 둔갑**했다.
⇒ **«곁이냐»를 후보 자신이 들고 있게** 고쳤다. 🔴 **순번은 정체성이 아니다** — 앞의 것이 빠지면 뒤의 것이 그 자리를 물려받는다.

## 잰 것 / 못 잰 것

- **잰 것**: 1440 폭 · chromium · 6걸음 · 전/후 각 1회(값이 결정적이라 회차 간 흔들림 없음).
- 🔴 **못 잰 것**: **7~9번째 걸음**(6번째가 사람이 눌러야 넘어가는 걸음 — 처방 사정거리 밖) · **좁은 폭**(<md 는 바텀 시트라 이 산식을 안 탄다) · chromium 외 엔진.
- **lint**: 이 파일의 eslint 오류는 **처방 전 5건 → 후 5건**(같은 종류·줄 번호만 이동). **내가 늘리지 않았고, 원래 있던 것도 못 줄였다.**
