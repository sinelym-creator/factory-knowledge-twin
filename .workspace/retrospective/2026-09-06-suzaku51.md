# 회고 — 스자쿠 51대 (2026-09-06 09:40 ~ 12:0x)

## 사이클 요지
- 승격 24 완결 확인(#862 · main `5f17b54` · 배포 delta 0) · **T5-3 완결 판정 67/68**(#867).
- G1b seed 직결(#863 · 처방 = 경로 일치 · 치환본 불채택 · 독검 #864) · G1c benchmark-smoke 활성(#865 · 색인 0→exit 5 · hit 8/10·9/10 4 run 동일 · 독검 #866) · O-54 러너 정정(#868 · rc 7 「색인 없음」 · `error.code` 정본) · O-54 CI 반영(#869 개설).
- 정리 = levi2 워크트리 9본 · senku2 10본 · 로컬 브랜치 5본 · 원격 lane = `senku2-d53b` 만.
- 교대 = levi2 55→56(폐하 하명 모델 복귀 · 미결 ① 3점째 = 90분 무폴백) · senku2 52→53(자격 50.1 · 자비스 집행 청구).
- API 한도 불통 09:58~10:42(44분 · 고장 아님 · 팀 커밋·푸시 선행으로 유실 0).

## 내 오기 5
1. **checks 완료 전 병합**(09:57 #863 · 새 sha 푸시 직후 CLEAN 을 보고 병합) — CLEAN ≠ 전건 완료. 처방 = pending 0 실측 뒤 병합(이후 5건 준수).
2. **발주 전제 오기**(11:06 `--out` 이 exit 5 앞에서 미도달) — 러너 코드 118/170행을 안 보고 썼다. 처방 = 발주 전 실물 grep(같은 형태 51대째 재발).
3. **E1/E2 혼용**(11:14 「503 index_unavailable = 값」) — 이름은 소스 축(E2), 값은 원문(E1) · 리바이2가 갈랐다.
4. **원장 행 마커 미갱신**(11:39 #867 hygiene 빨강) — 머리글만 67 로 고침 · O-34 검사기가 잡음(그물이 산 증거).
5. 시각 표기 오기 0(51대) — 좌석 2건(센쿠2 `date 10:52`) 지적.

## 배운 것
- **「없다 ≠ 안 봤다」**: code 빈 값 = 파서가 봉투 정본(`{"error":{code}}`) 을 안 본 것. 판정선은 「200 위 hit 0」이 아니라 「일관 응답 + 부재를 가리키는 code」.
- **원문을 버리는 계측기는 두 번 청구한다** — artifact 에만 있고 로그에 없으면 run 을 더 쓴다.
- **킬 범위 = 착지 시각(mergedAt ↔ 킬 ts)** — 「bg 가 죽는다」 미측 단정이 두 사람 사이에서 굳었다.
- 러너 exit 5 「검색이 죽었다」와 서버 「색인 없음」은 같은 문장이 아니다 — 원인 오지칭은 코드 축이 아니라 실측(503 20건)으로 E1.

## 이월
T5-2 live 순종 축·O-48 · O-53(캐시 효과 n=2) · required check 지정(폐하 관문) · AF 페르소나 3본 #123 · `levi2-t51pr2`·`senku2-d53b`(#617) 보류.
