# 회고 2026-09-06 — 스자쿠 48대 (00:36~02:03)

## 사이클
- 자비스 95대 교대 집행으로 기동(첫 훅 7% = 내 값) → 폐하 취침 01:12 「각자 권한 자율 위임」 · 01:09 「디벨롭 승격은 내 승인없이 스자쿠판단에」.

## 한 일(E1)
- **T7-44 run 문서 검색 hybrid 완결**: 계약 v0.2.0 #805 → 구현 #804 → 스키마+케이스 #806 → r1 조건부 #807(② FAIL 기전 = `hybrid._fuse(TOP_K=5)` 를 세 축이 나눠 써 record 1건이 doc-chunk 칸을 먹음 · 근거 23→22) → 판정선 개정 #808(어휘 다리 청크 = 측정 기록 강등 · ②' doc-chunk 5/5 & 근거 ≥23 · 대조군 `v0.5-d85`) → 수리 #809(과수집 top_k×2 → record 제거 → 상위 TOP_K 절단) → r2 PASS #810 = **기준선 v0.6**.
- **승격 22**(main `5177e74` #811 · 폐하 위임 첫 집행) · `promote-artifacts` rc 0(promptSha 불변 · 게이트웨이 무접촉) · production ai-api 교체(센쿠2 · `.Id` 변경 · 구 세대 보존 · env 12 · replay 바인드 산출물 판) · 외부 재검 PASS 6/6 #813(두 vantage · 대조군 오류 2+1 로 38/38 검출력 증명 · **O-18 축 닫힘** = 공개 build == main tip 최초 성립 · 시점 사실).
- O-34 마커 CI 칸 #803 · O-41 종결(전제 오기) · O-37 등재+E1(#815/#816 · approved 45·superseded 15·draft 0) · 무대 처분 5본 #802 + `senku2-*` 12본 정지 · 워크트리 **52→13** · runbook §7-3 정정 · CLAUDE.md §5 위임 개정 + 워크트리 ④(미커밋)·⑤(거부 뒤 잔여) 축 · 자비스 95→96 교대 집행(오살 0 · 러너 7/7). 원장 **66/68**.

## 🔴 오기(형태 = 실물 안 보고 씀)
1. ②' 문턱 「≥ d85 22」 — 22 는 FAIL 낸 hybrid 값 · d85 = 23(리바이2 정정). 「방금 FAIL 한 값이 다음 합격선이 된다」 자리.
2. 발주문·설계 §2.2 「`strategy` 를 step.started/completed 양쪽」 — `stepStarted` 는 additive 자리 0(실물 `_step` extra = completed 만).
3. 센쿠2에 `evidence/**` 산출물 발주 — 검증 좌석 독점 scope(§33.2) · 발주 전 lane 표 대조 0.
4. 시각 라벨 「`date 01:29`」 추정(실측 01:26:56) · CIM 문자열 조회가 내 명령줄에 자기매치 3회(판정은 집행기로만).
5. PR 개설 5분 미완 = heredoc 경로 변수 미설정 → `/pr-body.md` · 파이프 뒤 `rc=$?` = `tail` 값(rc-zero 재발).
6. 워크트리 발주에 `Remove-Item -Force` 를 절차로 박음 → 5본 「무엇을 잃었는지 말할 수 없음」(④ 축 신설의 근거).

## 배운 것
- 「거른다」와 「몇 칸 받아 오는가」는 다른 축 — 필터만 넣으면 슬롯을 잃는다.
- 게이트에 «데이터 성질»(질문 1건의 어휘 다리)을 두지 않는다 — 코드가 산 것(슬롯 보전)만 게이트.
- 부재는 어떤 색도 못 낸다 — 대조군을 같은 스트림에 심어 검출력을 먼저 증명한다.
- 위임 뒤에도 절차(무대 검증→판정→집행→통보)는 그대로 · 허가 «대기»만 빠진다.
- 「실패한 작업이 그 실패를 재는 계측기를 함께 망가뜨린다」 — remove 거부 → `--ignored`·`status` 사망 · 목록에서 사라짐 ≠ 정리됨 · 판정은 파괴 «전» 값으로.
- 「`-d` 가 값이었다」 — 강제 옵션은 증인을 죽인다 · 거부 자체가 두 번째 증인 · 고아 lane 은 `-d` 먼저, 거부분은 blob 대조 뒤에만 `-D`.
- 자비스→스자쿠 발신은 멘션 필수(멘션 없으면 wake 0 · E1 재확인) · 군단장 순찰 폭 10 은 ctx 비용 큼.
- watchdog 무인 복구 = FKT 좌석은 폐기 경로(자비스 카드 #101 · 아침 재기동).

## 보완사항
- 이월: 좌석 유지 결정(폐하 기상 뒤) · `levi2-t76`(`:8820` 무대 + 미커밋 tests 3) · `senku2-d53b`(#617) 처분 · 과수집 배수 2 근거(record 분포 먼저) · `ci-unit` ignored 잔여 「모른다」 · watchdog 재기동 확인(자비스).
